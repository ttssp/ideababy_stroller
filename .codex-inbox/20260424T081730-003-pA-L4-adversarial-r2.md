# Codex Task · 003-pA · L4 Adversarial Review Round 2

**Created**: 2026-04-24T08:17:30Z
**Recommended model**: gpt-5.4
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~12-20k
**Previous round**: R1 verdict **BLOCK** · 3 blocker / 5 high / 4 med-low(见 `.codex-outbox/20260424T071844-003-pA-L4-adversarial-r1.md`)

## Your role

你是 GPT-5.4 xhigh,对 spec v0.2(R1 反馈已落地)做 **R2 复审**。你已在 R1 发出 BLOCK;现在验证:

1. 3 个 blocker 是否**真正关闭**(不只是换措辞)
2. 5 个 high 是否**全部处理**
3. 4 个 medium/low 是否**全部处理**
4. 操作员 3 项决策(D-OP1 USD 真硬帽 / D-OP2 MacFUSE 默认 + OQ5 开放 / D-OP3 仅手工 rsync)是否**忠实落进 spec**
5. 是否引入**新的 regression / 新 blocker / 新矛盾**

## Read in this order

1. `.codex-outbox/20260424T071844-003-pA-L4-adversarial-r1.md` ← **你上一轮的 findings**,R2 必须逐项核对
2. `specs/003-pA/spec.md` v0.2 ← 新 C20/C21/C22 + OQ5 + §5 估时重写 + §6 O1/O5/O7 验证重写
3. `specs/003-pA/architecture.md` v0.2 ← §5.1 budget 前置拒绝(新)+ §6 fail-closed(重写)+ §7 pip/uv 等价白名单 + §8 Stuck 唯一真相源契约表(新)
4. `specs/003-pA/tech-stack.md` v0.2
5. `specs/003-pA/SLA.md` v0.2 ← §1.1 USD 验证 + §1.4 macOS-compat
6. `specs/003-pA/risks.md` v0.2 ← R6 改写 + R11 新增 + §8 OQ5
7. `specs/003-pA/non-goals.md` v0.2 ← N4-11 跨机器
8. `specs/003-pA/compliance.md` v0.2 ← §4.4 demo artifact distribution checklist
9. `specs/003-pA/dependency-graph.mmd` v0.2
10. `specs/003-pA/tasks/T010.md T012.md T017.md T019.md T022.md T027.md T028.md`(R2 强改过的核心)+ 随机抽 3-5 个其它 task 检查一致性

## Verify matrix(必答)

对你 R1 findings 逐项做判定,每条返回 status ∈ {CLOSED / STILL_OPEN / REGRESSED / NEW_CONCERN}:

### R1 Blockers
- **B1 Stuck 契约闭合** — architecture §8.1-§8.7 是否真是"唯一真相源"?采样周期 / 窗口边界 / 转移表 / disk_io 阈值 / 冷启动策略 / false negative / stuck_lock + unlock 全部到位?T017/T022 是否引用了 §8?正负样本矩阵 8 场景够不够?
- **B2 USD 硬帽前置拒绝** — architecture §5.1 是否真实现 proxy 预估拒绝(不只是文字)?T010 是否扩到 8h 含 `budget_estimator.py` + `prereject_middleware.py` + `test_proxy_prerejects_on_budget.py`?T018 是否降为"第二道兜底"?failure/retry/cache-miss 入账规则是否显式?
- **B3 `.claude/` fail-closed** — architecture §6 是否禁纯 `chmod -R a-w` 作生产路径?refuse-to-start 路径在哪?OQ5 开放项是否真开放?Python 面 EACCES integration test 是否写进 T012 verification?R6 mitigate 是否重写?

### R1 High
- **H1 HF_TOKEN 最小 scope** — architecture §1 外部 actor / T011 Known gotchas 是否明写?默认无 token 跑公开 demo?
- **H2 O1 7-day rehearsal** — spec §6 O1 验证是否重写?T027 是否加了 rehearsal_script.sh(未新建 T029)?
- **H3 pip/uv 契约统一** — architecture §7 三条等价白名单是否清晰?T015 / T002 / spec 一致?
- **H4 compliance demo artifact** — compliance §4.4 checklist 是否存在?T027/T028 release gate 是否引用?
- **H5** R1 已标"处理完成",R2 只验证无 regression

### R1 Medium/Low
- **M1** SLA §1.4 是否改 `psutil.Process(pid).environ()`(macOS-compat)?
- **M2** `validate_quality` 契约是否在 T020 + T021 统一严格 gate?
- **M3** `license-check.yml` 是否分配给 T028?
- **M4** T002 "Unsloth lock fail 即 fail 不注释"是否改?

## 新增验证(R1 未覆盖)

1. **操作员 D-OP1/D-OP2/D-OP3 忠实度**
   - D-OP1:USD 硬帽真实是 proxy 前置,不是文字包装?SLA 验证方式是否可跑?
   - D-OP2:OQ5(MacFUSE 是否装)开放项是否真开放?若操作员拒装,refuse-to-start 的 UX(报错消息、引导文档)是否可接受?
   - D-OP3:C9 收窄 claim 成"仅手工 rsync 远端重跑"?T019 机器指纹校验 + T027 rsync playbook 是否只教"远端重跑"而非"跨机 resume"?N4-11 是否显式在 non-goals?

2. **R11 缓解(新 risk)**
   - Opus 工时从 38.4% 降到 15.1%(task-decomposer R2 声称),你按 task 元数据独立核算
   - 降级的 task(T010 T011 T013 T019 T023 等)acceptance 是否保持严苛(Codex R1 叮嘱"只是 reviewer 换人,不是降标准")?
   - 特别检查 T019 resume + 机器指纹(新 Sonnet 级),是否因降级导致 contract 松懈?

3. **估时口径对齐**
   - spec §5 phase-level estimate 是否真重算?task-decomposer R2 报告总 152h × 1.5 = 228h 是否匹配 spec?
   - 关键路径长度(task-decomposer R2 报 59h,R1 报 61h)是否合理?

4. **依赖图变化**
   - T017 → T019 新增依赖(can_resume 依赖 stuck_lock)是否在 mermaid 中?
   - 8 个 Phase 1 节点并发度真实吗(R1 挑战"solo 6 并发不现实")?

## 新 blocker 排查(常见 regression)

- 修 B1 新增 `stuck_lock` 文件持久化,会不会引入**文件竞态**(worker 崩溃时 lock 未清,pars 下次启动 refuse)?
- 修 B2 新增 proxy `prereject_middleware`,会不会对 streaming 响应做错预估(Claude 流式输出 token 数未知)?
- 修 B3 强制 MacFUSE + bindfs,**Linux** 用户(若有)是否还能跑?architecture 是否 cover?
- H2 O1 rehearsal 加进 T027(+4h)— 是否实际覆盖"一周内 1 baseline + 2 variants + compare"?还是只走半周?
- D-OP3 机器指纹校验会不会误判(同机 Python patch 升级)?

## Write to `.codex-outbox/<TS>-003-pA-L4-adversarial-r2.md`

格式:

```markdown
# Adversarial Review · 003-pA · R2

**Reviewed**: <ISO>
**Reviewer**: gpt-5.4 xhigh
**Previous round**: R1 BLOCK (3 blocker / 5 high / 4 m-l)
**Files read**: <list>

## R1 findings status

### Blockers
- B1: <CLOSED/STILL_OPEN/REGRESSED> — <证据行号>
- B2: ...
- B3: ...

### High
- H1-H5: ...

### Medium/Low
- M1-M4: ...

### 操作员决策忠实度
- D-OP1 / D-OP2 / D-OP3:CLOSED?证据行号

## New findings in R2

### New Blockers(若有)
| # | Location | Issue | Why block |

### New High(若有)
| # | Location | Issue | Suggested action |

### New Medium/Low(若有)
(列表)

### Regression(若有)
(修某条时引入的新矛盾)

## PRD faithfulness check(复核,R1 曾判 O1 部分映射)
- [ ] O1 / O2 / O3 / O4 / O5 / O6 / O7 各自忠实度
- [ ] Scope IN/OUT 无泄漏

## Model routing 复核(R1 曾 concern)
- 工时 Opus / Sonnet / Codex / Haiku 百分比独立核算
- 目标 Opus ≤ 15% 是否达成?

## Top 3 things operator still must verify

1. ...
2. ...
3. ...

## Verdict

One of:
- **CLEAN** — R1 findings 全关,无新 blocker,可进 build
- **CONCERNS** — 非阻塞;操作员决定现在修还是带风险 log 进 build
- **BLOCK** — 仍需修复

## Orchestrator note

- 本轮共发现 X new blocker / Y new high / Z regression
- 最不确定判断
- 若进 R3 建议聚焦
```

## Rules

- 引用文件 ≤ 15 词原文;超过给行号 paraphrase
- 每条判定必须有**具体行号**证据,不接受"看起来 ok"
- 若发现 R1 的某 finding 被"文字游戏关闭"(改了措辞但没改实质),标 STILL_OPEN + 高亮
- 新发现的 blocker **不优于** R1 旧 blocker 的优先级;是"新的独立问题"
- R2 通常应该比 R1 短得多(~60% 篇幅);若找不到新问题,直接 CLEAN

## 预期结果

- 理想:CLEAN(所有 R1 findings 关闭 + 无 regression)→ spec 可 build
- 可接受:CONCERNS(少数 medium/low 未尽,但操作员可带风险 log)
- 若 BLOCK:操作员进入 R3,最多 4 轮后必须决定 ship 或 re-scope
