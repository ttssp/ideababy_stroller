# Codex Task · 003-pA · L4 Adversarial Review Round 3

**Created**: 2026-04-24T10:07:40Z
**Recommended model**: gpt-5.4
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~6-10k(短轮 · 预计 CLEAN)
**Previous rounds**:
- R1 BLOCK · 3 blocker / 5 high / 4 med-low
- R2 BLOCK · 0 new blocker / 1 new high / 2 regression(全是 doc / source-of-truth 回退)

## Your role

对 spec v0.2.1(R2 patch 已落地)做 **R3 复审**。只验证 5 个 precise fix + 1 个 risks.md 追加:

### R3 verify matrix

对每条返回 CLOSED / STILL_OPEN / NEW_CONCERN:

1. **B1 STILL_OPEN(T017/T022 stuck 粒度回退)**
   - 验 `tasks/T017.md:86, 109` 已清掉 `maxlen=6` / "60s 轮询" / "10s 采样"
   - 验 `tasks/T022.md:75, 93` 合成 trace 粒度已改 5s/tick 至少 180 tick
   - 两个 task 必须引用 `architecture.md §8.1` 作唯一真相源

2. **H1 STILL_OPEN(HF_TOKEN 契约分叉)**
   - 验 `spec.md:78` 附近:`ANTHROPIC_API_KEY` 与 `HF_TOKEN` 分开,**HF_TOKEN 明示 gated 例外**(由 orchestrator 临时注入 read-only,下载完移除),与 `architecture.md §1/§5` + `T011` 一致

3. **H2 STILL_OPEN(O1 phantom T029)**
   - 验 `spec.md:244` 的 O1 验证引用已 `T029 → T027`(指 `tasks/T027.md:81-85` rehearsal_script.sh)
   - 验 spec.md 全文 grep "T029" 无残留

4. **New High #1(T019 指纹过严)**
   - 验 `tasks/T019.md:36-46, 83-85`:`machine_fingerprint.py` 拆出 **hard_reject_fields**(`gpu_name` / `cuda_version` major.minor / `os_major`)与 **warning_fields**(`python_version` / `os_release` patch 级)
   - 验 `can_resume` 按两组分支;hard 非空才 reject,warn 非空仅 logger.warning
   - 验 Verification 加"warning 不 reject 验证"(Python patch / OS patch 允许 resume)

5. **Regression · spec §5 估时口径**
   - 验 `spec.md:203-236` 已改 146h→152h,~220h→228h
   - 验 Phase 分布 Phase 0=24h / Phase 1=41h / Phase 2=56h / Phase 3=31h 合计 152h
   - 验 关键路径改为 ~74h + 路径 `T001→T004→T005→T007→T010→T013→T017→T019→T023→T024→T027→T028`
   - 验 spec.md 全文 grep "146h" / "220h" / "310-410h" 无残留

6. **Model routing Opus 15.1% 压线**
   - 验 `risks.md` R11 追加了说明("v0.1 实际 15.1%,压线超 0.1pp 可接受;v0.2 若要进一步降,考虑 T017 拆分")
   - 验 数字 152h 已替换旧 146h/23h 算法

## Read in order

1. `.codex-outbox/20260424T081730-003-pA-L4-adversarial-r2.md` ← 你上一轮 R2 findings
2. `specs/003-pA/spec.md` v0.2.1
3. `specs/003-pA/risks.md` v0.2.1
4. `specs/003-pA/tasks/T017.md`
5. `specs/003-pA/tasks/T019.md`
6. `specs/003-pA/tasks/T022.md`
7. `specs/003-pA/tasks/T011.md`(H1 相关,验证未意外改)
8. `specs/003-pA/architecture.md`(不应有改动,验证 R2 CLEAN 保持)

## 不要做的

- **不要**重新审查 R2 已判 CLOSED 的 findings(B2/B3/H3/H4/H5/M1-M4/D-OP1/D-OP2/D-OP3)
- **不要**挖新设计问题 — R3 是 doc patch 验证,不是 fresh review
- **不要**引用 architecture.md 的 R2 CLEAN 段落去挑 R1 已关的 blocker

## Write to `.codex-outbox/<TS>-003-pA-L4-adversarial-r3.md`

**目标篇幅:2-3 KB**(短轮)。

格式:

```markdown
# Adversarial Review · 003-pA · R3

**Reviewed**: <ISO>
**Reviewer**: gpt-5.4 xhigh
**Scope**: R2 BLOCK findings 6 条 patch verification only

## R2 findings status

- B1 T017/T022 粒度: <CLOSED/STILL_OPEN>(行号证据)
- H1 HF_TOKEN 分叉: <>
- H2 O1 T029 phantom: <>
- New High #1 T019 指纹: <>
- Regression §5 估时: <>
- R11 Opus 15.1% 说明: <>

## PRD faithfulness(短答)

所有 O1-O7 仍忠实于 PRD?(上轮已逐项验;R3 只报有无 regression)

## Verdict

- **CLEAN** → spec ready for build(预期)
- **CONCERNS** → 少数 med/low 可带风险 log 进 build
- **BLOCK** → 若真的还有 source-of-truth 分叉

## Orchestrator note

- New findings: 0 / X / Y
- 最不确定:<若有>
- 下一步建议:<CLEAN 则 "操作员可 `/parallel-kickoff` 开工"; BLOCK 则聚焦 X>
```

## Rules

- 引用 ≤ 15 词原文;超过给行号
- 每条 CLOSED 必须有**具体行号**证据
- 若真有 STILL_OPEN,明示是 spec-writer **回退**还是 **漏改**
- 若 CLEAN,简短结论即可,不需冗长总结
