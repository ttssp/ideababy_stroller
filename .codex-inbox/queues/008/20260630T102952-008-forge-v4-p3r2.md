# Codex Task · 008 · forge v4 · P3R2(finalize)

**Queue**: 008
**Created**: 2026-06-30T10:29:52Z
**Recommended model**: gpt-5.5
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~6-10k(reuse-session)
**Kickoff form**: reuse-session
**Forge version**: v4
**Phase**: P3R2
**Convergence mode**: strong-converge

## Session hint(reuse from P1+P2+P3R1 session)

你已读过(本轮**请勿重读**):forge-config / 你自己的 P1+P2+P3R1 / X 中所有标的 / 对方 P1+P2(你在前轮已读)/ 各轮模板。

本轮**新读**:
- `.claude/skills/forge-protocol/SKILL.md` 的 **P3R2 template** 部分(strong-converge 分支)
- **`discussion/008/forge/v4/P3R1-Opus47Max.md`** ← 对方(Opus)的 P3R1,现在可以看了
- `discussion/008/forge/v4/moderator-notes.md`(若存在 — 大概率仍无)

**HARD CONSTRAINT(reuse-session only)**:do NOT re-read files you read in previous rounds unless listed above。

## Your role

forge v4 P3R2 — 按 **convergence_mode = strong-converge** finalize:
- 必须 finalize **单一 verdict**;残余分歧降级为 v0.2 note
- 若 R2 后双方仍真冲突 → §2 显式标 "unresolved"(synthesizer 会处理)
- 给 synthesizer 的 W 形态草稿建议必须具体到可执行

## CONSTRAINTS

- 严格按 strong-converge 行事,不混用 preserve-disagreement
- §1 对 P3R1 §3 每条分歧给最终立场 + 让步/反驳/部分接受
- §2 单一 verdict(200-400 字);无 unresolved 则显式写「无 unresolved」
- §4 给 synthesizer 的草稿建议必须对应 forge-config 的 **W = next-PRD draft + decision-list + next-dev-plan**(三件都要给)
- 引用对方原句 ≤15 words

## 收敛上下文(供你校准)

双方 P3R1 已**几乎同构**:verdict 都是「GO, gated refactor」,3 条分歧也同(用途颗粒度 / spike 门槛 / KG 权重),且都是程度/落地层面非方向冲突。对方 P3R2 已 finalize:
- D1 → 用「候选目标/验证门」措辞写跟单 + PRD 显式标「build runtime 不得据此自行实现信号产品」(防 V4 写进 PRD 本体)
- D2 → spike pass 条件 = 产生 decision-grade 证据(分层报:stance/conviction/action × 反讽/非反讽 × high-confidence 子集 + FP-signal 一等风险),**不由 forge 拍固定百分比**,数值留 operator
- D3 → KG-24/25 作框架可靠性附带必修入 W,实装留 XenoDev,不喧宾夺主
- 单一 verdict = GO · 三件事(① 008↔004 消费契约分两层:无悔下游 now + 跟单 gated;② 信号 spike 硬前置;③ 字典第1层 now / 2-3层 gated;边界:008 只产可溯源结构化+置信度,信号决策留 004)· 无 unresolved。

**本轮你独立 finalize 你自己的 P3R2**。若你同意,§2 给同向单一 verdict;若你对某条收敛措辞有更优意见,在 §1 提并让步收敛,不要为分歧而造分歧。

## Write

`discussion/008/forge/v4/P3R2-GPT55xHigh.md`(template 见 forge-protocol §"P3R2 template" · strong-converge 分支):
- §1 对每条分歧的最终立场 + 让步
- §2 联合 verdict(单一,200-400 字;无冲突则写「无 unresolved」)
- §3 残余分歧降级为 v0.2 note
- §4 W 形态草稿建议(next-PRD 决策点 + decision-list 4 列 + next-dev-plan milestone,三件都要)

700-1200 字。

## When done

Write `.codex-outbox/queues/008/20260630T102952-008-forge-v4-p3r2.md` with:
- **Completed**: <ISO>
- **Files written**: discussion/008/forge/v4/P3R2-GPT55xHigh.md (+ word count)
- **Verdict**: CLEAN | CONCERNS | BLOCK
- **Convergence**: converged | preserved-2-paths | unresolved
- **Final verdict 关键句**(strong-converge)
- **给 synthesizer 的草稿建议是否齐全**(W = next-PRD + decision-list + next-dev-plan 三件对应)
- **Notes for Claude Code**: 任何 orchestrator 需知道的
