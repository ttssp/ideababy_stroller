# Codex Task · 008 · forge v3 · P2(参照系评估)

**Queue**: 008
**Created**: 2026-06-20T00:48:22Z
**Recommended model**: gpt-5.5
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~6-10k
**Kickoff form**: reuse-session
**Forge version**: v3
**Phase**: P2

## Session hint(only meaningful if Codex reuses session from P1)

你已读过(本轮请勿重读):
- `.claude/skills/forge-protocol/SKILL.md` 的 P1 部分
- `discussion/008/forge/v3/forge-config.md`
- 你自己的 `discussion/008/forge/v3/P1-GPT55xHigh.md`
- X 中所有标的(含 _external/ 两份快照 + PRD + v2 stage)

本轮**新增需读**:
- `.claude/skills/forge-protocol/SKILL.md` 的 P2 template 部分
- **`discussion/008/forge/v3/P1-Opus47Max.md`** ← 对方(Opus)P1,现在可读
- `discussion/008/forge/v3/moderator-notes.md`(若不存在则跳过)

**HARD CONSTRAINT**(reuse-session only):do NOT re-read files you read in the previous round of this Codex
session unless explicitly listed above。

## Your role

forge v3 P2 — 按 Z=对标 SOTA 跑搜索(架构/凭据边界层)+ 消化对方 P1。

## CONSTRAINTS

- **搜索约束(Z = 对标 SOTA · 架构/凭据层)**:
  - **forbidden = 法律检索 / 是否违法 / ToS / DMCA**(合规 operator 担责 · 见 K binding)
  - forbidden = tech-stack-deep-dive / pricing / 实施细节
  - 建议 ≥3 检索方向(与你 P1 §3 不确定项对齐):
    1. **credential broker / sidecar / secretless 模式**(持 key 主体独立、agent 不读凭据的工业范式 ——
       CyberArk Secretless / Vault Agent / Infisical AI-agent credential brokering 一类)
    2. **认证会话复用 + cookie 隔离**(持 key 进程独占会话、编排方不经手 cookie ——
       Playwright persistent context / storageState / 独立 userDataDir 一类)
    3. **分页抓取失败模式**(cursor vs offset、列表截断、增量 checkpoint、`has_more`/`next` 末页信号 ——
       直接对你 P1 §3#2「moduleContentList 分页语义未知」)
- §1 至少 3 条对标(每条:SOTA 事实 + 对 3 候选向的含义)
- §3 至少 3 条修正(P1 哪些站住、哪些被推翻)
- verbatim quote ≤15 words

## 本轮重点(orchestrator 提示 · 非 binding · 你独立判断)

Opus P2 已就同一 SOTA 提出一个**收敛假设**,请你独立验证或反驳:
- **② 持 key daemon 与 ③ agent 出计划,可能不是三选一,而是一条管线的不同层** —— credential broker/sidecar
  是「持 key 执行基座」(②的底座),③的「agent 出计划 + operator 确认」是其上的控制面,①侦察兵是诊断/fallback
  层。即真正 verdict 形态可能是**分层组合**(③控制流 + ②broker 基座 + ①侦察兵),而非单选一个候选。
- **broker 防 agent 滥用的机制** = 对端点/参数**白名单**(agent 意图须落预定义端点集,broker 不盲转发任意
  URL)。请判断这是否足以兜住「agent 不持 key 易、agent 不能滥用持 key 进程难」(你 P1 §3#1)。
请在 §3 明确:你**接受**分层组合框架,还是认为应保留单一候选 verdict(及为何)。strong-converge 下这是 P3
要收的核心结构,P2 先各自表态。

## Write

`discussion/008/forge/v3/P2-GPT55xHigh.md`(template 在 forge-protocol SKILL §"P2 template"):
- §1 SOTA 对标(表格,≥3 条)
- §2 对方 P1 + 外部材料消化(K 无外部链接;重点消化 Opus P1 与你 P1 的异同)
- §3 修正后的视角(≥3 条:站住/被推翻,含对「分层组合 vs 单一候选」的表态)

600-1100 字。

## When done

Write `.codex-outbox/queues/008/20260620T004822-008-forge-v3-p2.md` with:
- **Completed**: <ISO>
- **Verdict**: CLEAN | CONCERNS | BLOCK
- **Headline**: SOTA 对标一句话("我们 vs SOTA 的核心 gap 是 X" / 或「3 候选向的 SOTA 定性是 X」)
- **Searches**: <n>,主要 source 列表
- **修正点**:P1 哪些站住、哪些被推翻(简表)+ 对「分层组合 vs 单一候选」的表态
- **Notes for Claude Code**: 任何 orchestrator 需知道的

Filename **must match** inbox basename:`20260620T004822-008-forge-v3-p2.md`
