# Codex Task · 001-pA · L4 Adversarial Review Round 2

**Created**: 2026-04-23T16:56:11Z
**Recommended model**: gpt-5.4
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~18-25k
**Max runtime**: ≤ 60 min
**Prior round**: R1 (`20260423T142034-001-pA-L4-adversarial-r1.md`) verdict BLOCK with 3 blockers

---

## Your role

You are GPT-5.4 xhigh performing **Round 2 adversarial review** on fork `001-pA` ("PI Briefing Console"). R1 returned verdict BLOCK with B1/B2/B3. Spec-writer has applied fixes + a follow-up patch (Q1/Q2/Q4 closure). Your job is to verify the fixes are real, not a paper exercise — then issue an R2 verdict.

## Context — what changed since R1

### Blockers claimed closed

**B1 — per-paper summary authoritative storage**
- Fix: new table `paper_summaries` (id, paper_id, topic_id, summary_text, llm_call_id, model_name, prompt_version, created_at) with CHECK(≤3 sentences) + UNIQUE(paper_id, topic_id, prompt_version)
- Touched files: architecture §5/§5.1/ADR-6, spec §2/§3/§4 (D15)/§6/Glossary, compliance §2, risks.md (+TECH-8), tech-stack.md (LLMProvider `summarize()` returns SummaryRecord)
- Tasks: T003 (ships table), T004 (SummaryRecord type), T013 (writes row), T014 (JOINs in briefing), T015 (reads from paper_summaries), T016 (displays summary), T023 (includes in export)

**B2 — skip.why mechanical enforcement (red line 2)**
- Fix: 3-layer defense
  - DB: `actions.skip_requires_why` CHECK (action != 'skip' OR char_length(btrim(why)) >= 5)
  - API: `src/lib/actions/recordAction.ts` 400 with `SKIP_WHY_REQUIRED` code
  - UI: `src/components/skip-why-input.tsx` inline expand with disabled submit until trim ≥ 5 chars
- Touched files: spec §3 C6 rewording + §4 D16 + §6 4 new verification hooks + Glossary; architecture §5 + ADR-7; risks.md (+TECH-9)
- Tasks: T003 (CHECK), T015 (recordAction + skip-why-input + server action), T016 (displays why prominently)

**B3 — task contracts close**
- Fix part 1: T003 front-loaded 2 new tables (`paper_summaries`, `export_log`) + 3 new columns (`labs.first_day_at`, `labs.allow_continue_until`, `fetch_runs.notes`) + 2 CHECKs (skip.why, summary sentence cap); total table count 12 → 14
- Fix part 2 (subsequent Q1/Q2/Q4 patch): +5 auth columns to T003 (`seats.invite_token_hash/invite_expires_at/invited_at`, `sessions.revoked_at/last_active_at`)
- File_domain + depends_on rewrites: T011 owns `daily.ts`; T014/T021 co-own; T015 owns `today/page.tsx`; T022/T024 co-own; T024 depends_on [T003, T014, T015, T022]; T020 depends_on [T010, T015]; T021 depends_on [T003, T011, T014]
- T030 `grants.sql` enumerates paper_summaries + export_log for webapp_user + worker_user
- T032 schema_version assertion changed `'1.0'` → `>= '1.1'` (T023 bumped export schema)
- OPEN-QUESTIONS-FOR-OPERATOR.md records Q1/Q2/Q4 resolution

### Deltas
- spec.md: 0.1 → 0.2 → 0.2.1
- architecture.md: 0.1 → 0.2 (+ADR-6, +ADR-7, table count 12→14)
- Total hours: 107 → 113 → 114h (1h headroom on 115h soft cap)
- Critical path: 51h (R1 claim) → 54h (Codex R1 correction) → 58h (R2 decomposer) → 59h (after +1h T003 bump)

### Deferred (operator explicit, NOT addressed in this round)
- H1–H10 from R1 review (all kept open for operator judgment)
- Q3 (T033 self-report sampling skip.why) — to be done at T033 kickoff
- M1–M4 / L1–L2 from R1 minor findings

---

## Files to read (focus order for a fast verification)

### Changed since R1 (verify fixes)
1. `specs/001-pA/spec.md` — check D15, D16, C6 rewording, §6 4 new verification hooks
2. `specs/001-pA/architecture.md` — check ADR-6, ADR-7, §5 table count, paper_summaries + export_log in ER
3. `specs/001-pA/tasks/T003.md` — verify ALL 10 schema additions actually enumerated in Outputs (2 tables + 3 labs/fetch cols + 5 auth cols + 2 CHECKs + unique); estimated_hours = 10
4. `specs/001-pA/tasks/T013.md` — writes to `paper_summaries` with prompt_version + llm_call_id; NOT to `llm_calls.response_text`
5. `specs/001-pA/tasks/T015.md` — 3-layer skip.why enforcement (recordAction + skip-why-input + inline expand)
6. `specs/001-pA/tasks/T014.md` — depends_on includes T011, reads paper_summaries in assembly
7. `specs/001-pA/tasks/T021.md` — depends_on includes T014, co-owns daily.ts
8. `specs/001-pA/tasks/T022.md` — co-owns today/page.tsx
9. `specs/001-pA/tasks/T023.md` — writes export_log audit; includes paper_summaries in export; schema_version 1.1
10. `specs/001-pA/tasks/T024.md` — depends_on [T003, T014, T015, T022]; co-owns today/page.tsx; reads labs.first_day_at
11. `specs/001-pA/tasks/T030.md` — grants.sql enumerates paper_summaries + export_log
12. `specs/001-pA/tasks/T032.md` — schema_version >= '1.1'
13. `specs/001-pA/dependency-graph.mmd` — 25 nodes, 113h (note: was 113h before T003 +1h; decomposer didn't regenerate for the Q1/Q2/Q4 patch; minor discrepancy)
14. `specs/001-pA/OPEN-QUESTIONS-FOR-OPERATOR.md` — 4 Qs with resolution markers

### Unchanged but verify consistency
15. `specs/001-pA/tech-stack.md` — SummaryRecord shape
16. `specs/001-pA/risks.md` — TECH-8, TECH-9 added; SEC-1/TECH-1/DOGFOOD-1 mitigations tightened
17. `specs/001-pA/compliance.md` — paper_summaries + export_log data assets listed
18. `specs/001-pA/SLA.md`, `non-goals.md` — should be untouched since R1

### For faithfulness
19. `discussion/001/001-pA/PRD.md` (unchanged — source of truth)
20. `.codex-outbox/20260423T142034-001-pA-L4-adversarial-r1.md` (your R1 output; reference only)

---

## What to verify (MANDATORY — each must be answered)

### V1 — B1 real-close check
- [ ] `paper_summaries` schema matches spec (5 required cols + constraints) in T003 Outputs AND architecture §5 ER AND compliance §2
- [ ] T013 writes to `paper_summaries`, NOT to `llm_calls.response_text`
- [ ] T015 `/today` loader reads summary from `paper_summaries`, not `llm_calls`
- [ ] T023 export includes `paper_summaries` rows in the JSON envelope (with prompt_version for reproducibility)
- [ ] No residual reference to "summary stored in llm_calls" anywhere in the 7 touched files

### V2 — B2 real-close check
- [ ] DB layer: `actions.skip_requires_why` CHECK present in T003 with the exact predicate
- [ ] API layer: T015 Outputs include `src/lib/actions/recordAction.ts`; signature validates; returns 400 SKIP_WHY_REQUIRED
- [ ] UI layer: T015 Outputs include `src/components/skip-why-input.tsx`; implementation plan describes inline-expand with disabled submit until trim ≥ 5
- [ ] spec §6 has 3 runnable verification checks (one per layer) + 1 for B1 CHECK
- [ ] T016 displays skip.why prominently (not hover/tooltip) per architecture §5 data model annotation
- [ ] ADR-7 documents the 3-layer rationale

### V3 — B3 real-close check
- [ ] T003 Outputs enumerate ALL these:
      2 tables (paper_summaries, export_log),
      3 labs/fetch cols (labs.first_day_at, labs.allow_continue_until, fetch_runs.notes),
      5 auth cols (seats.invite_token_hash, invite_expires_at, invited_at, sessions.revoked_at, last_active_at),
      2 CHECKs (skip_requires_why, summary_sentence_cap),
      1 UNIQUE (paper_summaries)
- [ ] T003 estimated_hours = 10
- [ ] T011 file_domain includes `src/workers/daily.ts` (owner)
- [ ] T014 file_domain declares co-ownership of daily.ts; depends_on [T011, T012, T013]
- [ ] T021 file_domain declares co-ownership of daily.ts; depends_on [T003, T011, T014]
- [ ] T022 file_domain declares co-ownership of today/page.tsx; depends_on [T015, T020, T021]
- [ ] T024 file_domain declares co-ownership of today/page.tsx; depends_on [T003, T014, T015, T022]
- [ ] T030 grants.sql enumerates paper_summaries + export_log for both webapp_user + worker_user
- [ ] T032 schema_version assertion `>= '1.1'`

### V4 — DAG integrity
- [ ] No cycles
- [ ] No orphans
- [ ] All referenced task IDs exist
- [ ] Co-owned files have strict dependency edges (no parallel across co-owners)
- [ ] Critical path calculation matches (expected ~59h after T003 10h bump)

### V5 — Drift check
- [ ] Any NEW drift introduced by the fix? (e.g., did adding paper_summaries create a downstream API contract that no task implements?)
- [ ] Any residual reference to old design (e.g., "summary in llm_calls", "skip.why optional", "labs.first_day_at will be added in auth migration") — zero tolerance
- [ ] Does spec.md version bump match the magnitude of change? (0.2 minor for scope; 0.2.1 patch for clarification)

### V6 — Original R1 concerns that should be implicit-fixed
These R1 concerns might have been accidentally addressed by the B1/B2/B3 fixes — verify:
- R1/M3 — T014 no-shift text consistency: verify the 2 places are now unified (R1 review claimed these existed; R2 should confirm whether B1 fix touched it)
- R1 decomposer's note "T013 signature change to (paper_id, topic_id)" — confirm implemented in T013

### V7 — Fresh concerns (optional but welcome)
If in scanning the fixes you notice NEW blockers or high-severity issues **introduced** by the fix (e.g., paper_summaries index strategy missing, export_log lacks retention policy, B2 inline-expand breaks mobile keyboard flow), flag them.

Do NOT re-raise H1–H10 from R1 — operator explicitly deferred those. You may note in passing if a fix accidentally addressed one (e.g., "H5 export CI could be layered on top of export_log now existing") but don't treat them as gates.

---

## Write your review to this exact path

`.codex-outbox/20260423T165611-001-pA-L4-adversarial-r2.md`

## Required output structure

```markdown
# Adversarial Review · 001-pA · R2

**Reviewer**: GPT-5.4 xhigh
**Completed**: <ISO>
**Runtime**: ~<NN> min
**Files read**: <list>
**Prior round**: R1 verdict BLOCK → this round verifies 3 blocker fixes + Q1/Q2/Q4 patch

## Verification summary

| Check | Pass | Note |
|---|---|---|
| V1 B1 paper_summaries end-to-end closure | ✅/⚠️/❌ | ... |
| V2 B2 3-layer skip enforcement | ✅/⚠️/❌ | ... |
| V3 B3 task contracts close | ✅/⚠️/❌ | ... |
| V4 DAG integrity | ✅/⚠️/❌ | ... |
| V5 No new drift introduced | ✅/⚠️/❌ | ... |
| V6 Incidentally-addressed R1 nits | ✅/⚠️/❌ | ... |

## New blockers (if any) — MUST fix before Phase 0
(usually empty if R1 fixes are real)

## New high-severity concerns (if any)

## Residual R1 H1–H10 status
Note: these are deferred by operator — not gates. Just confirm whether any were accidentally addressed:
- H1 O4 unfalsifiable: <still open / partially addressed>
- H2 operator visibility: ...
- H3 stale briefing fallback: ...
- (only list those relevant)

## Verdict
One of:
- **CLEAN** — all fixes verified; no new blockers; spec ready for Phase 0 kickoff (assuming operator accepts deferred H-list as risk-register items)
- **CONCERNS** — fixes mostly real, 1–3 minor issues to address; operator decides to patch now or log and proceed
- **BLOCK** — ≥1 fix NOT real (design-level contradiction persists) or NEW blocker introduced by the fix

Justify in ≤ 3 sentences.
```

## Constraints

- Quote ≤ 15 words verbatim per file
- Cite section anchors (e.g. "T003 Outputs bullet 3", "spec §6 line 47", "architecture ADR-6")
- If you find a text-only reference that wasn't updated (e.g., task file still says "writes to llm_calls"), report it as a blocker — design contradictions matter more than naming
- Be terse. R2 should be faster than R1 because scope is narrow.
- Max 1500 lines output.

## Escape hatch

If your verification shows the spec is now genuinely ready (CLEAN verdict is justified), say so plainly — don't invent concerns to demonstrate thoroughness. Operator has an explicit "next-step gating" decision waiting.

Write to `.codex-outbox/20260423T165611-001-pA-L4-adversarial-r2.md`. Begin.
