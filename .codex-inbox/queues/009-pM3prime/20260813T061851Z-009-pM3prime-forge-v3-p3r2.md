# Codex Task · 009-pM3prime · forge v3 · P3R2(finalize)

**Queue**: 009-pM3prime
**Created**: 2026-08-13T06:18:51Z
**Recommended model**: gpt-5.6-sol
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~6-10k
**Kickoff form**: reuse-session
**Forge version**: v3
**Phase**: P3R2

**convergence_mode**: `strong-converge`(从 forge-config frontmatter 读取 · **本轮硬约束**)

## Session hint(reuse-session)

**已读,本轮请勿重读**:forge-config · 你自己的 P1/P2/P3R1 · 对方 P1/P2/P3R1 · X 全部 9 标的 · 你检索的全部 SOTA 材料。

**本轮新读**:
1. `discussion/009/009-pM3prime/forge/v3/P3R2-Opus47Max.md`(对方 P3R2 · **本轮必读**)
2. `.claude/skills/forge-protocol/SKILL.md` 的 **§"P3R2 template"** 段(走 **strong-converge** 分支)
3. `moderator-notes.md`(若存在,binding)

## HARD CONSTRAINTS

- **NO new search this round**
- **strong-converge**:§2 必须给**单一** verdict(200-400 字),或**显式标 unresolved**
- 残余分歧一律降级为 **v0.2 note**(§3),不得以并列 verdict 形式留存
- §4 必须给 **W 形态产出的初步草稿建议**,逐项对应 `W = verdict-only + decision-list + refactor-plan`
  —— 这是 synthesizer 的直接输入,**要具体不要抽象**
- 引用对方原句 ≤15 words
- 700-1200 字

## 本轮状态:A 侧已就你 P3R1 的全部四条做了处置

1. **分歧 4(你新提的)· A 侧全盘让步。** A 承认「边界**真信号**留存率」是范畴错误,
   且是 forge v2 已命名过的那一个(stability ≠ validity);改为**预注册边界候选留存率**。
2. **分歧 1 / 2 / 3 · 全部收敛**,且 2 和 3 采**你的措辞**(`pass_semantic: none`;执行态 STOP + 认识态 UNKNOWN 双态 + ledger 双记)。
3. **A 侧已偿付实验债**:四类上限 + 唯一命令(**含一条诚实的能力边界交代**)+ 逐项三态判据 + 预先承诺解读 + 停止规则,全部落在 P3R2 §1。

## 🔴 本轮你的首要职责:**验收那份实验契约,不是称赞它**

你在 P3R1 立的准入条件是「**缺一即反对探针进入最终 verdict**」。请逐条验收 A 侧 §1 的偿付:

1. **四类上限**(N=6 / 102 条 / 单跑 ≤35 min / 总 ≤3.5 h 且 ≤$10)—— 数值依据是否成立?
   特别是:**记录数 102 是复用已有三次读数的同一批**(确定性抽样)—— 这个复用**有没有隐藏问题**?
   (例如:同一批记录被反复用于"标定测量工具",是否构成对该批的过拟合?会不会让 M1 的结论不可外推到扩样后的语料?)
2. **唯一命令** —— A 侧诚实交代了「跑数据可以一条循环命令,**算指标需要新脚本**⇒ 探针是 XenoDev 侧一个 task,
   不是 IDS 敲一行」。**这算不算满足了你的准入条件?** 若不算,请明说要删探针。
3. **三态判据** —— A 侧把判据全做成**比较型**(`J_agg` 的 bootstrap CI 与 `J_1` 比),理由是"我们已经看过 0.44,
   此时拍绝对阈值落在 v2『看数后动门槛』阴影里"。**这个规避方式成立吗?**
   ⚠ 请特别检查:`J_1` 本身是**从同一批数据算出来的**,拿它当比较基准会不会造成循环?
   bootstrap CI 在 N=6(3+3)下**是否真有区分力**,还是必然落进 indeterminate?
   **如果 M1 在这个设计下几乎注定 indeterminate,那这个探针就是在花 3.5 小时买一个已知答案 —— 请当场指出。**
4. **前置依赖** —— A 侧主张探针必须在 (a) 之后跑,依据是 P(6 跑全完成) ≈ 35%。核对这个算术与前提。
5. **预先承诺的解读**(M1=fail/indeterminate ⇒ 触发路线改判议题;M1=pass 且 M2<50% ⇒ 代价须写进 verdict
   且密度读数不得与历史 41/73/71 直接比较)—— **有没有漏掉某个结果组合的处置?**

**验收结论必须落在 outbox 的 `probe_contract_verdict` 字段**:`accept` / `accept-with-fixes`(列出 fixes)/ `reject`。

## 另请复核一处 A 侧可能过头的表述

A 侧 §1 自记「这是我本轮**第三次**被自己的证据推翻…**这次不是**(自己发现的)⇒ 单侧自检不足以挡住范畴替换」。
这属于**过程反思**,不是技术结论。若你认为它不该进 stage doc,请在 §3 说明;
若你认为它**该**进(作为 forge 机制本身的证据),也请说明理由 —— **这条会被 synthesizer 采信,所以要你表态。**

## Write

`discussion/009/009-pM3prime/forge/v3/P3R2-GPT55xHigh.md`,按 P3R2 template 的 **strong-converge** 分支:

- **§1** 我对每条分歧的最终立场 + 让步(**含上述 5 条验收结论**)
- **§2** 联合 verdict(**单一** · 200-400 字;若仍冲突,显式标 unresolved)
- **§3** 残余分歧降级为 v0.2 note
- **§4** W 形态产出的初步草稿建议(逐项对应 verdict-only / decision-list / refactor-plan · **具体**)

## When done

写 `.codex-outbox/queues/009-pM3prime/20260813T061851Z-009-pM3prime-forge-v3-p3r2.md`:

- **Completed**: `<ISO>`
- **Files written**: `<list + word count>`
- **Headline**: `<一句话>`
- **Verdict**: `CLEAN | CONCERNS | BLOCK`
- **Convergence**: `converged` / `unresolved:<n>`
- **joint_verdict**: `<§2 原文>`
- **probe_contract_verdict**: `accept` / `accept-with-fixes:<list>` / `reject:<reason>`
- **v02_notes**: `<n>` + 各自一行
- **process_reflection_ruling**: `include` / `exclude` + 理由一行
- **Notes for Claude Code**

⚠ Filename **must match**:`20260813T061851Z-009-pM3prime-forge-v3-p3r2.md`
