# Codex Task · 009-pM3prime · forge v1 · P2(参照系评估)

**Queue**: 009-pM3prime
**Created**: 2026-07-23T09:35:00Z
**Recommended model**: gpt-5.6-sol
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~8-12k
**Kickoff form**: reuse-session
**Forge version**: v1
**Phase**: P2

## Session hint(only meaningful if Codex reuses session from P1)

你已读过(本轮请勿重读):
- `.claude/skills/forge-protocol/SKILL.md` 的 P1 部分
- `discussion/009/009-pM3prime/forge/v1/forge-config.md`
- 你自己的 `discussion/009/009-pM3prime/forge/v1/P1-GPT55xHigh.md`
- X 中所有标的(handback / PRD / spec / gold_layer1&2.py / risks / dogfood KG-42)

本轮**新增需读**:
- `.claude/skills/forge-protocol/SKILL.md` 的 P2 template 部分
- **`discussion/009/009-pM3prime/forge/v1/P1-Opus47Max.md`** ← 对方(Claude Opus 4.8)P1
- `discussion/009/009-pM3prime/forge/v1/moderator-notes.md`(若存在,binding;可能不存在)

**HARD CONSTRAINT**(reuse-session):do NOT re-read files read in previous round unless listed above.

## Your role

You are **GPT-5.6-Sol xhigh**, Reviewer B, forge v1 on **009-pM3prime**, Phase 2 —— 按 Z 跑 SOTA 搜索 + 消化对方 P1。

## CONSTRAINTS

- **搜索约束(Z = 对标 SOTA)**:SOTA-benchmark search only;forbidden = tech-stack-deep-dive / pricing / 实施细节。
- **本轮搜索焦点**(围绕议题核心「异源模型能否当独立第二源」):
  1. LLM-as-judge 的独立性 / self-preference bias / 判者与被判同源时的相关误差
  2. 多模型集成判断(ensemble / multi-judge)错误相关性实证 —— 异源到什么程度(不同 vendor/架构/训练语料)才降低相关误差
  3. echo-chamber / 模型同构盲区 / correlated failures 的实证研究
  4. 人机混合验收(human-in-the-loop acceptance / human-LLM agreement)何时人不可替代、何时 LLM 可替代
  5. 跨谱系模型在**事实性抽取/指称消解**类任务(vs 主观判断类)上的错误独立性差异 —— 因为本任务 Layer-2 核心是「NL→ticker 5 元组映射」偏事实性
- §1 至少 3 条对标(标的 X' vs 参照项 vs 它怎么做 vs 我们怎么做 vs gap vs source URL)
- §2 必须处理对方 P1 提出的关切(尤其 A 侧 P1 §3 的三个不确定点 + A 侧架构维度判 **keep** vs 你 P1 判 **refactor** 的分野)
- §3 至少 3 条修正(P1 判断哪些站住、哪些被 SOTA 推翻)· verbatim quote ≤15 words

## 关键对抗提醒(binding · 元层自证)

forge v5 §underweights 自认「双模型强收敛回声室是最大 underweight」(v5 全程零方向分歧、SOTA 只加固不推翻)。
本轮你我(GPT-5.6-Sol × Claude)P1 已**再次高度收敛到 refactor**。→ P2 你**必须主动找能推翻/证伪 refactor 方向的 SOTA 证据**
(如:"跨 vendor 模型在冷门实体消歧上仍共享训练语料盲区 → 异源不解决 correlated failure"),而非只搜加固 refactor 的证据。
若搜完确实找不到反证,明说;但不许只做同向加固(那就是在复现要审的坑)。

## Write

`discussion/009/009-pM3prime/forge/v1/P2-GPT55xHigh.md`(template 见 forge-protocol SKILL §"P2 template"):
- §1 SOTA 对标(≥3 条表格)
- §2 对方 P1 关切消化(逐条)
- §3 修正后的视角(≥3 条,站住/被推翻)

文件头 `<Model>` 写真实模型名 `GPT-5.6-Sol xhigh`。600-1100 字。

## When done

Write `.codex-outbox/queues/009-pM3prime/20260723T093500Z-009-pM3prime-forge-v1-p2.md` with:
- **Completed**: <ISO>
- **Files written**: <list + word count>
- **Headline**: SOTA 对标一句话 verdict(「我们 vs SOTA 的核心 gap 是 X」· 尤其:异源模型当第二源在 SOTA 里有无先例/反例)
- **Searches**: <n> + 主要 source 列表
- **Verdict**: CLEAN | CONCERNS | BLOCK
- **修正点**:P1 哪些站住、哪些被推翻(简表)
- **反证搜索结果**:主动找到的「推翻 refactor」证据(有/无 + 关键一条)
- **Notes for Claude Code**: orchestrator 需知的

Filename **must match** this inbox basename: `20260723T093500Z-009-pM3prime-forge-v1-p2.md`
