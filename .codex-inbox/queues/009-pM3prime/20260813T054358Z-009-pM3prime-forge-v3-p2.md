# Codex Task · 009-pM3prime · forge v3 · P2(参照系评估)

**Queue**: 009-pM3prime
**Created**: 2026-08-13T05:43:58Z
**Recommended model**: gpt-5.6-sol
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~8-12k
**Kickoff form**: reuse-session
**Forge version**: v3
**Phase**: P2

## Session hint(reuse-session · 省 token)

**已读,本轮请勿重读**:
- `forge-config.md`(X/Y/Z/W/K + TEXT 常量块)
- 你自己的 `P1-GPT55xHigh.md`
- X 中全部 9 个标的(PREREG / PRD / v2 stage doc / HANDBACK-LOG 28·29·33 / census.py / sampling.py / gold_layer2.py / spec.md / SLA.md)
- 你越界读过的 `tests/contract/test_e1_citation_consistency.py` 与 `tests/integration/test_census_cli.py`
- forge-protocol SKILL.md 的 P1 段

**本轮新读**:
1. `discussion/009/009-pM3prime/forge/v3/P1-Opus47Max.md`(对方 P1 · **现在可以读了**)
2. `.claude/skills/forge-protocol/SKILL.md` 的 **§"P2 template"** 段
3. `discussion/009/009-pM3prime/forge/v3/moderator-notes.md`(若存在,binding)
4. 你自己检索到的 SOTA 材料

## Your role

Reviewer B,Phase 2 = **参照系评估(with search)**。Z mode = **对标 SOTA**。

## HARD CONSTRAINTS

- 本轮**必须**跑 **≥3 次 SOTA 检索**;paraphrase findings,verbatim quote **≤15 words**
- 允许:领域内已有产品 / 学术研究 / 失败案例;**禁止**:tech-stack 深潜 / 定价 / 实施细节
- 不写 PRD / dev plan / refactor 详细方案(那是 Phase 4)
- **§3 必须显式写出:我 P1 的哪些判断站住了、哪些被自己的检索推翻了。**
  ⚠ 承 forge v2 **反自欺条款**:「证据在场却被解释掉」是独立失败模式。
  若你要解释掉某条证据,须写明该解释的**可证伪点**。

## 本轮四个指定任务(除自由检索外)

### 任务 A · 🔴 独立核实我(A 侧)在 P2 提出的一条**推翻自己 P1** 的判断

我在 `P2-Opus47Max.md` §3 修正 2 主张:**实现比 SLA 更严,而不是 SLA 太严**。依据是
`specs/009-pM3prime/SLA.md` §"依赖 SLA" 原文写着「调用失败 → **重试/退避** · **连续**失败 hard-fail 留错」,
而 `census.py` 的实现是**单条失败即 break + 全回滚、per-record 零重试**。

**请独立核实这条,不要照单接受。** 具体请回答:
1. 该 SLA 条文是否真如引述?它的作用域是否真覆盖 census 提取路径(还是只约束别的依赖)?
2. `census.py` 是否真无 per-record 重试?OpenAI SDK 默认传输层重试覆盖哪些错误类型,是否覆盖了我们实测的
   失败类型(空 content / 坏 JSON / `usage` 缺失)?
3. 若这条成立 —— **议题 ② 是否根本不需要动任何冻结契约?** 若不成立,错在哪一步?

这条若成立,直接化解 K 的核心 tradeoff(严格性 vs 可跑性);**若我错了,请当场推翻。**

### 任务 B · 🔴 一条**可查事实**,我三次 WebFetch 官方文档全部 `Socket is closed`,没查到

**DeepSeek chat completions API 是否提供 `seed` 参数,或任何可复现性 / determinism 保证?**
(顺带:`system_fingerprint` 的官方语义是什么?)

这条**直接决定议题 ③ 的处方**:有 seed 且真起作用 ⇒ 轮内非确定性可能大幅收窄;无 ⇒ 只剩「多次采样 + 聚合」一条路。
**请务必查到,并给出出处。查不到就明说查不到 —— 不得靠推演补。**

### 任务 C · 对方 P1 与我 P1 的一处分歧,请在 §3 表态

你 P1 §1 工程纪律段写「invalid 又回滚 E1 trial 行,因此失败尝试不进入该账」。
我在 P2 §3 主张:**回滚是事实,但推论不成立** —— ledger grain 是 `variant × caliber × horizon`(spec C1/O11),
同一 variant 重跑本就不新增行;**真正的病是 grain 太粗**(同一 grain 下三次跑产出三份不同语料,账上无法区分)。
这个区别有实际后果:按"回滚"修会动 fail-closed,按"grain"修只加记账维度。**你同意还是反对?**

### 任务 D · 你 P1 §3 的三条不确定,请用检索正面攻

尤其第 1 条(**集合型抽取该用哪种信度指标、最少重复几次**)。
我 P2 检索到的相关物:`TARr@N` / `TARa@N`(arXiv 2408.04667)· 两阶段多数投票(zero-shot NER)·
OLAF 把标注当测量(arXiv 2512.15979)。但**都没给阈值和最少 N**。
若你也找不到成熟阈值,请**明说"文献不给,须由我们自定并预注册"** —— 这本身是可用的裁决输入。

## Write

`discussion/009/009-pM3prime/forge/v3/P2-GPT55xHigh.md`,按 P2 template:

- **§1 SOTA 对标**(表格:标的 / 参照项 / 它怎么做 / 我们怎么做 / gap / URL)
- **§2 用户外部材料消化**(K 中无外部链接 → 写 N/A + 一句说明)
- **§3 修正后的视角**(P1 哪些站住 / 哪些被推翻 · **必须含上面任务 A / B / C / D 的四条结论**)

600-1100 字。

## When done

写 `.codex-outbox/queues/009-pM3prime/20260813T054358Z-009-pM3prime-forge-v3-p2.md`:

- **Completed**: `<ISO>`
- **Files written**: `<list + word count>`
- **Searches run**: `<n>` + 各自主题
- **Headline**: `<一句话>`
- **Verdict**: `CLEAN | CONCERNS | BLOCK`
- **任务 A 结论**: `A 侧修正 2 成立 / 不成立` + 一句理由
- **任务 B 结论**: DeepSeek 是否有 `seed` / 可复现性保证 + 出处(或明确"查不到")
- **任务 C 结论**: 同意 / 反对 + 一句理由
- **任务 D 结论**: 找到成熟阈值 / 文献不给
- **Notes for Claude Code**: orchestrator 需要知道的一切

⚠ Filename **must match** this inbox file's basename:
`20260813T054358Z-009-pM3prime-forge-v3-p2.md`
