# Codex Task · 009-pM3prime · forge v5 · P3R2(finalize)

**Queue**: 009-pM3prime
**Created**: 2026-08-16T06:22:00Z
**Recommended model**: gpt-5.6-sol
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~12-18k
**Kickoff form**: reuse-session
**Forge version**: v5
**Phase**: P3R2

## Session hint(reuse-session)

**本轮新读**:
- `.claude/skills/forge-protocol/SKILL.md` 的 **P3R2 template · strong-converge 模式**
- 🔴 `discussion/009/009-pM3prime/forge/v5/P3R2-Opus47Max.md` ← **对方 P3R2**
- `moderator-notes.md` **未更新**,与你上轮读的同一版

**HARD CONSTRAINT**(reuse-session):不要重读上一轮已读过的文件,除非本任务显式列出。

## 本轮的起点:A 侧四条分歧**三全让一让**,已移动到你的立场

A 侧 P3R2 的让步逐条如下,**你不需要再争这四条,直接在此基础上 finalize**:

1. **`-04` 映射表** —— **全让**。承认「没有消费 gate 的映射表就是又一个产生侧目录,与我诊断的
   ledger 同病」。最终形态:先裁载体等级(`codified` / `note` / `explicitly-uncodified`),
   映射表**必须绑定消费 gate + 结账证据**。
2. **「使选项集完备」** —— **完全让步,A 侧承认自己错**。采用你的二分:**语义穷举**(不可能)≠
   **程序全定义**(可能且必须);`known + unclassified → 具名解释者 → 留痕复审`,**未分类不得自动通过**。
   A 侧撤回「完备性不是目标」的表述(其危险读法是放弃 fail-closed)。
3. **`-05` 先方法后状态** —— **让步**,承认是伪排序。每态绑定 `{允许方法, 已知敞口, 退出证据}`;
   masking 敞口(自启动图前 ~50 点)作 `insufficient_history` 态的必载字段。
4. **五要件不充分(你新提的分歧 4)** —— **全让,A 侧称这是本轮对它最重的一击**:
   五要件全部描述义务本身、无一描述消费动作与结账证据 ⇒ **模板犯了它所诊断的病**。
   采纳 `clock_or_reachable_event`(两者皆无 ⇒ fail-closed,不得伪造到期日)+ 补
   `consumer_action` 与 `settlement_evidence` 两问。

⇒ **联合 verdict 已收敛到你的七问消费契约。** 动作分类双方一致:
`-04`/`-05`/`-12` refactor · `-11` new · `-09` refactor + new;总体定向 refactor 不 redesign。

## Your role

forge v5 P3R2 —— **strong-converge finalize**:产**单一 verdict**;残余分歧降级为 v0.2 note
(**每条必带 `due_gate`**,承 v4 元条款)。

⚠ **请重点做三件事,而不是复述已收敛的部分**:

**① 检验 A 侧 P3R2 §2 的四条「必须进正文的硬内容」有没有夹带私货或过度外推**:
① 载体分三级、「显式不入正文」是合法态(OLRC);② **复审主体必须与 owner 分离**(DO-178C);
③ 小样本三档 + 具名判定人(NCHS)⇒ CN/HK 报 `not_estimable`、US 字面满足不构成对 CN/HK 的重判依据;
④ 分层可得性是汇总能力定义的一部分(BCBS 239)⇒ `-09` 必须新建 market 观测轴。
**这四条是从你我各自的检索面跨界引用的 —— 请检查引用是否忠于原体系的适用条件。**

**② 正面处置 KG-42 同族(A 侧列为 v0.2 note 1 · 本轮最大敞口)**:
七问的 `owner` 与「复审主体须与 owner 分离」在**只有一个人的仓**里**不可同时满足**。
A 侧判「不解,写成具名敞口,due_gate = `forge:006:phase0`」。**你同意吗?**
若有可在单人仓成立的分离形态(时间分离 / 角色分离 / 跨模型分离 / 外部第二源),**现在说**;
说不出就明确签署「本轮不解」——**不要沉默带过**。

**③ 给 synthesizer 的 §4 草稿建议必须具体到可执行**:W = **verdict-only + decision-list + refactor-plan**。
decision-list 需带 `obligation path`(产生 gate → 消费 gate)与 `evidence_grade`
(`independent` / `SOTA-corrected` / `post-hoc-accepted`)两列;refactor-plan 落点具体到**文件与节**。

## CONSTRAINTS

- **strict strong-converge**:必须 finalize 单一 verdict,不得混用 preserve-disagreement
- 若 R2 后仍有真冲突,§2 显式标 `unresolved` + 双方立场各自完整(synthesizer 会处理)
- 残余分歧降级 v0.2 note,**每条必带 `due_gate`**;消费点未接线的须写明 `exposure`
- 引用对方原句 ≤15 words
- **NO new search**
- 红线:M1=pass **不解锁**扩样与 E2 · 41 条盲标路 **CLOSED 不重开** · 不改 C6 **数字** ·
  **不当场改** producer / validator / SHARED-CONTRACT schema · 不跨入 PRD / dev plan

## Write

`discussion/009/009-pM3prime/forge/v5/P3R2-GPT55xHigh.md`(template 见 SKILL §"P3R2 template" strong-converge 段)

**700-1200 字。**

## When done

写 outbox 同名文件 `.codex-outbox/queues/009-pM3prime/20260816T062200Z-009-pM3prime-forge-v5-p3r2.md`,含:

- **Completed**: <ISO>
- **Files written**: <list + word count>
- **Verdict**: CLEAN | CONCERNS | BLOCK
- **Convergence**: converged | preserved-2-paths | unresolved
- **Final verdict 关键句**(单句)
- **对四条硬内容的检验结论**:逐条 忠于原体系 / 过度外推 + 一句理由
- **KG-42 处置**:同意「不解 + 具名敞口」/ 提出可行分离形态(具体写)
- **v0.2 note 清单**:每条 + due_gate + exposure(若未接线)
- **给 synthesizer 的草稿建议是否齐全**(verdict-only / decision-list / refactor-plan 三项)
- **Notes for Claude Code**
