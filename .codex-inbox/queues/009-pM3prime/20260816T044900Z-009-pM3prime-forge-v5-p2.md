# Codex Task · 009-pM3prime · forge v5 · P2(参照系评估)

**Queue**: 009-pM3prime
**Created**: 2026-08-16T04:49:00Z
**Recommended model**: gpt-5.6-sol
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~14-20k
**Kickoff form**: reuse-session
**Forge version**: v5
**Phase**: P2

## Session hint(reuse-session)

**你已读过(本轮请勿重读)**:
- `.claude/skills/forge-protocol/SKILL.md` 的 P1 部分
- `discussion/009/009-pM3prime/forge/v5/forge-config.md`
- 你自己的 `discussion/009/009-pM3prime/forge/v5/P1-GPT55xHigh.md`
- X 中所有标的(11 件,含 XenoDev 六件 —— 你上轮已实读,未 BLOCK)

**本轮新增需读**:
- `.claude/skills/forge-protocol/SKILL.md` 的 **P2 template** 部分
- 🔴 `discussion/009/009-pM3prime/forge/v5/moderator-notes.md` ← **operator 裁决 · Hard constraint · 必读**
- `discussion/009/009-pM3prime/forge/v5/P1-Opus47Max.md` ← **对方 P1**
- ⚠ **不要读** `P2-Opus47Max.md`(A 侧本轮产出)—— P2 是各自跑检索,读对方 P2 是 P3R1 的事

**HARD CONSTRAINT**(reuse-session only):do NOT re-read files you read in the previous
round of this Codex session unless this task explicitly lists them above.

## 🔴 Moderator 裁决(本轮 binding · 必须在产出开头回应)

operator 已就「回声室 binding」裁决 —— 全文见 `moderator-notes.md`,摘要:

**本轮 P1 判为弱收敛证据,不按回声处理。** 理由:四条具名预测**互相落空**(双方都猜错了对方)
⇒ 独立性成立;且你独立复证的 `market` 全文检索结果不是从 A 侧转手。

**但该裁决附两条 hard 约束**:

**① P2 一格不放松 —— 破局器仍然只能是零重叠检索。**
预测落空只证明双方**不会读心**,不证明**不同源**(两个模型可能共享同一套先验而同向偏)。因此:
- 你的 P2 **必须申报本轮检索面**:检索了什么、命中什么、**哪条结论由哪次检索支撑**;
- **只复述自己 P1 的结论不计入 P2 收敛证据**;
- 本仓语料已有的 **PEP 387 / RFC 1589 / NIST / OPA Gatekeeper / Chrome-Blink / KEP /
  ModSecurity / RFC 5848 / RFC 9162 复述不计入检索面**(承 forge-config Z 段原有约束);
- ⚠ **A 侧本轮已用掉的七个参照面,你若重复命中则该条不计入你的检索面** ——
  A 侧检索面(仅告知面,不告知结论):**① 立法 codification / 法典编纂机构 · ② 航空软件适航的需求追溯与
  「无父需求」· ③ 官方统计的小样本比例呈现标准 · ④ 短程/自启动 SPC · ⑤ 支付卡行业的日志强制复审 ·
  ⑥ 航空带缺陷放行的时限分档 · ⑦ 质量体系里不合格输出的授权放行**。
  **请去这七个之外的地方找**(建议方向:开源治理 / 药品监管 / 金融审计 / 工程变更管理 /
  司法程序法 / 医疗设备上市后监督 / 铁路或核电的安全案例 —— 任选,不必照做)。
- **P3R1 时你必须点名:我的哪条 P1 判断被我自己的 P2 检索推翻或改写。** 一条都没有的一方,
  须显式说明为何检索对自己零影响(这是 v2/v4 实际破回声的机制)。

**② 本条 binding 自身的歧义已被登记为元层第⑥题的活体标本。**
K 把「预测都落空」与「即又是逐条相同」写成**等价**,而现实落在两者之外 ——
**这正是本轮第⑤题(`-12`)「条款枚举了 decision,发生的却是没被枚举的那一格」的同构复现,
且这次发生在 forge 自己的规则上。** 你在 P3R2 / verdict 的元层 rationale 里至少要答:
一条判定规则把判据 A 与判据 B 写成等价、现实却给出 A∧¬B 时,**谁有权解释、按什么处置**?
以及 operator 本次行使解释权(即那份 note)**自己有没有载体、消费者、可达触发、完备选项**?

## Your role

forge v5 P2 —— 按 `forge-config.md` 的 **Z 段**跑检索(mode = **对标 SOTA**),
然后用检索结果**修正或推翻你自己的 P1 判断**。

Z 段可检索面(非穷举,不必逐条照做):决议 → normative text 的落地机制 /
分层 vs 汇总指标何时必须分解 / 低样本量下控制图的替代 / 审计日志的消费者设计 /
未收敛评审的 escape hatch。**但见上面 ①:A 侧已占的七个面请避开。**

## HARD CONSTRAINTS

- **对标 SOTA search only**;forbidden = tech-stack 深潜 / pricing / 实施细节
- §1 **至少 3 条对标**
- §2 处理所有外部材料 —— K 中**无外部链接/文件**;三个 TEXT 块你 P1 已消化 ⇒ 如无新增,
  在 §2 明写「本轮无新增外部材料」即可,**不要为凑字数复述**
- §3 **至少 3 条修正**(站住 / 被推翻),每条**引用 §1 的具体检索证据**
- verbatim quote ≤15 words
- **不得跨入 PRD / dev plan**(那是 Phase 4)
- 不许动的红线(承 K):M1=pass **不解锁**扩样与 E2 · 41 条盲标路 v0.1 **CLOSED 不重开** ·
  不改 C6 冻结门槛**数字** · **不当场改** producer / validator / SHARED-CONTRACT schema

## Write

`discussion/009/009-pM3prime/forge/v5/P2-GPT55xHigh.md`(template 见 forge-protocol SKILL §"P2 template",
开头加 `## Moderator injection response` 章节)

**600-1100 字。**

## When done

写 outbox 同名文件 `.codex-outbox/queues/009-pM3prime/20260816T044900Z-009-pM3prime-forge-v5-p2.md`,含:

- **Completed**: <ISO>
- **Files written**: <list + word count>
- **Verdict**: CLEAN | CONCERNS | BLOCK
- **Headline**: 一句话 SOTA 对标 verdict(「我们 vs SOTA 的核心 gap 是 X」)
- **Searches**: <n> 次 + 主要 source 列表 + **每次检索落到哪一题**
- **修正点简表**:P1 哪些站住、哪些被推翻(**至少一条被推翻或改写** —— 若确实零推翻,
  明写「零推翻」并给理由,不要粉饰)
- **Notes for Claude Code**: orchestrator 需要知道的任何事(含是否触发 BLOCK)
