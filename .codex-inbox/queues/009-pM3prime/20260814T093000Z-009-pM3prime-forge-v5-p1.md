# Codex Task · 009-pM3prime · forge v5 · P1(独立审阅)

**Queue**: 009-pM3prime
**Created**: 2026-08-14T09:30:00Z
**Recommended model**: gpt-5.6-sol
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~14-20k
**Kickoff form**: oneshot
**Forge version**: v5
**Phase**: P1

## Your role

You are Reviewer B(GPT-5.6-Sol xhigh),forge **v5** on fork `009-pM3prime` Phase 1。
**审阅人姿态 —— 评判已存在物,不是 daydream。**

⚠ **本轮审阅对象**:前四轮依次审 v1 金标标注的**验收主体**、v2 那套验收的**测量效度**、
v3 产出信号的**那台机器**、v4 **这套治理机制自身**。
**v5 审的是 v4 结论的下一层:v4 裁定「机制存在但没绑定谁在何时做什么」,
本轮问的是 —— 绑定之后,到底绑在哪儿。**

五条到期义务各暴露一种**绑定落空**的方式:

| 义务 | 落空方式 | 现场证据(IDS 已亲验) |
|---|---|---|
| `OB-...-v4-04` 校准监测周期 | **载体缺失** | 定形只活在 ledger 的 `evidence` 字段;PRD / spec / SLA 三处 grep 全零命中 |
| `OB-...-v4-11` census_run 消费点 | **消费者缺失** | 表已建、每跑落一行,今天无任何环节读它 |
| `OB-...-v4-05` 统计型重校准间隔 | **触发条件不可达** | 控制图要多点历史,本仓可用点仅 0.44 / 0.4078 / 41-73-71 |
| `OB-...-v4-12` codex-review SKILL §4.2 | **选项集不完备** | 条款给三选项,实际走了第四种 |
| `OB-...-v4-09` per-market 跨次分解 | **自我判据到期** | note 自写「下次 firing 仍不处置 = 拖延」,今天就是那个下次 |

🔴 **回声室警告(binding · 第五轮 · 来自 K)**:本 fork 的 P1 已**连续四轮**逐条同姿态收敛
(v2 / v4 都是靠 P2 零重叠检索才破的)。**你在 P1 若与 A 侧同姿态,那是警报不是信号。**

⇒ **本轮 K 新增硬要求**:你的 **§2 必须给出至少一条具名预测**,形如
「**我判 X,但我预期 A 侧会判 Y**」。P1 交换后若双方预测**都落空**(即又是逐条相同),
**本轮 P1 按回声处理,不得作为收敛证据**。

请优先寻找 **A 侧可能因为立场而看不见的东西**。A 侧是本轮 gate 的执行者、
第 37 条决议的撰写者、以及 `-11`/`-12` 两条义务的**创立者** —— 它对自己刚立的条目
天然缺乏敌意,这是你的结构性优势面。

## HARD CONSTRAINTS

- **NO web search this phase**(那是 P2)
- **DO NOT read** `discussion/009/009-pM3prime/forge/v5/P1-Opus47Max.md`(parallel independence · 该文件已存在,请勿打开)
- 不允许写 PRD / dev plan / refactor 详细方案(只产 first-take)
- 不允许跨入 SOTA 对比(那是 P2)
- **§0 必须引用 K**
- 按 Y 三视角组织 §1 现状摘要 + §2 first-take 评分(**含具名预测**)+ §3 最不确定的 3 件事
- 800-1500 字
- ⚠ **X 中任一标的读不到(沙箱拒读外部 repo 等)⇒ §0 必须显式声明「skipped due to access」,
  并改用 forge-config 里的三个 TEXT 块。不要假装读过。**

## Read in order

1. `discussion/009/009-pM3prime/forge/v5/forge-config.md`(拿 X/Y/Z/W/K + 收敛强度 + **三个 TEXT 块**)
2. `discussion/009/009-pM3prime/forge/v5/moderator-notes.md`(若存在,binding)
3. X 中的全部标的(逐条见下)
4. `.claude/skills/forge-protocol/SKILL.md`(P1 template)
5. `AGENTS.md`(若存在)

## X 标的清单

**本仓(✅ 可达)**

1. `framework/obligations/ledger.jsonl`(75 行 JSONL)
   - ★ **本轮五题的原文在这里**。读 `OB-009-pM3prime-v4-04` / `-05` / `-09` / `-11` / `-12` 的
     **最后一行**(append-only · 同 id 取末行)。
     ⚠ **重点看 `title` / `evidence` / `note` 三个字段说的是不是同一件事** ——
     v4 第⑤题的形态(义务写标题、判据写 note、被执行的是更弱那方)在本轮五条里**是否复发**,
     是免费的第一手观察。**顺带看 `-01` 与 `-10` 两条 2026-08-14 的结账行**(今天刚写)。
2. `discussion/009/009-pM3prime/forge/v4/stage-forge-009-pM3prime-v4.md`(268 行)
   - **前轮裁决,对本轮有约束力**。重点:#decision-matrix **#4 / #5 / #9** 三行 ·
     **三条 v0.2 note**(第 194-201 行)· §Verdict 元条款「**本 verdict 自己不得犯它所诊断的病**」·
     §underweights 全节。
3. `discussion/009/009-pM3prime/PRD.md`(666 行 · v1.1)
   - ⚠ **逐字读 §6「科学重判触发条件」三条(第 511-514 行)+ 条件 1 未满足注记(第 516-533 行)** ·
     §7 红线 3(C4 convert 四要件)· 头部 v1.1 段。
4. `discussion/009/handback/HANDBACK-LOG.md`(1952 行 · **选读**)
   - **只读第 37 条**(文件末尾)。含本轮 gate 的直接前身:IDS 九项独立复核表、
     F1(SKILL §4.2 第四选项)完整事实链、F3(gap ① 表述失真)、义务拆账明细表。
     第 36 条可选读(F2 口径两读先例)。**不必通读。**
5. `framework/warn-registry/README.md`(119 行)
   - 只读 §「三态裁决」+ §接线状态表(第 83-85 行三条 `xenodev:*` gate 均 ❌)。

**XenoDev-009 build repo(⚠ 外部 repo · 沙箱可能 BLOCK —— BLOCK 则用 forge-config 的 TEXT 块,勿假装读过)**

6. `/home/ys/codes/XenoDev-009/specs/009-pM3prime/spec.md`(610 行)
   - `-04` **该回写而未回写**的候选载体之一。重点:§3 **C4 行(:302)** · §4 **D-13(:331)** · §6.3 映射表。
7. `/home/ys/codes/XenoDev-009/specs/009-pM3prime/SLA.md`(154 行)
   - 候选载体之二。重点:**E1「提取稳定性」行**(判据列现有三条 SELECT)。
8. `/home/ys/codes/XenoDev-009/projects/004-pB/src/decision_ledger/extraction/aggregation_probe.py`(1321 行)
   - **`-09` 的落点 + `-11` 的数据面**。看 `_run_batch` 结果矩阵组装段 +
     `_persist_aggregation_probe_result` / `_mark_aggregation_probe_result_completed` 两阶段发布段。
   - ⚠ **建议顺手做一次全文 `market` 检索**(A 侧做过,结论写在它的 P1 里;你独立做一次)。
9. `/home/ys/codes/XenoDev-009/projects/004-pB/alembic/versions/0023_add_census_run_and_aggregation_probe_result.py`(144 行)
   - `census_run` / `aggregation_probe_result` 两表 DDL。**`-11` 要定的「谁在何时读」,读的就是这两张表。**
10. `/home/ys/codes/XenoDev-009/.claude/skills/codex-review/SKILL.md`(548 行)
    - ★ **`-12` 的条款载体**。只读 **§4.2「4 轮上限」表格(第 421-430 行)** + 上下文。
11. `/home/ys/codes/XenoDev-009/specs/009-pM3prime/results/T015-aggregation-probe-result-20260813.json`(150 行)
    - 真实结果矩阵。⚠ **IDS 已实证 `market` 零命中**;注意 `per_run_summary` 六行的 `census_verdict` 全为
      `below_threshold_advisory`。

## 本轮 K(逐条,binding)

**① `-04`:定形了却没有条款载体。** 「每个被消费批次必测必落库 / 身份口径变化立即新基线 /
XenoDev 产证、IDS 接受基线与升版」这套东西今天**只存在于 ledger 的一个 JSON 字段里**。
要的答案:该写进哪份文档的哪一节,**以及为什么 v4 当时没写 —— 后半问比前半问重要。**

**② `-11`:审计表建好了,谁读?** 定出「**谁、在什么时点、读到什么就必须停下**」。
**明禁**「建议在分析时参考」这类**无主体**句式。

**③ `-05`:触发条件不可达时,具名敞口该怎么活着。** 控制图画不出来是事实,**不许看数造 N**。
但「等数据够了再说」在本仓已被证明**等于消失**。要一个**在数据不足期间仍然产生行为**的形态。

**④ `-09`:per-market 分解,做还是不做。** ⚠ 陷阱先说破:**成本近零 ≠ 结果有意义**。
US 三次 40/73/65 分解出来大概能用;**CN 1/0/2、HK 0/0/4 分解出来是不可解释的读数**。
而 PRD §6 条件 1 字面只要求「至少一个 per-market」——**US 一个就字面满足了**。
真问题:**字面满足算不算满足?** 算,则条件 1 本身是弱判据,该改;不算,则条件 1 该写成什么。

**⑤ `-12`:条款的选项集不覆盖实际发生的动作。** SKILL §4.2 给三选项,实际走了第四种
(修复后 ship 不复核);而 HANDBACK-LOG **第 34 条**又确立过「持续收敛可超上限」的**相反先例**。
给一个**能写回 §4.2 表格**的答案,并说明**跨仓怎么送过去**(条款载体在 XenoDev,裁决场在 IDS)。

**不许动的**:M1=pass 不解锁扩样与 E2 · 41 条盲标路 CLOSED 不重开 ·
C6 冻结门槛**数字**不动 · **不当场改** producer / validator / SHARED-CONTRACT schema(框架级走 forge 006)。

**元层(第⑥题 · 写进 rationale · 不占 W 配额)**:v4 已认定「本 verdict 自己不得犯它所诊断的病」。
**请对 v5 自己的产出做同样检查** —— 你给的每条裁决:**载体是什么 · 消费者是谁 ·
触发条件可不可达 · 选项集全不全。做不到就别写那条。**

⚠ **另**:T016 hand-back 把 `-04` 相关 gap 说成「无拦截**或留痕**」,而留痕恰是它**自己交付的**
(`census_run.declared_sample_size`,读任何 record 之前写入)。**这是本 fork 第一例保守方向的失真**
(此前六轮的失真几乎全在乐观方向)。若你认为**保守方向与乐观方向的失真需要不同的治理动作**,
在 §2 说;认为一样,也明说 —— **不要沉默带过。**

## Output

写到 `discussion/009/009-pM3prime/forge/v5/P1-GPT55xHigh.md`,
并把同一份内容写到 outbox:
`.codex-outbox/queues/009-pM3prime/20260814T093000Z-009-pM3prime-forge-v5-p1.md`

**outbox 文件名必须与本 inbox 文件名逐字一致**(HEAD 一致性合同;不一致会让 cdx-queues 永远 pending)。

结构(按 forge-protocol P1 template):
- **§0** 我读到的标的清单(逐条标 ✅ 读到 / ⏭ skipped due to access)+ 阅读策略 + **K 摘要**
- **§1** 现状摘要(按 Y 三视角:工程纪律 / 测量效度·统计方法学 / 规范工程,每视角 1-3 段)
- **§2** First-take 评分(五题 + 元层,逐条 keep / refactor / cut / new + 理由)
  **+ 至少一条具名预测「我判 X,预期 A 侧判 Y」**
- **§3** 我现在最不确定的 3 件事

若遇到无法继续的阻塞,在 outbox 首 30 行内写 `Verdict: BLOCK` + 原因。
