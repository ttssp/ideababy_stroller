# Codex Task · 009-pM3prime · forge v4 · P3R2(finalize)

**Queue**: 009-pM3prime
**Created**: 2026-08-14T05:15:00Z
**Recommended model**: gpt-5.6-sol
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~7-12k
**Kickoff form**: reuse-session
**Forge version**: v4
**Phase**: P3R2
**convergence_mode**: **strong-converge**(从 forge-config frontmatter 读取 · 必须 finalize 单一 verdict)

## Session hint(reuse-session)

**你已读过(本轮请勿重读)**:forge-config.md(含 TEXT 块)· X 全部 10 项 ·
你自己的 P1/P2/P3R1 · 对方 P1/P2/P3R1 · forge-protocol 的 P1/P2/P3R1 部分。

**本轮新增需读**:
1. `discussion/009/009-pM3prime/forge/v4/P3R2-Opus47Max.md`(对方 finalize · **本轮主要对象**)
2. `.claude/skills/forge-protocol/SKILL.md` 的 **P3R2 部分**
3. `discussion/009/009-pM3prime/forge/v4/moderator-notes.md`(若存在,binding)

## HARD CONSTRAINTS

- **NO new search**
- **strong-converge**:§2 必须给**单一** verdict;仍冲突处必须**显式标 `unresolved`**,不得含糊带过
- §4 必须**逐项对应 forge-config 的 W**(verdict-only / decision-list / refactor-plan),要具体不要抽象
- 引用对方原句 ≤15 words

## A 侧 P3R2 的四条最终立场(**三让一守**)

- **分歧 1(四前提)—— A 侧全盘让步采你的。** 且承认这是自己引用不完整:P2 §1 逐字抄了 NIST 的
  「exact times of failure are recorded」,却在 P3R1 把它漏出三前提之外。终稿采**四前提** + fallback。
- **分歧 3(版本化回退)—— A 侧让步采你的措辞。** 承认「旧结论作废」写错了,
  统一为「**历史不可变;授权被后续 `caliber_version` supersede**」,新版本只撤销旧版**未来消费权**。
- **分歧 4(per-market 不作硬产出)—— A 侧让步采排除。**
  ⚠ 但指出你在分歧 4 引的 A 侧原句「处置重点应从『继续找』转向『动作绑定』」是**误引** ——
  那句是 A 侧对**议题⑤**说的,不是对 per-market 说的。不影响裁定。
  A 侧追加要求:「显式未满足说明」**必须落到 `PRD.md` §6 条件 1 的注记**,只写进 stage doc 等于没写。
- **分歧 2(K 是否部分未满足)—— A 侧接受你的 ✅,但拒绝「优化」这个词。** 见下,这是本轮唯一未完全合拢处。

## 🔴 A 侧唯一守住的一条,请你正面裁

A 侧接受你的结构性拆分(**监测周期**已定形 / **重校准间隔**未定形),并据此把 §4 自检该条**改判 ✅**。
但 A 侧主张:剩下那块**不是「v0.2 优化」而是「具名敞口」** ——
优化是让已有能力更好,而这里缺的是**一项能力**:仅有「身份/口径变化即新基线」触发器时,
**身份稳定而缓慢漂移的场景结构上无人覆盖**(NIST 控制图存在的理由正是它)。

**并由此引出 A 侧最想写进终稿的一条:**

> **本 verdict 自己不得犯它所诊断的病。** 四题共同病灶是「机制存在未绑定到谁在何时做什么」;
> 若把「重校准间隔待 control history 足量后定」写成一条**没有消费点**的 v0.2 note,
> 它就是同一形态的**新增第五处,而且是我们亲手造的**。
> ⇒ 该 note **必须带 `due_gate`**。

**本轮实测的接线事实(A 侧在本次 forge 的 Step 0.6/0.7 亲验,可直接采信)**:
- `forge:009-pM3prime:phase0` —— **已接线**,且**今天真的开火了**(拦下 4 条 obligation + 1 条 warn)
- `forge:006:phase0` —— 已接线(C4 warn 已 defer 至此)
- `xenodev:handoff-intake` / `xenodev:task-review` / `xenodev:ship-gate` —— **均未接线**

⇒ A 侧给 synthesizer 的硬约束:Step 6.5 登记义务时,凡消费点为 `xenodev:*` 者
**必须** `consumer_wired: false` + `exposure` 必填;凡本轮 v0.2 note **必须**有 `due_gate`。

**请你裁**:(i)「具名敞口 vs v0.2 优化」采哪个措辞;(ii) v0.2 note 必带 `due_gate` 这条是否同意;
(iii) 若同意,`due_gate` 取 `forge:009-pM3prime:phase0` 是否合适。

## Write

`discussion/009/009-pM3prime/forge/v4/P3R2-GPT55xHigh.md`,按 P3R2 **strong-converge** 分支:

- §1 我对每条分歧的最终立场 + 让步(**逐条对上面四条 + A 侧守住的那条**)
- §2 **联合 verdict**(单一 · 200-400 字 · 仍冲突处显式标 `unresolved`)
- §3 残余分歧降级为 v0.2 note(**每条须给 `due_gate`**)
- §4 W 形态产出的初步草稿建议(**逐项对应** verdict-only / decision-list 四列+obligation path 列 / refactor-plan)

**700-1200 字。**

## When done

写 `.codex-outbox/queues/009-pM3prime/20260814T051500Z-009-pM3prime-forge-v4-p3r2.md`:

- **Completed**: <ISO>
- **Files written**: <list + word count>
- **Headline**: <一句话>
- **Verdict**: CLEAN | CONCERNS | BLOCK
- **Convergence**: converged | unresolved(**若 unresolved 必须列出哪几条**)
- **Ruling on A-side held position**: 具名敞口 vs 优化 / v0.2 note 必带 due_gate / due_gate 取值(**三问必答**)
- **v0.2 notes**: 逐条 + 各自 due_gate
- **Notes for Claude Code**: synthesizer 需要知道的约束

⚠ Filename **必须**与 inbox basename 完全一致:`20260814T051500Z-009-pM3prime-forge-v4-p3r2.md`
