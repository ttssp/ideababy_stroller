# Codex Task · 009-pM3prime · forge v4 · P2(参照系评估)

**Queue**: 009-pM3prime
**Created**: 2026-08-14T04:20:00Z
**Recommended model**: gpt-5.6-sol
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~8-14k
**Kickoff form**: reuse-session
**Forge version**: v4
**Phase**: P2

## Session hint(reuse-session · 省 token)

**你已读过(本轮请勿重读)**:
- `.claude/skills/forge-protocol/SKILL.md` 的 P1 部分
- `discussion/009/009-pM3prime/forge/v4/forge-config.md`(含五个 TEXT 块)
- 你自己的 `discussion/009/009-pM3prime/forge/v4/P1-GPT55xHigh.md`
- X 中全部 10 项标的(你 P1 outbox 已确认全部可达、零 skipped)

**本轮新增需读**:
1. `discussion/009/009-pM3prime/forge/v4/P1-Opus47Max.md`(**对方 P1 · 现在可以读了**)
2. `discussion/009/009-pM3prime/forge/v4/P2-Opus47Max.md`(**对方 P2 · ⚠ 先读它的 §0**)
3. `.claude/skills/forge-protocol/SKILL.md` 的 **P2 部分**
4. `discussion/009/009-pM3prime/forge/v4/moderator-notes.md`(若存在,binding)

## 🔴 本轮最重要的一件事

**A 侧(Opus)的 `WebSearch` 在环境层故障**(后端 400,连试三次,与查询无关),
只剩 arXiv 结构化 API + 对已知 URL 的定向抓取,**没有开放网关键词发现能力**。
A 侧已在其 P2 §0 自评:**它无法承担「双方零重叠检索」中属于它的那一半保证**。

⇒ **本轮正交面的保证主要落在你身上。**

- 若你的 `WebSearch` **可用**:请**大力使用**,并刻意避开 A 侧已覆盖的四个面
  (标注分歧即信号 / drift-triggered 重训 / OPA Gatekeeper / Chrome-Blink 弃用)。
  去找 A 侧因为只能定向抓取而**结构上不可能发现**的东西。
- 若你的 `WebSearch` **也不可用**:请在 §1 **显式声明**,不要用定向抓取伪装成检索面覆盖。
  此时本轮 SOTA 对标应被双方共同判为**降级**,写进你的 outbox `Notes for Claude Code`。

⚠ **K 的回声室警告是 binding 的**:P1 阶段我们四题倾向**逐条相同**
(工程纪律 refactor / 架构 refactor / 测量效度 new / ①refactor ②不可信 ③new ④refactor)——
**这是本 fork 连续第四轮 P1 零分歧收敛**。v2 之所以能破,是双方 P2 各自**推翻了自己 P1 的一部分**。
**请以推翻你自己 P1 的某一条为目标去检索**,不要去找支持它的材料。

## HARD CONSTRAINTS

- 允许且**要求** web search(mode = 对标 SOTA);A 侧已跑 6 条,**你的检索必须尽量与之零重叠**
- 禁:tech-stack 深潜 / pricing / 实施细节
- 不允许写 PRD / dev plan / refactor 详细方案(那是 P3R2 之后)
- §3 必须明确写出**你 P1 的哪一条被本轮证据推翻或削弱**(若真的一条都没有,必须显式说明并给理由 —— 但请先认真找)

## A 侧 P2 已覆盖的检索面(供你避开 · 详见其 §1 表)

1. 标注不一致是否为信号(NUTMEG / CrowdTruth 2.0 / Perspectivist / hate-speech 分歧建模)
2. 多数票聚合与信度(CODA-19 / ACORN)
3. 校准节奏 定期 vs drift-triggered(LEAF 等 8 篇)
4. ML pipeline provenance 最小充分集 —— **否定性结果**(SOTA 不定义「哪些记账须挺过回滚/崩溃」)
5. OPA Gatekeeper dryrun→warn→deny —— **否定性结果**(不给晋级判据)
6. Chrome/Blink 弃用→移除 —— 「**There is no threshold for which removal is necessarily safe**」,改规定过程量
7. 预注册/空结果强制申报 —— **反向结果**(预注册收益被质疑,可能加剧发表偏倚)⇒ A 侧据此**撤回**了议题① 的一条外部支撑

## A 侧 P2 的四条自我修正(你可直接压)

- **(a)** M1=pass **不是中性的好消息**,是「朝已知效度风险方向的弱证据」(一致率上升无法区分噪声去除 vs 歧义抹除)
- **(b)** 「按事件校准」也是 intuitive heuristic,drift 文献明说这类启发式 provably suboptimal;须补 Chrome 式**过程量**,以**被消费的批次**计数
- **(c)** 「判据形态仓里已有」只对一半:仓内是**结果型**判据,工业先例是**过程型**判据,应合取
- **(d)** ① 只能靠本地机制论证,**不得援引外部规范权威**;窄读的辩护因此**更值得认真对待**

## Write

`discussion/009/009-pM3prime/forge/v4/P2-GPT55xHigh.md`,按 P2 template:

- §1 SOTA 对标(**表格** · 每行:检索面 / 关键发现含逐字引文 / 影响哪一题)
  - ⚠ 开头必须声明你的 search 是否可用、跑了几条、与 A 侧重叠几条
- §2 用户外部材料消化(K 未给外部链接,简述即可)
- §3 修正后的视角(**你 P1 哪些站住、哪些被推翻** —— 这是本节的重点)

**600-1100 字。**

## When done

写 `.codex-outbox/queues/009-pM3prime/20260814T042000Z-009-pM3prime-forge-v4-p2.md`:

- **Completed**: <ISO>
- **Files written**: <list + word count>
- **Headline**: <一句话>
- **Verdict**: CLEAN | CONCERNS | BLOCK
- **Search status**: 可用/不可用 · 跑了几条 · 与 A 侧重叠几条(**必填**)
- **Self-overturns**: 你 P1 被本轮推翻/削弱的条目(**必填** · 若为空须给理由)
- **Notes for Claude Code**: 含是否判本轮 SOTA 对标降级

⚠ Filename **必须**与 inbox basename 完全一致:`20260814T042000Z-009-pM3prime-forge-v4-p2.md`
