# Codex Task · 009-pM3prime · forge v4 · P3R1(联合收敛 R1)

**Queue**: 009-pM3prime
**Created**: 2026-08-14T04:50:00Z
**Recommended model**: gpt-5.6-sol
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~7-12k
**Kickoff form**: reuse-session
**Forge version**: v4
**Phase**: P3R1

## Session hint(reuse-session)

**你已读过(本轮请勿重读)**:forge-config.md(含 TEXT 块)· X 全部 10 项 · 你自己的 P1/P2 ·
对方 P1/P2 · forge-protocol 的 P1/P2 部分。

**本轮新增需读**:
1. `discussion/009/009-pM3prime/forge/v4/P3R1-Opus47Max.md`(对方 P3R1 · **本轮主要对象**)
2. `.claude/skills/forge-protocol/SKILL.md` 的 **P3R1 部分**
3. `discussion/009/009-pM3prime/forge/v4/moderator-notes.md`(若存在,binding)

## HARD CONSTRAINTS

- **NO new search this round**(避免 R1 又开搜索发散;P2 的证据已够)
- 必须给**初步 verdict**(单段 3-5 行,**直接给立场,不要 hedge**)
- 必须列 **≥1 关键分歧**(若真无,§3 写「无 —— 双方对齐,R2 重点在草案」并说明理由)
- 引用对方原句 **≤15 words**
- 必须做 **§4 与 K 的对齐性自检**(逐条 ✅/⚠/❌ · 这是 quality bar,不得跳过)
- 收敛模式 = **strong-converge**

## 本轮状态

**P1 = 回声(四题倾向逐条相同,本 fork 连续第四轮);P2 = 真对抗**(你我检索面零重叠,
且各自推翻了自己 P1 的一部分:A 侧 4 条 / 你 3 条)。⇒ **本轮 verdict 的可信度落在 P2,不在 P1。**

A 侧认为本轮最强的两条外证据是**你带来的**:
- NIST:统计控制「can only guarantee comparability among measurements」——不保证准确
- RFC 5848/9162:缺口可检测审计存在,但**审计失败后的动作不规定**
  ⇒ A 侧据此判定**议题⑤的形态非本仓独有**,处置重点应从「继续找第五处」转向「动作绑定」

## A 侧 P3R1 的三条关键分歧(**请逐条裁**)

**分歧 1 · `OB-15` 写实现还是写判据 —— A 侧已让步采你的立场。**
A 侧原主张「finding (a) 不可 defer,落点是写穿透」,现改为:兑现判据写成三前提
(① 观察边界预先固定 ② exposure 可计 ③ 失败批次内容身份可证),实现路线交 build 侧选,
**证不出三前提则回退到写穿透**。⇒ 请确认这个「A + 判据 + 证不出则 B」形态是否忠实于你引的
NIST censored analysis(注意其前提「exact times of failure are recorded」),或需要修正三前提的措辞。

**分歧 2 · ③ 现在到底能不能定出周期 —— A 侧比你保守,需要你压。**
A 侧主张:你给的形态(每批监测 + control 数据决定重校准)**正确但当前不可执行**,
因为 NIST 的 control-chart 路径要求**多点历史**,而本仓全部可用点只有
两跑 Jaccard 0.44、本次 J_1=0.4078、历史密度 41/73/71 ⇒ **控制图画不出来**。
故此刻只能定三段式:(i) 每批必测并落库以开始积累 (ii) 身份/口径变化立即启动新基线
(iii) **周期本身显式标为「待 control history 达 N 点后回填」**。
⇒ **若你认为现在就能定周期,请给出用现有 1-3 个点定周期、且不落入「看数后设门槛」的具体路径;
否则请收敛到三段式,并把 N 的取值列为 v0.2 note。**

**分歧 3 · ④ 的「可回退」与本仓 C6「预注册不可回改」正面冲突 —— 你 P2 未处理,这是本轮唯一双方都没解的结构性冲突。**
你引的 Kubernetes KEP 把 rollback metrics/测试列为并列必填;ModSecurity 要求 DetectionOnly 期真实误杀审阅。
但本仓 C6「预注册不可回改」+ forge v2「移动球门」判据意味着:**一旦 convert 成门禁并升 `caliber_version`,
退回 report-only 会被自己的反自欺条款挡住** ⇒ **convert 在本仓是单向门**,而工业先例把可回退当作 convert 的**前提**。
两条出路:
- (a) 承认单向 ⇒ convert 的证据门槛必须**高于**工业先例(错了不能退)
- (b) 设计不违反 C6 的回退形态(如:回退 = 再升一个新 `caliber_version`,旧 gate 结论**作废**而非**篡改**)
A 侧倾向 **(b)**,理由是 (a) 会把 convert 推到永远达不到。**请独立裁,并检查 (b) 是否真的不违反 C6 与「移动球门」判据。**

## 另需你表态的两点

1. **A 侧 §4 自检把 ③ 判为对 K「部分未满足」**(周期未定形)。strong-converge 下这是否可接受?
   还是应当在 v0.2 note 里给一个可执行的 N?
2. **per-market 分解**:双方 P1 独立撞到「结果矩阵零 per-market vs PRD §6 科学重判条件 1 要求
   至少一个 per-market 跨次读数」。它**不在四条义务的字面范围内**,把它列为本轮硬产出是否算判据膨胀
   (v3 §underweights 4 的教训)?**请给明确 include / exclude。**

## Write

`discussion/009/009-pM3prime/forge/v4/P3R1-GPT55xHigh.md`,按 P3R1 template:

- §1 整合摘要
- §2 我的初步 verdict(单段 3-5 行 · 直接给立场)
- §3 关键分歧清单(逐条:我的立场 / 对方立场 + ≤15 words 引用 / R2 期望收敛方向)
  - **必须对上面 A 侧三条分歧逐条裁**,可另加你自己的新分歧
- §4 与 K 的对齐性自检(逐条 ✅/⚠/❌)

**600-1000 字。**

## When done

写 `.codex-outbox/queues/009-pM3prime/20260814T045000Z-009-pM3prime-forge-v4-p3r1.md`:

- **Completed**: <ISO>
- **Files written**: <list + word count>
- **Headline**: <一句话>
- **Verdict**: CLEAN | CONCERNS | BLOCK
- **Rulings on A-side disagreements**: 分歧 1 / 2 / 3 各一行裁定(**必填**)
- **New disagreements raised**: 你新提的分歧(若有)
- **Notes for Claude Code**

⚠ Filename **必须**与 inbox basename 完全一致:`20260814T045000Z-009-pM3prime-forge-v4-p3r1.md`
