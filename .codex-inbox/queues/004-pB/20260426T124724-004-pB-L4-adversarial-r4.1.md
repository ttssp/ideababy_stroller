# Codex Task · 004-pB · L4 Adversarial Review Round 4.1 (极短审, 收口最后 4 处)

**Created**: 2026-04-26T12:47:24Z
**Recommended model**: gpt-5.4
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~3-6k (R4.1 仅验证 4 处遗漏真清)

---

## 你的角色

R4 verdict 是 **CONCERNS** (不是 BLOCK), 你自己说 "**主修补大体到位...都还是文字/任务包层面的残留, 不需要回滚 PRD 或重启 spec 决策**"。

operator 选了路径 (a) — **极小 R4.1 文案收口, 把 4 处遗漏清干净**。

operator 直接 Edit (不派 spec-writer subagent), 因为是文字级 patch。

R4.1 仅清你 R4 review 中明确指出的 4 处 + 1 处衍生:

1. `architecture.md:492` `commit 阶段 wall-clock` → 全程口径
2. `architecture.md:636` 字面 `≤ 10s` → 改写, 避免 grep 误判
3. `T020.md:56-57` OP-1 风险快照度量 `commit 阶段 wall-clock` → 全程
4. `T020.md:144` alert banner "降级 B-lite 链接" → CLI/runbook 文案

bump architecture.md `0.3.1 → 0.3.2`, Revision History 加 v0.3.2 entry。

T020.md 无 version field, 直接 Edit (R4 spec-writer 之前已确认 task 文件直接改即可)。

---

## 你的任务 (极短审)

仅验证 R4 4 处 + 1 衍生 finding 真清干净, 确认无新 finding, 给出 **CLEAN** 或最多 **CONCERNS (微 medium 残留)** 裁决。

### 必验证 (R4 引用 4 处 + 1 衍生)

1. **architecture.md:492** — 是否真改为 "commit 入档 wall-clock < 1s; 全程 wall-clock < 30s 含 draft"?
2. **architecture.md:636** — 字面 `≤ 10s` 是否真消失 (改写为 "R3 不再有 R2 的 10 秒宽容期")?
3. **T020.md:56** — OP-1 录入耗时 p95 度量是否改为 "**GET /decisions/new → POST commit 200 OK 全程 wall-clock**"?
4. **T020.md:144** — alert banner verification 是否改为 CLI 文案 (`scripts/toggle_b_lite.py engage` + `docs/runbooks/b_lite.md`)?

### 横扫验证

1. **architecture.md** 全文搜 `commit 阶段 wall-clock` 是否只在 Revision History v0.3.2 entry 残留 (审计历史, 不算违规)?
2. **architecture.md** 全文搜字面 `≤ 10s` 或 `miss ≤ 10s` 是否只在 Revision History / release_log schema 注释残留 (审计/历史, 不算违规)?
3. **T020.md** 是否还有任何 "降级 B-lite 链接" 文案残留 (除了 R3/R4 修订说明)?
4. **算术**: sum(T001-T022 estimated_hours) 仍 = **160h**?

---

## 必读

- R4 outbox: `.codex-outbox/20260426T123538-004-pB-L4-adversarial-r4.md`
- R4.1 修订:
  - `specs/004-pB/architecture.md` v0.3.1 → **v0.3.2** (Revision History 第 23 行 v0.3.2 entry)
  - `specs/004-pB/tasks/T020.md` (line 56, line 144 直接 Edit)

---

## 写输出 (`.codex-outbox/20260426T124724-004-pB-L4-adversarial-r4.1.md`)

```markdown
# Adversarial Review · 004-pB · R4.1

**Reviewed**: 2026-04-26T<ISO>
**Reviewer**: GPT-5.4 xhigh
**Spec version**: v0.3.2

## R4 4 finding 真清验证

| R4 finding | R4.1 修订 | 实际清了吗? | 行号佐证 |
|-----------|-----------|-------------|---------|
| arch.md:492 commit 阶段 wall-clock | 改全程口径 | ✅/⚠️/❌ | arch.md:492 |
| arch.md:636 字面 ≤10s | 改写避免 grep 匹配 | ✅/⚠️/❌ | arch.md:636 |
| T020.md:56 OP-1 commit 阶段 | 改全程口径 | ✅/⚠️/❌ | T020.md:56 |
| T020.md:144 banner 降级 link | 改 CLI/runbook | ✅/⚠️/❌ | T020.md:144 |

## 横扫验证

- [ ] arch.md 全文 `commit 阶段 wall-clock` 只在 Revision History?
- [ ] arch.md 全文 `≤ 10s` 只在 Revision History / schema 注释?
- [ ] T020.md 无 banner link 残留?
- [ ] sum estimated_hours = 160h?

## Verdict

- **CLEAN** (4 finding 全清, spec 可进 build) ← 预期
- **CONCERNS** (有微 medium 残留, 但 ship-able)
- **BLOCK** (主要 finding 仍残留 — 极不可能, 因为 R4.1 是文字级直改)

## Why this verdict

(1-2 句, 引行号)

## 下一步建议

如 CLEAN: operator 走 `/parallel-kickoff 004-pB <task-ids>` 进 build phase。

如 CONCERNS: operator 评估剩余 medium 是否值得再补一轮, 或带风险 ship。
```

---

## 写法要求

- **极简**: R4.1 是 4 处文字级直改, 你 5 分钟内验证完
- **每条引行号**
- **不审 R3 / R4 已 close 项** (除非 R4.1 改动让它们退化)
- **中文输出**

开始。R4.1 后, 如果 CLEAN 我就走 /parallel-kickoff 进 build。
