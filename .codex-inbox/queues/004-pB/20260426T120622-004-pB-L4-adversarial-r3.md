# Codex Task · 004-pB · L4 Adversarial Review Round 3

**Created**: 2026-04-26T12:06:22Z
**Recommended model**: gpt-5.4
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~12-18k (R3 是定向验证, R1+R2 已 prime context)

---

## 你的角色

你是 GPT-5.4 xhigh, 对一份 R2 BLOCK 后做了 R3 surgical 修订的 spec 包做 **R3 adversarial review**。

R2 给了 5 个 BLOCK + 4 个 ⚠️ (R1 没真 close 的) + 1 个 HIGH. spec-writer 现在宣称 R3 surgical 修了所有 13 项。**human 选了路径 A (守 PRD 原口径), 不动 PRD**。

你的任务 (按优先级):

1. **验证 R3 是不是真的守住了 PRD §S1 / §6 O5 原口径** (R2 spec-writer 偷偷改 PRD 度量被你抓了, R3 这次得真守住)
2. **验证 R2 5 个 BLOCK 是否真消失** (引行号佐证)
3. **验证 R1 4 个 ⚠️ + 1 个 HIGH 是否真 close**
4. **检查 R3 是否引入新问题** (尤其 T008 503 失败路径 / Rebuttal fast-path 工程估算 / lane contract-level 隔离)
5. **判断 spec 是否可以进 build** (CLEAN / CONCERNS / BLOCK)

---

## 路径 A 的核心承诺 (R3 必守, 你必查)

PRD §S1 / §6 O5 原口径: "**单次决策录入 < 30 秒**" — 是从 GET `/decisions/new` 到 commit 完成入档**全程** wall-clock < 30s, **包含 draft 阶段 LLM 调用**。

R2 spec-writer 暗中把它改成"preview 200 OK 后到 commit 200 OK < 30s" — 把 draft 阶段 LLM 等待挪出 30s 计时之外. 这是 spec 越权改 PRD 的 anti-pattern, 被你 R2 review 抓出来。

R3 的核心 deliverable 就是 **回退这个改动**. spec-writer R3 报告说做到了, 你必须独立验证。

---

## 必读 (R3 修订核心 + R2 上下文)

### R2 review 原文 (必先看, 才知道每项 finding 是否真修了)
- `.codex-outbox/20260425T042650-004-pB-L4-adversarial-r2.md` (70 行, R2 BLOCK verdict, 5 BLOCK + 4 ⚠️ + 1 HIGH)

### R1 review 原文 (R1 4 个 ⚠️ 在 R2 没真 close, R3 必须真 close)
- `.codex-outbox/20260425T030218-004-pB-L4-adversarial-r1.md` (104 行)

### R3 spec 修订 (主审对象)
- `specs/004-pB/spec.md` **v0.3** (Revision History 在 §0 之后, 列每项改动)
- `specs/004-pB/architecture.md` **v0.3** (Revision History 在 header 后, 重点看 §3.1 / §3.3 / §3.6 / §9 不变量)
- `specs/004-pB/risks.md` v0.3 (OP-2 mitigation 重写, 列出 R3 真砍的 task)
- `specs/004-pB/open-questions.md` v0.3 (Q11 删除, Q7 默认假设修订)
- `specs/004-pB/dependency-graph.mmd` (头部加 R3 修订注释)

### R3 task 修订 (采样必读 ≥ 7 个)
**核心**:
- `tasks/T007.md` — R3 加 contract-level lane 隔离 + runtime test
- `tasks/T008.md` — R3 删 placeholder fallback, 改 503 (B-R2-2 核心)
- `tasks/T009.md` — R3 timing test 度量改回**全程** wall-clock (B-R2-1 核心)
- `tasks/T010.md` — R3 cache warmer 范围明确 (仅 ConflictReport, 不预热 Rebuttal)
- `tasks/T013.md` — R3 Rebuttal fast-path 配置 (Sonnet 4.6 + max_tokens=100 + temperature=0.3 + timeout=3.0s)
- `tasks/T020.md` — R3 ImportError: pass → log warning + strict mode assert
- `tasks/T022.md` — R3 删 UI toggle, 改 CLI

**配套验证**:
- `tasks/T011.md` / `tasks/T015.md` / `tasks/T016.md` / `tasks/T017.md` / `tasks/T018.md` / `tasks/T019.md` / `tasks/T021.md` — R3 估时砍至总 160h 边界

### PRD / Context (验证 spec 仍 faithful)
- `discussion/004/004-pB/PRD.md` (尤其 §S1 + §6 O5)
- `discussion/004/HANDOFF-L4.md`

---

## 必验证项 (按 R2 finding 一一验证)

每项请勾 **✅ 真 close** / ⚠️ **部分 close** / ❌ **未 close** / ⛔ **新引入问题**, 引行号佐证。

### B-R2-1 验证 — spec 是否真守 PRD §S1 / O5 原口径?
**R3 修向**: spec.md §0 / §1 O5 / §3 C11 / §4 D13 / D19 / D21 / §6 O5 verification 全部回退到 PRD 全程 wall-clock 口径。

**你必须验证**:
1. **spec.md §0 一句话 framing 段**: 是否真的不再有 "preview 200 OK 后才计时" 这种偷换概念? 是否真的写"全程"?
2. **spec.md §1 O5 度量口径**: 起点是不是 "GET `/decisions/new` 200 OK"? 终点是不是 "POST commit 200 OK"? 是不是说"全程 wall-clock < 30s 包含 draft 阶段 LLM"?
3. **spec.md §3 C11**: rigidity 是不是 "Hard (kill-switch)"? 是不是没有"preview 后才计时"的例外?
4. **spec.md §4 D13**: 是不是真说 "全程 wall-clock < 30s 包含 draft 阶段 LLM 调用"? 不分段?
5. **spec.md §4 D19**: 是不是删除了 R2 的 "draft 阶段 LLM 同步等待 ≤ 10s 不算入 30s" 字样?
6. **spec.md §4 D21**: 是不是承认 Rebuttal 不可缓存? Rebuttal fast-path 配置 (Sonnet 4.6 / max_tokens / temperature) 是否合理 ≤ 3s?
7. **spec.md §6 O5 verification**: timing test 度量起点终点是不是改回全程?
8. **spec.md §7 Glossary**: "录入 < 30 秒" 这个术语是不是统一为 GET → commit 全程?
9. **spec.md §8**: R2 引入的 Q11 (cache miss 5-10s 例外) 是不是真删除了?
10. **architecture.md §3.3**: sequence diagram 起点是不是 GET /decisions/new, 终点是 commit 200 OK? cache miss > 5s 是 503 还是 placeholder fallback?

### B-R2-2 验证 — T008 placeholder fallback 是否真删了?
**R3 修向**: T008 删除 placeholder fallback, 超时 → 503 + 错误页, 不允许 commit 用 placeholder 继续。

**你必须验证**:
1. **T008.md Goal / Implementation plan**: 真的没有 "fallback to placeholder no-divergence ConflictReport" 这种字样?
2. **T008.md Verification**: 加 `test_draft_timeout_blocks_commit` 单测 (mock cache miss + LLM 6s 超时 → 503)?
3. **T008.md Verification**: 加 `test_no_placeholder_in_conflict_report` 单测 (cache miss + LLM 失败 → 503 raise, 不写 placeholder)?
4. **architecture.md §3.3 sequence diagram**: 503 失败路径是否画清楚 (替代 placeholder fallback)?
5. **architecture.md §9 不变量**: 是否新增 #13 "ConflictReport.signals 永远来自真实 LLM, 无 placeholder fallback"?
6. **路径选择审视**: 503 这种 hard fail 在用户 UX 视角是否 acceptable? Codex R3 应该判断: 比起 placeholder 蒙混 commit 危害, 503 + "请等 cache warmer / 重试" 是不是合理? (这是 spec-writer R3 主动列出的待审 #1)

### B-R2-3 验证 — D21 latency 论证是否诚实?
**R3 修向**: 诚实承认 Rebuttal 不可缓存; Rebuttal LLM 走 fast-path (Sonnet 4.6 / max_tokens=100 / temperature=0.3 / timeout=3s).

**你必须验证**:
1. **spec.md §4 D21**: 真的承认 Rebuttal 不可缓存 (因输入依赖 draft.intended_action + draft_reason)?
2. **architecture.md §3.6**: cache warmer 是否明确"仅 ConflictReport, 不预热 Rebuttal"?
3. **T013.md**: 是不是真有 fast-path 配置 (Sonnet 4.6 / max_tokens / temperature / timeout)?
4. **T013.md Verification**: 是否加 latency 测试 (mock LLM 2s, 测完成 < 3s)?
5. **关键审视**: Sonnet 4.6 + max_tokens=100 + 极简 prompt 在 Anthropic API 上**实测 95th percentile** 真能 ≤ 3s 吗? 这是工程估算, 是 spec-writer R3 主动列出的待审 #2. 你的判断 (基于你对 Claude API 的 benchmark 知识)?
6. **失败模式**: 若实测 p95 > 3s, 全程 30s 还能保住吗? D21 说 "若 95th percentile draft > 5s 走 OP-1 mitigation", 这个 fallback 路径在 risks.md OP-1 是不是真的有完整 mitigation chain (UX 降级 / B-lite)?

### B-R2-4 验证 — task pack 真的砍到 ≤ 160h 了吗?
**R3 修向**: 真砍 21h (T011 -3h / T015 -5h / T016 -3h / T018 -1h / T019 -3h / T021 -4h / T022 -2h), 181h → 160h 边界.

**你必须验证**:
1. **T011/T015/T016/T018/T019/T021/T022 estimated_hours 字段**: 真的减少了相应 hour?
2. **T011 (错位矩阵)**: 真的删 CSS 着色 / 排序? Goal/Implementation plan 砍简化?
3. **T015 (月度 review)**: 真的删 LLM 摘要 / PNG 图表? 仅 markdown? 这砍法是否影响 PRD §S4 monthly review 期望? (spec-writer R3 主动待审 #3)
4. **T016 (笔记 wiki)**: 真的删 LLM 语义去重? 仅 content_hash 同步去重? 是否影响 R7 防"学习假装" + PRD §S4 笔记 wiki 自动去重承诺?
5. **T018 (post-mortem)**: 真的删自动 nudge / 7-30 天后弹窗?
6. **T019 (学习检查)**: 真的删 LLM 抽取 / 出题, 改 human 手编 master checklist yaml + agent 仅评分?
7. **T021 (onboarding)**: 真的删 in-app tooltip? 模板合并为 3 个?
8. **T022 (B-lite)**: 真的删 `/settings/b-lite` UI toggle, 改 CLI? `scripts/toggle_b_lite.py` 路径在 alert banner 触发时, human 真愿意切到终端跑 CLI 吗? (spec-writer R3 主动待审 #4)
9. **risks.md OP-2 mitigation**: 真的写"已砍至 160h 边界" + 列 7 个 task 的细分?
10. **spec.md §5 task breakdown 总估**: 真的写 "~120-160h" (不再 165h)?
11. **算术验证**: 自己求 sum(T001-T022 estimated_hours) — **必须真的等于或 ≤ 160h** (R2 你抓出来 R2 报 152h 实际 181h 三个数字打架, R3 不能再有这个问题)

### B-R2-5 验证 — cache miss 例外是否真删
**R3 修向**: B-R2-1 + B-R2-3 联合修复后, Q11 (cache miss 35-40s 例外) 自然消失。

**你必须验证**:
1. **spec.md §8**: Q11 真删除了?
2. **open-questions.md**: Q11 真改为 "R3 删除" 段?

### ⚠️ R1 4 个 ⚠️ + 1 HIGH 是否真 close

#### ⚠️ B1 (R2 verdict 标 ⚠️, 因 R2 越权改 O5 度量假装修了)
**R3 修向**: B-R2-1 + B-R2-2 联合修复. O5 度量改回原口径 + T008 删除 placeholder.
**Codex R3 验证**: 这两条都真做了, B1 才真 close. 否则继续 BLOCK.

#### ⚠️ B3 (R2 verdict 标 ⚠️, lane 隔离仅到 AST)
**R3 修向**: architecture.md §3.1 IDL 加 contract-level 隔离 (lane __init__ 强制 (LLMClient, SourceDataProvider), 不接受 registry / 其他 lane); T007 加 runtime test (`test_lane_runtime_contract.py`).

**你必须验证**:
1. **architecture.md §3.1**: lane __init__ signature 真的限制为 `(LLMClient, SourceDataProvider)`? 不接受 registry 或 list[StrategyModule]?
2. **SourceDataProvider** 是否仅暴露**只读** advisor_report / portfolio / ticker / env_snapshot?
3. **T007.md Verification**: 是否真有 runtime test (用 inspect.signature 检查 lane __init__ 参数, 强制不含 registry/modules/lanes)?
4. **风险**: 这种 contract 是否过紧 (e.g. 未来想加 metrics_collector 依赖时, 必须改 SourceDataProvider 才能加, 不能直接给 lane)? (spec-writer R3 主动待审 #5)

#### ⚠️ H1 (R2 verdict 标 ⚠️, T015/T017 task header 仍有旧 blocks)
**R3 修向**: T015.blocks: [T022], T017.blocks: [], T020.depends_on: [T002, T003, T010] (移除 T015/T017 依赖 T020); pause_pipeline ImportError: pass 改为 log warning + strict mode assert.

**你必须验证**:
1. **T015.md**: blocks 字段真的不再含 [T020] 旧约束?
2. **T017.md**: blocks 字段真的不再含 [T020]?
3. **T020.md**: depends_on 真的限定 [T002, T003, T010]?
4. **T020.md pause_pipeline.py**: ImportError 处理改为 logger.warning + os.environ check + AssertionError?
5. **T020.md Verification**: 加 `test_pause_all_in_strict_mode_raises` 单测?
6. **dependency-graph.mmd**: 头部注释是否反映 T015/T017 不 block T020?
7. **风险**: strict mode 默认值 + CI 是否要默认 strict? (spec-writer R3 主动待审 #6)

#### ⚠️ M2 (R2 verdict 标 ⚠️, B-lite 定义不一致)
**R3 修向**: T022 删整个 UI toggle, 仅 CLI + runbook + meta_decisions audit 表; spec.md §7 Glossary B-lite 定义同步.

**你必须验证**:
1. **T022.md file_domain**: 真的删除了 router_meta_decisions.py / templates/settings/b_lite.html?
2. **T022.md Verification**: 加 "源码 grep `settings/b-lite` 0 命中" 验证?
3. **spec.md §7 Glossary B-lite**: 是不是写 "v0.1 不实现 UI toggle, 也不实现 /settings/b-lite 路由"?

---

## 新审查点 (R3 引入的, 必查)

### N1 · T008 503 失败模式 vs UX
spec-writer R3 待审 #1: 503 这种 hard fail 是否 acceptable?

**思考方向**:
- 比起 R2 placeholder 蒙混 commit (实质违反 R10 + 假装看到分歧), 503 + "请等 cache warmer 完成" 是不是合理?
- cache warmer (D11) 在 advisor_report 入库时预热 30-50 关注股 × 当周 advisor_week_id, 命中目标 ≥ 95%. 实际首次启动 / 关注股新增时, miss 率会高一段, human 用户体感是不是可接受?
- OP-1 mitigation 路径 (cache miss > 5s 触发 B-lite) 是否真的有详细 chain 在 risks.md?

**你的裁定**: 是 BLOCK / CONCERNS / acceptable?

### N2 · Rebuttal fast-path ≤ 3s 工程估算
spec-writer R3 待审 #2.

**思考方向**:
- Sonnet 4.6 + max_tokens=100 + 极简 prompt 在 Anthropic API benchmark, p95 latency 是多少? (你应该有这方面知识)
- 网络抖动 / API 限流 时 p95 会超 3s 吗?
- 若 p95 实测 > 3s, D21 说"走 OP-1", 但首次发生时 human 已经经历几次 503 才意识到 — 是否需要 spec 加更明确的"自动 fallback 到 conflict report only (跳过 rebuttal)"? 但这又会破坏 R6 "calibration first" framing (没反方就少一道 calibration).

**你的裁定**: 是 BLOCK / CONCERNS / acceptable?

### N3 · T015 / T016 砍法是否影响 PRD scope?
spec-writer R3 待审 #3.

**思考方向**:
- T015 月度 review 砍到纯 SQL 聚合 + markdown, 是否影响 PRD §S4 monthly review 的 calibration 证据聚合期望?
- T016 笔记 wiki 砍到只 content_hash 同步去重, 是否影响 R7 防学习假装 (3 个月自查)?
- 这些砍法是 R3 真砍而非纸上 cut list, 但是否破 PRD scope IN?

### N4 · T022 CLI 替代 UI toggle
spec-writer R3 待审 #4.

**思考方向**:
- alert banner 触发时 (失败告警), human 看到 "运行 CLI" 字样, 在情绪低迷状态下是否真愿意切终端?
- 比起 UI 一键 toggle, CLI 摩擦更高 → 可能 human 不切, 系统继续坏下去?
- 但 UI toggle 砍掉省 2h, 在 160h 边界算计中是否真的 worth it?

### N5 · lane __init__ contract 是否过紧
spec-writer R3 待审 #5.

**思考方向**:
- 未来想加 metrics_collector / tracing / feature flags 依赖时, 必须经 SourceDataProvider 才能加 — 这是不是 over-engineering?
- 但反方: 严苛 contract 是 R9 红线工程化的必要代价 (R2 抓到 R2 spec 仅 AST 不防 DI), R3 加严是修对 ⚠️ B3?

### N6 · strict mode 默认值
spec-writer R3 待审 #6.

**思考方向**:
- pause_pipeline ImportError 在 T015 ship 前是 graceful warning, ship 后变 strict mode assert. CI 默认是不是要 strict?
- 若 CI 默认 graceful, 风险: T015 ship 后 wiring 静默失效, alert 触发时 pause 不真生效.
- 若 CI 默认 strict, T015 ship 前 CI 全失败 (ImportError raise) — 不能容忍.
- 折中: CI 默认 strict, T015 ship 前在 T015 prerequisite test 中 skip pause test? 这是 spec-writer 留给 Codex 判断的。

---

## 写输出 (`.codex-outbox/20260426T120622-004-pB-L4-adversarial-r3.md`)

```markdown
# Adversarial Review · 004-pB · R3

**Reviewed**: 2026-04-26T<ISO>
**Reviewer**: GPT-5.4 xhigh
**Spec version**: v0.3

## R2 BLOCK 验证矩阵 (5 条)

| R2 ID | R3 修订声明 | 实际修了吗? | 行号佐证 | 备注 |
|-------|------------|-------------|---------|------|
| B-R2-1 | spec 改 PRD O5/C11 度量口径 → 回退 | ✅/⚠️/❌ | spec.md:NN; arch.md:NN | ... |
| B-R2-2 | T008 placeholder fallback → 删 | ✅/⚠️/❌ | T008.md:NN; arch §9 #13 | ... |
| B-R2-3 | D21 latency 论证 → 诚实 + fast-path | ✅/⚠️/❌ | spec.md:NN; T013.md:NN | ... |
| B-R2-4 | task pack → 真砍 ≤ 160h | ✅/⚠️/❌ | sum hours; risks.md:NN | ... |
| B-R2-5 | cache miss 例外 → Q11 删 | ✅/⚠️/❌ | spec.md:NN; open-q.md:NN | ... |

## R1 4 ⚠️ + 1 HIGH 真 close 验证

| R1 ID | R3 修订声明 | 实际修了吗? | 行号佐证 |
|-------|------------|-------------|---------|
| ⚠️ B1 | O5 + T008 联合 close | ✅/⚠️/❌ | ... |
| ⚠️ B3 | contract-level lane 隔离 | ✅/⚠️/❌ | ... |
| ⚠️ H1 | T015/T017 metadata + strict mode | ✅/⚠️/❌ | ... |
| ⚠️ M2 | T022 CLI + Glossary 一致 | ✅/⚠️/❌ | ... |

## 新引入问题 (R3 衍生)

| # | Location | Issue | Severity | Fix direction |

## R3 spec-writer 主动提出 6 个待审点

1. T008 503 失败模式 (B-R2-2 替代) — 你的裁定:
2. Rebuttal fast-path ≤ 3s 工程估算 (B-R2-3 配套) — 你的裁定:
3. T015/T016 砍法是否影响 PRD scope — 你的裁定:
4. T022 CLI 替代 UI toggle (M2 + B-R2-4) — 你的裁定:
5. lane __init__ contract 过紧 (⚠️ B3) — 你的裁定:
6. strict mode 默认值 (⚠️ H1) — 你的裁定:

## 算术验证 (B-R2-4 关键)

sum(T001-T022 estimated_hours) = ___ h
spec.md §5 总估 = ___
risks.md OP-2 mitigation 写 = ___

是否三处一致 ≤ 160h?

## Verdict

选其一:
- **CLEAN** (R2 5 BLOCK + R1 4 ⚠️ 全 close, 无新 BLOCK; spec 可进 build)
- **CONCERNS** (大部分 close, 1-2 项有遗留或新 medium; operator 决定是否带风险 ship)
- **BLOCK** (R2 BLOCK 仍有未真 close 的, 或 R3 引入新 blocker)

## Why this verdict

(3-5 句, 引行号)

## 给 operator 的最终建议 (≤ 5 条)
```

---

## 写法要求

- **每条验证必须引行号**
- **算术验证 (B-R2-4) 是关键**: R2 你抓出三处数字打架, R3 必须真一致 ≤ 160h. 你必须自己 sum 验证, 不能信 spec-writer 报告
- **守 PRD 原口径是 R3 唯一目标**: 若 spec.md §0 / §1 / §3 / §6 仍有"preview 后才计时" "draft 阶段不算" 之类字样, 直接 BLOCK
- **不假装 R1/R2 已全 close**: 即使 spec-writer 报告说全修了, 你独立验证. 任何"声明修了但实际没改" → BLOCK
- **中文输出**

## Workflow

1. 读 R2 outbox + R1 outbox
2. 读 spec.md 全文 (尤其 §0 / §1 / §3 / §4 D13/D19/D21 / §6 / §7 / §8 / Revision History)
3. 读 architecture.md §3.1 / §3.3 / §3.6 / §9 不变量 / Revision History
4. 读 task md (T007/T008/T009/T010/T013/T020/T022 至少, 其他酌情)
5. **算术验证**: sum estimated_hours, 对照 spec.md §5 + risks.md OP-2
6. 6 个 R3 主动待审点逐一裁定
7. 写 outbox, 引行号

开始。这是 R3, 是路径 A 的最后一次大修, 如果还 BLOCK 就需要 human 重新决定路径 (改 PRD 度量口径走路径 B / 或 park 重 scope)。
