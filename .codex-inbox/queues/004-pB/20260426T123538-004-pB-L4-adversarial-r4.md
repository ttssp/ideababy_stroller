# Codex Task · 004-pB · L4 Adversarial Review Round 4 (短审)

**Created**: 2026-04-26T12:35:38Z
**Recommended model**: gpt-5.4
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~6-10k (R4 是定向收口验证, 极短审)

---

## 你的角色

你是 GPT-5.4 xhigh, 对一份 R3 BLOCK 后做了 R4 surgical 收口 patch 的 spec 包做 **R4 adversarial review**。

R3 verdict 是 **BLOCK 但只差临门一脚** — Codex 自己说 "**方向对了, 只差全文收口**", 并预言 "**R4 后预期不需要 human 改 PRD, 只需短审即可决定 CLEAN 或 CONCERNS**"。

R4 没改任何结构 / 决策 / estimated_hours, 仅做 **文字级 surgical patch** 让 spec package 全文一致回到 R3 已守住的 PRD 原口径。

你的任务**仅限**:
1. **验证 R3 4 个 finding 是否真收口** (引行号佐证)
2. **不审新结构** (R4 没改结构)
3. **不审 R1/R2/R3 已 close 的项** (除非 R4 改动让它们退化)
4. **判断 verdict**: CLEAN (全部真收口) / CONCERNS (1-2 项有遗留, ship-able with risk log) / BLOCK (主要 finding 仍未真改)

---

## R3 引用的 4 项 finding (R4 必须真 close)

### #1 (BLOCK) · architecture.md 多处残留 R2 字样
**R3 引用位置**: architecture.md:47, 635, 711, 758, 782-788
**R4 修向**:
- 47: `miss ≤ 10s` → `miss ≤ 5s, R3`
- 635: `R2 新增风险...miss 上限 10s + spinner` → `R2 引入 + R3 收紧的风险...≥ 95% 命中 + 5s + 503 + OP-1, 不再 ≤10s 宽容`
- 711: Anthropic 故障处理 `10s spinner 升级` → `5s → 503 + 错误页, 不允许 placeholder fallback`
- 758: 不变量 #1 改为 R3 守 PRD 口径表述 (commit < 1s, draft ≤ 5s, 全程 < 30s 含 draft LLM)
- 782-788: release_log.jsonl `commits` 字段 → `total_durations` + `commit_latencies` 拆分

**你必须验证**:
1. `architecture.md:47` 是否真的不再有 `≤ 10s`?
2. `architecture.md:635` 是否真的写 "不再 ≤10s 宽容"?
3. `architecture.md:711` 是否真的写 "503 + 错误页, 不允许 placeholder fallback"?
4. `architecture.md:758` 不变量 #1 是否真改为全程 wall-clock 表述?
5. `architecture.md:782-788` schema 是否真用 `total_durations` 字段名? draft sample 是否 ≤ 5s (R4 报告说改成 4.7s 不是 7.3s)?
6. **横扫剩余 architecture.md**: 是否还有任何残留 `≤ 10s` / `commit-only` / `commit 阶段 wall-clock` 字样? (R4 应做了全文扫除)

### #2 (HIGH) · B-lite 两套故事
**R3 引用位置**: T020.md:49, architecture.md:684-687, spec.md:267
**R4 修向**: 全部改为 CLI + runbook, 不再有 engage link / banner 点击 / `/settings/b-lite` UI toggle 残留

**你必须验证**:
1. `T020.md:49` 是否真改为 "banner 仅显示 CLI/runbook 提示, 不提供 engage 链接"?
2. `architecture.md:684-687` 是否真改为 "CLI + runbook"?
3. `spec.md:267` Glossary B-lite 是否一致 (R3 已写, R4 应未触动 — 验证仍一致)?
4. **横扫**: 是否还有任何"点击 link" / "engage button" / `/settings/b-lite` UI 残留?

### #3 (MEDIUM) · open-questions.md Q7 body 与 T019 不一致
**R3 引用位置**: open-questions.md:148-151
**R4 修向**: Q7 默认假设改为 `(b) human 手编 master checklist` 与 T019 一致

**你必须验证**:
1. `open-questions.md:148-151` Q7 默认假设段是否真改为 "(b) human 手编"?
2. 是否引用 `docs/learning_checklist.yaml` (与 T019 一致)?

### #4 (LOW) · spec.md §4 D16 与 T011 R3 不一致
**R3 引用位置**: spec.md:166
**R4 修向**: D16 改为 "纯 HTML table" (T011 R3 砍 CSS 着色)

**你必须验证**:
1. `spec.md:166` D16 是否真改为 "纯 HTML table"?
2. 是否注 "T011 砍 3h CSS 着色, v0.2+ polish"?

### 额外 (R3 Codex 自查待审 #6 CONCERN) · strict mode CI 默认值
R4 spec-writer 自己加了不变量 #15 (architecture.md:773)。

**你必须验证**:
1. `architecture.md` §9 不变量是否真有 #15 (新增 R4 项)?
2. #15 是否说 "CI 默认 strict, T015 ship 前 skip mark, T015 PR 必须移除 skip"?

### Q11 残留处理 (R3 BLOCK B-R2-5 残留半套)
**R3 引用位置**: spec.md:312-319 (R2 删除线 Q11 小节)
**R4 修向**: 整段删除 (Codex R3 line 86 建议 "把 spec §8 的删除线 Q11 收到 revision history, 不要挂在正文里")

**你必须验证**:
1. `spec.md` §8 是否真的不再有 `### Q11.` 小节?
2. spec.md §Revision History v0.3 entry 是否仍有"删除 R2 引入的 Q11"字样 (足以做审计旁证)?

---

## 必读

### R3 review 原文
- `.codex-outbox/20260426T120622-004-pB-L4-adversarial-r3.md` (88 行)

### R4 修订
- `specs/004-pB/architecture.md` v0.3 → **v0.3.1** (Revision History 第 22 行)
- `specs/004-pB/spec.md` v0.3 → **v0.3.1** (Revision History 第 19 行)
- `specs/004-pB/open-questions.md` v0.3 → **v0.3.1** (header)
- `specs/004-pB/tasks/T020.md` (无 version 字段, 改 line 49)

---

## 写输出 (`.codex-outbox/20260426T123538-004-pB-L4-adversarial-r4.md`)

```markdown
# Adversarial Review · 004-pB · R4

**Reviewed**: 2026-04-26T<ISO>
**Reviewer**: GPT-5.4 xhigh
**Spec version**: v0.3.1

## R3 4 finding 收口验证

| R3 ID | R4 修订声明 | 实际修了吗? | 行号佐证 |
|-------|------------|-------------|---------|
| #1 BLOCK arch.md 多处残留 | R4 改 5 处 + 横扫 | ✅/⚠️/❌ | arch.md:47, 635, 711, 758, 782-788 |
| #2 HIGH B-lite 两套故事 | R4 改 T020:49 + arch:684-687 | ✅/⚠️/❌ | T020.md:49; arch.md:684-687 |
| #3 MEDIUM Q7 body | R4 改 (b) human 手编 | ✅/⚠️/❌ | open-q.md:148-151 |
| #4 LOW D16 CSS 着色 | R4 改纯 HTML table | ✅/⚠️/❌ | spec.md:166 |
| Q11 残留半套 | R4 整段删除 | ✅/⚠️/❌ | spec.md §8 (无 Q11 小节) |
| 待审 #6 strict mode | R4 加不变量 #15 | ✅/⚠️/❌ | arch.md §9 #15 |

## 横扫验证 (R4 全文收口承诺)

- [ ] architecture.md 全文搜 `≤ 10s` 0 命中? (除 Revision History v0.2 entry 历史记录)
- [ ] architecture.md 全文搜 `commit 阶段 wall-clock` 0 命中? (除 Revision History 历史)
- [ ] T020.md / architecture.md / spec.md 三处 B-lite 描述全部一致 (CLI + runbook)?
- [ ] sum(T001-T022 estimated_hours) 仍 = 160h?

## 新引入问题 (R4 衍生, 若有)

(R4 仅文字级 patch, 不应引入新结构问题; 但若有疏漏请列)

## Verdict

选其一:
- **CLEAN** (R3 4 finding 全 close, 横扫验证全过, 无新问题; spec 可进 build)
- **CONCERNS** (大部分 close, 1-2 项有 medium/low 遗留; operator 决定带风险 ship)
- **BLOCK** (R3 主要 finding 仍未真改, 或 R4 引入新 blocker)

## Why this verdict

(2-3 句, 引行号)

## 下一步建议

如果 CLEAN:
- spec 已 build-ready, operator 可走 `/parallel-kickoff 004-pB <task-ids>`

如果 CONCERNS:
- operator 决定: (a) 现修剩余 medium/low; (b) 带风险 ship + risks.md log

如果 BLOCK:
- 建议下一轮 R5 修向...
```

---

## 写法要求

- **极短审**: R4 的 4 项 finding 都是文字级, 你只需快速验证 + 横扫. 不要对 R3 已 close 项做新审查
- **每条验证必须引行号**
- **横扫验证是关键**: R3 自己说"R4 后预期 CLEAN/CONCERNS", 但 R4 spec-writer 主动列出"未触动 T009.md release_log 字段名" — 请快速 verify T009.md 是否仍写 `commits` (而 architecture.md 已改 `total_durations`). 若不一致, 这是新 finding (Medium)
- **中文输出**

## Workflow

1. 读 R3 outbox (短)
2. 读 R4 修订点 (architecture.md 关键 line, spec.md, open-q.md Q7, T020.md:49, T009.md release_log)
3. 横扫 architecture.md `10s` / `commit-only` 残留
4. 横扫 B-lite 三处一致性
5. 写 outbox

开始。R4 后预期 CLEAN 或 CONCERNS。
