# Codex Task · 009 · forge v3(forge-lite)· P1(独立审 provider 双轴契约)

**Queue**: 009
**Created**: 2026-07-03T09:38:25Z
**Recommended model**: gpt-5.5
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~8-12k
**Kickoff form**: oneshot
**Forge version**: v3
**Phase**: P1
**Scope**: forge-lite（仅 P1 双方独立审 → 直接收敛定契约 · 不跑 P2 SOTA/P3,除非发现设计分歧）

## Your role

GPT-5.5 xhigh, Reviewer B, forge v3(forge-lite)on idea 009 Phase 1。
**审阅人姿态** — 评判已存在契约 + 定新契约形状,不是 daydream。

**背景**:handback-review 009 决议 T012（M2 realized-return）的 codex R6 —— **entry 执行价被 entry 后的
数据修订污染**。根因 = `get_adjusted_close` 用**单一 `query_as_of`** 同时管两件事:选哪条 price_daily 行
（价格版本轴）+ 选哪个复权因子（因子轴）。operator 已决议**选 A「执行时可见」(PIT 严格)** → 需**分离两轴**:
entry raw close 按 entry as_of 取、factor 按 exit as_of 取。**方向已定**,本轮定**分离双轴的最干净接口形状**。

## HARD CONSTRAINTS

- NO web search（forge-lite · 契约方向已定 · 不需 SOTA）
- DO NOT read `discussion/009/forge/v3/P1-Opus47Max.md`（parallel independence · 必须独立）
- **不重议 operator 已定的选 A**（执行时可见）—— 只定接口形状,不翻语义
- 按 P1 template（见 PROTOCOL.md）
- §0 引 K + §1 现状 + §2 三候选(a/b/c)first-take 评分 + §3 最不确定 3 件事
- 800-1500 字

## ⚠️ 沙箱可达性

X 有 2 个在 `/home/ys/codes/XenoDev/`（interface.py + provider.py · **要改的契约 + 病灶实现**）。
本机 linux · XenoDev 在 `/home/ys/codes/XenoDev`（不是 mac `/Users/admin`）。读不到就 §0 标 skip,
但**别 BLOCK** —— T012 hand-back（IDS 本仓 · 含 R6 复现 + 契约描述）足以支撑 first-take。读不到就在 Notes 提示加 scope。

## Read in order
1. `discussion/009/forge/v3/forge-config.md`（X/Y/Z/W/K + forge-lite scope + 三候选 a/b/c 定义）
2. X 标的（下列）
3. `discussion/009/forge/v3/PROTOCOL.md` §"P1 template"

## X 标的清单（4）
1. `discussion/009/handback/20260703T085623Z-009-pForge-20260703T085623Z.md` [IDS 本仓] — T012 hand-back ·
   **R6 复现 + 2 决议项全文 · 核心 X**（读 §1/§2:entry=100@04-27→04-30修90→exit=110 单轴误算）
2. `/home/ys/codes/XenoDev/projects/004-pB/src/decision_ledger/price/interface.py` [XenoDev] —
   现 PITPriceProvider 契约 · `get_adjusted_close(ticker,*,market,target_date,query_as_of,adjust)` **单一 query_as_of**
3. `/home/ys/codes/XenoDev/projects/004-pB/src/decision_ledger/price/provider.py` [XenoDev] —
   T003 实装 · **病灶 SQL L49-54**:`SELECT ... close_raw, adj_factor_fwd, adj_factor_back ... WHERE trade_date<=:target
   AND as_of<=:query_as_of ORDER BY trade_date DESC, as_of DESC, source ASC LIMIT 1`（一条 SQL 取整行 = close_raw+factor
   绑同一 as_of）
4. `discussion/009/009-pForge/PRD.md` §7 M1 契约（已落 entry 价 PIT 语义）+ `HANDBACK-LOG.md` T012 条（决议溯源）

## 本轮要定的核心（三候选 · 请独立评分）

分离双轴的最干净接口形状:
- **(a) 加第三参数** `get_adjusted_close(..., query_as_of, factor_as_of=None)`:query_as_of 管 close_raw 版本轴,
  factor_as_of 管因子轴(默认 None→退化=query_as_of 向后兼容)。
- **(b) 拆两个原子读** `get_raw_close(as_of)` + `get_factor(as_of)`,调用层自算 adjusted。
- **(c) 调用层重组,provider 签名不动**:T012 用现签名两次调用重组(⚠ 评估技术可行性 —— provider 返已算 adjusted,
  能否从两次不同 as_of 结果重组出 entry_raw×exit_factor?)。

评估每个:解不解 R6 / 冲击面(向后兼容 T004/T005/T007) / OUT-10 边界(改 009 自有 PITPriceProvider 可以 · 碰 004
PriceDataProvider 禁) / provenance 语义(AdjustedPricePoint 要不要记双 as_of)。

## 产出
写 `discussion/009/forge/v3/P1-GPT55xHigh.md`（P1 template）:
- §0 标的清单(沙箱可达性)+ K 摘要
- §1 现状摘要(Y1 契约正确性/PIT 语义 · Y2 器官边界/OUT-10)
- §2 三候选 a/b/c first-take 评分 + 理由 + 你倾向哪个
- §3 最不确定 3 件事

写完 outbox 放同名文件。**若你也收敛到某候选 → 明说"可进 synthesizer 定契约";若发现设计分歧(如某候选有未见的坑)→ 明确列,可能升级补 P2。**
