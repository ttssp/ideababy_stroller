# Codex Task · 009 · forge v2 · P3R2(finalize)

**Queue**: 009
**Created**: 2026-07-01T07:00:14Z
**Recommended model**: gpt-5.5
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~4-8k(reuse-session)
**Kickoff form**: reuse-session
**Forge version**: v2
**Phase**: P3R2
**convergence_mode**: **strong-converge**(从 forge-config.md frontmatter 读 —— 本轮必须产**单一** verdict)

## Session hint(only meaningful if Codex reuses session from v2 P1/P2/P3R1)

你已读过(本轮**请勿重读**):
- `.claude/skills/forge-protocol/SKILL.md` 的 P1/P2/P3R1 部分
- `discussion/009/forge/v2/forge-config.md` + `discussion/009/forge/v1/stage-forge-009-v1.md`
- 你自己的 v2 `P1/P2/P3R1-GPT55xHigh.md`
- 对方的 v2 `P1/P2-Opus47Max.md`
- X 中所有标的

本轮**新增需读**:
- `.claude/skills/forge-protocol/SKILL.md` 的 **P3R2 template**(strong-converge 分支)
- **`discussion/009/forge/v2/P3R2-Opus47Max.md`** ← 对方(Opus)v2 P3R2 finalize,**必读**
- `discussion/009/forge/v2/moderator-notes.md`(若存在 — 大概率仍无)

> ⚠ 若新开 session:上面"已读过"的需重读拿上下文。

**HARD CONSTRAINT**(reuse-session only):do NOT re-read files read in previous rounds unless listed above。

## Your role

forge v2 P3R2 — 按 **strong-converge** finalize:产**单一** verdict(蓝图定稿),残余分歧降级为 v0.2 minor note。**NO new search。**

## CONSTRAINTS

- **严格 strong-converge**,不允许混用 preserve-disagreement 双 path 结构。
- strong-converge 时若 R2 后**仍**冲突,§2 显式标 "unresolved" —— 但双方 P3R1 均判 0 分歧,预期无 unresolved。
- §4 给 synthesizer 的草稿建议**必须具体到可执行**,逐项对应 forge-config.W 的 3 件(refactor-plan 目标态图 / free-essay 全貌 / next-dev-plan 分期路线)。
- 引用对方原句 ≤15 words。

## 双方 P3R1 已收敛(你写 P3R2 前必读)

**双方 P3R1 均判「0 分歧」。** 你 P3R1 的旋钮清单 + contract tests 清单,**Opus P3R2 已全部采纳无修改**。所以这已经是定稿,你 P3R2 只需确认 + 补强 + 给 synthesizer 草稿。

**单一 verdict(Opus P3R2 §2,你若同意照说一致)**:009 目标态蓝图 = 七环节契约化闭环图(非统一壳)。主链 008采集/结构化→回测PIT数据层+alpha头/calibration头→StrategyModule独立lane→004决策/纪律→calibration反馈回流。唯一关键新器官=PIT价格历史层(日线+复权+PIT ticker+退市感知,defer多市场/汇率/tick)。Strangler Fig 分期:M1数据层→M2 alpha头(最先出数字最独立)→M3 calibration+两条回流线→M4 gated图谱/蒸馏,每期独立可用。三条硬约束定稿:① 回流 human-on-the-loop(旋钮建议→human确认生效,动作/universe/决策权永远手动)② 两档详略(现建器官画定量契约+AC,未来插槽只画签名+gate)③ 回测层 contract tests。

**旋钮清单(双方一致 · 你 P3R1 给的)**:可"建议→确认→自动套用"= source confidence multiplier/cap、alpha lane 启用/降权、devil's advocate 阈值、复盘提醒频率、calibration 报告窗口;永远手动 = 买卖持有等待动作、单笔最终决定、自动下单/调仓、跨源综合分、新增顾问/扩大 universe、图谱/蒸馏升为决策者。

**contract tests(双方一致)**:as-of 防未来、唯一价格源、复权/退市样例、alpha 不跨源合并、calibration 不静默改阈值、StrategyModule source_id 唯一且 lane 不见 registry、004 无 winner/recommended/aggregate 字段。

**你 P3R2 该做**:(1) §1 确认两条待固化项已定稿(采纳);(2) §2 与 Opus 对齐的单一 verdict(若完全同意可说"与对方 verdict 一致"并补强);(3) §3 残余 v0.2 note;(4) §4 给 synthesizer 逐项 W 草稿(refactor-plan/free-essay/dev-plan)。若你发现 Opus finalize 有不能接受处,§2 标 unresolved —— 但基于 P3R1 预期干净收敛。

## Write

`discussion/009/forge/v2/P3R2-GPT55xHigh.md` using P3R2 template(**strong-converge 分支**):
- §1 我对每条分歧的最终立场 + 让步(逐条对 P3R1 的待固化项)
- §2 联合 verdict(**单一**,200-400 字;若仍冲突标 unresolved)
- §3 残余分歧降级为 v0.2 note
- §4 W 形态产出的初步草稿建议(逐项对应 refactor-plan / free-essay / next-dev-plan)

700-1200 字。

## When done

Write `.codex-outbox/queues/009/20260701T070014-009-forge-v2-p3r2.md` with:
- **Completed**: <ISO>
- **Files written**: discussion/009/forge/v2/P3R2-GPT55xHigh.md (+ word count)
- **Verdict**: CLEAN | CONCERNS | BLOCK
- **Convergence**: converged | unresolved(预期 converged)
- **Final verdict 关键句**:一句话
- **给 synthesizer 的草稿建议是否齐全**:refactor-plan / free-essay / next-dev-plan 三件是否都给了
- **Notes for Claude Code**
