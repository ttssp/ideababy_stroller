# Codex Task · 009 · forge v2 · P2(参照系评估 · 画蓝图专项)

**Queue**: 009
**Created**: 2026-07-01T06:13:10Z
**Recommended model**: gpt-5.5
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~5-10k(reuse-session)
**Kickoff form**: reuse-session
**Forge version**: v2
**Phase**: P2

## Session hint(only meaningful if Codex reuses session from v2 P1)

你已读过(本轮**请勿重读**):
- `.claude/skills/forge-protocol/SKILL.md` 的 P1 部分
- `discussion/009/forge/v2/forge-config.md`
- `discussion/009/forge/v1/stage-forge-009-v1.md`(v1 verdict 地基)
- 你自己的 `discussion/009/forge/v2/P1-GPT55xHigh.md`
- X 中所有标的(v1 stage / 004 PRD / 008 PRD / 004 strategy 代码 / 004 DB schema)

本轮**新增需读**:
- `.claude/skills/forge-protocol/SKILL.md` 的 **P2 template** 部分
- **`discussion/009/forge/v2/P1-Opus47Max.md`** ← 对方(Reviewer A · Opus)v2 P1,**必读**
- `discussion/009/forge/v2/moderator-notes.md`(若存在 — 大概率仍无)

> ⚠ 若你是**新开的 Codex session**:上面"已读过"的需重读拿上下文;reuse 省 token 约束只在你确实延续 v2 P1 session 时生效。

**HARD CONSTRAINT**(reuse-session only):do NOT re-read files read in the previous round of this Codex session unless listed above。

## Your role

forge v2 P2 — 按 Z 跑 SOTA 搜索 + 用 SOTA 把你 v2 P1 的蓝图 first-take 压到证据上。**审阅人姿态不变,死守 v1 verdict 不推翻,蓝图 ≠ 一次性建完(防 V4)。**

## CONSTRAINTS

- **搜索约束(forge-config.Z = 对标 SOTA,聚焦"目标态架构 + 分期演化")**:
  - **allowed**:投资/量化闭环的**目标态架构分层** / **分期演化路线**先例 / 多器官**接口契约设计**范式 / 承诺壳+alpha 层同架构共存先例 / 复盘反馈环怎么闭合
  - **forbidden**:tech-stack-deep-dive / pricing / 具体实施细节 / **重复 v1 已做的回测方法论 SOTA(TipRanks/DSR/PBO 已定,别重刷)**
- §1 至少 **3 条** SOTA 对标(建议覆盖:目标态架构分层 / 分期演化模式 / 你 P1 §3 三个不确定点里最有判断价值的)
- §2 处理外部材料 —— 本轮 K 内无额外 URL/文件,X 5 标的即全部一手材料,§2 据实标"无新增"。
- §3 至少 **3 条**修正(P1 哪些站住、哪些被 SOTA 明确化/推翻)。
- verbatim quote ≤15 words。

## 给你的 context:Reviewer A(Opus)v2 P1+P2 已收敛到哪

**双方 v2 P1 高度对齐**(你读 P1-Opus47Max.md 会看到):同样七环节闭环、非统一壳、四念头同位置、同样发现 004 DB schema 已埋 `would_have_acted_without_agent`(反事实字段)+ `post_mortem_json` + `conflict_reports` 无 winner 字段(= v1 AC-1 有 DB 背书);同样判"价格历史层是唯一关键新器官"。**你我 §3 三个不确定点几乎逐条相同** —— 这三点就是 P2 SOTA 该打击的。

Opus P2 已检索到的 SOTA(供你**交叉验证或反驳**,不是抄):
- **目标态架构分层**:量化系统规范 = data→strategy→backtesting→execution,modular+feedback-driven,各层 isolated 降耦合 —— **直接验证七环节形态**;operator 痛点"各做各的"靠契约解决非靠大壳。
- **反馈回路自动 vs 人工(不确定点1)**:自适应系统可自动调参但有过拟合风险(解药=AIC 惩罚项)。Opus 判:**v0.1 回流 = human-on-the-loop(回测算建议旋钮值→human 确认生效)**,全自动 gated —— 因为过拟合风险正是 operator(不懂投资)判不了的 + 守 K binding①。
- **价格数据层范围(不确定点2)**:PIT 数据要处理 publication lag/revision/survivorship/**复权(拆分分红)**。Opus 判:v0.1 = **日线+复权+退市感知,defer 多市场/汇率/tick**;复权必须 v0.1 做(否则拆分当暴跌)。
- **增量演化**:**Strangler Fig 模式**(Fowler)—— 新回测器官 latch 到 004/008 上 piece by piece 长起来,每期独立可用,是 V4 直接解药。
- **蓝图详略度(不确定点3)**:TOGAF 定性视图 vs 定量可测 spec。Opus 判:**按器官成熟度分两档** —— 现建器官(数据层/alpha 头)画定量契约(接口+AC),未来插槽(图谱/蒸馏)只画定性边界(签名+gate)。调和"充分彻底 vs 防 V4"。

**Opus 给 P3R1 预告的 2 条待收敛**(你 P2 §3 若同意/反对请表态):
- (A) 回流线 human-on-the-loop 的**确切边界**:哪些旋钮允许"确认后自动"、哪些永远手动?
- (B) 蓝图**两档详略的分界**:现建器官 vs 未来插槽的确切清单?

## Write

`discussion/009/forge/v2/P2-GPT55xHigh.md` using P2 template:
- §1 SOTA 对标(表格;每条:SOTA 现状 + 对 009 蓝图的启示)
- §2 用户外部材料消化(据实标"无新增")
- §3 修正后的视角(至少 3 条;尤其对三个不确定点你的 SOTA 依据是否支持 Opus 的答案)

600-1100 字。

## When done

Write `.codex-outbox/queues/009/20260701T061310-009-forge-v2-p2.md` with:
- **Completed**: <ISO>
- **Files written**: discussion/009/forge/v2/P2-GPT55xHigh.md (+ word count)
- **Headline**: SOTA 对标一句话("我们 vs SOTA 的核心 gap / 或 SOTA 验证了蓝图哪块")
- **Verdict**: CLEAN | CONCERNS | BLOCK
- **Searches**: <n>,主要 source 列表
- **修正点简表** + **对 A/B 两条待收敛的立场**(给 P3R1 用)
- **Notes for Claude Code**
