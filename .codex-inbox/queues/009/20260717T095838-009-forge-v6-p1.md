# Codex Task · 009 · forge v6 · P1(独立审阅)

**Queue**: 009
**Created**: 2026-07-17T09:58:38Z
**Recommended model**: gpt-5.5
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~10-15k
**Kickoff form**: oneshot
**Forge version**: v6
**Phase**: P1

## Your role

You are GPT-5.5 xhigh, Reviewer B, forge v6 on idea 009 Phase 1。
**审阅人姿态** — 评判已存在物,不是 daydream。

本 forge 是 **forge-lite+P2 契约专场**(非全 idea 重审):裁定 009-pM3prime E1 的
extraction proposer 输出锚定契约(KG-40:LLM 产字符偏移在真跑中 56/58 截断 0 span,
E1 真密度读数不成立)。你的 P1 = 独立 first-take 四个子决策 (a)(b)(c)(d),细节见
forge-config.md 的 K 段。

## HARD CONSTRAINTS

- NO web search this phase(那是 P2)
- DO NOT read `discussion/009/forge/v6/P1-Opus47Max.md`(parallel independence)
- 不允许写 PRD / dev plan / refactor 详细方案(只产 first-take)
- 不允许跨入 SOTA 对比(那是 P2)
- §0 必须引用 K(用户判准)
- 按 Y 视角组织 §1 现状摘要 + §2 first-take 评分
- **K-(d) binding:消费面追平必须逐面枚举,不许只顺病灶**(KG-36 教训:序列化面最易 silent 丢)

## Read in order

1. `discussion/009/forge/v6/forge-config.md`(拿 X/Y/Z/W/K + 收敛强度 — K 有 4 个子决策 + 7 条 binding)
2. `discussion/009/forge/v6/moderator-notes.md`(若存在,binding)
3. X 中的全部标的(逐条列在下面 §"X 标的清单")
4. `.claude/skills/forge-protocol/SKILL.md`(P1 template)

## X 标的清单(11 个 · 全在本仓可读 · 无沙箱外路径)

**IDS 侧(直读)**:
1. `discussion/009/handback/20260717T081757Z-009-pM3prime-20260717T081757Z.md`
   —— 核心 X:KG-40 真跑事实(56/58 截断 0 span)+ 根因 + 候选①② + 守门救场记录(整文件读)
2. `discussion/009/009-pM3prime/PRD.md` —— §4-E1 blocked-by-KG-40 状态注记 + §7 五条 BLOCK
   硬约束(尤其 §7-1 trial ledger / §7-2 文本 PIT / §7-3 引文锚定+提取稳定性)+ §11 OQ
3. `discussion/009/forge/v5/stage-forge-009-v5.md` —— **只读** §"BLOCK 级硬约束"(L261-265)
   + §"What this menu underweights"(L335-368)两段,不重读全文

**快照件(`discussion/009/forge/v6/snapshots/` · 源 = XenoDev-009 工作区 @ b51b5b2 ·
含未 commit 的 DeepSeek 接线 = 病灶现场,IDS 内可直读,无跨仓沙箱问题)**:
4. `snapshots/census.py`(638 行 · 病灶本体)—— 重点:`_build_extraction_prompt` L191-215
   (字符偏移契约 = 根因)· `build_llm_proposer` L247-323(JSON mode · finish_reason 检测 ·
   失败计数)· `_decide_verdict` L326-348(三态 verdict)· CLI 输出/WARN L558-628
5. `snapshots/extractor.py`(179 行)—— C3 锚定消费面:`extract()` 守门流 + `_classify_reject`
   (界内/空白检查)+ `is_weakly_anchored`(弱锚审计)
6. `snapshots/caliber.py`(157 行)—— 口径 schema:`Citation`(span offsets)·
   `ExtractionRejectionReason`(冻结枚举 · 注意:0017 CHECK 从此导出,新增值的连锁成本)·
   `SuspectedSignal` 5 要件 · `CALIBER_DOC`
7. `snapshots/gold_layer1.py`(157 行)—— 金标 Layer-1:`Span=(source_id,start,end)`
   **exact-match** P/R/F1 —— 偏移语义的隐性消费面
8. `snapshots/test_e1_citation_consistency.py`(144 行)—— C3/C4 契约测试现编码
   (回锚切片断言 + 确定性断言)
9. `snapshots/0017_add_extraction_tables.py`(246 行)—— DDL 序列化面:extracted_signals
   span 列 · trial_ledger 两态 · density_threshold 不可回改触发器
10. `snapshots/E1-real-census-runbook.md`(156 行)—— 真跑操作面:§"真跑前必知" ·
    §3 成本/chunking 注 · §4 结果解读纪律(注意与 census.py 原子性表述的一致性)
11. `snapshots/E2-design-inputs.md`(99 行)—— 下游耦合:gate extractor_variant gap ·
    E2 读契约草案

## Write

`discussion/009/forge/v6/P1-GPT55xHigh.md` using P1 template:
- §0 我读到的标的清单 + 阅读策略(必须引 K)
- §1 现状摘要(按 Y 两视角:契约正确性/LLM 能力贴合 · 工程纪律/守门不变量)
- §2 First-take 评分(两 Y 维度 keep/refactor/cut/new + **逐子决策 (a)(b)(c)(d) first-take**)
- §3 我现在最不确定的 3 件事(给 P2 检索方向)

800-1500 字。

## When done

Write `.codex-outbox/queues/009/20260717T095838-009-forge-v6-p1.md` with:
- **Completed**: <ISO>
- **Files written**: <list + word count>
- **Headline**: <one-line 现状一句话总结>
- **Verdict**: CLEAN | CONCERNS | BLOCK
  - BLOCK 用于 X 中标的不可达(本轮全部标的在 IDS 仓内,预期可达)
- **First-take summary**: 两 Y 维度倾向 + 四子决策 (a)(b)(c)(d) 各一句立场
- **Notes for Claude Code**: 任何 orchestrator 需要知道的(包括是否触发 BLOCK)

Filename **must match** the inbox file basename so HEAD pointer works:
`20260717T095838-009-forge-v6-p1.md`
