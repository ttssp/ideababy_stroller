# Codex Task · 007a-pA · L4 Adversarial Review Round 4 (HARD CAP — FINAL)

**Queue**: 007a-pA
**Created**: 2026-05-08T14:57:30Z
**Recommended model**: gpt-5.5
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~6-10k(reuse-session 进一步 narrow)
**Kickoff form**: reuse-session(R4 与 R1+R2+R3 上下文重叠 ~90%)

## Your role

R4 of L4 adversarial review。**这是 hard cap 最后一轮 — 无 R5**。

R3 verdict = BLOCK 但 narrow:
- 1 new blocker(C14 contract 与 D17/D20 矛盾)
- 2 high concern(`--on` 不清 review_mode / trust_summary 不显示 review_mode 状态)
- 4 medium drift
- 2 partial(B-R2-3 in C14 + M-R2-3 risks:71 PROD-3 health_check 残留)

spec-writer R4 narrow fix 已完成。**你的任务**:
1. 核查 R3 finding 是否真闭环
2. R4 修订引入的 microcode-level edge cases
3. 给 verdict — 如 BLOCK,explanation 必含 ship-with-risk vs accept-block 的具体建议(因为 R5 不存在)

## Session hint(只读 R4 改动文件 + R3 outbox)

R4 修订改动文件:
- `specs/007a-pA/spec.md` — frontmatter Revised + R3/R4 标注;**§3 C14 重写**(独立 review_mode 字段,与 D17/D20 一致);§4 加 D21(`--on` 完整恢复语义);§5 task summary L121 + Schedule risk 表 L146 措辞同步;§6 加 2 verification 行(`--on` clears review_mode + trust_summary shows review_mode);Schedule sum 段 R3/R4 注
- `specs/007a-pA/architecture.md` — frontmatter Source spec v0.2→**v0.3**;§5.4 加 `review_mode_set_at` ISO timestamp field(set_review_mode helper 自动写,backward-compat:旧 state.json 缺该 field → None);§5.5 模板加 `## Hook Operational Status` 段 + 24h stuck-true WARNING
- `specs/007a-pA/SLA.md` — frontmatter Source spec v0.3;Review session 流程加 `--on` 完整恢复 vs `--review-mode-off` 部分恢复区分
- `specs/007a-pA/risks.md` — frontmatter Revised + R3/R4 标注;**SCH-1** 数字更新(22.75h within PRD v1.1 23h cap;L 从 H 降为 M);**PROD-3** mitigation health_check → CLI(grep --count-agent-emit + trust_summary --check-enabled)
- `specs/007a-pA/tasks/T012.md` — `--on` 实装 `set_enabled(true) AND set_review_mode(false)` + 条件 stdout(prev review_mode 为 true 时打印 "review_mode cleared");spec_ref 加 D21
- `specs/007a-pA/tasks/T020.md` — Goal R3/R4 H-R3-2 fix;模板 `## Hook Operational Status` 段;main() 加 review_mode 渲染 + WARNING 计算;3 verification 行
- `specs/007a-pA/tasks/T031.md` — 加 TestOnClearsReviewMode + TestTrustSummaryShowsReviewMode 两个 TestCase;hours 注释
- `specs/007a-pA/tasks/T032.md` — README Daily commands 加 `--on` 完整恢复教学 + `--on` vs `--review-mode-off` 用法指引

R3 outbox 仍在仓库:
- `.codex-outbox/queues/007a-pA/20260508T142213-007a-pA-L4-adversarial-r3.md`(R3 verdict BLOCK + 1 blocker + 2 high + 4 medium + 2 partial 全文)

R1 / R2 outbox 也在仓库供 reference。

**HARD CONSTRAINT**: 在 reuse-session 模式下,**只读 R4 改动文件 + R3 outbox**。R1/R2 旧版 spec 不重读。

## Verification responsibility(R4 严格 — narrow scope)

### R3 blocker must close

- **B-R3-1 C14 contract drift**:核查 `spec.md §3` C14 行是否重写为新 review_mode 语义(独立字段 / hook gate / O3 正交)。**核查方法**:
  - grep `spec.md` 中"review.?mode.*alias|review.?mode.*equivalent|review.?mode.*= --off|--off.*review.?mode" 期望残留命中**全在 historical revision note**(显式标"R3/R4 fix:不再标 alias")
  - C14 字面是否会让 build worker 实装新 review_mode 字段而非 `--off` alias

### R3 high concerns must close

- **H-R3-1 `--on` 不清 review_mode**:R4 走 Option 1 — `--on` set_enabled AND set_review_mode(false) + 条件 print "review_mode cleared"。**核查方法**:
  - T012 实装 pseudocode 是否真有这个组合
  - T031 是否有 TestOnClearsReviewMode test case
  - T032 README 是否教学 `--on` = full restore vs `--review-mode-off` = exit review only
  - D21 显式记录"完整恢复"语义存在
- **H-R3-2 trust_summary 不显示 review_mode**:R4 加 `## Hook Operational Status` 段 + 24h stuck WARNING + `review_mode_set_at` field。**核查方法**:
  - architecture.md §5.5 模板是否加段
  - T020 main() 是否加渲染逻辑 + WARNING 计算
  - T031 是否有 TestTrustSummaryShowsReviewMode test case
  - architecture.md §5.4 schema 加 `review_mode_set_at` field 是否标 backward-compat(缺失 → None)

### R3 partials must close

- **B-R2-3 in C14**:已包含 B-R3-1
- **M-R2-3 risks:71 PROD-3**:核查 PROD-3 mitigation 是否真替换为 CLI 入口

### R3 medium fixes 4 项

- M-R3-1 SCH-1 22.75h within PRD v1.1 23h cap
- M-R3-2 architecture.md frontmatter v0.3
- M-R3-3 SLA.md frontmatter v0.3
- M-R3-4 spec.md task summary alias 措辞

### NEW · R4 修订自身可能的 edge cases

R4 修订引入了:state.json 加 `review_mode_set_at` ISO timestamp field、`--on` 改为同步动两个 field、trust_summary 加 24h stuck WARNING、4 个新文件 v0.3 bump。

**spec-writer 自报 R4 余下风险**(你应在 outbox 评估):
1. **`review_mode_set_at` field 是 schema-bump 还是 backward-compat patch**:R4 标 backward-compat(缺失 → None),但严格 reviewer 可能 challenge "新 field 应 v0.4 schema-bump"。这是字面规则边缘 case
2. **`{review_mode_warning}` 空字符串渲染**:T020 用空字符串占位,architecture.md §5.5 说"加一行" — implementation-level 视觉问题(空白行),不构成 contract-level BLOCK

**你也应探测的 edge cases**:
- **edge a**: `--on` 在 review_mode 已经 false 的情况下行为是?Print"review_mode cleared"会误导(实际没动)。R4 fix 说"prev 为 true 时才 print" — 核查 T012 pseudocode 真实装这个条件
- **edge b**: 24h stuck WARNING 的"24h"是相对当前 trust_summary 跑的时间还是 14 天 dogfood 起点?如果 review_mode 在 day 5 set 但 day 14 trust_summary 才跑,24h * 9 day = stuck very long → WARNING 必触发 — 这是 by design 还是 over-trigger
- **edge c**: `review_mode_set_at` 在哪些事件清空?`--review-mode-off` 应清(set_review_mode(false));`--on`(R4 改为 set_review_mode(false))也应清。但若 set_review_mode helper 不显式清 timestamp,旧 timestamp 残留可能误导 trust_summary 24h 计算

### NEW · grep 验证(R4 修订承诺的 grep 结果)

spec-writer 自报 3 项 grep 全合规:
- spec.md `review-mode.*alias|equivalent|= --off`:6 命中,全 historical revision note
- specs/ `health_check`:16 命中,全 historical / negative
- architecture.md / SLA.md frontmatter Source spec:已升 v0.3

**你的任务**:复跑这 3 个 grep,确认 spec-writer 自报合规真实。

## Write

`.codex-outbox/queues/007a-pA/20260508T145730-007a-pA-L4-adversarial-r4.md`,
内容:

```markdown
# Adversarial Review · 007a-pA · R4 (FINAL)

## R3 finding closing status (post-R4 narrow fix)

| R3 finding | Status | Verification evidence |
|---|---|---|
| B-R3-1 C14 contract drift | CLOSED / PARTIAL / OPEN | grep result + 文件 line refs |
| H-R3-1 `--on` 不清 review_mode | CLOSED / ... | T012 pseudocode + T031 test + T032 README |
| H-R3-2 trust_summary 不显示 review_mode | CLOSED / ... | architecture §5.5 + T020 + T031 |
| Partial-1 PROD-3 health_check | CLOSED / ... | risks.md PROD-3 行 |
| M-R3-1 SCH-1 数字 | CLOSED / ... | risks.md SCH-1 |
| M-R3-2 architecture v0.3 | CLOSED / ... | frontmatter |
| M-R3-3 SLA.md v0.3 | CLOSED / ... | frontmatter |
| M-R3-4 spec.md task summary alias | CLOSED / ... | spec.md L121/L146 |

## R4 修订引入的 edge cases

(if any — spec-writer self-reported 2 边缘点 + 你探测的 a/b/c)

## New blockers (R4 引入或漏抓的)

(if any — keep in mind R4 是 hard cap)

## New high-severity concerns

## Medium / Low

## PRD faithfulness check (post-R4)

- [ ] Spec C14 与 D17/D20/D21 一致(独立 review_mode 字段)
- [ ] §6 verification 含 `--on` 清 review_mode + trust_summary 显示 review_mode 两条
- [ ] D8 whitelist = {Bash, Edit, Write, Claude Code lifecycle hook block}(无 Read)
- [ ] D21 显式记录 `--on` 完整恢复语义
- [ ] schema field `review_mode_set_at` 标 backward-compat

## Top 3 things the operator personally must verify

(if any — focus on R4 edge cases)

## Verdict

**One of**:
- **CLEAN** — R1+R2+R3+R4 fix 全闭环,spec ready to build。Open issues 推 build 之后或 v0.2
- **CONCERNS** — 几条 high concern 可推 follow-up,operator 决定接受 risk 或修(因为 R5 不存在,operator 选择面是"接受 risk + log to risks.md ship" vs "回 L3 重 scope")
- **BLOCK** — explanation 必含具体建议:走 ship-with-risk(更新 risks.md log open blockers 后 build)还是 abandon-and-rescope(回 L3 / L4 起新 PRD)

(R4 是 hard cap;若 BLOCK,explanation 必给 operator 明确路径选择)

---
**Quote ≤15 words verbatim**.
```
