# Codex Task · 007a-pA · L4 Adversarial Review Round 3

**Queue**: 007a-pA
**Created**: 2026-05-08T14:22:13Z
**Recommended model**: gpt-5.5
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~8-12k(reuse-session 进一步 narrow)
**Kickoff form**: reuse-session(R3 与 R1+R2 上下文重叠 ~85%)

## Your role

R3 of L4 adversarial review。R2 verdict = BLOCK,3 new blocker(B-R2-1 PRD propagation / B-R2-2 O3 dual-metric residue / B-R2-3 review-mode×O3 collision)+ 3 high concern + 4 medium。**全部是 R2 修订的 propagation drift,不是新设计错**。

spec-writer 已经走完 R3 propagation fix。**你的任务**:
1. 核查 R2 finding 是否真闭环
2. 探测 R3 修订自身引入的新问题
3. **特别注意**:R3 在 Option A 路径加了 `state.review_mode` 字段(独立于 enabled),hook gate 改为 `enabled AND NOT review_mode`。这是 spec design 决策,需要审 invariant 是否 hold

## Session hint(只读 R3 改动文件)

R3 修订改动文件:
- `specs/007a-pA/spec.md` — frontmatter PRD ref v1.0→v1.1;§1 O3 改写;§2 IN-8 binary-only + Activity Health + Schema Health 三段;§3 C1 行 12-23h + PRD v1.1 source;§4 D17 改写 + **D20 新增**(O3 与 review_mode 正交);§5 Schedule risk 表(22.75h);§6 Verification O3 stale-save;Glossary review_mode + hook still enabled
- `specs/007a-pA/architecture.md` — frontmatter v0.3;§3 ADR-1 + ADR-4 rationale;§5.1 hook 入口契约移 Read + 加 review_mode hook gate + 显式空 stdin pre-read;§5.2 Stage A1b review_mode + Stage B 后 D16 边界 examples;§5.4 schema 加 `review_mode: bool` + set_review_mode helper
- `specs/007a-pA/SLA.md` — v0.3;PIPE_BUF→POSIX O_APPEND;C14 review_mode 同步
- `specs/007a-pA/risks.md` — v0.3;TECH-1+TECH-3 health_check 替换;SEC-2 D19 false-positive ack;COMM-2 binary-only;BUS-1 review-mode×O3 正交
- `specs/007a-pA/HANDOFF.md` — Revised + PRD v1.1;mini-PRD C1 12-23h;C14 R2 B-R2-3
- `specs/007a-pA/dependency-graph.mmd` — notes R2 B-R2-3 + CP 22.75h
- `specs/007a-pA/tasks/T002.md` — 1.0→1.25h;set_review_mode helper(invariant 不动 enabled)
- `specs/007a-pA/tasks/T011.md` — 显式 `if not raw.strip(): return`;hook gate 加 review_mode 短路
- `specs/007a-pA/tasks/T012.md` — `--review-mode` set_review_mode(True);加 `--review-mode-off`
- `specs/007a-pA/tasks/T021.md` — OOS health_check 替换
- `specs/007a-pA/tasks/T031.md` — TestStaleSaveTagRebinding 改 set_review_mode;加 TestO3PassesDuringReviewMode
- `specs/007a-pA/tasks/T032.md` — Daily commands 加 `--review-mode` 与 `--off` 区别;privacy.md SEC-2 双层防御 + false-positive ack

R2 outbox 仍在仓库:
- `.codex-outbox/queues/007a-pA/20260508T133336-007a-pA-L4-adversarial-r2.md`(R2 verdict BLOCK + 3 blocker + 3 high + 4 medium 全文)

R1 outbox 也在仓库供参考:
- `.codex-outbox/queues/007a-pA/20260508T124631-007a-pA-L4-adversarial-r1.md`

**HARD CONSTRAINT**: 在 reuse-session 模式下,**只读上面列出的 R3 改动文件 + R2 outbox**。其他 R1/R2 阶段已读文件不重读,除非你需要交叉对比某条文本是否真改。

## Verification responsibility(R3 严格)

### R2 blockers must close

- **B-R2-1 PRD propagation**:核查 spec frontmatter / spec §3 C1 / HANDOFF mini-PRD 是否全部 12-23h + PRD v1.1 引用。**核查方法**:`grep -rE "10-20|11\\.5-16\\.5" specs/007a-pA/` 期望命中只在 historical revision notes(显式标 "R2 B-R2-1 fix:从 X 修订为 Y" 类)
- **B-R2-2 O3 dual-metric**:核查 spec §2 IN-8 + risks COMM-2 是否 binary-only + activity 单列。**核查方法**:`grep -rE "dual.*metric|enabled.*AND.*24|24.*agent" specs/007a-pA/` 期望 0 违规命中
- **B-R2-3 review-mode×O3**:核查 D20 新增 + state schema review_mode + hook gate `enabled AND NOT review_mode` + T031 TestO3PassesDuringReviewMode。**核查方法**:invariant — review-mode true 时 hook 应静默暂停但 enabled 仍 true → O3 PASS;`--off` 才动 enabled

### R2 high concerns must close

- **H-R2-1 Read in arch §5.1**:`grep "Read" specs/007a-pA/architecture.md` 应只在 historical / negative reference("Read 移出 v0.1 白名单" 类)出现
- **H-R2-2 HANDOFF stale C1**:HANDOFF.md mini-PRD 应是 12-23h
- **H-R2-3 T011 empty-stdin**:T011 pseudocode 应有 `if not raw.strip(): return` 在 json.loads 之前;empty stdin = legal noop;malformed JSON = +schema_error_count

### Medium fixes 4 项

- M-R2-1 SLA.md PIPE_BUF;M-R2-2 risks O3 dual;M-R2-3 risks health_check;M-R2-4 T032 privacy.md 与 D19 对齐

### NEW · R3 修订自身可能的新问题

**R3 修订引入了**:state.json 加 `review_mode: bool` 字段、hook gate 改 `enabled AND NOT review_mode`、D20 新增 O3-review_mode 正交决议、T011 显式 empty-stdin 短路、T032 README 改写 review flow。核查这些变化是否引入 invariant 违反或 corner case:

- **edge 1 · review_mode 持久化故障**:若 operator 跑 `friction --review-mode` 后系统重启或 state.json 损坏,review_mode 默认值是什么?如果默认 false,operator review session 中突然 hook 开始 emit;如果默认 true,operator 可能不知道 review_mode 还开着
- **edge 2 · review_mode 与 launchd 交互**:day-14 launchd-trigger 跑 trust_summary 时,review_mode 是 true 怎么办?trust_summary 输出会不会因 review_mode 跳过某段?O3 PASS 但 schema_error_count 长期 0 → R2 H3 mitigation 失效(无 emit = 无 schema check)
- **edge 3 · `friction --on` 是否清 review_mode**:T012 改了什么?`--on` 是只动 enabled 还是也清 review_mode?如果只动 enabled,operator 跑 `--review-mode` 后跑 `--on` 仍 stuck in review-mode
- **edge 4 · D16 边界 examples**:spec-writer 报告说加了 5 行 examples,核查是否真给 operator 信号(strong-signal pattern 优先于 length,这个优先关系在 judge.py 决策代码中是否真实装)
- **edge 5 · spec-writer 自报 R3 残留**:JudgeError 同时与"valid JSON 但 schema 缺字段"和"hook gate 关闭"共用 silent skip 路径,T010/T011 边界 JudgeError 语义未显式区分。**这条 spec-writer 自己 surface,你应在 outbox 评估**

### NEW · grep 验证(R3 修订承诺的 grep 结果)

spec-writer 自报 4 项 grep 结果全合规:
- `10-20|11.5-16.5`:3 处命中,全 historical revision notes
- `dual.*metric|enabled.*AND.*24|24.*agent.*emit`:14 处命中,全 informational / hook gate / revision note
- `health_check`:12 处命中,全 historical / negative reference
- `Read` in architecture.md:3 处命中,全 negative reference

**你的任务**:复跑这 4 个 grep,确认 spec-writer 自报合规真实,即"残留命中确实是 historical / negative reference 而非 stale prescriptive 用法"。

## Write

`.codex-outbox/queues/007a-pA/20260508T142213-007a-pA-L4-adversarial-r3.md`,
内容:

```markdown
# Adversarial Review · 007a-pA · R3

## R2 finding closing status (post-R3 propagation fix)

| R2 finding | Status | Verification evidence |
|---|---|---|
| B-R2-1 PRD propagation | CLOSED / PARTIAL / OPEN | grep result + 文件 line refs |
| B-R2-2 O3 dual-metric | CLOSED / ... | ... |
| B-R2-3 review-mode×O3 | CLOSED / ... | invariant: review_mode 期间 enabled 仍 true → O3 PASS;hook 短路 |
| H-R2-1 Read in arch §5.1 | CLOSED / ... | ... |
| H-R2-2 HANDOFF stale C1 | CLOSED / ... | ... |
| H-R2-3 T011 empty-stdin | CLOSED / ... | ... |
| M-R2-1 PIPE_BUF in SLA | CLOSED / ... | ... |
| M-R2-2 risks O3 dual | CLOSED / ... | ... |
| M-R2-3 risks health_check | CLOSED / ... | ... |
| M-R2-4 T032 privacy.md | CLOSED / ... | ... |

## R3 修订引入的新问题

(if any — be honest about edges introduced by review_mode design)

## New blockers (R3 引入或 R1+R2 漏抓的)

(if any — list blockers with location, issue, why blocks, fix direction)

## New high-severity concerns

(if any)

## Medium / Low

## PRD faithfulness check (post-R3)

- [ ] Spec frontmatter PRD reference = v1.1
- [ ] Spec §3 C1 = 12-23h with PRD v1.1 Revision log source
- [ ] HANDOFF mini-PRD C1 = 12-23h
- [ ] §1 O3 字面与 PRD §"Success" O3 字面一致(binary enabled-only)
- [ ] D8 whitelist = {Bash, Edit, Write, Claude Code lifecycle hook block}(无 Read)
- [ ] D20 显式 O3 与 review_mode 正交决议存在

## Top 3 things the operator personally must verify

(if any — focus on R3 introduced edges + spec-writer's own concern about JudgeError boundary)

## Verdict

**One of**:
- **CLEAN** — R1+R2+R3 fix 全闭环,无新 blocker,spec ready to build。Open issues 可推 build 之后或 v0.2
- **CONCERNS** — 几条 high concern 可推 follow-up,不阻塞 build;operator 决定接受 risk 或修
- **BLOCK** — 仍需 R4(R4 是 hard cap,后无 R5)

(必含 1-2 段 explanation。注意 R4 是上限,如果你 R3 仍 BLOCK,explanation 应明确建议 operator 是否走 R4 还是接受 BLOCK ship-with-risks)

---
**Quote ≤15 words verbatim**.
```
