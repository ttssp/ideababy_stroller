# Codex Task · 007a-pA · L4 Adversarial Review Round 1

**Queue**: 007a-pA
**Created**: 2026-05-08T12:46:31Z
**Recommended model**: gpt-5.5
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~15-25k
**Kickoff form**: oneshot

## Your role

You are GPT-5.5 xhigh performing **adversarial review** on a just-generated
spec package. Your goal: find where this spec will fail in production. Be
hostile to the design, not to the operator.

## Read in order

- `specs/007a-pA/spec.md`(6-element contract — 6 outcomes, 13 constraints, 15 prior decisions, 12 verification rows)
- `specs/007a-pA/architecture.md`(C4 L1 + L2 mermaid + 5 ADRs + component contracts)
- `specs/007a-pA/tech-stack.md`(Python 3.11+ stdlib only / 0 deps / launchd)
- `specs/007a-pA/SLA.md`(simple form,single v0.1 段)
- `specs/007a-pA/risks.md`(22 risks: 8 TECH + 4 OPS + 4 SEC + 2 COMM + 2 LEGAL + 1 BUS + 1 PROD)
- `specs/007a-pA/non-goals.md`(26 non-goals)
- `specs/007a-pA/dependency-graph.mmd`(13 task DAG · 4 phase · CP=9.0h · sum=18.5h · max-parallel-width=3)
- `specs/007a-pA/tasks/T001.md` ... `T033.md`(sample 3-5 tasks; not all)
- `specs/007a-pA/HANDOFF.md`(SHARED-CONTRACT v1.1.0 hand-off 协议产出)
- `discussion/007/007a-agent-emit/007a-pA/PRD.md`(verify spec faithfulness)
- `discussion/007/007a-agent-emit/L3/stage-L3-scope-007a-agent-emit.md`(L3 menu context — 为什么选 hybrid 路径而非 pure A / B / C)

## Challenge these specifically

**核心挑战焦点**(spec-writer 自己 surface 出的最 risky 三处,你需要逐一 stress-test):

1. **TECH-2 静态规则误报洪水(H likelihood × H impact)的 mitigation 是否足够**。目前 mitigation 是 `--threshold high` + `--off` + `[debugging-friction-detection]` 黑名单关键字。挑战:
   - operator 在 dogfood 期间能否记得这三个止血手段?
   - `--threshold high` 的 confidence H 边界(judge.py 决策表)是否过于宽松,是否实际上把 medium 行为划成 H?
   - `[debugging-friction-detection]` 黑名单依赖 operator 显式打 task description tag — 真实 dogfood 中这一步会不会被忘?

2. **OQ-C 双指标(D7)与 O3 verification 的循环依赖**。O3 用"双指标都 pass = adoption signal"。但 trust-summary 自己不 emit;关键是,如果 14 天 dogfood 中段因 BUS-1 / OPS-2 中断,operator 回来发现 entry 数低,但 state.enabled 仍 true。此时 O3 的"最近 24h 有 ≥ 1 条 agent-emit entry"会 fail,即便 operator 仍信任 hook。挑战:
   - 这种 "operator 暂离" 场景下 O3 是否过于严格?
   - 是否需要改成 "since friction --on / 7 天" 而非 "最近 24h"?

3. **PROD-1 + PROD-2 的 mitigation 都依赖 hybrid self-interview prompt(D14)— 但 prompt 文案是英文 1 句话,中文 operator 是否会真心答还是 "relieved" 一笔带过?**。目前 mitigation 是 README 强调"1 句话即可"。挑战:
   - prompt 文案是否需要 elaborate 一点(eg 三选一 framing 加 "if X happens, what would have made it different?")?
   - 若 operator 答 "both" 但不写 why,O6 verification 仅查"prompt 下行非空"会判 pass,但实际产品价值 marginal — 是否需要更强的最小 evidence threshold(如 ≥ 20 字)?
   - hybrid 与 full Candidate B 6 题协议之间的 1-vs-6 抉择,在 day 14 是否会让 operator 意识到 "1 题不够" 时已经太晚补救?

**额外标准 9 个挑战角度(逐一过)**:

4. **Concurrency safety**:hook 多次并发 fire(operator 同时跑多个 task)会不会撞写 v4-friction-log.md?file lock 是否在 hook.py 设计?
5. **Data consistency**:state.json 写入时崩溃(eg launchd 中途退出)如何恢复?
6. **Failure recovery**:upstream Claude Code hook framework 在某次 release 改了 payload 格式,hook.py 怎么 detect / fail-loud?
7. **Security boundaries**:friction-log 的路径硬编码 docs/dogfood/v4-friction-log.md — 若 operator 在不同 IDS-similar 仓库使用,是否会跨仓污染?如果路径不存在 hook 创建目录,是否会在意外仓库创建该目录?
8. **Operational cost**:14 天 dogfood × 平均 N task/天 × 平均 hook fire 频率 = 多少 entry?是否会让 friction-log 超出 grep-friendly 阈值(eg > 10000 行)?
9. **PRD faithfulness**:spec.md 是否漏掉 PRD 中任何 outcome (O1-O6)?是否加了 PRD 没要求的 feature?
10. **Non-goal leakage**:spec.md 是否 sneak in 了 non-goals.md 列出的 26 项中任何一条?
11. **Task DAG sanity**:13 task / 4 phase 是否有循环依赖?T020(trust_summary)是否真依赖 T013(grep)而非反过来?CP=9h 是否过于乐观?
12. **Solo-operator viability**:Yashu 一人 5-10h/week × 1-2 周 = 10-20h,与 sum=18.5h 工程量是否过紧?critical path 9h 在 5h/week 下要 2 周才能跑完,实际还有 ADP V4 dogfood 同时跑 — 这是 ship 风险,不是 build 风险

**Hand-off 协议挑战**(本 PRD 是 SHARED-CONTRACT v1.1.0 第一个真实 PRD,HANDOFF.md 也要 stress-test):

13. **HANDOFF.md schema 转换表**(共 9 行 IDS → ADP 映射)是否漏了 IDS PRD 任何字段?
14. **operator 切仓后第一步 — 把 IDS PRD §"User persona" + §"Core user stories" 提炼为 1-2 段 short feature description**(HANDOFF.md 给了建议提炼:79 字英文)是否真的能让 ADP sdd-workflow skill 产出对的 spec.md 骨架?提炼得太短可能丢 6 个 outcome 中至少一半 detail。

## Write

`.codex-outbox/queues/007a-pA/20260508T124631-007a-pA-L4-adversarial-r1.md` 内容如下:

```markdown
# Adversarial Review · 007a-pA · R1

## Blockers (must fix before proceeding)

| # | Location | Issue | Why it blocks | Fix direction |
|---|---|---|---|---|

(if any blocker found — be honest about each;若无 blocker,留空表头 + 说"(no blockers found)")

## High-severity concerns (should fix; ok as follow-up with owner+deadline)

| # | Location | Issue | Suggested action |

## Medium / Low findings

(brief list,one line each)

## PRD faithfulness check

- [ ] Every PRD outcome (O1-O6) has corresponding spec verification
- [ ] No spec feature outside PRD scope IN
- [ ] No spec feature in PRD scope OUT (cross-check non-goals.md)
- [ ] HANDOFF.md schema covers all IDS → ADP fields per SHARED-CONTRACT v1.1.0

## Top 3 things the operator personally must verify (human judgment required)

1. ...
2. ...
3. ...

## Verdict

**One of**:
- **CLEAN** — no blockers, optionally minor fixes — spec ready to build
- **CONCERNS** — non-blocking; operator decides whether to fix now or ship with risk log
- **BLOCK** — must fix before round 2

(write the chosen verdict in **bold,逐字英文 word**;必含 explanation 1-2 段)

---
**Quote ≤15 words verbatim from any file**.
```
