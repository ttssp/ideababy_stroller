# Codex Task · 007a-pA · L4 Adversarial Review Round 2

**Queue**: 007a-pA
**Created**: 2026-05-08T13:33:36Z
**Recommended model**: gpt-5.5
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~10-15k(reuse-session 减半)
**Kickoff form**: reuse-session(R2 与 R1 上下文重叠 ~80%)

## Your role

You are GPT-5.5 xhigh performing **adversarial review R2**。R1 verdict = BLOCK
with 3 blockers + 6 high concerns + 5 medium-low。spec-writer 已经走过 R2 修订,
你的任务:**核查所有 fix 是否真闭环 + 暴露新引入的问题(可能!)**。

## Session hint(只读这些新 / 改动文件)

R2 修订改动文件(R1 后):
- `specs/007a-pA/spec.md` — Version 0.1 → 0.2,**重写**了:O3 / O6 outcome,§5 task list 移 ghost T022,§6 verification 全部用真实 entry point。新加 D16/D17/D18/D19。Schedule risk 显式段
- `specs/007a-pA/architecture.md` — §5.2 完全重写**两阶段** judge.py(独立 confidence classification + threshold 过滤);§5.3 PIPE_BUF 措辞修正;§5.4 加 schema_error_count;§5.5 trust_summary 加 `--check-enabled` / `--check-self-interview` flag
- `specs/007a-pA/risks.md` — TECH-2 mitigation 重写;TECH-9 schema drift 新增;SCH-1 schedule overshoot 新增
- `specs/007a-pA/non-goals.md` — 1.12 Read tool v0.2;4.6 / 4.7 v0.2
- `specs/007a-pA/SLA.md` — Schema health metric;Review ergonomics 含 review-mode
- `specs/007a-pA/HANDOFF.md` — Step 3 升级为 mini-PRD handoff block(加 Problem/Context、UX principles、Biggest risk)
- `specs/007a-pA/dependency-graph.mmd` — Notes 加 R1 修订标注
- `specs/007a-pA/tech-stack.md` — Version → 0.2;D19 redact 实装
- `specs/007a-pA/compliance.md` — Privacy posture 加 stderr secret redact
- `specs/007a-pA/tasks/T002.md` / T003 / T010 / T011 / T012 / T013 / T020 / T030 / T031 / T032 / T033 — 全部有改动(详见 R2 修订报告 + git diff)
- `discussion/007/007a-agent-emit/007a-pA/PRD.md` — **PRD 也改了一处**!operator 显式 sign-off C1 budget 从"10-20h" → "12-23h",接受 12.5% overshoot;Revision log 写在 PRD 末尾

R1 review 仍在仓库:
- `.codex-outbox/queues/007a-pA/20260508T124631-007a-pA-L4-adversarial-r1.md`(R1 verdict + 3 blocker + 6 high concern 全文)

**HARD CONSTRAINT**: 在 reuse-session 模式下,**只读上面列出的新 / 改动文件**和 R1 outbox。其他 R1 阶段已读文件不重读。

## Verification responsibility

针对 R1 的每条 finding,核查 R2 fix 是否真闭环:

### Blockers must close

- **B1 (threshold relabel)** → 检查 `architecture.md §5.2` 的两阶段决策表是否真去耦合;`tasks/T010.md` 测试 `test_judge_threshold_actually_filters`(M/L 在 threshold=high 时不写 friction-log)是否在 task verification 段显式断言;`tasks/T030.md` 是否加了正负 case 测试。**核查方法**:读 architecture.md §5.2 + T010 + T030,看是否有 H/M/L feature classification 独立 + threshold 整数 ordering 真过滤
- **B2 (verification 引用不存在的模块)** → 检查 spec.md §6 的 O1/O3 verification commands 是否引用 `friction_tap.tools.count_entries` 或 `friction_tap.tools.health_check`。改后应该用 `python -m friction_tap.grep --count-agent-emit` 或 `python -m friction_tap.trust_summary --check-enabled`。**核查方法**:grep 旧 module 名应 0 命中;新 entry point 应在 tasks 里真实装
- **B3 (O3 不忠 PRD)** → 检查 spec.md §1 + §6 O3 是否回到 PRD 原意 = "hook 仍 enabled / 没被 friction --off"(严格 binary)。activity freshness 应单列为 informational metric,不进 O3 verdict。**核查方法**:对比 PRD §"Success" L78 与 spec.md §1 O3 用词应一致;trust_summary 输出应 separated "## Adoption" 与 "## Activity health" 两段

### High concerns must close

- **H1 (O6 evidence threshold)** → spec.md §1 + §6 O6 是否要求 label + reason ≥ 20 CJK / 8 English;`tasks/T020.md` self-interview prompt 模板是否固定双行格式;`tasks/T031.md` 是否加了 prompt format integration test
- **H2 (adjudication tag race)** → spec.md 是否加了 C14 hard constraint(review session 必先 `--off`);`tasks/T012.md` 是否加 `--review-mode` flag;`tasks/T031.md` 是否加 TestStaleSaveTagRebinding 集成测试;README 是否加 review flow 教学
- **H3 (payload schema drift)** → state.json 是否加 `schema_error_count` monotone counter;hook 异常是否 +1 + last_run.log;trust_summary 是否输出 `## Schema health` 段;install.sh 是否跑 post-install real-payload smoke test
- **H4 (Read 越界 OQ-E)** → spec.md §4 D8 whitelist 是否移除 Read;non-goals.md 是否显式写 v0.2;tasks/T010 + T030 是否加 Read 反向断言
- **H5 (HANDOFF lossy)** → HANDOFF.md Step 3 是否升级为 mini-PRD block 含 Problem/Context + User persona + O1-O6 + Scope + C1-C14 + UX principles + Biggest risk + Verification 八节;Schema 转换表是否加 3 行(Problem/Context, UX principles, Biggest risk)
- **H6 (T022 ghost + sum)** → spec.md §5 task list 是否完全 13 个 ID 一致;dependency-graph.mmd 是否 13 节点;sum 是否重新计算 22.5h;Schedule risk 是否显式段

### Medium-low(2 critical fixed,3 deferred)

- 修了 **PIPE_BUF 措辞** + **D19 stderr redact**,核查 architecture.md §5.3 + tasks/T003.md 实装
- 延期 v0.2 三项是否在 non-goals.md 显式列(`-f` persistence / `--reset-state` / entry-volume math)

### NEW · PRD changed

**PRD v1.0 → v1.1** 也是 R2 fix 的一部分:operator 显式 sign-off C1 budget overshoot。核查:
- PRD.md C1 行是否更新为 12-23h
- PRD.md 末尾是否有 Revision log 节,标记 2026-05-08 sign-off
- spec.md C1 引用是否同步(若有)— 检查 spec.md §3 Constraints 表是否标 v1.1 PRD 来源

### NEW · 检测引入新问题

R2 修订引入了 ~10 个改动(D16/D17/D18/D19 + 多个 task hours bump + schedule risk)。核查:
- 边界 case:D16 confidence-feature 决策表是否有反直觉边界(eg "stderr len 50 = M / 49 = L" 截断式)?有无显式 rationale?
- D17 review-mode 与 D7 双指标(O3 enabled 检查)的交互:operator 在 review-mode 期间 enabled = false,O3 verdict 是否会因此 false fail?
- D18 schema_error_count 与 BUS-1 operator 暂离:14 天没操作 + schema-error 累积怎么解读?
- D19 redact regex 7 类 token shape 是否会误杀正常字符串(false positive redaction)?会损失 friction 信号吗?
- 22.5h 总工程量在 critical path 10h 单线程下,operator 5-10 hr/week 假设的 wall-time 计算是否过于乐观?是否漏算 review 时间(本身就是 2 周 dogfood 期内)?

## Write

`.codex-outbox/queues/007a-pA/20260508T133336-007a-pA-L4-adversarial-r2.md`,
内容:

```markdown
# Adversarial Review · 007a-pA · R2

## R1 → R2 closing status

| R1 finding | Fix status |
|---|---|
| B1 threshold relabel | CLOSED / PARTIAL / OPEN(详细) |
| B2 verification commands missing | CLOSED / ... |
| B3 O3 不忠 PRD | CLOSED / ... |
| H1 O6 evidence threshold | CLOSED / ... |
| H2 adjudication tag race | CLOSED / ... |
| H3 schema drift counter | CLOSED / ... |
| H4 Read whitelist | CLOSED / ... |
| H5 HANDOFF schema | CLOSED / ... |
| H6 T022 + sum | CLOSED / ... |
| M1 PIPE_BUF 措辞 | CLOSED / ... |
| M2 D19 redact | CLOSED / ... |
| M3-5 deferred v0.2 | DOCUMENTED ✓ |

## New blockers (R2 引入或 R1 漏抓的)

(if any — be honest)

## New high-severity concerns

## Medium / Low

## PRD faithfulness check (post-R2)

- [ ] Spec §1 O3 字面与 PRD §"Success" O3 字面一致(binary enabled,不附加 24h / activity)
- [ ] HANDOFF Step 3 mini-PRD block 完整覆盖 IDS PRD 全 8 字段
- [ ] D8 whitelist 与 PRD OQ-E candidate 一致(无 Read,4 项 = Bash + Edit + Write + Claude Code lifecycle hook block)
- [ ] PRD v1.1 C1 budget 12-23h 与 spec.md sum 22.5h 一致

## Top 3 things the operator personally must verify

1. ...

## Verdict

**One of**:
- **CLEAN** — R1 + R2 fix 闭环,无新 blocker,spec ready to build
- **CONCERNS** — 几条 high concern 可推 follow-up,不阻塞 build
- **BLOCK** — 仍需 R3

(必含 1-2 段 explanation)

---
**Quote ≤15 words verbatim**.
```
