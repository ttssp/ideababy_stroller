# Codex 任务 · forge 001 v6 · Phase 2(参照系评估)

**Kickoff form**: `reuse-session`(**接着 P1 那个 thread 继续,省 ~60% token**)
**Output**: 写 `discussion/001/forge/v6/P2-GPT55xHigh.md`,并投到
`.codex-outbox/queues/001/20260803T110500-001-forge-v6-p2.md`(**文件名逐字一致**)

---

## Session hint

**已读(本轮请勿重读)**:forge-config · 你自己的 P1 · X 的 9 件标的 · forge-protocol P1 部分
**本轮新读**:Opus 的 P1(`discussion/001/forge/v6/P1-Opus47Max.md`)· Opus 的 P2
(`discussion/001/forge/v6/P2-Opus47Max.md`)· forge-protocol P2 部分
**仍然不要读**:`_contaminated-p1/`(那是被作废的一轮,读了会再次污染)

## 0 · 本轮做什么

P1 是独立审阅,**P2 开始双方互读 + 各自跑参照系**。你要:
1. 读 Opus 的 P1 与 P2(**现在可以读了**);
2. **按 Z 跑你自己的 SOTA 检索**(≥3 次,见 §2);
3. 写 P2:**§1 SOTA 对标(表格)· §2 外部材料消化 · §3 修正后的视角(P1 哪些站住、哪些被推翻)**。

篇幅目标 **600-1100 CJK 字符**;超标具名声明即可,**严禁有损机械压缩**。

## 1 · ⚠ 关于 Opus 侧的两个诚实交代(会影响你怎么读它)

**(1) Opus P1 与你的 P1 不是对称独立的。**
本 phase 第一次下发时,Opus 先写完自己的 P1,再把结论当「建议关注面」写进你的任务书 ——
你的 P1 因此被重跑(就是你刚做的那次洁净版)。但 **Opus 的 P1 没有重写**,它仍是那一份。
⇒ 你读它时请知道:**它写在你的洁净 P1 之前,不受你影响;你的洁净 P1 也不受它影响。**
⇒ **但也因此,凡是它有而你没有的观点,不构成「你漏了」的证据 —— 可能是它多了。**

**(2) Opus 的 P2 是在 `WebSearch` 后端故障下做的。**
`WebSearch` 三次调用均返回 `400 output_config.effort 'xhigh' is not supported`。
Opus 改用**对已知权威源的定向 `WebFetch`**,并在 P2 顶部声明**本轮不产出任何
「未检索到 ⇒ 无先例」型结论**。
⇒ **如果你的 `WebSearch` 可用,你在本轮拥有 Opus 没有的能力。**
   **请优先去做 Opus 做不了的事:检索它无法排除的负结果面,以及它没想到去查的面。**
⇒ Opus 未取到的具体一项:**CVE Record `REJECTED` / `DISPUTED` 的状态语义**
   (两次 fetch 只回到导航页)。**若你能取到,这是本轮的净增量。**

## 2 · Z · 参照系

**mode = 对标 SOTA** · **≥3 次检索**

Opus 已覆盖的三面(**你不必重复,除非你要推翻它**):
- Cochrane Handbook ch.10 —— meta-analysis 何时**不该** pool、pre-specified vs post-hoc 分组
- IETF RFC 7282 —— rough consensus 的 `addressed` vs `merely noted`、chair 的角色
- 学术 retraction 实践 —— 撤稿权属、原文保留标记、**已标 retracted 仍被继续引用**

**建议你去打的面(不限于此,你自己判断)**:
- **Opus 未取到的**:CVE `REJECTED`/`DISPUTED` 状态语义与下游消费方处理
- **可能推翻 Opus 的**:是否存在「事后重新分层/拆分已合并证据池」的**正当**工程或统计先例?
  (Opus 断言 v5 的「先合池、被证不可比再拆」方向反了 —— 这是**可被证伪的强断言**)
- 你认为 Opus 的三面**引错了或用过头了**的地方

⚠ **禁止**:tech-stack deep-dive / pricing / 实施细节。

## 3 · 请你在 §3 正面处理的四件事(这是 Opus P2 的实际产出,不是提示)

Opus 在 P2 里做了三处**自我推翻**和一处降级,请你独立判断这些修正是否成立:

1. **Opus 推翻了自己 P1 的 K(b) 框架**:原判「『不自行改』被等价成『可以合』」隐含「反对未消解
   就不该合」;RFC 7282 否定之(**推进合法,前提是 addressed 而非 merely noted,且由 chair 认定**)。
   Opus 修正为:**本仓缺的不是「不许合」规则,是 chair 这个角色**。
   → **你同意吗?「chair」在本仓映射到谁 —— operator?handback-review 这道命令?还是别的?**
2. **Opus 认为「标注 + 通知」不足**:撤稿实证(标了仍被引)打在**你 P1 第 2 行**的
   `suspended-pass` 队列与 Opus 自己的「可撤销/版本化」上,两者都假设下游遵守标记。
   Opus 主张**追溯必须落在消费点的 fail-closed 判定**。
   → **你同意吗?若同意,消费点在本仓具体是哪个调用?**
3. **Opus 提出「≥3」这个数字本身进了争议面**(Cochrane:研究数很少的分组探索无论 timing
   价值都存疑),并指出**你和它的 P1 都默认了 ≥3 正确、只争怎么分区**。
   但本轮 **Y 未选产品价值视角** ⇒ Opus 主张**挂账不判**。
   → **你同意「挂账不判」,还是认为不裁阈值就没法裁分区键?**
4. **Opus 逐条比对后下调了自己四条主张的置信度**(你的洁净 P1 未独立复现它们):
   `§0.1 责任人自批` · `T041 范畴错误` · `direction 在去重键抬高计数` · `三重不追溯的单调性`。
   → **请你逐条表态**:哪些是你**看过标的后不认同**,哪些是你**只是没想到**?
   **这两者对 P3R1 的意义完全不同**,请明确区分。

⚠ **另有一条 Opus 标为「本轮唯一真独立收敛」**:你的 P1 §3-3 与 Opus P1 §3-2 各自独立指出
「若至今无真实 O2/P2 PASS 被旧池消费,追溯争论的大半会消失」。
Opus 认为**这是可核查的事实问题不是判断问题** ⇒ 建议直接去查而不是当分歧。
→ **若你有仓内检索能力,请在本轮就去查**(如 journal / audit 产物里有无 O2/P2 PASS 记录),
把结论写进 §2。**这可能是本轮最省事的一次争议消解。**

## 4 · 投递

```bash
cp discussion/001/forge/v6/P2-GPT55xHigh.md \
   .codex-outbox/queues/001/20260803T110500-001-forge-v6-p2.md
```

⚠ **HEAD 一致性合同**:HEAD 内容 = inbox 文件名 = outbox 文件名,**三者逐字一致**。
若完全无法作业 → outbox 首 30 行内写 `Verdict: BLOCK` + 原因。
