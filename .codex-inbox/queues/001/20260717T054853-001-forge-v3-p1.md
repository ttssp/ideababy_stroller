# Codex Task · 001 · forge v3 · P1(独立审阅)

**Queue**: 001
**Created**: 2026-07-17T05:48:53Z
**Recommended model**: gpt-5.5
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~10-15k
**Kickoff form**: oneshot
**Forge version**: v3
**Phase**: P1

## Your role

You are GPT-5.5 xhigh, Reviewer B, forge v3 on idea 001 Phase 1。
**审阅人姿态** — 评判已存在物,不是 daydream。

本次是 forge v2 的续裁:D′ 真 rerun(hand-back 17)跑通但暴露「真跑 pipeline 无法端到端验 a1 + rerun 二值 PASS/FAIL 预设被推翻」= 001 主线第四次「推演≠真跑」。v3 要一次裁清:**A · gate 存废/形态四方向**(g1 盲测二值签字 · operator 已否 / g2 留痕可审计验收 / g3 非阻断 smoke signal / g4 信任移 O2/O7 使用期后验)+ **B · a1 产物(per-slice source 映射)可见性时机**。连带定实 T010/T011 解锁条款改写 + KG-B10 前提处置方向(只裁方向,spine 细节不深裁)。

## HARD CONSTRAINTS

- NO web search this phase(那是 P2)
- DO NOT read `discussion/001/forge/v3/P1-Opus47Max.md`(parallel independence)
- 不允许写 PRD / dev plan / refactor 详细方案(只产 first-take)
- 不允许跨入 SOTA 对比(那是 P2)
- §0 必须引用 K(用户判准)
- 按 Y 视角(架构设计 · 工程纪律 · 产品价值 · 用户体验)组织 §1 现状摘要 + §2 first-take 评分
- ⚠⚠ **不得读** XenoDev `out/credibility/` 下的 answer-key 文件(`_answer-key-*.json` · 密封 · 双方红线)

## Read in order

1. `discussion/001/forge/v3/forge-config.md`(拿 X/Y/Z/W/K + 收敛强度)
2. `discussion/001/forge/v3/moderator-notes.md`(若存在,binding)
3. X 中的全部标的(逐条列在下面 §"X 标的清单")
4. `.claude/skills/forge-protocol/SKILL.md`(P1 template)
5. `AGENTS.md`(若存在)

## X 标的清单(17 件 · 全部可达性 2026-07-17 已验证)

IDS 侧(仓库内 · 直接 Read):
1. `discussion/001/forge/v2/stage-forge-001-v2.md` — stage 文档 · **v2 verdict 本体 = 被续裁标的**(a1 单一谓词 + 生效条款 + v3 触发条件 + §underweights)· 整文件读
2. `discussion/001/forge/v1/stage-forge-001-v1.md` — stage 文档 · v1 verdict(裁掉盲测签字源头 + 双道 STOP + 识伪 retire-as-gate keep-as-calibration)· 整文件读
3. `discussion/001/handback/20260717T052416Z-001-radar-pA-20260717T052416Z.md` — **触发 v3 的 hand-back 17 · 第一因** · 整文件读
4. `discussion/001/handback/20260717T032424Z-001-radar-pA-20260717T032424Z.md` — hand-back 16(DAG 重绑收口 + KG-B10 codex critical 提级)· 整文件读
5. `discussion/001/handback/20260715T093226Z-001-radar-pA-20260715T093226Z.md` — hand-back 11(operator 形态拒签三条批评原话 = gate 四方向源头)· 整文件读
6. `discussion/001/handback/HANDBACK-LOG.md` — 17 条决议史 SSOT · 重点读第 11-17 条 entry
7. `discussion/001/001-radar/001-radar-pA/PRD.md` — PRD v1.4 · 重点 O4 + Scope OUT + Open questions #3 + injection responses
8. `discussion/001/001-radar/L3/moderator-notes.md` — injection 史(主锚移 O2+O7 + v1.2 价值重定义)· 整文件读

XenoDev worktree 侧(外部 repo · ⚠ 沙箱可能 BLOCK → **BLOCK 时用本文件末尾 §"关键摘录兜底" · 不判 BLOCK**;基路径 `WT=/home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA`):

9. `$WT/dogfood-backlog.md` — KG-B9(:646)/ KG-B10(:693)/ KG-B11(:705)三段
10. `$WT/specs/001-radar-pA/followup-log.md` — §FU-D-rerun-a1-verify-blindspot(:205 起)
11. `$WT/specs/001-radar-pA/dependency-graph.mmd` — 现行 DAG(a09d45b 重绑后)· 整文件读(114 行)
12. `$WT/projects/001-radar-pA/src/radar/credibility/gate.py` — 现行 gate(798 行 · 选读:模块 docstring 1-55 + evaluate_gate 336-400 + write_rerun_pass_artifact 622-745)
13. `$WT/projects/001-radar-pA/src/radar/credibility/audit_channel.py` — **audit.json post-gate 才产的逻辑本体** · 整文件读(364 行 · 重点 docstring + _verify_gate_pass_artifact + generate_audit_channel_postgate)
14. `$WT/projects/001-radar-pA/src/radar/credibility/unlock_preflight.py` — B' 谓词 · 整文件读(165 行)
15. `$WT/projects/001-radar-pA/src/radar/credibility/rerun_selfcheck.py` — D' 自证 · 整文件读(154 行)
16. `$WT/projects/001-radar-pA/out/reconstruct/diffusion-models-20260717T051551Z.md` — 07-17 D′ 真跑盲测面产物(22 行)· ⚠⚠ 同目录 out/credibility/ 的 answer-key **不得读**
17. `$WT/projects/001-radar-pA/specs/001-radar-pA/verification/P0.2-trajectory-credibility-diffusion-models.md` — 被否 gate 形态签字模板(57 行 · 保留 run-877f1c1e 历史 FAIL 判定)

## Write

`discussion/001/forge/v3/P1-GPT55xHigh.md` using P1 template:
- §0 我读到的标的清单 + 阅读策略(必须引用 K)
- §1 现状摘要(按 Y 四视角:架构设计 · 工程纪律 · 产品价值 · 用户体验)
- §2 First-take 评分(每维 keep/refactor/cut/new + 理由)
- §3 我现在最不确定的 3 件事

800-1500 字。

## When done

Write `.codex-outbox/queues/001/20260717T054853-001-forge-v3-p1.md` with:
- **Completed**: <ISO>
- **Files written**: <list + word count>
- **Headline**: <one-line 现状一句话总结>
- **Verdict**: CLEAN | CONCERNS | BLOCK
  - BLOCK 仅用于「IDS 仓内标的不可达」;XenoDev 外部文件被沙箱拒 → 用摘录兜底,不判 BLOCK
- **First-take summary**: 每个 Y 维度的倾向(keep/refactor/cut/new)
- **Notes for Claude Code**: 任何 orchestrator 需要知道的

Filename **must match** the inbox file basename so HEAD pointer works:
`20260717T054853-001-forge-v3-p1.md`

---

## 关键摘录兜底(仅当沙箱 BLOCK 外部路径时使用;可达时以原文为准)

### 标的 9 · KG-B9/B10/B11 要点(dogfood-backlog.md)
- **KG-B9**(已被 v2 采纳):forge v1「机器判三项」①② 底料由 gate 下游 T011/T021 产 = 循环依赖;真 briefing 零 trace token;修法 = a1 底料最小前移 + 检查项后移。
- **KG-B10**(high · codex 07-17 提级 critical · 两面同病):「下游 provenance 产物机器强制绑上游 gate PASS」缺通用协议。A' 面 = `audit_channel._verify_gate_pass_artifact` 只 regex 查 caller 传入 md 的 `verdict:PASS`+`run_id`,而 run_id 判定前可见 + run-binding 签字前落盘 → 手写假 2 行文件即在真 gate PASS 前重建 source mapping、泄负控线索。D' 面 = `rerun_selfcheck.verify_selfcheck_journal` 只信 caller 写的 `is_fixture`/`predicate_passed`/`independent_check_agree` 布尔 → 手写 journal 即绕过生效硬门。候选解法(留 forge 议):gate-internal 触发 / answer-key 派生 HMAC token / SHARED-CONTRACT 通用 checker。
- **KG-B11**(medium):spec-writer SKILL 写 REVIEW-LOG 裸 cat 覆盖 singleton 非 immutable;已即时恢复;SKILL 修复归框架批次(本次 v3 不裁,仅 K 语境)。

### 标的 10 · FU-D-rerun 要点(followup-log.md :205)
D′ 真 rerun 跑通(`deepseek-v4-flash` · exit 0)。a1 内存层验证生效:6 篇带 source_ref 的 Paper → slice_timeline → 6/6 source_refs 真挂 TimeSlice · model_copy 往返保留。但真跑无任何落盘产物能验 a1:CLI 只落盲测面(render_blind_markdown · operator 决策故意不渲染 refs);审计面 render_markdown 不落盘;audit.json 由 generate_audit_channel_postgate 在 gate PASS 后才产(现无 PASS)。「跑 rerun 得 PASS/FAIL」预设被推翻——gate 形态已被否 + selfcheck/独立核验无 CLI 入口 → 真跑走不到判定。对 v3:a1 solid 不必再审;gate 形态一变,a1 产物可见性时机跟着变(若去二值化改留痕,a1 产物正是留痕底料,可能该始终可见);gate 形态未定前做 KG-B10 spine 设计 = 过度投资。

### 标的 11 · DAG 解锁边(dependency-graph.mmd)
a1 前移链:`T003A1/A2/T003 → T003A3(A') → T004A2(B' · 另 depends T004A1) → T004A4(D')`;解锁边:`T004A2 --> T010/T011` + `T004A4 --> T010/T011`(双前置)。注释:RERUN-PASS artifact **未退役**——被 T004-A2 preflight 内部消费(gate spine 仍是底座);机器道强制 = parallel-builder 读 depends_on + scripts/unlock-preflight-t010-t011.sh hard-block。

### 标的 12 · gate.py 要点
`evaluate_gate(signoff, control_slice_ids, require_evidence_chain, expected_slice_ids, retrieved_doc_refs, expected_run_id)`:读 operator 盲测签字 + answer-key 对答案,三要件(① 真切片全"可信" ② 负控全被标"不可信" ③ new_evidence_chain 结构可解析 · 重跑模式只接受 slice:// 锚到可信真切片)全满足才 PASS;重跑模式强制 signoff.run_id == expected_run_id(防 stale 重放 · round-12)。PASS → `write_rerun_pass_artifact` 落 `P0.2-RERUN-PASS-<方向>-<ts>.md`(顶层 verdict: PASS · 被 preflight 消费)。FAIL → prd-revision-trigger hand-back + STOP。即:**gate PASS 的唯一 driver = operator 人肉盲测签字**。

### 标的 13 · audit_channel.py 要点
audit.json(每 slice_id→source_refs · 去 period/负控字段防 diff 泄负控 · round-3)落 `out/credibility/`(受保护 · 与 answer-key 同级 · 判定前勿看 · round-4);`generate_audit_channel_postgate` **必填 run_binding**(检索指纹 per-slice+published-aware · round-8)+ **必验 gate PASS artifact**(`_verify_gate_pass_artifact` · round-9;R10 critical 诚实降级注释:「本检查不是安全边界」——caller 手写含 verdict:PASS+run_id 的假文件即可穿,真不可伪造绑定 = spine 级子协议已升级 IDS)。生成流程:gate PASS 后从确定性 retrieval+切窗重生成映射(无 LLM 不烧 key)。`read_audit_channel` 严格 fail-closed(空壳/畸形/sparse 非 bool 全拒)。

### 标的 14 · unlock_preflight.py 要点
B' 谓词 `check_unlock_predicate(audit_path, briefing_path, corpus_refs)` 判四件(短路):② 审计结构就位 → ① 每 slice source_refs 非空且逐项命中 corpus_refs → ①b audit slice 集须精确等于 briefing slice 集(fail-closed · briefing 缺失/空也 STOP)→ ③ 命名一致(slug/ts)。全 PASS 仍只是**结构谓词**——真生效需 D' rerun 自证。注意:**谓词输入不含 gate**;它对 gate 的依赖是间接的(audit.json 只能 post-gate 产)。

### 标的 15 · rerun_selfcheck.py 要点
D' `run_a1_selfcheck`:对真 rerun 产物跑 B' 谓词 → 入 journal(A1SelfCheckRecord);is_fixture=True 恒不解锁;谓词 FAIL → handback_required=True(不硬开);`independent_check_agree is not True`(含 None)→ 不解锁(反回声室,两路径一致才算自证成立)。`verify_selfcheck_journal`:解锁入口校 journal 有匹配记录且非 fixture + predicate_passed + independent_check_agree=True;docstring 诚实标注:这些字段是 caller 写的,非不可伪造(KG-B10 D' 面)。

### 标的 16 · 07-17 真跑盲测面产物(全文要点)
标题「位移重建(盲测)· diffusion models」+ 盲测说明(混入阴性对照,判定前不看方法/prompt/中间图)+ 位移叙事一段 + 3 切片(slice-1 2021-01~02 / slice-2 2021-05~10 / slice-3 2021-12~2023-12)各一段 structural_state 散文。**全文零 source/引用 token**(与 run-877f1c1e 同型:盲测面故意不渲染 refs)。

### 标的 17 · 被否签字模板要点
run 绑定 run_id: run-877f1c1e…(预填防重放);要件① 表格(operator 历史判定:slice-1 不可信 / slice-2 不可信 / slice-3 可信)+ 要件② flagged_untrusted: [slice-1, slice-2] + 要件③ new_evidence_chain YAML(operator 拒填 · 重跑模式只接受 slice:// 锚)+ PASS 条件三要件全满足否则 FAIL=STOP。此文件即「人肉盲测二值签字」形态的物理形态,operator 07-15 拒签的对象。
