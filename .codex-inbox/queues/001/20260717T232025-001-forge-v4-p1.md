# Codex Task · 001 · forge v4 · P1(独立审阅)

**Queue**: 001
**Created**: 2026-07-17T23:20:25Z
**Recommended model**: gpt-5.5
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~8-15k
**Kickoff form**: oneshot
**Forge version**: v4
**Phase**: P1

## Your role

You are GPT-5.5 xhigh, Reviewer B, forge v4 on idea 001 Phase 1。
**审阅人姿态** — 评判已存在物,不是 daydream。

本次 v4 议题**极窄**(v3 续裁):forge v3 生效条款「对 07-17 真产物立即自证 PASS」的前提在真跑下
不成立(07-17 产物是 pre-split 校准产物,v3 自己的评估/校准分流禁止评估自证消费它 = 第五次
「推演≠真跑」)。**只裁一件事:生效条款标的重裁**,三选项 α/β/γ(见 forge-config K)。

## HARD CONSTRAINTS

- NO web search this phase(那是 P2)
- DO NOT read `discussion/001/forge/v4/P1-Opus47Max.md`(parallel independence)
- 不允许写 PRD / dev plan / refactor 详细方案(只产 first-take)
- 不允许跨入 SOTA 对比(那是 P2)
- §0 必须引用 K(用户判准)
- 按 Y 视角(架构设计 / 工程纪律 / 产品价值)组织 §1 现状摘要 + §2 first-take 评分
- ⚠⚠ **红线**:XenoDev `out/credibility/` 下同 stem `_answer-key-*.json` / `*.runbinding.json`
  是密封盲测答案,**只可指认文件名,不得读内容**

## Read in order

1. `discussion/001/forge/v4/forge-config.md`(拿 X/Y/Z/W/K + 收敛强度)
2. `discussion/001/forge/v4/moderator-notes.md`(若存在,binding)
3. X 中的全部标的(逐条列在下面 § "X 标的清单";XenoDev 5 件若沙箱拒读,**用本文件附录摘录,不判 BLOCK**)
4. `.claude/skills/forge-protocol/SKILL.md`(P1 template)

## X 标的清单

IDS 侧(本仓库,直接读):
1. `discussion/001/forge/v3/stage-forge-001-v3.md`(stage 文档 · v3 verdict 本体 = 被续裁标的;重点:Verdict 段生效条款原文 + §underweights v4 触发条件 + Decision matrix)
2. `discussion/001/handback/20260717T092547Z-001-radar-pA-20260717T092547Z.md`(触发 v4 的 hand-back 19 · 第一因;重点:§4 codex 9 轮 + v4 触发核心 + §6 α/β/γ 三选项)
3. `discussion/001/001-radar/001-radar-pA/PRD.md`(PRD v1.5;重点:O4 生效条件写死原文 + Open questions #3)
4. `discussion/001/handback/HANDBACK-LOG.md`(19 条决议史;重点:第 11/16/17/18/19 条 = 五次「推演≠真跑」lineage)

XenoDev worktree 侧(外部 repo `/home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA/`,⚠ 沙箱可能拒读 → 用附录摘录):
5. `projects/001-radar-pA/src/radar/credibility/selfcheck_sequence.py`(D″ 自证序列本体;三道 fail-closed 守卫 = 挡 β 的代码面;→ 附录 A)
6. `projects/001-radar-pA/src/radar/cli/reconstruct.py`(A″/C″ 分流实现;→ 附录 B)
7. `scripts/selfcheck-07-17.sh`(生效自证手跑脚本 = STOP 复现入口;→ 附录 C)
8. `projects/001-radar-pA/out/reconstruct/diffusion-models-20260717T051551Z.md`(07-17 真产物本体 = 标的错配物证;→ 附录 D **全文**)
9. `projects/001-radar-pA/tests/test_selfcheck_sequence_forge_v3.py`(离线序列测;→ 附录 E 测名清单)

## Write

`discussion/001/forge/v4/P1-GPT55xHigh.md` using P1 template:
- §0 我读到的标的清单 + 阅读策略(引用 K)
- §1 现状摘要(按 Y 视角:架构设计 / 工程纪律 / 产品价值)
- §2 First-take 评分(keep/refactor/cut/new + 理由)
- §3 我现在最不确定的 3 件事(留给 P2 搜索 / P3 协商)

800-1500 字。**对 α/β/γ 给出诚实第一反应立场**(K 已声明 XenoDev 侧倾向 γ,你不必顺从——
若你独立判断 α 或 β 或第四条路更对,直说并给理由)。

## When done

Write `.codex-outbox/queues/001/20260717T232025-001-forge-v4-p1.md` with:
- **Completed**: <ISO>
- **Files written**: <list + word count>
- **Headline**: <one-line 现状一句话总结>
- **Verdict**: CLEAN | CONCERNS | BLOCK
  - BLOCK 仅用于 IDS 侧标的不可达(XenoDev 5 件沙箱拒读时用附录摘录,不判 BLOCK)
- **First-take summary**: 每个 Y 维度的倾向(keep/refactor/cut/new)+ α/β/γ 初步立场
- **Notes for Claude Code**: 任何 orchestrator 需要知道的

Filename **must match** the inbox file basename so HEAD pointer works:
`20260717T232025-001-forge-v4-p1.md`

---

## 附录(XenoDev 5 件关键摘录 · 沙箱拒读时的兜底;可达时以原文为准)

### 附录 A · selfcheck_sequence.py 关键面(共 315 行)

三道 fail-closed 守卫(`run_selfcheck_sequence` 入口,codex 9 轮 adversarial 建成):

1. **①a 正文形状**(`_require_eval_briefing_shape`):denylist(盲测专属标记 `**盲测说明**` /
   `混入了阴性对照切片` / `位移重建(盲测)`)早拦 + **正向 allowlist**——按 slice heading 分段、
   逐段要求块级来源块 `**来源(source refs...)**:`(评估面 `render_markdown` 独有 · 盲测面从不产),
   单 pass 连续 fence-aware parser(fence 状态跨 heading 传递),与 B′ coverage 逐字节复用同一
   `_BRIEFING_SLICE_LINE_RE`(单一真相)。
2. **①b 校准 stem**(`_reject_if_calibration_stem`):briefing 所在 root 的 sibling
   `out/credibility/` 存在同 stem `_answer-key-*.json` / `*.runbinding.json` → fail-closed;
   root 不可判定(briefing 不在 `<root>/out/reconstruct/` 下)也 fail-closed(防 cwd 绕过)。
3. **② 注入 retriever 强制 fixture**:`effective_is_fixture = is_fixture or (retriever is not None)`
   ——只有默认真 OpenAlex 路径可铸非 fixture 生效解锁(防合成数据自报真 rerun)。

诚实边界(docstring 原文要点):「直调公开 `generate_eval_audit_for_selfcheck` +
`run_a1_selfcheck(is_fixture=False)` 仍可绕 ②(wrapper-only)· caller 布尔 independent_check_agree
亦自报 = 不可伪造 provider-proof spine 子协议 · 已升级 IDS · 本轮显式冻结不建」。

`generate_eval_audit_for_selfcheck`:确定性 retrieval + 切窗生成 audit.json(无 LLM · 不烧 key ·
不要求 gate PASS · **不复用** postgate 函数避免牵动 KG-B10 冻结绑定面)。

### 附录 B · cli/reconstruct.py 分流面(共 244 行)

`run_reconstruct(..., calibration: bool = False)`:
- **评估 run(默认)**:不注入负控 → `write_narrative_markdown`(human-readable 审计面,含
  source_refs)+ `write_audit_channel`(生成时落 audit.json)。**不产** answer-key/签字模板/
  run-binding。真跑路径用真 OpenAlexRetriever + 真 LLM(DeepSeek,从 env 读 key,C8)。
- **校准 run(--calibration)**:注入负控(密码学随机 seed)+ answer-key 密封 + 盲测面
  (`render_blind_markdown`)+ run-binding + 签字模板。**不落 audit.json**(选项 A:泄露面
  收窄 = 物理不存在,无同 stem 可 diff 兄弟文件)。零解锁语义只入 journal。

⚠ 注意:**评估 run 的 narrative 生成要走真 LLM**(`_build_real_llm` → DeepSeekLlmClient)——
这就是 α/γ 的「烧 key」代价来源;自证序列本身(附录 A)不烧 key,但它消费的评估 briefing
须由评估 run 产出。

### 附录 C · selfcheck-07-17.sh 头部「已知张力」注记(原文要点)

「07-17 现有真产物(diffusion-models-20260717T051551Z)**本身是校准/盲测产物**
(control_slice_ids=['slice-3'] · mode=fake_citation · 有同 stem answer-key/runbinding 兄弟)——
它由 pre-v3 run 形态产(总注入负控)。forge v3 引入评估/校准分流后,评估自证**绝不该消费校准
briefing**(否则校准负控面流入评估解锁面 · 破 C″)。故本脚本对 07-17 产物**正确 fail-closed STOP**
(_reject_if_calibration_stem 命中同 stem 校准标记)。这是 v4 触发条件①『生效条款前提在真跑下
不成立』的兑现——不硬改 · 不宣称假 PASS。」
脚本主体:独立真 OpenAlex 检索派生 corpus_refs(DDC 双路径)→ `run_selfcheck_sequence(
retriever=None, is_fixture=False, independent_check_agree=True)`。退出码 0=PASS,非 0=STOP。

### 附录 D · 07-17 真产物全文(23 行 · 标的错配物证)

```markdown
# 位移重建(盲测)· diffusion models

> ⚠ **盲测说明**:请**只读下方叙事与切片**判可信性,判定前**不看**方法/prompt/中间图。
> 其中**混入了阴性对照切片**(故意喂错误信息)· 请标出你认为**不可信**的切片。

## 位移叙事

从2021年初至2023年底,扩散模型经历了从单一生成理论向多模态生成范式的结构位移:[…一段位移叙事…]

## 历史时间切片

### slice-1 · 2021-01 ~ 2021-02
这批工作分别从生成模型和社会网络两个方向扩展了扩散过程的理论与应用。

### slice-2 · 2021-05 ~ 2021-10
这批工作集中展开了扩散模型在图像、3D点云、时间序列、离散状态空间及语音合成等领域的结构扩展与
条件化改进,形成了以去噪扩散概率模型为基座、向多模态与多任务泛化的生成范式集群。

### slice-3 · 2021-12 ~ 2023-12
这批工作展示了扩散模型从图像生成的基本范式,被系统性地扩展和适配到图像分割、视频生成、文本引导
编辑、图像修复、推荐系统、机器人运动规划等多个下游任务中,[…]
```

**结构要点**:标题即「(盲测)」· 有盲测说明块 · 三个 slice 段**全部无 per-slice 来源块**
(评估形状 allowlist 特征缺失)· sibling `out/credibility/` 有同 stem `_answer-key-diffusion-models-
20260717T051551Z.json` + `diffusion-models-20260717T051551Z.runbinding.json`(密封,只指认不读)。
即 07-17 产物有**三层校准身份证据**,任何一层都足以让评估自证 STOP。

### 附录 E · test_selfcheck_sequence_forge_v3.py 测名清单(14 测)

- `test_selfcheck_sequence_generates_audit_without_llm_or_gate_pass`
- `test_selfcheck_sequence_runs_predicate_and_journals_when_fixture`(fixture 恒不生效解锁)
- `test_selfcheck_sequence_injected_retriever_forces_fixture`(守卫②)
- `test_selfcheck_sequence_fail_closed_on_calibration_stem_briefing`(守卫①b)
- `test_selfcheck_sequence_fail_closed_on_calibration_body_shape_even_clean_root`(守卫①a denylist)
- `test_selfcheck_sequence_fail_closed_on_marker_stripped_calibration_briefing`(守卫①a allowlist)
- `test_selfcheck_sequence_fail_closed_on_prologue_stuffed_source_markers`(逐段校验)
- `test_selfcheck_shape_stops_when_marker_on_heading_line` / `..._empty_marker_block` /
  `..._marker_in_code_fence` / `..._fence_opens_before_heading`(块级 + fence-aware)
- `test_briefing_parser_and_shape_gate_agree_on_fenced_headings`(B′/D″ 双 parser 一致)
- `test_selfcheck_shape_stops_on_fenced_heading_padding`(coverage STOP)
- `test_selfcheck_shape_passes_genuine_eval_block`(正向锚,不误拒真产物)
- `test_selfcheck_sequence_fail_closed_when_briefing_root_undeterminable`(root fail-closed)
- `test_selfcheck_sequence_briefing_coverage_stops_on_mismatch`(切窗漂移 STOP)

**语义要点**:离线 fake retriever = 合成产物 → `is_fixture=True` → 序列可跑通但**恒不生效解锁**;
「07-17 真产物已生效解锁」必须真跑(scripts/selfcheck-07-17.sh),而真跑对 07-17 产物正确 STOP。
