# Codex 任务 · forge 001 v6 · Phase 3R2(finalize)

**Kickoff form**: `reuse-session`(继续同一 thread)
**`convergence_mode`**: **`strong-converge`**(从 forge-config frontmatter 读)
**Output**: 写 `discussion/001/forge/v6/P3R2-GPT55xHigh.md`,投到
`.codex-outbox/queues/001/20260803T113500-001-forge-v6-p3r2.md`(**文件名逐字一致**)

---

## Session hint

**已读(勿重读)**:forge-config · 双方 P1 · 双方 P2 · 双方 P3R1 · X 的 9 件标的
**本轮新读**:Opus 的 P3R2(`discussion/001/forge/v6/P3R2-Opus47Max.md`)· forge-protocol P3R2 部分
**仍不要读**:`_contaminated-p1/`

## 0 · 硬约束

- **零新检索**
- `strong-converge` ⇒ **必须 finalize 单一 verdict**;若某条仍冲突,**显式标 `unresolved`**,不得含混
- 残余分歧降 v0.2 note,**每条必须写明「回头看的触发时点(具名主体)」与「在此之前的风险敞口」**
  —— ⚠ **禁止**「首次实施时复核」这类无主体无时点措辞(**v5 note 1 正是这样写的,那就是本轮的口子**)
- 篇幅目标 **700-1200 CJK 字符**;超标具名声明,严禁有损压缩

## 1 · 产出模板(strong-converge 分支)

- **§1** 你对每条分歧的最终立场 + 让步
- **§2** 联合 verdict(**单一** · 200-400 字)
- **§3** 残余分歧降级为 v0.2 note
- **§4** W 形态产出的初步草稿建议(**W = `decision-list` only**)

## 2 · Opus 侧的 finalize 摘要(供你核对与反对)

**四条分歧,Opus 认为全部收敛**:
- **D-1** 你已同意 `direction` 进分区/比较类、退出去重计数,去重改 stable claim identity ⇒ 收敛;
  残余「stable claim identity 谁定义/何时冻结」降 v0.2 note-2。
- **D-2** 你已确认 §0.1 把 O2 PASS 向定成 warn ⇒ 隔离条件足够,T040 立即局部起跑。
- **D-3** ⭐ **Opus 让步,完全采纳你的对称版本**。Opus 承认原提法「只标自己四条」是单向的、
  对自己有利;你点名的 **v5 探索出口 / CVE 二态 / chair 映射**「也不是 P1 独立复现」成立。
  ⇒ 采你的三级标注 `independent` / `SOTA-corrected` / `post-hoc-accepted`,逐行打在 decision-list。
- **D-4** 采你的合一方案(理由「分开则接口只是标签、消费点无输入」被 Opus 判为决定性)。

**Opus 的联合 verdict 要点**:池化保留但 block-PASS 出口关闭、降探索/warn;重开前置 =
池键 `decision_epoch + rubric + comparability_class` 且 epoch 开启时冻结不可变 + direction 退出去重
+ 去重改 stable claim identity;追溯二态(rejected-like 消费点机器可拒 / disputed-like 不得当 clean PASS);
**实测零旧 PASS ⇒ 现在立规则最便宜,但零受影响须落成具名核查记录**;needs-attention 非 veto 但
override 须 operator-chair 逐条写明 addressed、spec-writer 不得自任、无 chair 记录不得 clean PASS;
T040 立即局部起跑;三态接口 + 消费点 fail-closed 合为一个具名新 task 不塞 T041。

**Opus 的 v0.2 note 两条**:
- **note-1「≥3」阈值未裁** —— 触发时点 = **首个 epoch 关闭且池首次达 ≥3 进入抽样**那一刻,
  由 **operator** 回答「这 3 条合起来是否支撑一个我敢用的判断」;敞口 = 可能产出
  **结构合规但对决策无用的 PASS**,且该敞口**与分区键正交、不因修键而消失**。
- **note-2 stable claim identity** —— 触发时点 = **T021 落地 claim 规范化算法**时;
  敞口 = PASS 向持续 warn,**同义改写/仅换锚/跨 epoch 重复**三攻击面全程开着。
  Opus 另指出:**note-2 未闭合前 note-1 的触发时点不可能到达**(池达不到可信的 3)。

**Opus 建议 synthesizer 出 8 行矩阵**,evidence_grade 分布 = `independent` ×4(行 1/5/7/8)·
`SOTA-corrected` ×3(行 3/4/6)· `post-hoc-accepted` ×1(行 2)。

## 3 · 请你在本轮正面做的四件事

1. **核对 evidence_grade 的逐行赋值有没有对 Opus 过于宽厚**。这一列是你提议的,
   **你比 Opus 更有资格判它**。特别请复核行 1(Opus 标 `independent`,理由「双方 P1 各自到达,
   源出 codex round-3 原文」)与行 5、7、8 —— **若你认为其中任何一行该降级,直接降**。
2. **反向检查**:Opus 有没有把**你的**某条主张标高了?或者把某条**双方都没独立到达、
   纯粹从 X 标的原文抄来**的结论标成了 `independent`?
3. **v0.2 note 两条的触发时点是否真的可执行**(有主体、有时刻、能被观察到发生)?
   若某条仍是软的,请改硬或标 `unresolved`。
4. **最后一次机会推翻联合 verdict**:若你认为其中任何一条在 `strong-converge` 下不该收敛,
   **现在显式标 `unresolved` 并说明**,比在 stage 文档里含混过去要好得多。

## 4 · 投递

```bash
cp discussion/001/forge/v6/P3R2-GPT55xHigh.md \
   .codex-outbox/queues/001/20260803T113500-001-forge-v6-p3r2.md
```

⚠ **HEAD 一致性合同**:HEAD = inbox 文件名 = outbox 文件名,三者逐字一致。
