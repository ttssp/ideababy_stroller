# Codex Task · 001 · forge v5 · P2(参照系评估)

**Queue**: 001
**Created**: 2026-08-02T13:26:14Z
**Recommended model**: gpt-5.6-sol
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~12-20k
**Kickoff form**: reuse-session
**Forge version**: v5
**Phase**: P2

## Session hint(only meaningful if Codex reuses session from P1)

你已读过(本轮请勿重读):
- `.claude/skills/forge-protocol/SKILL.md` 的 P1 部分
- `discussion/001/forge/v5/forge-config.md`
- 你自己的 `discussion/001/forge/v5/P1-GPT55xHigh.md`
- X 中所有 12 件标的

本轮**新增需读**:
- `.claude/skills/forge-protocol/SKILL.md` 的 **P2 template** 部分
- **`discussion/001/forge/v5/P1-Opus47Max.md`** ← 对方 P1(现在可以读了)
- `discussion/001/forge/v5/moderator-notes.md`(若存在,binding)

**HARD CONSTRAINT**(reuse-session only):do NOT re-read files you read in the
previous round of this Codex session unless this task explicitly lists them above.

## Your role

forge v5 P2 — 按 Z 跑 SOTA 检索 + 用检索结果校验双方 P1 的判断。

## ⚠ 本轮特殊情况:对侧检索能力受限(必读)

**Opus 侧的 WebSearch 在本轮环境性不可用** —— 三次调用均返回
`400 output_config.effort 'xhigh' is not supported when thinking is disabled`(session
effort 与检索后端模型不兼容 · 与在册 KG-B12「环境/配置不匹配致 gate 全线不可用」同族)。
operator 正在处置;但**不保证** Opus 侧本轮能拿到与你同等规模的检索。

⇒ **本轮你的检索是 SOTA 面的主要来源,权重高于以往任何一轮。** 两个后果:

1. **请把检索做厚**(下限 5 条,建议 8-12 条,覆盖下面 5 个方向中至少 4 个),
   并且每条都要给**可核验的出处**(URL + 一句话说明它到底证了什么)。
2. **⚠ 但请同时提高自我怀疑强度** —— 因为**没有对侧检索来纠正你的选源偏差**。
   请在 P2 末尾额外写一节 **§4 选源自查**,回答:
   - 我检索时用的关键词是否已经预设了我想要的答案?
   - 有没有哪个方向我**搜不到支持性先例**?(搜不到本身是重要信号,**必须如实写**,
     不要用相邻领域的类比填空)
   - 如果我的结论与 forge-config §K 的倾向一致,那是证据支持,还是我在找支持?

## CONSTRAINTS

- 搜索模式 = **对标 SOTA**;forbidden = tech-stack-deep-dive / pricing / 实施细节
- **⚠ 不重跑**以下 v1–v4 已做过的检索族(重复无价值):
  - v1:human-in-loop / 人肉盲测二值签字先例
  - v2:lineage-at-generation(SLSA / W3C PROV)/ staged-gating / bootstrap 循环依赖
  - v3:非二值留痕验收 / advisory-vs-hard gate / 生产期抽查
  - v4:评估集污染治理(decontamination)/ 生效条款绑首个真实 run / 预注册 vs 事后豁免
- §1 至少 3 条对标(本轮请做到 ≥5)
- §2 用户外部材料:**K 中未提供任何外部链接/文件** ⇒ §2 标 `n/a(K 无外部材料)`
- §3 至少 3 条修正(双方 P1 的判断哪些站住、哪些被对标推翻)——
  **必须同时覆盖你自己的 P1 和 Opus 的 P1**,不要只挑对方的错
- verbatim quote ≤15 words

## Z · 本轮检索五方向(forge-config 原文)

1. **判据可达性 / 可判定性前置** — test oracle problem · mutation testing 的
   equivalent mutant 判定 · SLO error budget 的可达性与测量窗定义 · 形式化验收标准的
   satisfiability 检查。→ **有没有工程先例要求「gate 上线前先证它能被满足」?**
2. **判据该挂产出侧还是验收侧** — Goodhart's law / 指标可操纵性 ·「优化产出以满足验收」
   的反模式 · pre-registration 里 outcome definition 与 analysis plan 的分离 ·
   **sampling adequacy 不足时的既定处置(accumulate vs abort vs report-insufficient)**。
   → 直接对应「让 judge 凑 3 条 vs 跨 run 累积抽样」。
3. **安全控制的例外流程** — security control exception / risk acceptance 的正当化条件与
   **审批分离原则** · defense-in-depth 中两个 control 冲突时的裁决先例。
   → 直接对应 R4-F4「能不能给 C9 门①开豁免、**谁有资格批自己代码的例外**」。
4. **术语同一性** — DDD ubiquitous language / bounded context · 规范文档里同名不同义的
   治理先例(RFC 的 terminology section / 受控词表)。→ 直接对应「评估 run」撞车。
5. **测量窗起算点定义** — SLA/SLO 的 measurement window 起算约定 ·「N 个月内 ≥M 次」类
   条款在合同/合规文本里怎么定义起点才不留悬空。→ 直接对应 O7。

## 本轮最关键的三个待验命题(请优先用检索打)

这三条是双方 P1 的分歧点或共同盲点,**检索应当优先服务于它们**:

1. **O2 (c) 的「≥3 条非豁免判断」该挂哪一侧。**
   事实已定(**双方 P1 独立同意**):本次产物那 1 条判断**带证据锚 `e9`** ⇒ `evidence≠()`
   ⇒ **没有**走 `judge.py:92 sparse_coverage` 或 `:118 not anchor_map` 两条退路
   (两者都产 `evidence=()`)⇒ 是**正常 LLM 路径只返 1 条**。
   ⇒ 争点收窄为:**要不要在产出侧加下界?** 注意反向约束:spec §1-O4 明写
   「低置信处显式标注 · **不做真理机**」,硬凑 3 条可能正面违反它;而 spec §1-O1 长期层
   又明写 O2「**不靠 gate 一次发证,靠使用中持续校准兑现**」,与 (c) 要求单份产物扛满 3 条
   张力明显。**检索方向 2 直接打这个。**
2. **给 C9 门①补声明机制,和「事后开豁免」有无实质区别。**
   门②已有 `DECLARED_READ_SITES` 逐文件声明;门①**无任何豁免入口**。
   `writer.py:85-97` 自己把「给门①开豁免」列为三条坏路之首。
   ⇒ 争点:**预先存在的通用声明机制** vs **为具体一处现开的特批**,
   这条线在安全治理里怎么画、**谁批准**。**检索方向 3 直接打这个。**
3. **T040 能不能「窄起跑」。**
   双方 P1 都倾向「未定判据不能先焊成 BLOCK gate」,但也都注意到 T040 的阻塞面很窄
   (`depends_on` 三者已 ship;被阻的是 6 个测文件里 1 个的 1 条断言)。
   ⇒ 争点:**部分固化**(先做 O7/O4/C9 汇总测,(c) 与结果值语义留空)是正当的增量交付,
   还是「半个 gate 比没有 gate 更危险」?**检索方向 1 与 3 都相关。**

## Write

`discussion/001/forge/v5/P2-GPT55xHigh.md`(template 在 forge-protocol SKILL §"P2 template"):
- §1 SOTA 对标(表格 · ≥5 条 · 每条给 URL + 它证了什么)
- §2 用户外部材料消化 → 标 `n/a(K 无外部材料)`
- §3 P1 判断修正(≥3 条 · **兼顾双方 P1** · 每条标「站住 / 被推翻」+ 引 §1 证据)
- **§4 选源自查**(本轮特有 · 见上文「对侧检索能力受限」三问)

600-1100 字(§4 不计入)。

## When done

Write `.codex-outbox/queues/001/<TS>-001-forge-v5-p2.md` with:
- **Completed**: `<ISO>`
- **Files written**: `<list + word count>`
- **Headline**: `<one-line 对标后的一句话结论>`
- **Verdict**: CLEAN | CONCERNS | BLOCK
- **Searches run**: `<n>` + 逐条列检索式(便于 IDS 侧复核选源偏差)
- **Key reversals**: 哪些 P1 判断被对标推翻(双方各列)
- **Notes for Claude Code**: 含 §4 选源自查的要点 + 任何搜不到支持性先例的方向
