# Codex Task · 006 · forge v9 · P2(参照系评估 · with search)

**Queue**: 006
**Created**: 2026-08-10T09:21:46Z
**Recommended model**: gpt-5.6-sol(或本机 config 当前默认)
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~10-16k
**Kickoff form**: **oneshot(本文件自足 · 不假设你记得上一轮)**
**Forge version**: v9
**Phase**: P2

> ⚠ **为什么显式写 oneshot**:本仓刚实证 `cdx-run` 每次都新起 session
> (`codex "read <task> and execute…"`,未用 `codex resume`),而任务书若写 `reuse-session`
> 会让「已读勿重读」在冷 session 下变成「**根本不读**」(登记为 **KG-B14**)。
> 因此本文件**自足**:下面列的东西你**必须真的打开读**,不要因为「上一轮读过」而跳过。

## Your role

You are Reviewer B, forge v9 on idea 006, **Phase 2 · 参照系评估**。
**审阅人姿态** —— 评判已存在物,不是 daydream。

## HARD CONSTRAINTS

- ✅ **本轮允许 web search**(这是 P2 的核心动作),mode = **对标 SOTA**
- ✅ **必须读对方 P1**:`discussion/006/forge/v9/P1-Opus47Max.md`(P2 起不再 parallel-blind)
- 🔴 **禁止读 `discussion/006/forge/v9/P2-Opus47Max.md`** —— 对方 P2 已产出但**本轮对你封闭**。
  理由:P2 的价值在**两侧独立的 SOTA 覆盖**;你若读了对方的对标表,就只会继承它的锚点。
  这条来自 forge 001 v6 的教训(单侧检索导致某条 verdict 无跨模型交叉验证)。
  **对方 P2 在 P3R1 才对你开放。**
- ❌ 不允许写 PRD / dev plan / refactor 详细方案(那是 Phase 4)
- ❌ 不允许下 final verdict(那是 P3)
- 搜索范围约束:**SOTA-benchmark search only**;forbidden = tech-stack deep dive / pricing / 实施细节
- **诚实纪律(硬)**:任何一条检索方向若**未取到**,写「**未取到**」,
  **绝不允许**写成「未检索到 ⇒ 无先例 / 业界没有做法」。这是本轮在审的病症本身。

## 你必须读(按序)

1. `discussion/006/forge/v9/forge-config.md` —— X / Y / Z / W / K + 收敛强度(**Z 的 5 条检索方向在 §Z**)
2. `discussion/006/forge/v9/P1-GPT55xHigh.md` —— **你自己的 P1**(冷 session,请重读)
3. `discussion/006/forge/v9/P1-Opus47Max.md` —— **对方 P1**(本轮新开放)
4. `discussion/006/forge/v9/_x-batch-snapshot.md` —— 需要回查证据时用(Part 1 实测 / Part 3 现场原文)
5. `discussion/006/forge/v9/moderator-notes.md` —— **若存在**则为 binding(当前不存在,属正常)
6. `.claude/skills/forge-protocol/SKILL.md` 的 **P2 template** 段

## Z · 本轮 5 条检索方向(逐条覆盖,取不到就写未取到)

1. **tamper-evident / append-only 审计日志**(hash chain · WORM · Merkle)
2. **供应链证明 / provenance attestation**(SLSA · in-toto · Sigstore)——
   「这次评审真的跑过、且跑的是这份内容」如何做成可验证声明
3. **CI 强制执行门**(GitHub required status checks / branch protection / policy-as-code 如 OPA/Conftest)
   —— 「门实装了但没人调用」在业界怎么被结构性排除;**尤其:如何表达「该跑而未跑 ≠ 通过」**
4. **stale approval 失效机制**(GitHub `dismiss_stale_reviews` · Gerrit label 重置)——
   成熟判据与**误报边界**
5. ⭐ **code review 收敛的经验研究**(review 轮次与缺陷发现率 · defect escape rate · 何时该停)
   —— 对应 **XD-44**。**本方向对方侧未取到(其 `WebSearch` 后端 400 故障,第二次复发),
   本轮只能由你覆盖。** 请优先保证这一条有真实来源,并注明是否为同行评议研究。

## 本轮请你**额外**处理的三件事

1. **对你自己 P1 §1 的证伪尝试**:你 P1 写「现行系统并非没有机制…失效在入口未调用」。
   对方 P1 独立写了同构的一条(「四条机制侧全都写对了,失效整齐落在执行面」)。
   **两侧独立到达 ≠ 该判断被验证过。** 请用 SOTA 主动找**反例**:
   有没有成熟体系认为这类问题**就是** design gap、或**不该**用 obligation 层解决?
2. **SOTA 的限缩面(重要,不要只找支持)**:哪些方向是**采一个现成标准也解决不了**的?
   请明确写出「SOTA 给的是 A,但我们的问题是 B,A 不覆盖 B」的那些条目。
3. **对方 P1 §3-3 的 reframe 你完全没提**:对方怀疑 **XD-44 是 task 粒度过大(T042 一个 task 同时
   定义三态接口 + 退出码命名空间 + 消费点 verifier 三函数 + 五态一致性),不是轮次上限太低**,
   并明确要求你**独立判、别顺着说**。请在本轮**明确表态**(支持 / 反对 / 证据不足),
   **不得沉默** —— 沉默会被当成「无反对即通过」,而那正是本批在审的错误。

## Write

`discussion/006/forge/v9/P2-GPT55xHigh.md`,按 P2 template:

- **§1 SOTA 对标** —— 表格,**至少 3 条**(列:标的 X' / 参照项 / 它怎么做 / 我们怎么做 / gap / URL）。
  paraphrase 为主,verbatim quote ≤15 words。
- **§2 用户外部材料消化** —— forge-config §Z 明写「外部材料叠加:**无**」⇒ 写 `n/a` 并说明,
  **不要**把 SOTA 检索结果冒充成 operator 提供的材料。
- **§3 修正后的视角** —— **至少 3 条**,每条形如「P1 判断 X『原话』→ **站住**/**被推翻**(因为 SOTA …)」,
  且每条**必须引用 §1 的具体行**。请把上面「额外三件事」的结论也落在本节。

600-1100 字;超标请**具名声明**并说明为何不可有损压缩。

## When done

写 `.codex-outbox/queues/006/20260810T092146-006-forge-v9-p2.md`,含:

- **Completed**: <ISO>
- **Files written**: <list + 字数>
- **Searches run**: <n> + 每条 Z 方向的覆盖状态(**取到 / 未取到**,逐条列)
- **Headline**: 一句话
- **Verdict**: CLEAN | CONCERNS | BLOCK(BLOCK 仅用于标的不可达 / 搜索后端故障)
- **§3 修正摘要**:哪些 P1 判断站住、哪些被推翻(逐条一行)
- **对「额外三件事」的回答摘要**(三行,含对 XD-44 粒度 reframe 的明确表态)
- **Notes for Claude Code**

文件名**必须逐字一致**:`20260810T092146-006-forge-v9-p2.md`(HEAD 指针靠它)。
