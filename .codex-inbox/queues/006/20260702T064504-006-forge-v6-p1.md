# Codex Task · 006 · forge v6 · P1(独立审阅 · 跨机器可移植性)

**Queue**: 006
**Created**: 2026-07-02T06:45:04Z
**Recommended model**: gpt-5.5
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~10-15k
**Kickoff form**: oneshot
**Forge version**: v6
**Phase**: P1

## Your role

You are GPT-5.5 xhigh, Reviewer B, forge v6 on idea 006 Phase 1。
**审阅人姿态** — 评判已存在物(跨机器可移植性现状 + 已落地的 KG-29 修复),不是 daydream。

v6 的主题:**009-pForge M1 在 linux 新机首跑 IDS→XenoDev 全链,一天暴露 5 条同根框架缺陷**。此前
所有 dogfood 都在 mac。**同一根因 = 框架把某台 mac 的环境形态硬编码成默认**(绝对路径 `/Users/admin/...`、
codex 版本号钉死 `1.0.3`、env 工具链假设 uv 就位、目录形态假设仓根 tests/)。单机永不触发,跨机器全暴露。
你这一轮要**独立**(不看 Opus 侧)给出对这 5 条 KG 的现状评估 + first-take。

**5 条 KG**(全文见 X#1 快照):
- **KG-26** IDS `/plan-start` HANDOFF 模板 + SHARED-CONTRACT §6.2 硬编码 mac 路径(未修)
- **KG-27** XenoDev 三 SKILL 硬编码 mac codex 路径 + 钉死 `1.0.3` 版本(未修)
- **KG-28** parallel-builder/task 假设 uv+env 就位,新机四连坑(未修)
- **KG-29** fork id `009-pForge` 过不了 hand-back id 正则 → **已 operator 直接决策修(IDS `7164381`)· 本轮追认审计**
- **KG-30** parallel-builder red-green log 落点被 mirror 子项目 .gitignore 挡(未修)

## HARD CONSTRAINTS

- NO web search this phase(SOTA 对标是 P2)
- DO NOT read `discussion/006/forge/v6/P1-Opus47Max.md`(parallel independence · 这是 Opus 侧,必须独立)
- 不允许写 PRD / dev plan / refactor 详细方案(只产 first-take 评分)
- 不允许跨入 SOTA 对比(那是 P2)
- §0 必须引用 K(用户判准 · 见 forge-config §K)
- 按 Y 视角(跨机器可移植性 / 工程纪律)组织 §1 现状摘要 + §2 first-take 评分(5 条 KG 一一对应)
- §3 给出你最不确定的 3 件事
- 800-1500 字

## ⚠️ 沙箱可达性关键提示(影响是否 BLOCK)

X 的 5 槽里,**KG-27/28/30 的真代码现场在外部 repo `/home/ys/codes/XenoDev/`**(三个 SKILL 文件 + gen-handback)。
注意:本机是 **linux**,XenoDev 在 `/home/ys/codes/XenoDev`(**不是** mac 的 `/Users/admin/...` —— 那个路径
不存在正是 KG-26/27 的病本身)。

**如果你的 Codex 沙箱读不到 `/home/ys/codes/XenoDev/`**:
- 不要假装读了 —— §0 必须显式列出哪些标的因沙箱不可达被 skip
- 但**不要直接 BLOCK 整轮** —— X#1 backlog 快照(`discussion/006/dogfood-backlog-20260702.md` KG-26..30 段)
  已含每条缺陷的**详尽复现场景 + 关联文件行号 + 候选修法**,足以支撑一个「基于 backlog 描述 + IDS 本仓
  现场(KG-26 的 plan-start.md + SHARED-CONTRACT §6.2 直接可读)」的 first-take
- 只有当**本仓标的(backlog 快照 + plan-start.md + SHARED-CONTRACT)也读不到**才 BLOCK
- 在 outbox 的 Notes 里明确告诉 operator:为让 P2 能跑 XenoDev 三 SKILL 真代码对比,需把 `/home/ys/codes/XenoDev/`
  加进 Codex 沙箱 read scope

## Read in order

1. `discussion/006/forge/v6/forge-config.md`(拿 X/Y/Z/W/K + 收敛强度 strong-converge + 5 条 KG 定义)
2. `discussion/006/forge/v6/moderator-notes.md`(若存在,binding)
3. X 中的全部标的(逐条列在下面 § "X 标的清单")
4. `discussion/006/forge/v6/PROTOCOL.md`(P1 template · §"P1 template")

## X 标的清单(5 槽 · 跨机器可移植性现场)

> 标 `[XenoDev 外部仓]` 的需沙箱可达(`/home/ys/codes/XenoDev/`);`[IDS 本仓]` 直接可读。

**槽1 · backlog 快照(核心 · 5 条缺陷全文)** [IDS 本仓]
- `discussion/006/dogfood-backlog-20260702.md` — **只读 KG-26/27/28/29/30 段**(KG-1..25 历史背景不重审)。
  每条含复现场景 + 关联文件行号 + 候选修法 + operator 确认。**这是本轮信息密度最高的标的。**

**槽2 · KG-26 现场(IDS 侧硬编码 mac 路径)** [IDS 本仓]
- `.claude/commands/plan-start.md` — HANDOFF 模板 §workspace.build_repo + §1 `cd` 指令(搜 `/Users/admin`)
- `framework/SHARED-CONTRACT.md` §6.2 workspace schema — 4 字段示例表 + YAML 块(line ~635-651,全 mac 路径)

**槽3 · KG-27/28/30 现场(XenoDev 三 SKILL · 最重点)** [XenoDev 外部仓]
- `/home/ys/codes/XenoDev/.claude/skills/spec-writer/SKILL.md` — §3.1 L94-96(base 目录 mac · 但 find `*/` glob 逻辑**是对的**)
- `/home/ys/codes/XenoDev/.claude/skills/codex-review/SKILL.md` — 搜 `1.0.3` 与 `/Users/admin`(8+ 处 · mac 路径 **+ 钉死版本号**)
- `/home/ys/codes/XenoDev/.claude/skills/parallel-builder/SKILL.md` — L17/L58(mac+1.0.3);§2.1 L135-151(red-green log `tee tests/red-green-log/<TID>.log` 相对路径 = KG-30);§4.4 verifier(期望 log 随 commit);§0/§2 无 env preflight = KG-28
- 读取范围:整文件可读,但聚焦上述行 + 搜 `/Users/admin` / `1.0.3` / `red-green-log` / `uv run`

**槽4 · KG-29 已落地修复(追认审计 · 不推翻)** [IDS 本仓]
- `discussion/006/forge/v6/KG29-fix-commit-7164381.txt` — IDS commit `7164381` diff(charset 正则 `-p[A-Z]`→`-p[A-Z][a-zA-Z]*` + 对齐 `-v` 段 · 统一 5 处)
- **追认审计任务**:审这个放宽有没有引入 path-traversal 等**安全隐患**?构造对抗 id 样例(如 `009-pv0.2` / 多字符后缀边界 / 与 `-v` 段解析歧义)判断。**倾向"安全、不推翻"须你独立复核证实,别只信 Opus。** 实证:009 两包 6 约束全 PASS + `008/../../etc` 仍被四道防线拦。

**槽5 · KG-26 同族 evidence(修法射程要覆盖)** [XenoDev 外部仓 + IDS]
- `/home/ys/codes/XenoDev/lib/handback-validator/gen-handback.sh` — 产包 `to_source_repo`/`workspace.source_repo` 仍填 mac(009 两包 frontmatter 可证 · KG-26 同根但 backlog 未单列)
- validator 相对路径 papercut:`framework/xenodev-bootstrap-kit/handback-validator/validate-handback.sh` 约束5 路径提取要求 leading `/`(传相对路径误报 FAIL)

## 产出

写 `discussion/006/forge/v6/P1-GPT55xHigh.md`(P1 template · 见 PROTOCOL.md):
- §0 读到的标的清单(标明沙箱可达性 · 哪些 skip)+ 阅读策略 + K 摘要
- §1 现状摘要(按 Y1 跨机器可移植性 / Y2 工程纪律 组织)
- §2 first-take 评分(5 条 KG 逐条 keep/refactor/cut/new + 理由)
- §3 你最不确定的 3 件事

写完在 outbox 放同名文件。
