# Codex Task · 006 · forge v10 · P2(参照系评估)

**Queue**: 006
**Created**: 2026-08-11T05:19:48Z
**Recommended model**: gpt-5.6-sol
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~10-18k
**Kickoff form**: 🔴 **显式 oneshot · 自足** —— **刻意不写 reuse-session**
**Forge version**: v10
**Phase**: P2

> **为什么本轮不用 reuse-session**:**KG-B14** 已实证 `cdx-run` 会把 reuse-session 形态
> **静默降级为冷 session**(执行面零强制零留痕)。明知该机制坏掉还照协议模板写 reuse-session,
> 等于把「你已读过 X,本轮勿重读」这句话变成谎言。承 v9 P2 的同一处置。
> ⇒ **本任务自足**:该读的全部列在下面,不假设任何前序 session 上下文。

## Your role

forge v10 P2 —— 按 Z 跑检索 + 消化外部材料 + 修正你自己 P1 的视角。

## HARD CONSTRAINTS

- 🔴 **DO NOT read `discussion/006/forge/v10/P2-Opus47Max.md`** —— 与 P1 同理,
  **本轮要的是两侧独立的 SOTA 覆盖**。v9 唯一一次真正的证伪就来自 Codex 单侧检索,
  若两侧 SOTA 同源,那点信号也没有了。**Opus 侧 P2 本轮对你封存。**
- 搜索约束(Z = **对标 SOTA**):SOTA-benchmark search only;
  **forbidden** = tech-stack-deep-dive / pricing / 实施细节。
- §1 至少 3 条对标;§3 至少 3 条修正(站住 / 被推翻);verbatim quote ≤ 15 words。
- 不写 PRD / dev plan / refactor 详细方案。
- ⚠ **本轮 Y 仍只有「架构设计」**。

## Read

1. `discussion/006/forge/v10/forge-config.md`(**Z 的五条检索方向在 §Z**)
2. `discussion/006/forge/v10/P1-GPT55xHigh.md`(你自己的 P1)
3. 🔴 **`discussion/006/forge/v10/P1-Opus47Max.md`** ← **对方 P1,本轮解封,必读**
4. `discussion/006/forge/v10/_x-batch-snapshot.md`
   —— ⚠ **Part 3.2 已有一处订正块**(就是你 P1 抓出的 `consumer_wired` 计数错误,
   见下面 §"致谢与订正")
5. `discussion/006/forge/v10/moderator-notes.md`(若存在,binding;当前不存在)
6. `.claude/skills/forge-protocol/SKILL.md` 的 **P2 template** 部分

## 致谢与订正(先读,避免你在 P2 里重复核对)

你 P1 §Notes 抓出的计数错误**属实,已订正**:
- **原写**(Opus 撰的权威快照 Part 3.2 + P1 inbox):`consumer_wired: false` 是 **10/11**
- **真值**(逐行复核):**13 原始行 / 11 唯一 id;`true` 有 2 条**(`OB-006-v9-01`、`OB-006-v9-02`,
  **都**挂 `forge:006:phase0`、**都**已 satisfied)⇒ **`false` 是 9/11**
- 快照已加**可见订正块**(不静默改数 —— 本仓有「假事实照单流过四轮 forge」的先例,
  forge 001 v6 §underweight-10)。**实质结论不变**:build 侧 `xenodev:*` 三个 gate 仍全未接线。

## 🔴 本轮指派你单侧覆盖的三条 Z 方向(Opus 侧取不到)

**Opus 侧 `WebSearch` 三次同错 400**(`effort 'xhigh' not supported when thinking disabled`)——
**与 forge 001 v6、forge 006 v9 完全相同的后端故障,第三次复发**。Opus 侧 Z 已退化为
对已知权威源的**定向 WebFetch(不是检索)**,故以下三条方向**只有你能覆盖**:

1. **warn → error 的升级治理**(编译器 lint 分级演进 / deprecation 的 sunset 机制 /
   「未来不兼容」告警怎么被推成硬错误)——
   **核心问题:升级由什么触发?有没有自动到期?谁决定?`defer` 是不是合法结局?**
   ⚠ Opus 侧在此方向**取到了东西且它推翻了 Opus 自己 P1 的核心直觉**,
   但**具体是什么,本轮对你封存**(见上 HARD CONSTRAINT)——
   **请你独立检索,不要试图从 Opus P1 反推。** 若你独立到达同一处,那才是真的收敛;
   若你到达别处,那更有价值。
2. **技术债 / 例外的到期 ratchet**(new-code baseline · 「不新增」型质量门 ·
   遗留豁免的 grace period)—— 直接对应两个本仓问题:
   ① `check-8` 若升 hard-fail,**47/100 存量包**会被整体判 corruption(追溯性执法边界);
   ② hook 白名单的「精确路径条数只减不增」该怎么**被工具执行**而不是被写在文档里。
3. **review 终止条件按被审对象的「关键性分级」有无实证支撑** ——
   ⚠ v9 已取到:*70% 约 1–2 轮但精确轮次预测很差*,且**未取到任何支持统一停轮阈值的研究**。
   本轮**只补分级这一支**:是否有证据支持「按被审模块的风险/关键性调整评审强度」?
   这直接检验 Opus P1 的一条**假说**(见下)。

**Z 方向 1(policy-as-code 强制边)v9 已有反证**(OPA 只是 decision point,
enforcement 接线必须应用自己做),本轮**不必重投**,除非你发现下一层的新材料。

## 🔴 请重点攻击的两条(不得沉默)

**(a) Opus P1 的「关键性分级」假说 —— 请用 Z 方向 3 检验它。**
Opus P1 提出:*review gate 对被审模块的关键性无感 ⇒ 低风险面上永远能再找出一条 `[P2]`*,
证据是 T041(operator 自用批跑脚手架,6 轮 15 条)vs FU-R5F4(`gate.py` 真门禁,1 轮 0 条)。
⚠ **n=2,且提出者就是 Opus 自己**,快照 Part 5.1 已标它为假说。
**若 Z 方向 3 取不到支撑,请直说「无支撑」** —— 那会直接改变 XD-44′ 的修法方向。

**(b) Opus 在 P2 里第二次把本仓机制往 SOTA 上贴 —— 请攻击这个倾向本身。**
Opus P1 已自陈「对自己上一轮(v9)的机制有偏袒动机」,而你 P1 独立判它偏袒并反对了它的归并倾向
(**Opus 已接受你的反对**)。现在 Opus 在 P2 里**又**做了一次同类动作:
把本仓的 obligation ledger / forge Phase 0 gate 与它这轮取到的 SOTA 形态**建立对应关系**,
并据此**提高了对 v9 机制的评价**。
⇒ **这可能是同一个偏袒的第二次发作。** 请在你 P2 §3 明确表态:
这种「本仓机制 ≈ SOTA 某形态」的类比,**在你独立取到的材料里站得住吗?**
支持反对均可,**但不得沉默**。

## Write

`discussion/006/forge/v10/P2-GPT55xHigh.md`,按 P2 template:
- §1 SOTA 对标(表格 · ≥3 条 · 每条标出处)
- §2 用户外部材料消化(K 未提供外部链接/文件 ⇒ 标 "n/a" 即可)
- §3 修正后的视角(≥3 条:你 P1 哪些站住、哪些被推翻;**含对上面 (a)(b) 的表态**)

600-1100 字。

## When done

Write `.codex-outbox/queues/006/20260811T051948-006-forge-v10-p2.md` with:
- **Completed**: `<ISO>`
- **Files written**: `<list + word count>`
- **Verdict**: `CLEAN` | `CONCERNS` | `BLOCK`
- **Headline**: 一句话 —— 「我们 vs SOTA 的核心 gap 是 X」
- **Searches**: `<n>` + 主要 source 列表
- **修正点简表**:你 P1 哪些站住 / 哪些被推翻
- **对 (a) 的结论**:关键性分级假说 —— 有支撑 / 无支撑 / 证据不足
- **对 (b) 的表态**:Opus 的「本仓机制 ≈ SOTA」类比站得住吗
- **Notes for Claude Code**

Filename **must match** the inbox basename:`20260811T051948-006-forge-v10-p2.md`
