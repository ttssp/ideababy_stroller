# Codex Task · 006 · forge v6 · P2(参照系评估 · SOTA 对标 · 跨机器可移植性)

**Queue**: 006
**Created**: 2026-07-02T08:00:00Z
**Recommended model**: gpt-5.5
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~10-15k
**Kickoff form**: reuse-session（P1 已跑过 · 复用 session 省 token；若新 session 见下 Read in order）
**Forge version**: v6
**Phase**: P2

## Your role

GPT-5.5 xhigh, Reviewer B, forge v6 Phase 2。现在**可以联网搜 SOTA**。
本轮任务:读双方 P1（现在**可以读 Opus P1 了**）+ 跑 SOTA 搜索,用**外部锚**校准 5 条 KG 的修法方向。

## HARD CONSTRAINTS

- 本轮**允许 web search**（Z = 对标 SOTA · 跨平台工具链可移植性）
- 跑 **≥4 次 SOTA 检索**,聚焦:12-factor config / 硬编码路径→运行期解析(git rev-parse) / 版本 pin-vs-glob-lockfile / uv 可复现 env + mirror index / 防硬编码回归(grep/lint CI gate)
- forbidden:tech-stack-deep-dive 教程 / pricing / 无关实施细节
- §1 SOTA 对标必须是**表格**,每行 SOTA 惯例 → 命中哪条 KG → 对修法的锚定
- 不允许写最终 PRD / dev plan（那是 W,synthesizer 的活）
- 按 P2 template（见 PROTOCOL.md §"P2 template"）
- 800-1500 字

## Read in order（若复用 P1 session,1-2 已在上下文,重点 3-4）

1. `discussion/006/forge/v6/forge-config.md`（X/Y/Z/W/K · 已读）
2. `discussion/006/forge/v6/P1-GPT55xHigh.md`（你自己 P1）
3. **`discussion/006/forge/v6/P1-Opus47Max.md`**（Opus P1 · **P2 才可读** · 看分歧点）
4. `discussion/006/forge/v6/moderator-notes.md`（若存在,binding）
5. `discussion/006/forge/v6/PROTOCOL.md` §"P2 template"

## 本轮要用 SOTA 回答的核心问题（双方 P1 §3 高度一致的不确定）

> 双方 P1 独立收敛:5 条 KG 评分 5/5 一致（KG-26/27/30 refactor · KG-28 new · KG-29 keep-追认）,
> 且 §3 最不确定的都指向**同一个核心岔口**。请用 SOTA 给这几条外部锚:

1. **环境假设住哪一层?**（两侧 P1 §3 都问这个）—— 协议字面 / 生成时推断 / 运行时探测 / 环境变量?
   查 12-factor config + git rev-parse repo-root 解析 + monorepo 路径推断惯例。
2. **KG-27 版本号**:pin exact vs glob+lockfile 的 SOTA 共识?（注意 spec-writer 已用 `codex/*/scripts`+`sort -V` glob 范式 —— 这是否就是 SOTA 该推广的?）
3. **KG-28 env bootstrap**:uv 可复现 env 的 SOTA —— mirror index 该进 lockfile 还是环境变量(`UV_DEFAULT_INDEX`)?yanked-dep 该去 `--locked` 还是用 `--frozen`?（这条尤其查清,backlog 原提"去 --locked"可能次优)
4. **防再漂移守卫**:grep/ast-grep/semgrep 做 CI/pre-commit gate 拦硬编码的 SOTA 实践?对"单人双机非发行"场景,轻量 grep gate 够不够(vs 上重 CI)?

## X 补充（P2 可回看的现场,不必重读全文)

- KG-29 追认:你 P1 已实测 `009-pForge`/`006a-pM-v0.2` PASS · `009-pv0.2`/`008/../../etc` FAIL —— 本轮 SOTA 视角看**放宽 charset 的安全性有无 SOTA 反例**(OWASP path-traversal 输入校验惯例),确认或推翻"追认安全"。
- 五槽现场路径见 forge-config §X（KG-27/28/30 在 `/home/ys/codes/XenoDev/` 三 SKILL）。

## 产出

写 `discussion/006/forge/v6/P2-GPT55xHigh.md`（P2 template）:
- §1 SOTA 对标（**表格** · 每行 SOTA→KG→修法锚定 · 标来源）
- §2 用户外部材料消化（K 无额外 URL；可消化 Opus P1 与你 P1 的异同）
- §3 修正后的视角（你 P1 哪些站住 / 哪些被 SOTA 推翻或深化 · 尤其 KG-28 mirror/lock 处置）

写完 outbox 放同名文件。**若某条 SOTA 与 Opus P2 分歧,明确标出**（P3 要收敛的就是分歧）。
