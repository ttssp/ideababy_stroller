# Codex Task · 006 · forge v6 · P3R1(联合收敛 · 第 1 轮 · 跨机器可移植性)

**Queue**: 006
**Created**: 2026-07-02T09:00:00Z
**Recommended model**: gpt-5.5
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~8-12k
**Kickoff form**: reuse-session
**Forge version**: v6
**Phase**: P3R1

## Your role

GPT-5.5 xhigh, Reviewer B, forge v6 Phase 3R1（联合收敛第 1 轮）。
读双方 P2 + 收敛残留分歧 + 确认或挑战 Opus 锁的 verdict 骨架。**strong-converge 目标:朝单一 verdict 收。**

## HARD CONSTRAINTS

- 可选 web search（仅当要核某条 SOTA 才搜;本轮重点是收敛非新搜)
- 读 Opus P3R1 允许（这是收敛轮,要看对方骨架）
- 按 P3R1 template（见 PROTOCOL.md）
- 聚焦:确认/挑战 verdict 骨架 + 收 3 条残留 + **重点复核 Opus §3 的 mac `sort -V` caveat 处置**
- 600-1200 字（收敛轮可短）

## Read in order（复用 session 则重点 3-4）

1. `discussion/006/forge/v6/P2-Opus47Max.md`（Opus P2 · **本轮必读** · SOTA 对标 + KG-28 修正 + 分层修法）
2. `discussion/006/forge/v6/P2-GPT55xHigh.md`（你自己 P2）
3. **`discussion/006/forge/v6/P3R1-Opus47Max.md`**（Opus P3R1 · verdict 骨架 + 3 残留收敛 + 消化你的 mac sort -V caveat）
4. `discussion/006/forge/v6/PROTOCOL.md` §"P3R1 template"

## 本轮要收敛/确认的（Opus P3R1 已锁,请你确认或挑战）

### A. Verdict 骨架 = "环境假设分层住"（Opus §2 锁的矩阵），确认或改:
| 硬编码性质 | 修法机制 |
|---|---|
| 跨机器绝对路径 | 生成时推断 `git rev-parse --show-toplevel`/`realpath` |
| 仓内相对路径(log) | 运行时锚仓根 |
| 工具版本号 | 单一解析点 glob + `sort -V`（spec-writer 已用,推广）|
| index/env config | 环境变量 `UV_DEFAULT_INDEX` + `--frozen` |
| base 目录 | env / `$HOME` / XDG |
+ 防漂移守卫 `check-no-hardcoded-env.sh`（轻量 grep/pre-commit）
+ KG-29 追认 keep（安全 · 不推翻）

**问你**:这个分层骨架有没有漏的硬编码类别?有没有哪类机制选错?

### B. 你的 mac `sort -V` caveat —— Opus 已实测 + 处置,请确认:
- Opus 本机(linux)实测 `sort (GNU coreutils) 9.4` · `sort -V` 工作 · spec-writer 范式取到 1.0.4 ✅
- **mac 侧无法在 linux 机验** → Opus 判:进 verdict 作 **open-item + 可执行验收**（推广前必须 mac 上实测 `printf '1.0.3\n1.0.10\n' | sort -V`;不支持则用便携替代 `sort -t. -k1,1n -k2,2n -k3,3n` 或 `gsort`）。
- **问你**:这个处置够不够?便携替代选型你认可哪个?spec-writer SKILL 自称"macOS 默认 sort 支持 -V"这个 claim 你有无 SOTA 依据证实/证伪(GNU coreutils vs BSD sort)?

### C. 3 条残留的收敛结论（Opus §4 给的,确认或改):
1. **SSOT 半同步窗口** → 不追求消除(跨仓非原子),追求"窗口可见 + 同步 checklist/脚本 + SHA 复验"。认同?
2. **KG-30 log 随 commit** → 降级为"留证即可,不强制随 commit"(§4.4 verifier 改判磁盘可读+red/green 序列即 PASS)。认同?
3. **grep-gate 放哪** → IDS + XenoDev 各放一份。认同?

## 产出

写 `discussion/006/forge/v6/P3R1-GPT55xHigh.md`（P3R1 template）:
- §1 对 verdict 骨架的确认/挑战（A）
- §2 mac `sort -V` caveat 处置确认 + 便携替代选型（B）
- §3 3 条残留确认/改（C）
- §4 若仍有分歧,明确列（strong-converge 要 P3R2 消掉）；无分歧则明说"0 分歧,可进 P3R2 定稿"

写完 outbox 放同名文件。
