# Codex Task · 006 · forge v9 · P3R1(联合收敛 R1 · 标分歧)

**Queue**: 006
**Created**: 2026-08-10T09:55:00Z
**Recommended model**: gpt-5.6-sol(或本机 config 当前默认)
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~8-14k
**Kickoff form**: **oneshot(本文件自足 · 不假设你记得上一轮)**
**Forge version**: v9
**Phase**: P3R1

> ⚠ 同 P2:`cdx-run` 每次新起 session(KG-B14),故本文件自足。
> 下面列的文件**必须真的打开读**,不要因「上一轮读过」而跳过。

## Your role

Reviewer B,forge v9 · 006 · **Phase 3 R1 · 联合收敛(标分歧)**。审阅人姿态。

## HARD CONSTRAINTS

- 🔴 **零新检索**(R1 再开搜索会发散;SOTA 已在 P2 定案)
- ✅ **本轮对你全面开放**:双方 P1 **和** 双方 P2(P2-Opus47Max.md 在 P2 阶段对你封闭,**现在解封**)
- ❌ 不写 PRD / dev plan / 详细 refactor 方案(W 本轮**不产 next-dev-plan**)
- ❌ 不下 final verdict(那是 P3R2);本轮只出**草案 verdict + 分歧清单**
- `strong-converge` ⇒ R2 必须 finalize 单一 verdict,**所以 R1 的分歧清单要写得可裁**
- §4 的 K 对齐自检**不得省略**,K 第 6 条点名要求
- 🔴 **`moderator-notes.md` 已注入(Hard constraint · binding on Both)** ⇒ 见下方「你必须读」第 7 条;
  产出开头**必须**有 `## Moderator injection response` 一节

## 你必须读(按序)

1. `discussion/006/forge/v9/forge-config.md` —— **K 的六条 operator 关切 + 四条硬约束**(§K)
2. `discussion/006/forge/v9/P1-GPT55xHigh.md` —— 你自己的 P1
3. `discussion/006/forge/v9/P2-GPT55xHigh.md` —— 你自己的 P2
4. `discussion/006/forge/v9/P1-Opus47Max.md` —— 对方 P1
5. ⭐ `discussion/006/forge/v9/P2-Opus47Max.md` —— **对方 P2(本轮新解封)**。
   注意其 §0:对方 `WebSearch` 后端 400 故障(**第二次复发**),Z 退化为定向 WebFetch,
   **对方侧不产任何「未检索到⇒无先例」型结论**;XD-44 的实证方向**完全由你单侧覆盖**。
6. `discussion/006/forge/v9/P3R1-Opus47Max.md` —— **对方 P3R1(已产出,本轮可读)**
7. 🔴 `discussion/006/forge/v9/moderator-notes.md` —— **已存在,Hard constraint,binding on Both。
   2026-08-10T09:58Z 由 operator 经 `/forge-inject 006` 注入**(本任务书下发后追加,故此处为更新)。
   内容 = 本失败家族在**治理链**上的 3 次实证(含 🔴 **XD-43 的授权条目 forge v8 已裁定下发、
   28 天仍 ⏳**,以及 **v9 本轮违反了 v8 给它定的 intake 义务**)。
   ⇒ 你必须在 P3R1 开头加一节 `## Moderator injection response` 逐条回答其三问,
   并把结论写进正文(该文件 **Phase 4 synthesizer 不读**,只在回应节提一句会整条丢失)。
8. `.claude/skills/forge-protocol/SKILL.md` 的 **P3R1 template** 段

## 对方 P3R1 已经摆上桌的五条分歧(请逐条表态,不得跳过)

1. **KG-B11 / XD-43 评级词**:对方判 `new`,你 P1 判 `refactor`。对方认为这是**标签分歧不是实质
   分歧**,提议采复合措辞「实装件保留(refactor)+ obligation 层新增(new)」,按最贵部分记 `new`。
2. **XD-41 评级**:你判 `new`,对方 P1 判 `refactor` —— **对方已让步到 `new`**,
   但要求**并入簇①**(补审债 = ledger 里一条 `absent` 条目,不另立一个自己也会缺席的标记)。
3. **XD-44 建议 2(finding 类别重复作收敛信号)**:对方 P1 说 `defer`,读了你 P2 的实证后
   **主张升级为 `reject`**(理由:defer 等于「以后可能采」,但已有实证说它不可执法)。
   **请明确二选一。**
4. **被推翻的地基要不要写进 verdict**:对方主张把「纯 execution gap 判断被 SOTA 推翻」
   列为 verdict 的**具名理由**,而非悄悄换成新方案。
5. **XD-44 粒度 reframe 的落点**:实质已收敛(你 P2 已表态「支持但非唯一根因」)。
   对方倾向本轮**只写一条触发线**(单 task 同时定义 ≥N 个跨模块契约面 ⇒ 应拆),
   展开归 task-decomposer 另案。

## 本轮请你**额外**处理的两件事

1. 🔴 **对方 P3R1 §2 的 verdict 草案请你独立审,不要顺着说。**
   草案 = 「**obligation ledger**(独立于被检查对象、由权威 gate 预置的必经步骤清单;
   每步三态 `satisfied`〔绑当前内容身份〕/ `skipped`〔具名理由〕/ `absent`〔即拒〕;
   append-only 不可覆盖;consumer 可独立复证)+ XD-41/XD-44 两条校准 + 正式接手
   `SHARED-CONTRACT:993` post-P0 backlog」。
   请特别审:**三态是否够**(会不会需要第四态)、**`skipped` 的具名理由由谁签**
   (若仍由 producer 自报,你 P1 §3-2 的攻击面原样回来)、以及**ledger 本身会不会缺席**
   (递归终止点)。**你 P2 已给出 OPA 反证 + SLSA 同信任域两条限缩,请把它们用在这里。**
2. ⚠ **evidence_grade 提案**:对方 §4-K6 建议本轮 decision-list 沿用 forge 001 v6 立的第 5 列
   `evidence_grade`(`independent` / `SOTA-corrected` / `post-hoc-accepted`),
   理由是防止「双方都同意」被下游读成「两个模型独立复现」。**请表态支持或反对**,
   若支持,请给出**你认为哪几条该标什么** —— 尤其:对方 P1 §3-3 的粒度 reframe
   是你**被告知后表态支持**(该标 `post-hoc-accepted`),而你 P2 对「纯 execution gap」的
   推翻是**你独立用 SOTA 做到的**(该标 `SOTA-corrected` 或更高)。

## Write

`discussion/006/forge/v9/P3R1-GPT55xHigh.md`,按 P3R1 template:

- **§1 整合摘要** —— 读完双方 P1+P2 后,你对「这件事现在的样子」的整合理解(2-3 段,引证据)
- **§2 我的初步 verdict(草案)** —— **单段 3-5 行,直接给立场,不要 hedge**
- **§3 关键分歧清单** —— 每条含「我的立场 / 对方立场(引原句 ≤15 words)/ 我希望 R2 怎么收敛」;
  上面五条**逐条覆盖**,另可新增你自己的分歧
- **§4 与 K 的对齐性自检** —— 逐条 check K 的六条 + 四条硬约束;
  **K 第 6 条(回声室)必须显式回答「本轮结论本身有没有落进缺席即豁免」**

600-1000 字;超标请**具名声明**并说明为何不可有损压缩。

## When done

写 `.codex-outbox/queues/006/20260810T095500-006-forge-v9-p3r1.md`,含:

- **Completed**: <ISO>
- **Files written**: <list + 字数>
- **Headline**: 一句话
- **Verdict(草案)**: 一句话立场
- **五条分歧的逐条表态**(五行,每行:同意 / 反对 / 修正 + 一句理由)
- **对「额外两件事」的回答摘要**(含 evidence_grade 支持/反对 + 你给的分级建议)
- **仍未收敛的项**(留给 R2 的清单)
- **Notes for Claude Code**

文件名**必须逐字一致**:`20260810T095500-006-forge-v9-p3r1.md`。
