# Codex Task · 003 (PARS) · L3R1 (Scope R1)

**Created**: 2026-04-24T05:03:37Z
**Recommended model**: gpt-5.4
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~8-14k

## Your role

你是 GPT-5.4 xhigh,Debater B,L3R1 on idea **003** (PARS — Parallel Autonomous Research System)。你的工作:在 human 的 L3R0 intake 约束下,独立提出 **2-3 个 peer 候选 scope**(产品级 PRD 骨架),**不读** Opus 的 L3R1。

## HARD CONSTRAINTS

1. **NO web search** 这一轮
2. **DO NOT read** `discussion/003/L3/L3R1-Opus47Max.md`(并行独立)
3. **NO tech / architecture / stack / API** 内容 — 产品级 scope only(不写 FastAPI/SQLite/Docker 等等技术选型)
4. 每个候选必须是 **peer**(不是"主方案 + 备选"),各有独立立场
5. 对 intake 里每个 ❓ 项主动提供 options
6. 若 intake 没列红线,基于 L2 提议 3 条候选红线

## Read in order

1. `proposals/proposals.md`(根 proposal 003 条目)
2. `discussion/003/L2/stage-L2-explore-003.md`(L2 完整 PARS 方案,14 周 / 6 工程师版)
3. `discussion/003/L3/moderator-notes.md`(Pre-L3 对抗审查发现,合并评分 47/100,24 P0/P1/P2 缺口)
4. `discussion/003/L3/L3R0-intake.md` ← **最关键**,human 的硬约束 / 红线 / 未决 ❓ 都在这
5. `.claude/skills/scope-protocol/SKILL.md`(L3R1 模板)
6. `AGENTS.md`

## Intake 摘要(避免你漏看)

- **时间**:2-3 个月交付 v0.1
- **投入**:1 人 × 30+ h/周 ≈ 350-450h 总预算
- **用户**:single-operator(human 自己),**不是** 6 工程师团队
- **商业**:OSS 免费,无 SaaS 基础设施
- **优先级**(三选):速度 + 成本低 + 技术简单。**没选**:polish / 安全 / 差异化
- **红线**:不做多用户 / Docker / 调度 / Runpod
- **关键正向信号**:"会涉及一些后训练"(user notes)— LoRA / SFT 类任务**没被排除**
- **未决 ❓**:v0.1 跑哪类任务、是否多 worker 并行、是否 LLM-judge、后训练切口大小

## Write

`discussion/003/L3/L3R1-GPT54xHigh.md` using L3R1 template:

- **§0** Reading of intake(你对 human 约束的解读 + 计划处理的 ❓)
- **§1-3** Candidates A / B / (C) — 每个 peer 候选完整结构:
  - v0.1 一段话
  - User persona
  - Core user stories(3-5 条)
  - Scope IN
  - Scope OUT(explicit non-goals)
  - Success looks like(可观察)
  - 时间估算(honest,基于 350-450h 单人预算)
  - UX principles
  - Biggest risk
- **§4** Options for ❓ items(每个 ❓ 给 2-3 个具体选项)
- **§5** 建议红线(若你认为 intake 遗漏了关键红线)
- **§6** 真实用户访谈才能答的问题

**Length**: 800-1500 字(Opus 那边约 1300 字,你可以更紧凑或同量)

## Style notes

- 候选之间的**关键 tradeoff 轴**要明说(是"后训练 in/out"?"多 worker vs 串行"?"是否预留插件接口"?)
- 时间估算**不要粉饰** — 若 scope 装不进 450h,直接说,并在另一个候选里砍掉
- "single-operator + 会涉及后训练" 这组合如何落地 — 你的候选要直面这个张力,而不是回避
- 红线可以**扩展** moderator-notes 里的 P0 条目 — 例如 "不做多 worker 同时跑 LoRA 因为 P0-8 显存争抢";但不要堆砌全部 24 条,只挑与 v0.1 相关的

## When done

写 `.codex-outbox/<TS>-003-L3R1.md`:

- 你写入的文件 + 字数
- 每个候选一行 headline
- 你对哪些 ❓ 给了具体 option
- 你认为的关键 tradeoff 轴
- 任何 Claude Code(orchestrator) 应该知道的事
- 你在这一轮觉得最困难 / 最不确定的判断

---

## Context reminders

- **Moderator-notes 合并评分 47/100 是对 L2 那个 14 周平台版本的评估**,不是对你要给的 v0.1 候选的评估。但里面的 P0 关切(API key exfil、prompt injection、LoRA stuck 误杀、成本算错)对任何候选都**至少部分相关**。
- Intake 里 human **没勾** safety 作为优先级,但你不能因此让候选"0 安全" — API key 环境变量化 / 最小 deny 清单是 baseline
- L2 报告列出 9 个模块 M1-M9;你的候选应**显式说清**哪些模块保留 / 砍 / 合并。不要假装 M1-M9 这套分层继续存在
