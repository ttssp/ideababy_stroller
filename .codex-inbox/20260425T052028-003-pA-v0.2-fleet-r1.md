# Codex Task · 003-pA · v0.2 Fleet Mode L4 Adversarial Review R1

**Created**: 2026-04-25T05:20:28Z
**Recommended model**: gpt-5.4
**Recommended reasoning_effort**: xhigh
**Estimated tokens**: ~12-18k

## Your role

你是 GPT-5.4 xhigh 做 v0.2 fleet mode 的 adversarial review **R1**(minor bump,目标 1 轮 CLEAN,不打 4 轮)。

v0.1 (RecallKit) 已 ship + tag (`v0.1.0_idea003`) + 639 tests passing,**v0.2 在 v0.1 之上扩展**,核心约束:**不破坏 v0.1 任何契约**(D25 / R14 / O11)。

## Read

- `specs/003-pA/spec-v0.2-fleet.md` (v0.2 增量 spec,260 行)
- `specs/003-pA/spec.md` (v0.1 base spec,bump 到 0.3.0-dev,header 引用增量)
- `specs/003-pA/tasks/T029.md` ~ `T032.md` (4 个新 task)
- `projects/003-pA/pars/orch/orchestrator.py` (v0.1 RunOrchestrator 接口 — v0.2 不动)
- `projects/003-pA/pars/cli/main.py` (v0.1 CLI app 结构)
- `projects/003-pA/pars/ledger/config_schema.py` (v0.1 RunConfig)
- `discussion/003/003-pA/PRD.md` (v0.1 PRD,验证 v0.2 是否仍 PRD-faithful)

## Challenge specifically

1. **Backward compat 风险**(O11 + R14):v0.2 文件域是否真的 disjoint?T030 提到要在 RunConfig 加 `fleet_name`(scope 外微调)— 这会破坏 v0.1 e2e 套吗?Pydantic `extra="forbid"` 是否兼容?
2. **GPU lock 正确性**(C23/C26/R13):
   - PID 复用 + start_time 检测,在 macOS 上精度够吗?
   - fcntl.flock 在哪些场景失效?
   - dispatcher 多线程跑 N 个 RunOrchestrator,GIL 是否阻碍 5s tick 实时性?
3. **macOS refuse 是否够明确**(C27):mac 上跑 fleet start 会精确 exit 2 吗?fleet status/compare 在 mac 上真能工作吗(没有 nvidia-smi)?
4. **阻塞模式 + Ctrl+C 后孤儿 worker**(D21/C28/R15):dispatcher 退出后 worker subprocess 真的能继续跑 12h 不挂?stuck/budget monitor 是 RunOrchestrator 内部 daemon thread,主进程退出后这些 thread 会不会跟着死?
5. **DAG depends_on 字段 warning+忽略**(C25):有没有 silent 的副作用?用户写了 depends_on 期待行为,实际被忽略,会怎样?
6. **fleet_name 字段污染**:T030 / T029 / T032 都提到 fleet_name,但 v0.1 RunConfig 没这个字段。是新增 optional 字段(向后兼容)还是别的方案?是否有更干净的设计(例如 fleet metadata 单独存 `runs/.fleets/<fleet_name>.json`)?
7. **e2e 套是否真测到 O8/O9/O10**:T032 列了 5 e2e test 用合成模式(mock RunOrchestrator)— 合成测试能真的验证 GPU OOM 不发生(O9)吗?这是真实 GPU 才能验证的事情,合成测试只能验证编排逻辑。
8. **C25 depends_on 字段保留**:Pydantic Schema 是否会误把 depends_on 当 unknown field 拒掉(extra="forbid")?需要在 BatchRunSpec 显式声明 `depends_on: str | None = None` 否则验证失败。
9. **fleet status < 5s**(C24):60 个 fixture run 真能 < 5s 吗?yaml.safe_load × 60 在 mac mbp 实测多少?
10. **ETA 估算**(D24):epochs_done=0 时返回 None 合理,但 epochs_done=0.05(刚开始)算 ETA 会得到天文数字。需要更细的 threshold。

## Write

`.codex-outbox/<TS>-003-pA-v0.2-fleet-r1.md` 用以下格式:

```markdown
# Adversarial Review · 003-pA · v0.2 fleet · R1

## Blockers (必须修才能 build)
| # | Location | Issue | Why blocks | Fix direction |

## High-severity concerns (强烈建议修;可 follow-up)
| # | Location | Issue | Suggested action |

## Medium / Low findings
(brief list)

## v0.1 PRD faithfulness check
- [ ] v0.2 增量是否仍服务 PRD 原始 outcome?
- [ ] 有没有偷偷扩 scope 到 PRD 不打算做的方向(比如多用户 / 自动决策)?
- [ ] O8/O9/O10/O11 的验证是否真覆盖契约?

## v0.1 backward compat 检查
- [ ] T029-T032 file_domain 是否真 disjoint v0.1 关键模块?
- [ ] RunConfig.fleet_name 注入方式是否破坏 Pydantic extra="forbid"?
- [ ] 任一 task 中是否藏了对 `pars/orch/**` 的修改?

## Top 3 things operator 必须人工验证
1. ...
2. ...
3. ...

## Verdict
- CLEAN — spec ready to /parallel-kickoff
- CONCERNS — non-blocking,operator 自决是否修
- BLOCK — 必须修 R2
```

引用 ≤ 15 词。

