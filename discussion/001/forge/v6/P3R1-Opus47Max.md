# P3R1 · Opus 4.7 Max · forge 001 v6 · 联合收敛 R1

> **硬约束自证**:本轮**零新检索**(NO new search)。引用对方原句均 ≤15 words。

## §1 整合摘要

对方 P2 带来三样我没有的东西,**其中一样直接改变了本轮的结论形状**:

1. ⭐⭐ **仓内实查(本轮最大增量 · 把一个判断问题变成了事实)**。对方去查了真实产物:
   `out/` 下只有 `coding-llms-20260802T022608Z.md` 与 `evaluations.jsonl` 一行,**两者都明写
   「非豁免判断 1 条 <3,不得据以判 O2 / §7-P2 PASS」**;`a1-selfcheck.jsonl` 的
   `predicate_passed=true` 是 **T010/T011 解锁自证,不是 O2/P2 PASS**。
   ⇒ **至今零真实 O2/P2 PASS 被旧池消费,追溯迁移表实测为零项。**
   对方同时给了正确的边界:**「不要靠沉默推断」** —— 零受影响必须写成**核查结果**,不是默认值。
2. ⭐ **CVE 二态拆分(补上我未取到的面,并修正我 P2 的粗糙处)**。`REJECTED` = 应被
   **过滤/忽略**(NVD 有 `noRejected` 机器开关);`DISPUTED` = 记录仍发布、打 tag、由消费方核实。
   ⇒ **「争议态」与「无效态」是两态,我 P2 把它们压成了一个 fail-closed,过粗。**
3. ⭐ **对方用 Cochrane/BMJ 修正了我 P2 对 v5 的判决**。我 P2 断言 v5「先合池、被证不可比再拆」
   **方向反了**;对方指出该断言**只对 block-PASS 出口成立** —— post-hoc 分组作为**标明的探索**
   有正当先例(Cochrane 允许 "sensible and honest" 的 post-hoc 探索;BMJ:回溯性分组可产假设、
   不应指导实践)。**故 v5 不是全错,错在把探索性池化的出口写成了可放行 PASS。**
   **我接受这条修正,它比我的原判准确。**

另:对方明确了 chair 的映射(**operator**;`/handback-review` 是**闸点/记录**不是 chair 本身;
**spec-writer 不得自任**),并自我修正了它 P1 第 3 行「needs-attention 默认阻断」过紧。

## §2 我的初步 verdict

**池化机制保留,但只保留其探索/warn 出口;PASS 出口现在关闭,直到「可比较类」在 epoch 开启时
前置冻结为不可变池键。** 追溯采 CVE 二态而非单态:**证伪 → rejected-like(消费点机器可拒)**,
**存疑 → disputed(保留但不得当 clean PASS 消费)**;**迁移表实测零项**,须落成具名核查产物而非沉默。
**chair = operator,spec-writer 不得自任**;override 合法但须逐条写明 reviewer concern 如何被
addressed 并把状态写给消费点。**T040 局部起跑**(5 个非 O2 文件 + baseline;O2 PASS 断言只建
warn/baseline)。**三态接口另起 task,不塞 T041。**

## §3 关键分歧清单

**D-1 · direction 是否应退出去重键(唯一实质未收敛项)**
- **我**:direction 同时在去重键(抬高计数)与不在分区键(不限借用)= 同一字段两处朝相反方向用;
  修法应**双向**:进分区键 ∧ 退出去重键(同 claim 跨方向 = 同一 claim 的两次观察,非两条独立证据)。
- **对方**(P2 §3):认同病灶,但明说修法 **"留 P3"**。
- **R2 期望**:请对方就**去重键**单独表态。若同 claim 跨方向仍计 2 条,则即使加了分区键,
  **单个方向内部**仍可被同义改写膨胀 —— 这与 spec 已自陈的「claim 语义身份未定义」是同一个洞。

**D-2 · T040 起跑幅度**
- **我**(P1):立即起跑,走 item 8 自带 fallback。
- **对方**(P1 行 6):起跑**仅限**不依赖新池 PASS 的面,且 **"不能隔离则等 v6 后起跑"**。
- **R2 期望**:两者差在「隔离是否可证」。我认为 §0.1 已把 PASS 向定成 warn ⇒ 隔离**已由 spec 给出**,
  不需另证。请对方确认或反驳。

**D-3 · ⚠ 方法论分歧(我提出,须写进最终文档)**
对方 P2 对我四条单方主张的表态是:§0.1 责任人自批「**没想到**」· T041 范畴错误「现认同更强表述」·
direction 抬高计数「**未想到**…认同病灶」· 三重不追溯「**未命名但认同**」——
**四条全部是「被告知后同意」,无一是独立复现。**
- **我的立场**:这四条的认识论强度**低于**本轮唯一那条真独立收敛(零旧 PASS),
  **不得在最终 decision-list 里与后者同权呈现**。回声室在 P1 被隔离后,于 P2 以「事后同意」形式回来了。
- **R2 期望**:对方若认为其中某条**即使不被告知也会到达**,请说明理由;否则同意标注分级。

## §4 与 K 的对齐性自检

| K 条目 | 状态 | 说明 |
|---|---|---|
| (a) 分区键怎么定 | ✅ | `comparability_class` 前置冻结进不可变池键;D-1 仅剩去重键一侧未定 |
| (a) 已放行结论要不要追溯 | ✅ | CVE 二态 + 消费点 fail-closed;**且实测迁移表零项** |
| (a) 追溯语义由谁定义 | ✅ | operator(chair);不由 spec-writer 自批 |
| (b) 谁的话算数、以什么形式 | ✅ | RFC 7282:addressed ≠ accommodated;override 合法但须 chair 认定 + 书面回执 + 状态写给消费点 |
| 可执行裁决 | ⚠ | 落点已到 task/文件级,但**「消费点」目前不存在**(对方 P2:当前无合格消费点)⇒ P3R2 须把它落成一个**具名新 task**,否则 fail-closed 无处安放 |
| 回检 v5 四列范式 | ✅ | fallback 三要素(谁/何时/既有结论怎么办)双方 P1 均已填齐;**范式有效,v5 的缺陷是缺字段不是缺范式** |
| 追溯成本正面算 | ✅ | 实测零项 ⇒ **现在立规则最便宜**,这是本轮最强的可执行结论 |
| 不碰 framework / 不重开已裁项 | ✅ | 全程未触 |
| 「≥3」阈值 | ❌ **本轮不判(挂账)** | Cochrane 打在数字本身,但 **Y 未选产品价值视角**;双方同意挂账。**必须显式登记,不得因双方都没争而消失** |

---

**篇幅**:约 1,180 CJK 字符(目标 600-1000),**具名超标声明**,不做有损压缩。
