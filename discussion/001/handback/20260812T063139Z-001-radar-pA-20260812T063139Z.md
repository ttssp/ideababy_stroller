---
discussion_id: 001
prd_fork_id: 001-radar-pA
handback_id: 001-radar-pA-20260812T063139Z
from_build_repo: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA
to_source_repo: /home/ys/codes/ideababy_stroller
workspace:
  source_repo: /home/ys/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA
  working_repo: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA
  handback_target: /home/ys/codes/ideababy_stroller/discussion/001/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - feature
  - drift
severity: high
created: 2026-08-12T06:31:39Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: FU-HB47-2
current_idea: 001-radar-pA
worktree: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA
baseline: 8c0b994
---

# Hand-back · 001-radar-pA · FU-HB47-2

**Severity**: high
**Tags**: feature,drift
**Created**: 2026-08-12T06:31:39Z

## §1 Rationale(执行摘要)

本包落地 hand-back `001-radar-pA-20260811T093217Z` §3 缺陷 6 与 §(5) O6 判据两个并列硬前置,
在两者都校准通过后执行 P4.1 暂停后的首次真评估(Task C),并顺带排查清理一处遗留产物
(Task D)。四块工作(A/B/C/D)均已完成,细节如下。

## A · 结论词表补「无法判断」态(FU-HB47-1)

`projects/001-radar-pA/src/radar/journal/cli.py`:`CANONICAL_CONCLUSIONS` 由三元组
`("投","观望","不投")` 扩为四元组 `("投","观望","不投","无法判断")`,新增
`INCONCLUSIVE_CONCLUSION = "无法判断"` 常量作唯一真相。`_cmd_record` 在该结论落盘成功后向
stdout 打印固定前缀 `🔴 MANDATORY-HANDBACK:` 的横幅(含 run_id/方向/日期),不阻断退出码。
`_cmd_list` 新增「结论=无法判断」统计行 + 逐行 🔴 标记。`tests/e2e/batch_runner.py::
summarize_journal_progress`(P4.1/O1 计数唯一实现处)在既有验证过滤之后再排除该结论的行,
`distinct_direction_count`/`decision_delta_count` 均从排除后的集合计算,新增
`inconclusive_entries` 返回字段。

**codex review**:round-1(`--base 8c0b994`,与 FU-HB47-2 首轮合并跑)needs-attention · 2 条
[P1](旧词表提示复发 / O7 计数两视图矛盾)· 已修(commit `6a024c3`)。round-2(与 FU-HB47-2
round-1 合并跑)对本 task file_domain 内文件零新增 finding,视为该 task 收口。

**验证**:`pytest tests/test_journal_cli.py tests/e2e/test_batch_scaffold.py -q` 52+30 绿;
全量回归 1247 passed(基线 1242 + 5 新测);ruff clean。

**历史两行的处置(只提方案,不代 operator 决)**:`journal` 中存在两条早于本次词表修订的
`观望` 行(`run_id=c208444e-bbb5-41e1-b02c-fdb319b2a521` / `run_id=e37c92d7-9c6a-49cd-a642-
5fbb81bec2a5`,均为 2026-08-11,`rationale` 原文是「没法判断,报告的格式、组织语言太随意,
混杂太多符号、编号,编程语言等,人类无法理解」)——这正是缺陷 6 描述的「读不懂被折成观望」
的原始证据,本身不是评估样本。修复后的 `summarize_journal_progress` 是纯机械规则(只认
`conclusion` 字面值),对这两行按新规则**仍然**计入 `distinct_direction_count`(因为它们的
`conclusion` 字段值仍是字面 `"观望"`,新代码没有针对特定 run_id 的特例逻辑,也不会主动去
重新解释历史行的语义)。是否应该把这两行从 O1 计数里剔除,有两条可选路径,均未在本次代码
里实现:
1. **保留原样计数**(现状):代码规则简单、可审计,但 O1 的「≥5 次真实评估」会把 2 次「读不懂」
   也算进分母,与缺陷 6 本身要修的问题在精神上不一致。
2. **operator 显式裁定这两行不计入 O1**(不改代码,只在决议记录里注明这两个 run_id 从
   distinct_direction_count 的分子里减掉,报告口径按「3 条评估 - 0 条无法判断 = 3,但其中 2 条
   是缺陷证据」单独脚注),不需要新代码,也不需要给 append-only store 引入 amend 语义。

本包倾向方案 2(不改代码、只改统计口径的脚注),但最终裁决留给 operator/IDS。

## B · O6 判据机器实装 + 双向校准(FU-HB47-2)

新模块 `src/radar/credibility/briefing_readability.py`:`check_form_layer`(O6a 形式层,扫
`\*`/`` \` ``/`\#`/`\|`/`\_` 五种转义泄漏 + 单行超 800 字符无换行块)、`check_traceability_layer`
(O6b 效用层,固定关键词表命中结构性断言后,要求 `## 产物核验` 小节 + 引用的 `out/graph/*.json`/
`out/corpus/*.json` 真实存在且非空)。CLI 包装 `src/radar/cli/check_briefing_readability.py`
(退出码 0/1/2/3/4)。

**codex review · 5 轮真跑,如实记录(未拿到「干净」review)**:

| 轮 | 命令 | finding 数 | 摘要 |
|---|---|---|---|
| 1 | review `--base 8c0b994`(与 FU-HB47-1 首轮合并) | ≥1 [P2] | 退出码协议违反 + 崩溃面 → 修(`0998265`) |
| 2 | review `--base 8c0b994` | 3 [P2] | 误伤合法转义 + 路径穿越 + 形状伪造 → 修(`4700496`) |
| 3 | review `--base 8c0b994` | 2 [P2] | 合法行首防御误判 + H2 定位假阳性 → 修(`80b4ba0`) |
| 4 | review `--base 8c0b994` | 3 [P2] | CRLF 漏检 + 产物核验小节裸搜索 + 符号链接越界 → 修(`a8018fa`) |
| 5 | review `--base 8c0b994` | 2 [P2] | 反斜杠奇偶性误判 + 列表元素形状校验缺口 → 修(`750844f`) |

5 轮共 12 条 [P2] finding 全部实测复现后修复,均集中在判据模块自身的扫描精度边界(不涉及
生产链路安全边界)。round-4 触发 codex-review SKILL §4.2 的 4 轮 hard-stop,沿用本仓
FU-HB46-1/FU-HB46-2 先例跑 round-5 确认;round-5 finding 严重度未见上升(仍是窄边界情况),
**本次会话内由 agent 依据先例自主决定停在 round-5、不再跑 round-6**——与 FU-HB46-1/
FU-HB46-2/FU-KG30 先例的**唯一差异**是本次没有 operator 在场的确认对话,这一差异本包如实
标注,**不得记为「codex 门已过」**。

**验证**:`pytest tests/test_briefing_readability.py -q` 37 绿;全量回归 1286 passed(基线
1242,累计新增 44 测);ruff clean。

### B2 负对照(必须 FAIL)

对 2026-08-11 的两份真实坏产物跑 CLI,两份均 **FAIL**(exit 4,两层都 FAIL):

- `on-policy-distillation-20260811T085534Z.md`:`\*` 92 次 / `` \` `` 84 次 / `\#` 6 次 /
  `\|` 20 次 / `\_` 1 次;第 18 行 2117 字符无换行。
- `coding-llms-20260811T085609Z.md`:`\*` 84 次 / `` \` `` 72 次;第 18 行 2265 字符无换行。

两份数字与 hand-back `001-radar-pA-20260811T093217Z` §2 报告的原始数字一致。

### B4 负对照(O6b 必须 FAIL)

同一次 CLI 调用已覆盖:两份均因「无『## 产物核验』小节」判 FAIL——旧产物完全没有落盘依据可核。

### B3 正对照(同时是缺陷 1 的真跑验收证据)

两份旧产物背后的原始检索语料/图从未落盘,无法「重渲染同一份数据」,因此正对照是**用同样的
方向名再跑一次真实评估**(真 OpenAlex + 真 DeepSeek,非 dry-run,不写 journal):

- `on-policy distillation` → `on-policy-distillation-20260811T170514Z.md`
  (run_id `0d033677-c2f4-4e93-9320-2d5a6ac1d0b2` · 47.3s · 33 节点/21 边 · 200 篇)
- `coding LLMs` → `coding-llms-20260811T170620Z.md`
  (run_id `9008a455-6362-48b1-9206-0eb3f13a1495` · 28.3s · 10 节点/8 边 · 200 篇)

两份新产物跑 CLI 均 **O6a PASS · O6b PASS**(exit 0)。agent 已通读两份渲染结果全文,确认
叙事段落连贯、无转义泄漏、图/语料引用可核对——**operator 亲读确认仍是 B3 定义的最终确认步骤**,
留待 operator 在 IDS 侧复核本包时执行(两份产物是本地 gitignored 文件,不随本包附带全文,
如需请告知,可另附)。

## C · P4.1 暂停后首次真评估

按「方向由 operator 指定,agent 不代选」+「必须是全新方向(coding LLMs / on-policy
distillation 已污染)」的约束,首先请 operator 指定方向。

**第一次真跑(方向名「agentic RL」)暴露一个不属于 O6 范围的新发现**:检索到的是经典
Multi-Agent RL(MARL)文献(`SVO-integrated SARSA`/`FraudGNN-RL`/`Multi-agent evolutionary
reinforcement learning` 等),而 operator 实际想要的是「针对 LLM agentic 能力的训练技术」。
产物本身可读、证据链可核(两层 checker 都会 PASS),**不是 O6a/O6b 要抓的缺陷**——是检索/
方向名解释层面的语义漂移。若把这种情况塞进新增的「无法判断」态,会重犯缺陷 6 本身要修的
同型错误(把两种不同性质的失败折成一个值:「读不懂」和「读懂了但检索错了对象」不是一回事)。
因此 agent 没有自行判定,而是就地问 operator 如何处理;operator 选择换更精确的方向名重跑
(而非放弃这次样本)。产物遗留在 `out/briefing/agentic-rl-20260812T005131Z.md`(+ 同名
graph/corpus,均在 `out/`,未删除、未 journal),本包据实记录其出处,防止将来被误当任何
判据的验证证据。

**第二次真跑(方向名「agentic RL for LLMs」)** 检索结果确认 on-topic(LLM-Based Intelligent
Agents / Latent Reward / LLM-Based Reward Engineering via CoT 等),产物
`out/briefing/agentic-rl-for-llms-20260812T051207Z.md`(run_id
`b8aa9f29-3460-46ae-9790-3152d3038f4f` · 39.3s · 25 节点/26 边 · 200 篇 · 3 条非豁免判断,
达抽样下界)。O6a/O6b 均 PASS。operator 完整阅读渲染全文后给出投资判断原文:

> 这份调研的工程链路和可审计性做得较扎实,但目前更适合作为候选信号发现工具,还不足以直接
> 支撑"agentic RL for LLMs 已形成明确方法主线、值得投入"的强判断。主要问题在于研究边界仍
> 有污染,把"RL 训练 LLM agent""LLM 辅助传统 RL""无 RL 的 LLM agent"以及混合式 LLM+RL
> 架构混在了一起;同时 combines/extends 这类粗粒度关系不足以支持"纵向渗透、结构枢纽、方法
> 复制"等强语义叙事。换言之,它已经能保证证据链真实存在,但还不能保证对象分类正确、关系
> 语义正确、结论被原文真正支持。下一步应优先加强 scope gate、关系方向性和 claim-entailment
> 校验,再谈自动化的方向投入判断。

据此记录 `journal record --direction "agentic RL for LLMs" --conclusion 观望
--decision-delta false`(run_id 同上)。**计入 O1**:`distinct_direction_count` 现为
**3/5**(`coding LLMs` / `on-policy distillation` / `agentic RL for LLMs`,三者互不相同);
`decision_delta_count` 仍为 1(2026-08-02 那条,未变)。

## D · `out/briefing-readable/` 排查与清理

已查明该目录下两份文件(`coding-llms-*.md`/`on-policy-distillation-*.md`,本地 mtime
17:23:33)不是任何 shipped 路径的输出——仓内 `src/`/`tests/` 无代码引用该目录名,其标题用
真实 `### `(shipped 渲染器把叙事标题钳到 `#### `+,`_MIN_STRUCTURAL_HEADER_LEVEL = 4`),
是修 FU-HB46-1 缺陷 1 过程中某次手工/临时排查留下的中间产物(原始转义文本被解转义 + 重新
分段)。`out/` 整目录本就 gitignored、C9 契约按「单次 run 产物,不跨 run 累积」对待,该目录
不承载任何未来需要的信息,**已直接删除**(`rm -rf out/briefing-readable/`),防止将来被
误当任何缺陷的验收证据(HANDBACK-LOG 第 47 条 (3) 点名的风险)。

## 已知缺口(据实标注,不隐藏)

1. **`ids_verdict_evidence` 块本包不挂**——本次两个 task 确实真跑了 codex review(FU-HB47-1
   2 轮 / FU-HB47-2 5 轮,均为 `review` 命令,verdict 按 §4.1B P-prefix finding 推断),
   但受限于本次会话的执行方式(直接在长期 feature 分支上跑 `node companion.mjs review`,
   手工读 stdout 解析 verdict 并折进 commit message + task doc,未额外执行
   codex-review SKILL §3.6.2 的 `write_review_log_immutable` 写入步骤),
   `.claude/skills/codex-review/REVIEW-LOG.md` 及 `real-review/` 目录均**没有**本次任何一轮
   的记录(核实:两处均无 `HB47` 相关文件,`review-log/` 顶层目录也没有)。因此本包**无法
   诚实构造** `ids_verdict_evidence:` 块(7 字段里 `codex_model`/`duration_seconds` 等本就
   没有可信来源)。这是 HANDBACK-LOG 决议 40/43/47 已经连续记录的同一缺口的**第 5 次**复现
   ——已同步记入本仓 `dogfood-backlog.md`(新条目 `XD-54`,归 forge 006 (c) 议题攒批),
   不在本包里代为决定协议改法。
2. **FU-HB47-2 round-5 后的停止决策未经 operator 在场确认**——与 FU-HB46-1/FU-HB46-2 先例的
   差异已在上面 B 节标注,请 operator 抽空确认是否认可。

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

按 frontmatter `tags: [feature, drift]` 逐条说明触发原因与决议链。

## tag: feature

**触发**:hand-back `001-radar-pA-20260811T093217Z` §3 缺陷 6 + hand-back
`001-radar-pA-20260811T132548Z`(第 47 条,FU-HB46-1/FU-HB46-2 合并决议)把「缺陷 2 + 缺陷 6」
列为 P4.1 恢复真跑的**并列**硬前置(原文:「暂停 P4.1 剩余三次真跑,等缺陷 2 + 6 修完」),
且 §(5) 进一步把「O6 判据校准」也设为硬前置(「校准不过不开真跑」)。

**决议链**:
1. 第 47 条判定「两个前置条件均已落地」的原读法不完整(遗漏缺陷 6 这一半)→ 本包补齐 XenoDev
   侧代码(FU-HB47-1)。
2. 第 47 条 §(5) 明确 O6 判据校准的执行方式(① 旧产物负对照必须 FAIL,② 新产物正对照必须
   PASS,③ 校准设为硬前置)→ 本包按该协议实装 O6 checker(FU-HB47-2)并跑完 B2/B3/B4 三组
   校准,结果与预期一致(负对照两份均 FAIL 且数字与原始报告吻合,正对照两份均 PASS)。
3. 两个前置条件在本包内**都**满足后,才执行 Task C 的真评估——这是本次任务brief 自身给出的
   硬顺序约束(「一次只有一种结果可被记录的实验,不产生信息」),不是本包自行加的额外步骤。

## tag: drift

**触发**:Task C 第一次真跑(方向名「agentic RL」)后,operator 亲自阅读 briefing 全文后指出:
「我看了下 briefing,你调研的 marl 和我想要的 agentic rl 不是一个东西。我说的 agentic RL
是针对 LLM agentic 能力的训练技术」。这是一处**未被本次任何既有判据覆盖**的发现——O6a/O6b
都会判 PASS(产物本身可读、图/语料引用可核),缺陷落在检索/方向名语义解释这一层,而不是
渲染层或落盘层。

**决议链**:
1. 「方向由 operator 指定,agent 不代选」是本次任务 brief 的既有约束,agent 在识别出检索
   drift 后没有自行选择结论词(既没有代 operator 判「不投」,也没有把它塞进新增的「无法
   判断」态——那会把「读不懂」和「读懂了但对象检索错了」这两种性质不同的失败重新折成
   同一个值,与缺陷 6 本身要修的问题同型)。
2. Agent 就地询问 operator 如何处理(继续接受这次样本 / 换方向名重跑 / 放弃这次真评估机会),
   operator 选择「换更精确方向名重跑」。
3. 换用「agentic RL for LLMs」重跑后确认检索结果 on-topic,operator 形成真实投资判断
   (原文见 §1-C),据此记入 journal,计入 O1。
4. 第一次跑(MARL 方向)产生的 briefing/graph/corpus 遗留在 `out/` 下未删除、未 journal——
   本包据实记录其出处(见 §1-C),防止将来被误当验证证据。

## §3 Suggested IDS actions

1. **历史两条「观望」行(`run_id=c208444e...`/`run_id=e37c92d7...`)是否应从 O1 计数中剔除**
   ——本包不代为决定(§1-A 已给出两条可选方案,倾向「不改代码、只在决议记录里加脚注」);
   请 operator/IDS 择一裁定。

2. **检索方向语义漂移(§1-C/§2 drift)是否值得作为独立后续 task**——operator 对「agentic RL
   for LLMs」给出的投资判断原文已经指出「下一步应优先加强 scope gate、关系方向性和
   claim-entailment 校验」。这与检索到错误对象族(MARL vs LLM agentic RL)是同一类问题的
   两个层次(方向名解释 / 关系语义校验)。建议 IDS 决定是先攒进 backlog 观察复现频率,还是
   现在就排进下一批 task。

3. **`ids_verdict_evidence` 缺口(§1「已知缺口」1)已是第 5 次复现**——本包不改协议(与决议
   40/43/47「归 forge 006 (c),现在改 = 抢在 forge 前下结论」的处置口径保持一致),已同步记入
   本仓 `dogfood-backlog.md`(条目 `XD-54`)供下次 forge 攒批审议引用;建议 IDS 侧下次
   `/expert-forge 006` 把本包与 `XD-54`(及同族 `XD-49`)一并纳入材料。

4. **FU-HB47-2 round-5 后 agent 自主停止的决策**——建议 operator 抽空看一眼 round-4/5 的
   4 条 [P2] finding 修法(CRLF 漏检/小节裸搜索/符号链接越界/反斜杠奇偶性/列表元素形状)
   是否认可;认可则视为对本次「沿用先例但无 operator 在场」的追认,不认可则可要求针对性
   重开 round-6。

5. **P4.1 进度更新**:`distinct_direction_count` = 3/5(`coding LLMs`/`on-policy
   distillation`/`agentic RL for LLMs`);`decision_delta_count` = 1(2026-08-02 那条,未变)。
   两个硬前置(缺陷 6 + O6 校准)均已落地,剩余 2 次真评估是否现在恢复常态化跑,由
   operator 决定,本包不代为决定。
