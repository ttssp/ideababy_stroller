---
discussion_id: 001
prd_fork_id: 001-radar-pA
handback_id: 001-radar-pA-20260802T012127Z
from_build_repo: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA
to_source_repo: /home/ys/codes/ideababy_stroller
workspace:
  source_repo: /home/ys/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA
  working_repo: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA
  handback_target: /home/ys/codes/ideababy_stroller/discussion/001/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - drift
  - practice-stats
severity: high
created: 2026-08-02T01:21:27Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: codex-backfill-audit-R1-R5
---

# Hand-back · 001-radar-pA · codex-backfill-audit-R1-R5

**Severity**: high
**Tags**: drift,practice-stats
**Created**: 2026-08-02T01:21:27Z

## §1 Rationale(执行摘要)

codex 配额 **2026-07-31 提前恢复**(FU-A 包 §2.5 写的「重置 2026-08-02」已被事实推翻)。
按 operator 指示对**累积区间** `e89860e..df5ce24`(30 commits · 8 个 task-unit 全部在配额墙期间
走替代路径 merge)补跑 5 轮 codex adversarial-review。

**5 轮全部 verdict = needs-attention**,共 **24 条 finding(19 high / 5 medium)**,
**21 条已闭合**、**3 条未闭合**(逐条见 §2.5)。R5 是 operator 定的最后一轮,
**R5 的四条修法未经 codex 复核**(§4.1)。

⚠ **本包更正 XD-41 与 IDS 第 36 条沿用的数字**:那两处写的是「4 轮 21 条 / 11 条 high」,
机器数(`grep -cE '^- \[high\]'` 逐轮)是 **5 轮 24 条 / 19 high / 5 medium**。
「21」实际是**闭合数**不是 finding 数,「11 high」无对应口径。详见 §2.2 ——
IDS 要在 forge 006 上拿这批数据裁 XD-41,数字必须是机器数不是散文数。

**post-R5 HEAD(`df5ce24`)状态**(本 session 复跑复核,见 §2.7):
`1006 passed` · 改动 41 个 py 文件 ruff clean(全树残 2 条 F541 在
`tests/test_reconstruct.py:658-659`,既存 · **不冒称全树 clean**)· 两个仓级 shell `bash -n` OK ·
26 份真 task doc 过新解析器零误伤(套件内真测,非口头)。

## §2 触发原因 + 决议链

**tag=drift · practice-stats · severity=high**
—— high 的理由是 **11 条以上 high 级 finding 的存在本身**(不是残留风险):这批代码此前已被
「替代路径评审 + verdict ≠ BLOCK」放行并 merge,IDS 侧一直只有散文记载、无一手证据。

### 2.1 触发:IDS 第 36 条第 2 项

IDS 2026-08-02 决议 `20260730T034414Z`(FU-A)时发现该包已是 **stale snapshot**(写于 `5399661`),
而 07-31 16:27–17:47 又落了 R1–R5 五个 commit,**这批工作从未产 hand-back 包**。
第 36 条第 2 项要求补产。本包是该项的交付。

### 2.2 ⚠ 数字更正(机器数 vs 散文数)

| 轮 | verdict | high | medium | 合计 | 闭合 | 未闭合 |
|---|---|---|---|---|---|---|
| R1 | needs-attention | 5 | 1 | **6** | 5 | 1(F5) |
| R2 | needs-attention | 4 | 1 | **5** | 4 | 1(RF4) |
| R3 | needs-attention | 3 | 2 | **5** | 5 | 0 |
| R4 | needs-attention | 3 | 1 | **4** | 3 | 1(F4) |
| R5 | needs-attention | 4 | 0 | **4** | 4 | 0 |
| **合计** | 5×needs-attention | **19** | **5** | **24** | **21** | **3** |

口径:逐轮 REVIEW-LOG 的 `Findings:` 段落里 `^- \[high\]` / `^- \[medium\]` 的机器计数。
XD-41(`dogfood-backlog.md:1486`)写的「4 轮 21 条 / 11 条 high」是 R4 当天凭印象写的散文,
**两个数都不对**:21 恰好等于最终闭合数(巧合),11 无对应口径。
**建议 IDS 在 forge 006 上以本表为准**,并回填 XD-41 标题。
—— 这条本身也是一个小型「文档声称跑在代码前面」的实例:连**记录 finding 数**这件事,
不做机器计数就会漂。

### 2.3 为什么 scope 取累积区间而不是逐批

base 取 `e89860e`(= **最后一个真过 codex 的代码 commit**,T020 LlmJudge squash merge),
HEAD `df5ce24`,共 30 commits、8 个 task-unit(T021 / FU-KG29 / T022 / FU-KG30 / T030 / T031 /
T032 / FU-A)。

**至少 4 条 high 只有在累积区间下才看得见**(逐批评审结构上看不到):

1. **R1-F1 + R2-RF1**(provenance 伪造):注入口在 `cli/evaluate.py`(T022),
   而「谁能构造 renderer」这个面要连 `journal/cli.py` 的消费端(T030)一起看才显出后果 —— 跨 3 批。
2. **R1-F6**(jsonl 读回谓词**第 4 处**):前 3 处在 FU-A 的登记表里,第 4 处在**仓级 shell**
   `scripts/unlock-record-check.sh` 内嵌的 python —— 跨 FU-A 与 Unit 0 两批。
3. **R1-F2**(DOI 尾斜杠):`credibility/gate.py` 的 canonicalizer(既存)× `reconstruct/finalize.py`
   的集合 membership(T012)× `graph/builder.py` 的一对多兜底(T011)—— 三处的组合才是缺陷,
   单看任一处都「有兜底」。同轮还查出 `test_evidence.py` 与 `test_credibility_gate.py`
   对同型串**断言方向相反且两条都绿**(测的是两个函数)= KG-29 漂移被测钉死的形态。
4. **R2-RF1 / R3-F3 / R4-F2 / R5-F2**(journal 快照身份):同一道门被**连续四轮**逐层收紧,
   每一轮都是在复核**上一轮的修法** —— 逐批评审在结构上不做「复核上一轮修法」这件事。

### 2.4 24 条 finding 逐条

> `file:line` 是 **codex 报告时(收口前)**的位置。修后的当前位置见 §3。
> 「闭合」列:✅=本批已修 · ⛔=未闭合(理由见 §2.5)。

#### R1(`review-log/backfill-e89860e-2026-07-31-round1.md` · 6 条)

| # | 级 | 位置 | 病灶(一句话) | 修法 | 闭合 |
|---|---|---|---|---|---|
| F1 | high | `src/radar/cli/evaluate.py:84-90` | `dry_run=False` 仍优先用注入的 retriever/llm,而 provenance 只看调用者自传的 `dry_run` ⇒ 两个 fake 就能铸出表头写「全链真跑」的 briefing | 生产模式**拒绝一切注入**(rc=2),让 `dry_run=False` 真正蕴含「组件是本函数自己造的」;不是在 renderer 加类型检查(子类化即可绕) | ✅ |
| F2 | high | `src/radar/credibility/gate.py:115-122` | DOI body `rstrip("/")` 有损裁剪 ⇒ 两篇只差尾斜杠的论文落同一 key,节点/边挂进另一篇的窗 | 去掉 DOI 分支的 `rstrip("/")`(ISO 26324 suffix 不透明且允许含 `/`);arXiv 侧保留(id 格式不含 `/`) | ✅ |
| F3 | high | `src/radar/lifecycle/ephemeral.py:322-345` | `_import_aliases` 用 `ast.walk` 把所有作用域的 import 灌进一张平表 ⇒ 函数体里的 `import harmless as sqlite3` 覆盖模块级真 import,C9 两道静态门同时失明 | 同名多重绑定时**优先保会命中受监视读的那个**(fail-closed),不做完整词法作用域解析 | ✅ |
| F4 | high | `scripts/task-preflight.sh:55-59` | awk 只认 block 序列 ⇒ `preflight: ["x"]` 一条取不到,与「key 压根不在」压成同一个 exit 0 | key 不在→0;`preflight: []`→0;key 在但零条解析出→**exit 1**。**不采纳** codex「强制显式 `preflight: []`」那半(26 份 doc 里 23 份没这个 key,会波及无关文档) | ✅ |
| F5 | high | `specs/001-radar-pA/tasks/T022.md:58-62` | T022 勾了「真 OpenAlex + 真 DeepSeek + exit 0 + 产物落盘」,但唯一 briefing 随 worktree 删除(gitignored)⇒ **仓内无可复核物证** | **⛔ 未闭合** —— 需烧 key + operator 定方向。正由本 session 任务 2(O7 第 1 次真 evaluate)承接 | ⛔ |
| F6 | medium | `scripts/unlock-record-check.sh:123-131` | 步骤②走修好的 `read_selfcheck_records`(物理 `\n`),步骤③却 `splitlines()` ⇒ 含 U+2028/2029/0085 的合法记录被拆开 → 解锁门**假 STOP** | `split("\n")` + 带行号的保护解码;新增**仓级脚本登记面**(FU-A 登记表只扫 Python 包,看不见仓级 shell 里的内嵌 python) | ✅ |

#### R2(`...round2.md` · 5 条 · **其中 2 条是 R1 修法本身的洞**)

| # | 级 | 位置 | 病灶 | 修法 | 闭合 |
|---|---|---|---|---|---|
| RF1 | high | `src/radar/briefing/renderer.py:145-156` | **R1-F1 的残留**:R1 只封了 `run_evaluate` 入口,但 `MarkdownBriefingRenderer` 是公共导出件且 `dry_run` **默认 False** ⇒ 直接构造 renderer + 调公共 writer 就能落一份声称「全链真跑」的产物 | 引入 `RunProvenance` 三值(UNVERIFIED / DRY_RUN_FAKE / REAL_FULL_CHAIN),构造器**必填无默认** + 非 enum 一律 TypeError;`REAL_FULL_CHAIN` 只有「已拒绝一切注入」那条分支传得出去。连带:`test_briefing.py` 5 处构造点显式声明 UNVERIFIED —— 此前**测试自己在批量铸造「全链真跑」产物** | ✅ |
| RF2 | high | `src/radar/lifecycle/ephemeral.py:347-355` | **R1-F3 的残留**:两个候选都受监视时 `_collapse` 按字典序二选一 ⇒ `sqlite3`/`os` 同名 alias 取 `os`,真 sqlite3 读**漏报**。不是保守失败,是继续跑并漏报 | `_dotted` 返**候选集**,任一候选命中即算命中;`_collapse`/`_is_watched_module` 成死码**已删**(留着会让人误以为是承重防线)。同步更新 FU-A 的 `test_jsonl_records.py` 消费端(否则又是一处「同一谓词两个实装」) | ✅ |
| RF3 | high | `scripts/task-preflight.sh:43-74` | awk 仍只认列首逐字 `preflight:` ⇒ 引号键 `'preflight':` / 冒号前空格 / 重复键 三类**合法 YAML** 全走「无前置」放行 | 换 stdlib 严格解析器(不引 PyYAML),核心属性按 codex 要求:**存在但无法完整理解 = 失败**。实测 26 份真 doc **零误伤** | ✅ |
| RF4 | high | `scripts/task-preflight.sh:12-15` | 即使解析器完全正确,**没有任何入口强制调用它** —— `parallel-builder` SKILL §0 清单里没有这一行 ⇒ 这道门从未被执行过 | **⛔ 未闭合** —— SKILL 是**框架件**,按 dogfood 铁律不在 XenoDev 当场改。已补正式条目 **XD-43**(`dogfood-backlog.md:1568`)归 `/expert-forge 006` | ⛔ |
| RF5 | medium | `src/radar/journal/store.py:60-65` | `open("a")` + `f.write`:带缓冲、无短写检测、无 fsync ⇒ kill / 磁盘满可留半条记录,而读侧遇坏行**拒整份文件** = 一次部分失败让全部历史永久读不回 | `O_APPEND` 一次写整行 + 短写检测 + fsync,**三个面同时改**(只改一处就是造第 4 处 KG-29 漂移),契约钉进 FU-A 那张登记表。⚠ 有界收口不是闭合:事务性持久化归 backlog | ✅ |

#### R3(`...round3.md` · 5 条)

| # | 级 | 位置 | 病灶 | 修法 | 闭合 |
|---|---|---|---|---|---|
| F1 | high | `scripts/task-preflight.sh:84-105` | 「严格」解析器仍可被合法 YAML 绕过:双引号键 `"preflight":` 解码后就是 `preflight`,原文却不含该子串 ⇒ 整行被忽略 → 判「无前置」。合并键 `<<:` 同理 | 判据**反过来**:任何无法完整解析的顶层行一律拒,再用「26 份真 doc 零误伤」这道**反误伤门**证明没把门收成谁都过不了。⚠ 顺带**删掉一段不可达死码**(mutation 证 `KEY_RE` 字符集不含反斜杠 ⇒ 该守卫永不执行) | ✅ |
| F2 | high | `src/radar/lifecycle/ephemeral.py:454-477` | `_local_bindings` 不看源码顺序 ⇒ `from sqlite3 import connect; connect("prior.db"); connect = harmless` 中**已经发生**的真读盘被**稍后**那句赋值遮蔽掉 | 绑定表改成「名字 → **最早绑定行号**」,只有发生在调用点**之前**的重绑定才抑制。诚实边界写进 docstring:按源码文本顺序的近似,不是控制流分析;global/nonlocal 仍是已知空白 | ✅ |
| F3 | high | `src/radar/journal/cli.py:128-151` | `record` 只校验「可读 + UTF-8 + 非空」⇒ **README、别方向 briefing、乃至自陈 dry-run 的产物**都能成为 O5/O7 journal 行 | 只做两件**不发明新判据**的事:① 快照须是 briefing 且表头方向 == `--direction`;② 快照若**自己声明**「非 P2 证据」就拒(执行的是产物自己写下的那句话)。连带:测试夹具原本就是「`# briefing` + 任意文本」—— **测试自己在演示那个洞**,已改真形态 | ✅ |
| F4 | medium | `src/radar/journal/store.py:74-83` | **R2-RF5 的残留**:短写检测发生在**文件已被永久毒化之后** —— 前半条 JSON 已落盘,只抛异常不回滚 | 写前记长度,短写就 `ftruncate` 回去 —— 要么整条、要么什么都没有。三面同改。⚠ 回滚建立在本仓明写的「单用户无并发写」假设上 | ✅ |
| F5 | medium | `src/radar/briefing/writer.py:31-35` | 文件身份只有「截断 slug + 秒级 ts」,`write_text` 先截断再写 ⇒ 同秒并发/重试**静默覆盖烧过 key 的真跑证据** | 同目录临时文件 → fsync → `os.link` 发布(目标已存在则 `FileExistsError`,不覆盖)。⚠ 两处自踩写进注释:① 临时名若从 slug 派生会比正式名更长、撑爆 NAME_MAX(**自己写的门自己撞**);② 用 `open("x")` 而非 `os.open(O_EXCL)`,因为后者会被 C9 静态审计入读点 | ✅ |

#### R4(`...round4.md` · 4 条)

| # | 级 | 位置 | 病灶 | 修法 | 闭合 |
|---|---|---|---|---|---|
| F1 | high | `scripts/task-preflight.sh:87-105` | **R3-F1 的残留**:解析器**无条件跳过所有缩进行** ⇒ 一份每个键都缩进两格的**合法** YAML 每行都被跳过 → 判「无前置」。更糟:这些行**永远到不了**上一轮刚加的「看不懂就拒」兜底 | 算出根映射的**实际基准缩进**(第一条非空非注释行):等于它的算顶层键、比它深的算值、**比它浅的**无法归属→拒。block 序列结束判据同步改用同一基准(原判据 `raw[:1].isspace()` 在根缩进非 0 时**恒真 = 死的**) | ✅ |
| F2 | high | `src/radar/journal/cli.py:154-170` | **R3-F3 的残留 · 两头都不严**:① 在**全文任意位置**找表头 ⇒ 给任意文件补一行就能过;② 用「不含『非 P2 证据』」这个**负向**子串判来源 ⇒ 伪造只需**省略**该词元,而真 briefing 正文偶然出现该词元反被**误拒** | 表头必须在**首行**;来源改成**正向匹配** renderer 的 `REAL_FULL_CHAIN` 横幅本身(判据的单一真相就是那张横幅表)。连带:测试夹具改成从 `_PROVENANCE_BANNERS` 取真横幅,不手抄副本 | ✅ |
| F3 | high | `src/radar/lifecycle/ephemeral.py:366-390` | C9 漏掉**模块导入期**执行的读:外层遍历碰到 FunctionDef 直接跳过,递归进去又只扫 body ⇒ 装饰器 / 形参默认值 / 运行期注解**从未被检查**。而 `def load(db=connect("prior.db")):` 在 import 时就真读盘 | 进 body 之前,按**外层作用域**的遮蔽表扫签名上的表达式(`decorator_list` / `defaults` / `kw_defaults` / 注解 / `returns`) | ✅ |
| F4 | medium | `src/radar/briefing/writer.py:72-86` | 原子发布**缺目录 fsync**:只 fsync 文件内容,新建的目录项在崩溃/掉电后无持久性保证 ⇒ 函数已报成功而恢复后产物不存在 | **⛔ 未闭合 · 已升级 operator/IDS**(不是漏做)—— **两道自家守卫的真实冲突**,理由见 §2.5.3 | ⛔ |

#### R5(`...round5.md` · 4 条 · **其中 3 条是 R4 修法自身的残留绕过**)

| # | 级 | 位置 | 病灶 | 修法 | 闭合 |
|---|---|---|---|---|---|
| F1 | high | `scripts/task-preflight.sh:149-156` | **R4-F1 的残留**:YAML **注释不参与缩进结构**,`preflight: / - true / # note / - false` ⇒ block 循环在注释处当成「回到根层」break:收了 `true`、**永不执行后面的 `false`**,脚本还返回成功。这是同一个洞的**第五种写法**(前四:flow 序列 / 引号键 / 转义键 / 整体缩进) | 判 block 结束前先跳过空行与纯注释行。零缩进与整体缩进两种形态各补一条测 | ✅ |
| F2 | high | `src/radar/journal/cli.py:172-187` | **R4-F2 的残留**:只要求 `REAL_FULL_CHAIN` 横幅在**全文任意位置**出现 ⇒ 一份第 3 行明写 `DRY_RUN_FAKE`、正文却夹带真跑横幅的**自相矛盾**快照照样能成为 O5/O7 的 P2 journal 行 | renderer 的格式固定(1 行表头 / 2 行空 / 3 行 provenance),判据改成 `lines[2]` 与真跑横幅**逐字相等** | ✅ |
| F3 | high | `src/radar/lifecycle/ephemeral.py:366-378` | **R4-F3 的残留**:`_SCOPE_NODES` 只列函数与 lambda ⇒ 下钻进**类体/推导式**,把其中的 Store 名误记成**外层**绑定。真反例:模块级 `from sqlite3 import connect`,类体里 `connect = None`,随后模块级 `connect('prior.db')` —— 运行时类属性**不会**遮蔽模块 import,但扫描器以为已重绑定,把这次真读盘跳过了 | 把 ClassDef 与四种推导式列进作用域表 —— 绑定不再外泄,而其中的调用仍被当嵌套作用域扫到(类体与推导式在定义/求值时**真执行**,不能不扫)。⚠ 顺带修一处自踩:推导式没有 `.body`,统一读 `.body` 会当场 AttributeError → 抽出 `_scope_body` 收口 | ✅ |
| F4 | high | `src/radar/credibility/gate.py:152-157` | 通用 URL canonicalizer 把**第一个 `/` 之前的全部文本**当 host 整体 lower ⇒ ① 无路径 URL 的 query 一并进 host 组(`?doc=A` 与 `?doc=a` 同 key);② 带 userinfo 时 `User:Pw@` 也被 lower(**大小写敏感**)。与 R1-F2 **同族** | 改用 stdlib `urlsplit`,**只**归一 RFC 3986 明定不敏感的 scheme 与 hostname;userinfo / 端口写法 / path / query / fragment **逐字保留**;解析不了退回逐字身份。两条测:五组敏感分量不得折叠 + scheme/host 仍须折叠(**证收窄不是砸门**) | ✅ |

### 2.5 三条未闭合的逐条说明

**2.5.1 R1-F5(high · T022 真跑物证缺失)** —— 需**烧 key + operator 定方向**,不是能靠改代码关掉的。
正由本 session 任务 2(O7 时钟第 1 次真 evaluate,IDS 第 35 条 (D) + 第 36 条授权)承接。

**2.5.2 R2-RF4(high · preflight 无强制调用)** —— 改的是 `parallel-builder` SKILL = **框架件**。
按 CLAUDE.md dogfood 铁律**绝不在 XenoDev 当场改**(当场改 = 绕治理 = V4 失败模式)。
codex **R1/R2/R3 三轮重报**这一条;本批只做解析器侧收口 + 契约测,接线归 IDS。
已补正式条目 **XD-43**(`dogfood-backlog.md:1568`)。

**2.5.3 R4-F4(medium · 目录 fsync)—— 查清后刻意不修,是两道自家守卫的真实冲突**

codex 判得对:只 fsync 文件内容不够。**为什么不修**:POSIX 下持久化目录项必须 fsync 一个
**目录 fd**,而拿目录 fd 只能走 `os.open` —— C9 静态门 ① 对 `os.open` 一律保守计入成读点
(flags 是整数常量,静态判不了),而 `briefing/` 在评估链路清单里,**门 ① 没有逐文件豁免机制**
(那是门 ② 才有的)。真加进来门 ① 当场变红(本轮实测:`briefing/writer.py:98 os.open`)。

三条路各自的问题:
- ① 给门 ① 开豁免 = **为了让自己刚写的代码过而给安全门加豁免**,正是本仓反复警告的
  「门被人为关掉的经典前戏」;
- ② 把 helper 挪到非评估模块再 import = **利用门 ① 对 import 的已知失明去绕过它**,比 ① 更坏;
- ③ 改门 ① 让它能判 `os.open` 的 flags = 改的是 **C9 契约本身**,而 C9 的判据正躺在
  08-02 forge 的待裁清单里(T040)。

⇒ 归 operator/IDS。已在 `src/radar/briefing/writer.py:85-97` 逐条写明。

### 2.6 ⚠「产物能说谎」单独成节(XD-41 的实证材料)

> 本节是 IDS 裁 XD-41「替代路径可信边界」要的一手证据。
> **每条都注明:6 批子代理评审为什么结构上看不出来。**

**① 非 dry-run 路径接受注入 fake ⇒ briefing 表头照写「全链真跑 · PPV P2 终点产物」**
(R1-F1 · `cli/evaluate.py:84-90`)

- **子代理为什么没看出来**:T022 那批的验收条目写的是「**dry-run 产物不许声称真跑**」,
  子代理沿 task 验收条目走 → **反方向根本没人问**。评审 scope = task diff + task 验收表,
  而「验收表本身漏了一个方向」这件事,只有不继承作者叙事的另一条 harness 才会去问。

**② 公共 renderer 的来源声明是个默认 `False` 的布尔 ⇒ 什么都不做就等于声称真跑**
(R2-RF1 · `briefing/renderer.py:145-156`)

- **子代理为什么没看出来**:这条是 codex **复核 R1 修法**时抓的。6 批替代路径评审
  **每批独立**、结构上不做「复核上一轮修法」;而修完一个入口后,同源评审者的默认结论是「已修」。
- 更硬的证据:**测试自己在批量铸造这种产物** —— `test_briefing.py` 5 处构造点全部没传 provenance
  ⇒ 全部默认成「真跑」。**测试通过 ≠ 产物诚实**,而子代理看的正是「测试是否通过」。

**③ `journal record` 只校验「可读 + 非空」⇒ README 能成为 O5/O7 journal 行**
(R3-F3 · `journal/cli.py:128-151`,又经 R4-F2 / R5-F2 两轮才收敛)

- **子代理为什么没看出来**:T030 ship 时评审看的是 journal schema/store 的**正确性**;
  「快照文件到底是什么」这个谓词的**强度**问题要连 briefing 产物(T022)一起看 —— **跨 task-unit**。
- 同样硬的证据:`test_journal_cli.py` 的成功夹具原本就是「`# briefing` + 任意文本」——
  **测试自己在演示那个洞**。

**④ 两个仓级 shell 门(`task-preflight.sh` / `unlock-record-check.sh`)此前零测试覆盖**
(R1-F4 / R1-F6 各落一条,后续 R2-RF3 / R3-F1 / R4-F1 / R5-F1 全在同一个脚本上)

- **子代理为什么没看出来**:这两个脚本**不在任何 task 的 `file_domain` 里**
  (preflight 是 Unit 0 附产、unlock-record-check 是 T004 附属)。子代理评审的 scope 是
  **task diff**,于是「没被任何 task 认领的仓级件」在结构上不可见。
  本批新增 `tests/contract/test_repo_scripts_gates.py`(含 symlink 真仓跑真脚本 + 真 evidence
  bundle 端到端)才第一次给它们通电。

**⑤ 补一条 XD-41 未记的**:同一道 preflight 解析门被**连续五轮**逐层收紧
(flow 序列 → 引号键 → 转义键/合并键 → 整体缩进 → 注释行),每一轮都是**合法 YAML** 的新写法。
这说明「fail-open 的门」不是一次能修对的东西 —— 而替代路径下每批**只审一次**。

### 2.7 本 session 复跑复核了哪些数字(不照抄 commit message)

| 项 | 声明值 | 本 session 复核 | 结论 |
|---|---|---|---|
| 测试 | 1006 passed | `python -m pytest -q` → **`1006 passed in 5.53s`** | ✅ 真 |
| 改动文件 ruff | clean | 41 个改动 py 文件 → **`All checks passed!`** | ✅ 真 |
| 全树 ruff | 残 2 条 F541(既存) | 全树 → **`Found 2 errors`**,均在 `tests/test_reconstruct.py:658-661` | ✅ 真 · **不冒称全树 clean** |
| `bash -n` | OK | 两个仓级 shell → **OK** | ✅ 真 |
| 26 份真 doc 零误伤 | OK | `tests/contract/test_repo_scripts_gates.py:165` 是**套件内真测**(直接 glob 真 doc,且有 `len(tasks) >= 20` 前置断言防空转),随 1006 passed 通过;`ls specs/001-radar-pA/tasks/*.md \| wc -l` = **26** | ✅ 真 |
| mutation 5/5 | 全杀 | **未复跑**(需人工造 mutant) | ⚠ R5 时值,本 session 未复核 |

## §3 Suggested IDS actions

> ⚠ **XD-37 自我标注**:本节是全包唯一会让 IDS 花钱的部分,却**不在任何评审的视野里**——
> R1–R5 的 codex 审的是代码,没有一轮读过本节。上一包(FU-A)真的在这里翻过车
> (给 forge 的一条建议把**已被证伪**的启发式当「参考实装」推出去)。
> **本节每条都给 `file:line`,请逐行复核后再决定花钱。**

### 3.1 归 `/expert-forge 006`(框架件 · 攒批)

1. **XD-41 · 替代路径可信边界**(`dogfood-backlog.md:1486`)—— **标题数字请按本包 §2.2 回填**
   (4 轮 21 条/11 high → **5 轮 24 条 / 19 high / 5 medium**)。三条建议原文在 `:1522-1531`,
   本包新增两条一手证据:
   - **§2.6 五条「产物能说谎」逐条附了「子代理为什么结构上看不出来」** —— 成因是
     **scope 边界**(task diff / 验收条目 / 不跨批 / 不复核上一轮),不是「子代理不认真」。
     ⇒ 建议 forge 上把判据落在 **scope 定义**上,而不是「换个更强的评审者」。
   - **§2.6 ⑤**:同一道 fail-open 门要 **5 轮**才收敛,每轮都是合法输入的新写法。
     ⇒「一次评审放行」这个前提对 fail-open 类缺陷**本身不成立**,与评审者强弱无关。
2. **XD-43 · preflight runner 无强制调用**(`dogfood-backlog.md:1568` · **high**)——
   codex **三轮重报**(R1-F4 / R2-RF4 隐含 / R3 复述)。脚本自陈缺口在
   `scripts/task-preflight.sh:12-15`。建议原文 `:1600-1610`,要点:
   接进 `parallel-builder` §0 **+ ship 门验一条与当前 task/spec 内容绑定的成功执行记录**
   (task id + commit + 命令 + 退出码 + 内容摘要)—— 缺了后半条就还有「起跑时跑过 ≠ ship 的是同一份」的缝。
3. **XD-42 · 编号撞车后的引用悬空**(`dogfood-backlog.md:1532` · low)——
   `scripts/task-preflight.sh:15` 写着「已记 KG-34」而 KG-34 是**另一件事**(migration checklist)。
   **本 session 按 operator 指示不修**(改法要和「编号空间迁移时全仓回填」一起定)。
4. **hand-back schema 加 `review_path: codex | fallback` 机器字段**(XD-41 建议 3 · `:1529-1531`)。
   本包是最好的反例:IDS 现在只能靠读 §2.5 的散文才知道哪些 ship 是欠债的。

### 3.2 归 forge 001 / T040(spec 与 C9 契约面)

5. **R4-F4 的守卫冲突需要 IDS 裁**(`src/radar/briefing/writer.py:85-97` 写明了三条路的问题)。
   本质是 **C9 静态门 ① 对 `os.open` 的保守计入没有逐文件豁免机制**
   (`src/radar/lifecycle/ephemeral.py:624` 是门 ① / `:636` 是门 ② / `:135` 是 `DECLARED_READ_SITES`)。
   ⇒ 这条**正躺在 T040 待裁清单上**,建议**并案**裁,不要单独开一条。
   在裁定之前,`briefing/` 的产物在崩溃/掉电场景下**没有持久性保证**(如实记,非 blocking)。

### 3.3 本轮**不**要求 IDS 做的(免误排期)

- **不要求裁 R5 未复核**(§4.1)—— 那是 operator 的节奏决定,不是 IDS 的治理决议。
- **不碰 spec**:v0.6→v0.7 amendment(§1-O2 (c) + OQ-A1-1 + §7-P2 三条)按既定**攒到
  08-02 forge 001 之后**由 spec-writer 一次性落。本包**未改** `specs/001-radar-pA/spec.md`。
- **不改 `parallel-builder` SKILL**、**不起 T040 / T041 / FU-B / FU-C** —— 维持 IDS 既定处置。

## §4 已知缺口(如实记 · 非 blocking)

### 4.1 ⚠ R5 四条修法**未经 codex 复核**

operator 定的 **5 轮上限已用尽**。`df5ce24` 的四条修法(§2.4 R5 表)**只有本地 pytest +
mutation 背书,没有 codex 复核**。R1–R4 每一轮的修法都被下一轮复核过,**R5 是唯一没有的**。

### 4.2 ⚠ 上一条的**量化依据**:R5 四条 high 里 **3 条是 R4 修法自身的残留绕过**

| 轮 | 该轮 finding 中「上一轮修法的残留」 |
|---|---|
| R2 | **2 / 5**(RF1 是 R1-F1 残留 · RF2 是 R1-F3 残留) |
| R3 | **1 / 5**(F4 是 R2-RF5 残留) |
| R4 | **2 / 4**(F1 是 R3-F1 残留 · F2 是 R3-F3 残留) |
| R5 | **3 / 4**(F1/F2/F3 分别是 R4-F1/F2/F3 的残留) |

**残留率不降反升**(40% → 20% → 50% → **75%**)。这不是「越修越差」——
是**剩下的都是难的**:三处收紧点(preflight 解析 / journal 快照身份 / C9 作用域)
各自要 4–5 轮才收敛。**这就是「末轮无复核」风险的量化依据**:
在残留率 75% 的那一轮之后停手,先验上不应假设它已经收敛。

### 4.3 我对「是否值得起第 6 轮」的判断(IDS 未预设答案 · 这是我的判断与理由)

**判断:值得,但不是现在,也不是 5 轮的续跑 —— 应作为 forge 001/006 裁定之后的一次
「窄 scope 复核」,且优先级低于本 session 任务 2(O7 真跑)。**

理由四条:

1. **先验不利,但赔率在下降**。§4.2 的 75% 残留率支持「再跑还有」;但 R5 的三条修法
   在**性质上**与前四轮不同:R5-F1(跳注释行)/ R5-F2(`lines[2]` 逐字相等)/ R5-F3(作用域表补 5 个节点)
   都是**把判据从「搜索」改成「定位」**的收窄,不再是「再堵一种写法」的打地鼠。
   R5-F4 更是直接换成 stdlib `urlsplit`——**判据的单一真相搬到了标准库**。
   ⇒ 我估计第 6 轮的产出**低于**前五轮,但不为零。
2. **有两条 high 落在下一步真跑的可信度面上**,这是最实的理由:
   - **R5-F2**(真跑横幅绑定 `lines[2]`)—— O7 的 `journal record` 门**直接依赖它**;
   - **R5-F4**(URL canonicalizer)—— 影响 `graph/builder.py` 的别名回退锚点,
     即 briefing 里证据锚的**归属正确性**。
   ⇒ 若第 6 轮推翻这两条,本 session 产的 briefing 在 **P2 用途**上需重判
     (**O5/O7 计数用途不受影响** —— journal 行记的是 operator 的决策,不依赖证据链抽样判据)。
3. **但现在跑第 6 轮会重复一次「在判据冻结前不知道判据形态」的老错**。
   R4-F4(目录 fsync)已经证明:**C9 门 ① 的契约形态未定之前,这一片的「对」是不可判的**——
   而 C9 判据正在 T040 待裁清单上(08-02 forge 001)。第 6 轮若在裁定前跑,
   很可能又抓出一批「取决于 C9 怎么裁」的 finding = 空转。
   (与 009 KG-43 / T022 judge 无下界 / 07-28 gate 不可复现**同族第四例**:
   **判据可达性与稳定性应先于判据冻结**。)
4. **5 轮上限是 operator 的成本决定,不是收敛证据**。本包如实标注这一点,
   不把「跑满了」说成「收敛了」。

**具体建议的第 6 轮形态**(若 operator 采纳):forge 001/006 裁完 C9 与 XD-41 之后,
scope **只取 `4a4c5f5..df5ce24`(R5 那一个 commit)+ 本 session O7 真跑改动**,
focus 明写「R5 四条修法的残留绕过 + 真跑链路可信度」。**不要**再跑一次 30-commit 累积区间
—— 那个区间已被 5 轮覆盖,重跑的边际产出低且贵。

### 4.4 ⚠ 本包**未挂** `ids_verdict_evidence` 块 —— 理由(不是漏做)

7 字段中的 **`codex_model` 无法诚实填写**:R1–R5 的 review 产物是 companion 的**原始 stdout**
(`review-log/backfill-e89860e-2026-07-31-round{1..5}.md`),**没有 REVIEW-LOG frontmatter**,
其中不含 model 字段;`~/.codex/sessions/` 里 **07-31 一份 rollout 都没有**;
`~/.codex/config.toml` 里的 `gpt-5.6-sol` 是**今天的配置**,不能证明 07-31 跑的是它。

⇒ 填任何值都是**发明**。而 `validate-verdict-evidence.sh` 自己的头部注释
(`lib/handback-validator/validate-verdict-evidence.sh:9-11`)明写它
「**接受语法合法但未经证伪的 evidence metadata**」—— 挂一个填不实的块正是它警告的用法。

**替代:可机器复核的证据表**(sha256 均为本 session 实算):

| 轮 | review_log_path(repo-relative) | sha256 | verdict | findings_count | 产出时刻(本地 +08) |
|---|---|---|---|---|---|
| R1 | `review-log/backfill-e89860e-2026-07-31-round1.md` | `6fe43e380069ca5acf3d2e341b342bc5c7e61c0db0989047f1947b2dd01f39d4` | needs-attention | 6 | 2026-07-31 16:04:32 |
| R2 | `review-log/backfill-e89860e-2026-07-31-round2.md` | `821a30400ae72c6d2d52f6164b3f8eda4c1f613482d69233830f34792f2bbd77` | needs-attention | 5 | 2026-07-31 16:35:32 |
| R3 | `review-log/backfill-e89860e-2026-07-31-round3.md` | `5051744fb33bf7c9fb607e27d463d3e3a6d9151384744b1a363098b4d1751689` | needs-attention | 5 | 2026-07-31 17:00:08 |
| R4 | `review-log/backfill-e89860e-2026-07-31-round4.md` | `0a6700013ea03c6e874abe43b99f7d484153cbcb78be2f679a0e136c91a538f2` | needs-attention | 4 | 2026-07-31 17:20:02 |
| R5 | `review-log/backfill-e89860e-2026-07-31-round5.md` | `a8435d7e1ab08c62023b3848ddc5136deb1ea5bfe8ac1989c5e02aa070ce720f` | needs-attention | 4 | 2026-07-31 17:42:07 |

⚠ **每一轮的 verdict 覆盖的是「该轮修法之前」的树**,不覆盖该轮的修法。
特别地:**没有任何 verdict 覆盖 `df5ce24`**(§4.1)。

⚠ 顺带一个**框架级观察**(未记 backlog · 留给 operator 判是否值得记):
`.claude/skills/codex-review/REVIEW-LOG.md` 这个「latest pointer」**停在 2026-07-26**,
R1–R5 五轮**一条都没写进去** —— 补跑走的是 `review-log/` 这个 ad-hoc 目录。
即「本仓最近一次 codex review 是什么」这个指针,在最需要它的这批上是**过期的**。

### 4.5 KG-33 第 8 次复现

`lib/handback-validator/gen-handback.sh` 产出的 `to_source_repo` / `workspace.source_repo`
仍是写死的 mac 字面量 `/Users/admin/codes/ideababy_stroller`(而 `handback_target` 正确解析成
`/home/ys/...`)。本包**已手改这两行**为真路径。
⚠ 6 约束 validator **producer 模式对此不报错** —— 该字段无机器门,与 KG-33 的判断一致。
另:`--repo-root .` 会让 `from_build_repo` / `build_repo` / `working_repo` 全变成字面量 `.`,
本包**一并手改**为绝对路径(这一条 KG-33 未覆盖,是同一张模板的第二个口子)。

### 4.6 其它

1. **RF5 / R3-F4 的 jsonl 写入是有界收口,不是闭合**:事务性持久化 / 尾部恢复协议 /
   并发安全 = 存储层重设计,超出补跑评审授权,已记 backlog。回滚建立在本仓明写的
   「单用户无并发写」假设上。
2. **R3-F2 的 C9 顺序判据是「源码文本顺序的近似」,不是控制流分析**;
   `global` / `nonlocal` 仍是已知空白(写进 docstring)。方向上比收口前更保守(抑制更少 = 漏报更少)。
3. **本批未改 `specs/001-radar-pA/spec.md`(frozen)· 未动 `framework/` · 未起
   FU-B / FU-C / T040 / T041**。
4. **R1–R5 五个 commit 未 push**(`worktree-001-radar-pA` 相对 `origin/worktree-001-radar-pA`
   **ahead 5**;相对 `main` ahead 70 —— operator 定:集成基线 = 本分支非 main)。
