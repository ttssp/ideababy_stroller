---
discussion_id: "001"
prd_fork_id: 001-radar-pA
handback_id: 001-radar-pA-20260716T170153Z
from_build_repo: /home/ys/codes/XenoDev
to_source_repo: /home/ys/codes/ideababy_stroller
current_idea: 001-radar-pA
worktree: .claude/worktrees/001-radar-pA
baseline: 9cb881f70508488f406a494ac57e87db08d725ba
workspace:
  source_repo: /home/ys/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev
  working_repo: /home/ys/codes/ideababy_stroller
  handback_target: /home/ys/codes/ideababy_stroller/discussion/001/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - build-complete
severity: medium
created: 2026-07-16T17:01:53Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: T003-A3
---

# Hand-back · 001-radar-pA · forge 001 v2 verdict 落地(四模块 A'/B'/C'/D')

**Severity**: medium
**Tags**: build-complete
**Created**: 2026-07-16T17:01:53Z
**Baseline**: main `9cb881f`(forge-v2 landing 全部叠于其上 · worktree-001-radar-pA 分支)

## §1 做了什么(forge v2 Decision menu [A] 落地)

消费 IDS forge 001 v2 verdict(`discussion/001/forge/v2/stage-forge-001-v2.md` · HANDBACK-LOG 第 14 条 ·
PRD v1.4 dc8f4b4),在 XenoDev worktree-001-radar-pA 落地四模块。全程 TDD + codex adversarial-review 硬化。

### 模块 A'(T003-A3)· a1 per-slice source 映射前移 —— **DONE**(codex 10 轮硬化)
- `domain.TimeSlice` 加 `source_refs: tuple[SourceRef,...]=()`(plain · frozen 兼容 · additive-safe)。
- `slicer.slice_timeline` 从本窗论文(`_window_boundaries` 单一真相同源窗)挂 source_ref → slice。
- `reconstructor._refine_slice_state` model_copy 自动保留 refs(精化面不 silent 丢)。
- `writer.render_markdown`(审计面)渲染 source refs 供 O2 三击;**盲测面 render_blind_markdown 不渲染**
  (operator 决策:不改识伪难度 · 负控 refs/日期一致性归 O2/T021)。
- **仅 slice→source refs(OpenAlex DOI/URL · Paper.source_ref 已必填)· 不建图不做 judge · 不产结论词 ·
  不扩 N/M → 不越 RT-2**(gate.py:209 源码自认「未来 per-slice source 映射落地后可改回」= a1 正填此缺件)。
- **C' 折入 A'**(codex round-2)· 新增 `credibility/audit_channel.py`(机器可读审计产物)。

### 模块 B'(T004-A2)· unlock-preflight checker + 机器道 STOP wiring —— **DONE**(codex B'/D' review 3 high 修)
- `credibility/unlock_preflight.py` `check_unlock_predicate` 验 a1 单一谓词**四件**(结构就位 + 映射可解析 +
  **briefing 覆盖** + 命名一致)· 任一 FAIL → STOP + 结构化原因。
- **真 wire**(KG-B4 · codex review high #1):`scripts/unlock-preflight-t010-t011.sh`(真调度入口 · 退出码
  0=PASS/2=STOP)+ T010.md/T011.md 前置段引它作 hard-block command + 集成测真调脚本证 STOP → 非 0(非只测
  CLI 返回码 · 闭合「有 checker 无机器强制」)。
- **briefing 覆盖**(review high #2):audit slice 集须 == briefing slice 集(集合+顺序)· 防部分/错配
  provenance 解锁(a1 不变量 = 真 briefing 每 slice 都有可解析 refs)。
- **强制独立 corpus**(review high #3):`--corpus-refs` 生产解锁必填(缺则 fail closed)· 移除自 corpus
  默认(防待验证值与参照集同源的 fail-open)· `--allow-self-corpus` 调试模式绝不解锁。
- 只验结构不验语义(不把人判塞机器门)· PASS 是结构谓词 · 真生效仍需 D' rerun 自证。

### 模块 C'(折入 A' · 原 T004-A3 撤销)· 审计通道最小文件结构 —— **DONE**
- `audit_channel.py`:per-slice source 映射的机器可读产物(轻量文件级 · 无 DB/索引)· post-gate 生成。

### 模块 D'(T004-A4)· 真 briefing rerun 自证协议 + journal —— **DONE**(生效条件写死)
- `journal/schema.py` `A1SelfCheckRecord`(stage-0 治理动作留痕 · append-only)。
- `credibility/rerun_selfcheck.py` `run_a1_selfcheck`:真产物 + 谓词 PASS → unlock_allowed;fixture 恒不
  生效(K 第 4 条:生效只认真 briefing rerun);FAIL → 不硬开 + prd-revision-trigger;独立核验不一致 →
  反回声室兜底不解锁(DDC)。
- ⚠ 真 rerun 烧 DEEPSEEK key = **operator 动作**(`cli.reconstruct --direction "diffusion models"
  --from-date 2021-01-01 --to-date 2023-12-31` · run-877f1c1e 同窗)· 本 module 只读产物(C8 · 不见 key)。

### v1 模块 B(gate.py 降级校准工具)、D(journal schema)
- v1 模块 D journal 承入模块 D'(A1SelfCheckRecord)。v1 模块 B(gate.py 降级)现状 gate 已是 smoke test
  语义(v1.1 两层化已落)· 本次未再动 gate.py(a1 明确不改 gate 判据 · 见 §3 边界)。

## §2 硬前置真跑结果(逐面 grep 枚举 + RT-2 校准)

- **TimeSlice 消费面全枚举**(forge-lite KG-36 序列化面防 silent 丢 · 独立 Explore 子代理全量核实):构造根
  (slicer)/ 精化(reconstructor)/ 两渲染面(writer.render_markdown 审计 + injector.render_blind_markdown
  盲测)/ 签字模板(gate)/ 腐化(injector)/ domain 往返(model_copy/model_construct)。**a1 代价 = 真 S-M**
  (映射已存在 papers_in_slice · provenance 已存在 Paper.source_ref · 无新检索/图/judge)· **未触发 v3
  条件④「消费面代价远超 S-M」**。
- **RT-2 校准**(spec §5:120-156):a1 不违 RT-2(禁的是 judge/图/结论词/扩 N-M · a1 只前移检索 provenance)·
  **无写死冲突 · 未 STOP**。
- **测试**:307 passed(254 baseline + 53 a1/audit/preflight/selfcheck)· ruff clean · cov 95-100% 改动文件。

## §3 边界与铁律(严守)

- ✅ ①②(trace schema 完整性 / 3击可达 full checker)**未建**(v2 退役出解锁前置 · 迁 T021/T022 原位)。
- ✅ v1 stage 的 trace-validator **未建**(A/C 被 A'/C' 替换)。
- ✅ **未改 gate 判据**(gate.py doc 锚接受逻辑 = Phase 2/T021「未来可改回」· a1 只让映射存在)。
- ✅ **未自行改 IDS PRD**(PRD 有问题走 hand-back)。
- ✅ spec §5/DAG 前置文案改动:**留 spec-writer/operator**(见 §5 待办 · 我出 diff 建议未越权改 frozen spec)。

## §4 codex adversarial-review 硬化史(A' · 10 轮 · 证 review 真起作用)

| Round | Verdict | 核心 finding | 处置 |
|---|---|---|---|
| R1 | needs-attention | 审计面 source_ref markdown 注入 | _md_inline_safe 中和 |
| R2 | needs-attention | 真产物面缺口(refs 没进 CLI)+ 裸 URL autolink | audit_channel + CLI wire + scheme disarm |
| R3 | needs-attention | 审计产物 period 可 diff 泄 WRONG_DATE 负控 | 去 period + 移受保护目录 |
| R4 | needs-attention | source_refs 本身泄负控 + 零宽破坏真 ref O2 | 移 out/credibility + ref 白名单保可解析 |
| R5 | needs-attention | pre-signoff 物化根因(文件名可预测) | **改 post-gate 生成**(run_reconstruct 不产) |
| R6 | needs-attention | post-gate 无 run 绑定 + 死日期参数 + sparse bool | slice_id 绑定 + 真用日期窗 + 严格 bool |
| R7 | needs-attention | slice_id 位序标签不够绑 | 检索指纹 + run-binding metadata |
| R8 | needs-attention | 绑定 fail-open + 全局集哈希漏日期 | 强制绑定 + per-slice+published 指纹 |
| R9 | needs-attention | 生成没绑 gate PASS | gate_pass_artifact 校验(verdict=PASS + run_id) |
| R10 | needs-attention | **[critical]** gate PASS artifact 可伪造文本检查 + [high] direction/ts 未校验 | high 已修;**critical 升级 IDS**(见 §6) |

**B'/D' codex review**(base=e4fcb57):
- round-1 needs-attention · 3 high(全在 B':真 wire 缺 / briefing 覆盖缺 / corpus fail-open)· 均修(commit 9f1fbb7)。
- round-2 needs-attention · 2 high(briefing 缺失/不可解析 fail-open / D' 独立核验可跳过)· 均修(commit 4124927):
  briefing 缺失/空/格式漂移 → STOP fail-closed;D' 非 fixture 解锁须 independent_check_agree is True(None 不解锁)。
- round-3 verify 见附 review-log。

## §5 待 spec-writer/operator 落地项(我出 diff · 不越权改 frozen spec)

1. **⚠ BLOCKING(codex B'/D' review-3 high #2)· spec §5 DAG + T010/T011 frontmatter 结构改**:
   `dependency-graph.mmd` line 61-64 仍 `T004A1 --> T010/T011` · T010.md/T011.md frontmatter 仍
   `depends_on: [T004-A1]` · 新 T004-A2/T004-A4 节点**未入图** → 按 DAG/frontmatter 调度的 parallel-builder
   可**绕过**新 preflight/selfcheck(prose 前置改了但结构依赖没改)。**这是 frozen spec DAG/frontmatter
   结构改 = spec-writer 域**(本 session 只改了 T010/T011 的 prose 前置段 + 加了新 task doc · 未越权改
   frozen DAG 边/depends_on)。**spec-writer 须**:① DAG 加 T003-A3→T004-A2→T004-A4 链 + T010/T011
   depends_on 改 T004-A4;② 加测断言 DAG/frontmatter 不再有 T004A1 直连 T010/T011。**在此落地前,
   机器道 wiring 不完整**(preflight 脚本是真门,但调度图不强制走它)。
2. **spec §5 新增 amendment task 定稿**:T003-A3 / T004-A2 / T004-A4 已产 task doc(specs/001-radar-pA/tasks/)·
   待 spec-writer/operator 纳入 frozen spec 的 task 列表 + DAG 边(同 #1)。
3. **T010/T011 解锁真生效**:须 operator 跑模块 D' 真 rerun 自证(烧 key)· PASS + 独立核验一致方解锁
   (生效条件写死)· unlock 脚本第 4 参 journal 校验 D' 自证已跑(review-3 high #1 已 wire)。

## §6 升级 IDS 项(spine 级 · autonomous 判定 · 须 operator/forge 裁)

**post-gate provenance 不可伪造绑定 = gate-spine 子协议**(见 `specs/001-radar-pA/DECISIONS-forge-v2-landing.md`
D6/D7 + dogfood-backlog KG-B10):

- codex R5-R10 六轮把「审计产物防 pre-signoff 篡改重建」挖到底 · R10 [critical]:gate PASS artifact 文本
  检查可伪造(run_id 判定前可见 + run-binding 签字前落盘 → 手写假文件击穿)。
- **本 session 判定(bounded · 防 V4 + KG-B7 过度投资)**:不在 a1 当场建 spine 协议(当场建 = 绕治理动
  gate + answer-key 密码学机制)。诚实降级 `_verify_gate_pass_artifact` 为「非不可伪造安全边界 · 只结构检查」·
  pre-signoff 防篡改在协议落地前靠程序纪律(同 answer-key「判定前勿看」)· 不宣称机器强制。
- **请 operator/IDS 裁**:① gate-internal 触发(gate PASS 分支内直接产下游 gated 产物 · 消除公共 generate
  入口)· ② answer-key 派生不可伪造 token · ③ 抽 SHARED-CONTRACT 通用 checker(「下游 provenance 机器强制
  绑上游 gate PASS」· 受益所有 probe-gated 项目 = KG-B10 框架级)。拆独立 task / forge v3 由 operator 决。
- **两个面同病**(codex A' R10 critical + B'/D' review-3 high #3):A' 面 = 审计产物 gate-PASS artifact 可
  伪造;D' 面 = selfcheck 只信 caller 的 is_fixture/independent_check_agree 布尔(无机器可验证 real-rerun
  proof)。**同一子协议裁两面**:「机器如何不可伪造地证明下游 provenance 产于上游 gate 真 PASS 之后」。
  在协议落地前,两面都靠**程序纪律**(docstring 显式标注非不可伪造保证 · fail-closed 仍挡无意/低技巧误用)。

## §7 operator 下一步

1. IDS 跑 `/handback-review 001` 决议本 build-complete hand-back(采纳四模块 / 决 §5 spec 落地 / §6 spine 升级）。
2. spec-writer 落 §5 spec §5/DAG 前置文案 + 纳入 amendment task。
3. operator 跑模块 D' 真 rerun 自证(烧 key · run-877f1c1e 同窗)· PASS 方解锁 T010/T011。
4. §6 spine 子协议:operator 决 forge v3 / 拆 task / SHARED-CONTRACT 通用层。

## §8 未 push(领先 origin)

本 landing(20 commits on worktree-001-radar-pA · 叠于 main 9cb881f · A' 13 + B'/D' 5 + docs 2)**未 push**
(per CLAUDE.md · push 前需 operator 确认)。⚠ DEEPSEEK key(008/001 同 key)建议轮换(有一次泄露前科 · C8)。

**测试总计**:318 passed · ruff clean。**codex review 总计**:A' 10 轮 adversarial + B'/D' 3 轮 adversarial =
13 轮 · 全 finding 真实(证 cross-model review 有效)· 有界的全修 · spine/frozen-DAG 的显式升级(§5 #1
BLOCKING spec-writer · §6 IDS forge)。
