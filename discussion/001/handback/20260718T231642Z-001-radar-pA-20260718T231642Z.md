---
discussion_id: 001
prd_fork_id: 001-radar-pA
handback_id: 001-radar-pA-20260718T231642Z
from_build_repo: /home/ys/codes/XenoDev
to_source_repo: /home/ys/codes/ideababy_stroller
baseline: 9cb881f70508488f406a494ac57e87db08d725ba
worktree: .claude/worktrees/001-radar-pA
workspace:
  source_repo: /home/ys/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev
  working_repo: /home/ys/codes/ideababy_stroller
  handback_target: /home/ys/codes/ideababy_stroller/discussion/001/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - feature
severity: medium
created: 2026-07-18T23:16:42Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: forge-v4-clause-effectuation
---

# Hand-back · 001-radar-pA · forge-v4-clause-effectuation

**Severity**: medium
**Tags**: feature
**Created**: 2026-07-18T23:16:42Z

## §1 Rationale(执行摘要)

**forge 001 v4 §c 条款生效事件达成** —— 上一个 hand-back(`20260718T135400Z` · forge-v4-landing)显式标注为 `⏳ 条款生效(landed-not-yet-effective)` 的最后一态,本 session(2026-07-18 · autonomous · operator 显式授权烧 key)以**首个默认评估 run + 同一验收事件内 B′+D′ 自证 PASS** 兑现为 **✅ 条款生效(effective)**。

**这是第六次「推演≠真跑」判定 —— 落在真跑/生效侧(非被推翻)。** forge v4 §underweights 明写「首个评估 run 真跑自证 = 本 amendment 最后防线 · 若再 STOP = 第六次『推演≠真跑』= v5 触发条件①」。本次**未 STOP**:真跑走到判定并 PASS,amendment 最后防线兑现。上游 forge/推演 verdict 连续四次被下游真跑推翻的链(见 IDS KG · `forge-verdict-vs-empirical-ship`),在本条款上以**真跑 PASS** 收口。

**核心状态(forge v4 两态命名 · 本次推进第二态)**:
- ✅ **机制 landed**(上个 hand-back 达成):四模块 A″/B″/C″/D″ landed · g1 人肉盲测二值签字永久退役出解锁链。
- ✅ **条款生效(landed-and-effective)**(本次达成):首个评估 run 产物 `diffusion-models-20260718T231343Z` 在**同一验收事件内**跑 B′+D′ 自证 PASS → 条款生效 → T010/T011 机器解锁前置(unlock-preflight)PASS。

**真跑证据链(同一验收事件 · 一气呵成 · 无产物先用后证 / 无隔日补证)**:
1. **首个默认评估 run(烧 DEEPSEEK key)**:`radar.cli.reconstruct --direction "diffusion models" --slices 3 --from-date 2021-01-01 --to-date 2023-12-31`(**非 --calibration** · 非 --dry-run)· 真 OpenAlex(2021–2023 封闭历史窗)+ 真 DeepSeek(key C8 合规:子进程 eval `~/.bashrc` export 行进内存 · 值不进 stdout)。产:
   - `out/reconstruct/diffusion-models-20260718T231343Z.md`(评估 run 审计面 · human-readable · 含 per-slice 来源块)
   - `out/credibility/diffusion-models-20260718T231343Z.audit.json`(机器可读 · a1 per-slice source 映射)
   - **新 ts 无 sibling 校准标记** → 自证 ①b 同 stem 校准拒消费守卫**不命中**(与 07-17 校准样本 `20260717T051551Z` 干净隔离)。
2. **同验收事件内 B′+D′ 自证(无 LLM · 只烧 OpenAlex 免费检索)**:`scripts/selfcheck-07-17.sh "diffusion models" 20260718T231343Z`(同 stem evidence bundle · 窗缺省 2021→2023 与评估 run 逐字一致 · briefing 缺省由源码 `_slugify` 派生)。结果:
   - **B′ 结构谓词 PASS**:`{structure: True, resolvable: True, coverage: True, naming: True}`
   - **D′ 生效**:`unlock_allowed=True · handback_required=False` · `is_fixture=False`(默认真 OpenAlex 路径 · 非注入 retriever)· `independent_check_agree=True`(独立真 OpenAlex 检索派生 corpus 与序列内部 audit source_refs 双路径一致 · DDC 反回声室)
   - **audit_digest**(篡改证据):`sha256-64a6c0d182e7fdd05d018dca5e44aa01` · 入 append-only journal `out/journal/a1-selfcheck.jsonl`
3. **T010/T011 机器道解锁前置 PASS**:`scripts/unlock-preflight-t010-t011.sh <audit> <briefing> <独立corpus-refs> <journal>`(四参齐 · corpus-refs = **独立**真 OpenAlex 检索 200 refs · 非自 corpus fail-open)· **exit 0** · `verify_selfcheck_journal` 交叉核验 journal 内匹配本 audit+briefing 的非 fixture PASS 记录成立。**T010/T011 hard-block 解除**(机器核验 · 人不在判定回路 · g1 已退役)。

**验收指标**:评估 run 真跑成功(真 LLM 真检索)· B′ 四件全 PASS · D′ 非 fixture 生效 · unlock-preflight exit 0 · 同一验收事件内一气呵成(禁产物先用后证/隔日补证 = v5 触发条件④未触发)· 工作树干净(out/ gitignored · 产物留痕在 append-only journal + 本 hand-back)· 33 commit 领先 main。

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

**tag: `feature`** —— forge 001 v4 §c 条款生效事件(交付里程碑 · 承接上个 landed 里程碑的第二态)。gen-handback enum 无 `clause-effectuation`/`build-complete`,`feature` 为交付里程碑最贴合法值(沿用上个 hand-back `20260718T135400Z` 同判据 · 见 §3.5 enum 漂移在册)。

### 决议链(上游 → 本次生效)

1. **IDS forge 001 v4 verdict(采 γ · β cut)** —— 生效自证标的以正式 amendment 改写为「**首个评估 run 产物**」:每个评估 run 产物落地后同一验收事件内以同 stem evidence bundle 跑 B′+D′ 自证;首个自证 PASS = 条款生效事件(T010/T011 随之机器解锁)。**本次即该「首个评估 run 产物 + 首个自证 PASS」的兑现事件。**

2. **本次 XenoDev 消费 = 三前置门 + 执行序列**(operator 逐一显式授权):
   - **前置门①(key 轮换)**:operator 确认新 DEEPSEEK key 已就位(`~/.bashrc` L190 export · 在非交互早退 guard L6-8 之下)。诊断:`bash -lc` 非交互 source 因 guard 早退看不到 L190 → 改 `eval` 单行 export 进子进程内存(C8 合规 · 值不进 stdout · 长度校验 len=35 · 本体未打印)。**key presence 确认只查 presence/长度 · 全程未 grep/cat 出含 key 行内容**(守 C8 泄露前科铁律 · 凭据隔离 hook C12 两度 fail-closed 挡住动态路径读 · 已尊重不绕)。
   - **前置门②(方向)**:operator 选 `diffusion models`(缺省)。**关键澄清**:自证 ①b 守卫按**精确 stem `<slug>-<ts>`** 判校准标记(非 slug-only)· 故新评估 run(新 ts `20260718T231343Z`)与 07-17 校准样本(`20260717T051551Z`)干净隔离 → 不撞 selfcheck 脚本头警告的「校准样本 fail-closed STOP」(那是对**默认 ts** = 07-17 校准 ts 跑才会命中)。
   - **前置门③(授权烧 key)**:operator 显式授权跑首个默认评估 run(非 --calibration)。
   - **执行序列**:评估 run → 同事件自证 → 分流 PASS。**只复用现有 `run_selfcheck_sequence` 入口 + 三道守卫**(①a 评估形状 allowlist / ①b 同 stem 校准拒消费 / ② 注入 retriever 强制 fixture)· **无新增判据/旁路 flag/provider-proof spine**(forge v4 §b 铁律全守)。

3. **窗一致性设计决策(operator 确认)** —— 评估 run 与 selfcheck **必须同窗**(否则双路检索不同语料 → B′ coverage/D′ 一致性因窗不匹配 STOP · 非真生效信号)。operator 选封闭历史窗 2021-01-01→2023-12-31(= selfcheck 缺省窗 · 逐字一致 · 可复现 · 非 today-相对漂移窗)。

### ⚠ v4 §underweights 兑现监测(诚实标注 · 本次兑现结果)

- ✅ **首个评估 run 真跑自证 = amendment 最后防线**:**本次兑现**(真跑 PASS · 非第六次推翻)。v5 触发条件① **未触发**。
- ✅ **同一验收事件纪律(v5 触发条件④:产物先用后证/隔日补证)**:本次**一气呵成守住** —— 评估 run 产物落地后**立即**在同 session 同事件内跑自证 · 无隔日补证 · 无先用后证。仍是 runbook 纪律(未机器强制)· 但本次 operator 纪律 + task 铁律双守成立。
- ⏳ **provider-proof spine 未机器强制(KG-B10 D′ 面)**:本次非 fixture 生效**靠程序纪律**(operator 只经 `scripts/selfcheck-07-17.sh` 走默认真 OpenAlex 路径 · 未直调公开入口传 is_fixture=False)· 未宣称机器强制。**残留 · 归 IDS forge · 本次不当场改**(见 §3.2)。

## §3 Suggested IDS actions

### 给 IDS 的建议动作

1. **`/handback-review 001` 决议本 hand-back(条款生效)** —— 确认 forge v4 §c 条款生效事件达成:首个评估 run 产物 + 同事件自证 PASS + T010/T011 机器解锁前置 PASS。**「机制 landed → 条款生效」两态迁移在本 discussion 收口**;上游 forge/推演 verdict 连续四次被真跑推翻的链,在本条款以真跑 PASS 收口(第六次「推演≠真跑」落生效侧)。

2. **残留框架级 dogfood(在册 · 归 IDS forge · 本次不当场改)**:
   - **KG-B10(provider-proof spine · D′ 面)**:非 fixture 生效自证的 provider 可信性仍靠程序纪律(operator 只经 selfcheck 脚本走默认真 OpenAlex · 不直调公开 `generate_eval_audit_for_selfcheck` + `run_a1_selfcheck(is_fixture=False)` 绕 ② wrapper-only 守卫)。「不可绕过地证明 audit 产自可信默认 provider」= 不可伪造 provenance 绑定 = spine 子协议 · **已升级 IDS · 冻结不建**(防 KG-B7 过度投资 · 防 V4 当场动 spine)。本次真跑**未触及**该面(走的正是纪律要求的默认路径)· 供 forge 决落地时机。
   - **KG-B11(spec-writer SKILL 不产 immutable = R-Q7 债)**、**KG-B5 族(gen-handback template macOS 硬编码 / tag enum 漂移 / HANDOFF hash 漂移)**:随框架批次攒批走 forge · 原判维持(详见上个 hand-back `20260718T135400Z` §3.5/§3.6)。

3. **gen-handback template macOS 硬编码缺陷(KG-B5 族 · 本次再次触发)**:`templates/handback.template.md` 的 `to_source_repo` + `workspace.source_repo` 仍硬编码 `/Users/admin/codes/ideababy_stroller`(无占位符 · gen 不替换)· 且 worktree 内跑时 `REPO_ROOT` 解出 worktree 路径(非 canonical build_repo)。**6 约束验证器不校这两字段值**(check-5 只校 id/filename/discussion 一致 · check-3 校 3 identity 字段)故不阻断。本 hand-back 已**手工修正** `to_source_repo`/`workspace.source_repo`/`from_build_repo`/`workspace.build_repo` 4 字段为 Ubuntu canonical 值(对齐已投递实践 `20260716T170153Z` / `20260718T135400Z`)。真产源自 **worktree** `.claude/worktrees/001-radar-pA`(git worktree · 分支 main · baseline `9cb881f`)。**建议 IDS 决**:template 加占位 + env 化 · REPO_ROOT 与 section-base 解耦。

4. **T010/T011 后续(operator/build 侧 · 非 IDS 动作)**:机器解锁前置已 PASS · T010/T011 可起跑(parallel-builder 起跑前会再跑 unlock-preflight 作 hard-block 门 · 退出码 0 = 放行)。**注意**:生效**绑定本 evidence bundle stem** `diffusion-models-20260718T231343Z` · 若后续换方向/换窗起跑,需对新评估 run 重跑自证(每评估 run 各自生效 · 非全局一次性)。

## §4 6 约束自检记录(producer validator dry-run)

投递前已跑 `lib/handback-validator/validate-handback.sh` 6 约束自检(见交付时输出)· 三字段 canonical 值手工核对:`to_source_repo`/`workspace.source_repo` = `/home/ys/codes/ideababy_stroller` · `from_build_repo`/`workspace.build_repo` = `/home/ys/codes/XenoDev`(Ubuntu canonical · 非 template macOS 硬编码值)。直投 IDS `discussion/001/handback/`(不滞留 `.work/` · 守上次滞留前科铁律)。
