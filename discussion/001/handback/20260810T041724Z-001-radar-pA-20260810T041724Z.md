---
discussion_id: 001
prd_fork_id: 001-radar-pA
handback_id: 001-radar-pA-20260810T041724Z
from_build_repo: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA
to_source_repo: /home/ys/codes/ideababy_stroller
workspace:
  source_repo: /home/ys/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA
  working_repo: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA
  handback_target: /home/ys/codes/ideababy_stroller/discussion/001/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - feature
severity: high
created: 2026-08-10T04:17:24Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
current_idea: 001-radar-pA
worktree: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA
baseline: 505eaac
related_task: FU-R1F1-dry-run-provenance
---

# Hand-back · 001-radar-pA · FU-R1F1-dry-run-provenance

**Severity**: high
**Tags**: feature
**Created**: 2026-08-10T04:17:24Z

## §1 Rationale(执行摘要)

FU-R1F1-dry-run-provenance(IDS HANDBACK-LOG 第 41 条 chair accepted `[critical]`)已 ship。
`run_reconstruct` 此前只判 `calibration` 不判 `dry_run`,`dry_run=True` 的评估 run 会产出与真评估
完全同形、无 provenance 标记的产物,可被解锁自证链(`unlock_preflight`/`rerun_selfcheck`)误当真
评估消费;`dry_run=False` 生产分支也曾接受调用方注入 `retriever`/`llm`,可产出携带合法
`REAL_FULL_CHAIN` provenance 但底层数据是假的产物。

四件修法全部落地:① dry-run 产物隔离(四象限分流)② 不可移除 provenance 标记 + run 完整性绑定
③ 两个解锁入口改 allow-list fail-closed ④ 生产模式拒绝一切组件注入。codex adversarial-review
4 轮,round-1/round-2 命中的 2 条 `[high]` 均已修(content_digest 未绑真实内容 / provenance
footer 非 EOF 锚定 + TOCTOU 多次读)。round-3/round-4 命中的 1 条 `[high]` 揭示已冻结架构面
KG-B10 的具体可复现子场景(journal 记录只按路径字符串匹配,合法的第二次真实 rerun 覆盖同
slug/ts 可致旧 PASS 静默解锁未核验新内容)——按「牵动即停」铁律,task 内未修,归档
dogfood XD-46 升级 IDS forge。触 4 轮硬上限;operator 复核后判定该项属架构边界归属分歧而非
已验证活漏洞,裁定接受当前状态 ship,不再追加 codex 轮次(详见 §2)。

Ship 过程中额外发现:FU-R1F1 的 provenance allow-list 让 `file_domain` 外 12 个既有测试(分布
7 个文件 + 1 个仓级 shell 脚本)从通变红——这不是新漏洞,是本 task 自身声明的修法应有的、
正确的下游行为,只是原 task doc 的"零回归风险"论证方法本身不够严谨(单一 grep 关键词存在性
核对,未逐调用点核对参数取值)。operator 授权同批收口,已全部修复并披露(详见 §3),记
dogfood XD-45。

全量 `uv run pytest tests/ -q` → 1075 passed(0 回归);FU-R1F1 named verification(task doc
`## Verification` 全 9 项命名测试)全过;coverage `reconstruct.py` 86% / `unlock_preflight.py`
88% / `rerun_selfcheck.py` 100%;ruff clean。

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

**触发**:IDS 第 42 条 (2)(2026-08-04T00:18Z)授权起跑批次之一。FU-R1F1 本身是 IDS
HANDBACK-LOG 第 41 条 chair 对 codex round-1 finding 的 `[critical]` accepted 项,独立排入。

**codex adversarial-review 决议链**:

- round 1:`[high]` —— `content_digest`/`body_digest` 未绑定真实产物内容(可把合法 provenance
  粘贴到内容被替换的产物上)。修:消费端从产物自身持久化内容重算摘要,不信任声明的元数据
  (commit `b54e3c7`)。
- round 2:`[high]` —— provenance footer 非 EOF 锚定(footer 后可追加内容而不使 `body_digest`
  失效)+ `check_unlock_predicate` 对 audit/briefing 各自多次独立读盘(TOCTOU:两次读可能看到
  两个不同的合法版本,组合出对任何单一磁盘快照都不成立的 PASS)。修:footer 改 EOF 锚定 +
  谓词判定内单次读取快照贯穿全部子检查 + 生产端原子写(temp file + `os.replace`)作纵深防御
  (commit `d31cc3c`)。
- round 3/round 4:`[high]` —— journal 记录匹配只按 `audit_path`/`briefing_path` **字符串**,
  不要求匹配记录出自"当前磁盘上这份具体内容"。`run_reconstruct` 的 `ts` 可由调用方显式传入
  (非强制取当前时刻),配 `os.replace` 原子覆盖同名产物(round-2 收口的既有设计,且同 slug/ts
  覆盖重生成本身是 `audit_channel.generate_audit_channel_postgate` 的既有先例)——无需任何
  伪造:run A 真实自证 PASS 入 journal 后,run B(同样真实、同样 `REAL_FULL_CHAIN`、只是内容
  不同)对同一路径再跑一次,`verify_run_provenance_binding` 只验 run B 自身内容自洽(PASS),
  随后按路径命中 run A 的旧 journal 记录 → run B 从未被独立核验却被静默解锁。
  round-4 codex 认可 finding #1 之外的另一发现(`selfcheck_sequence.py` 相关)确属越 file_domain
  的架构问题,但**质疑**了本会话此前把 finding #1(journal 路径匹配)归为"KG-B10 冻结、task 内
  不可修"的判断——codex round-4 提出一个更窄的修法:利用已有的 `audit_digest` passthrough
  字段(`A1SelfCheckRecord.audit_digest`,已在 `journal/schema.py` 声明为"纯留痕"),不改
  `journal/schema.py` 本身,仅在 `rerun_selfcheck.py`(本 task file_domain 内)消费侧用它做
  版本核对。

**未采纳 round-4 该项建议的原因(供 IDS 判断是否重新授权)**:`journal/schema.py`(不在
file_domain 内)对 `audit_digest` 字段的语义有**三处独立文档**同时锁死为"纯留痕、不进
`verify_selfcheck_journal` 的绑定判定"(字段注释 + `tests/test_journal_schema_forge_v3.py`
文件头"牵动即停铁律" + 该文件一条显式断言"透传不改判定逻辑")。把该字段接进解锁判定,即便
只改 `rerun_selfcheck.py` 一侧的消费逻辑,实质是在本 task 内**解冻**一个被两轮独立文档
(forge v3 D″ + 更早的 B′/D′ review)明确、书面锁定要冻结、且已升级 IDS 的架构面(KG-B10)——
这正是 CLAUDE.md「重大架构转向必须先 forge,不静默停」要拦的行为。开发过程中曾开始按 round-4
建议实装,权限系统正确拦下了这处改动(标记为"在未获新授权的情况下逆转刚记录的治理决定"),
已撤销该处改动。

**operator 裁决(round-4 触顶后)**:复核 round-3/4 该项 finding 的性质后判定其为"架构边界归属
分歧"(该在本 task 内narrow-fix,还是必须等 KG-B10 经 forge 解冻)而非"已验证的活漏洞"(rounds
1-2 已堵住的才是),裁定接受当前状态 ship,该项转 IDS 决议(见 §3),不再追加 codex 轮次。

## §3 Suggested IDS actions

**待 IDS 决议的项(需要 chair 裁决,不是本 hand-back 自行拍板)**:

1. **KG-B10 journal 路径匹配的具体触发路径(dogfood XD-46)**——codex round-3/4 独立发现的
   "合法第二次真实 rerun 覆盖同 slug/ts,导致 journal 旧 PASS 静默解锁未核验新内容"这一具体、
   可复现子场景,是否要作为 KG-B10 forge 议程的输入(而非停留在此前"理论上攻击者可能……"的
   抽象层面)?若 IDS 判定这属于低门槛、值得优先堵的触发路径,建议 KG-B10 落地时优先覆盖。
   round-4 codex 提出的"利用已有 `audit_digest` passthrough 字段做窄修法"是否可行,也建议
   连带评估(本会话认为该字段的三处冻结声明构成足够强的"不得动"信号,但这是本会话的判断,
   最终解释权在 IDS/governance)。

2. **file_domain 冻结前"零回归"论证方法论(dogfood XD-45)**——task-decomposer 拆 `file_domain`
   时,对"消费点收紧"(如本 task 的 allow-list,影响**所有**历史构造过该产物形状的既有测试)与
   "生产点收紧"(如拒绝注入,只影响**直接调用且传特定参数组合**的既有测试)应否用不同强度的
   核实要求?建议归 forge 议程,可能影响后续 task 拆分的默认核实清单。

**已完成、无需 IDS 决议的项(informational)**:round-1/round-2 的 2 条 `[high]` 安全修复已落地
并 mutation 验证;12 个 file_domain 外既有测试的 companion 修复已同批完成,全量回归零失败;
两条 dogfood 记录(XD-45/XD-46)已入库。
