---
discussion_id: "001"
prd_fork_id: 001-radar-pA
handback_id: 001-radar-pA-20260717T052416Z
from_build_repo: /home/ys/codes/XenoDev
to_source_repo: /home/ys/codes/ideababy_stroller
current_idea: 001-radar-pA
worktree: .claude/worktrees/001-radar-pA
baseline: 9cb881f70508488f406a494ac57e87db08d725ba
workspace:
  source_repo: /home/ys/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev
  working_repo: /home/ys/codes/ideababy_stroller
  handback_target: /home/ys/codes/ideababy_stroller/discussion/001/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "bbf1b1889cd7dcb6"
tags:
  - prd-revision-trigger
severity: high
created: 2026-07-17T05:24:16Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: T004-A4
---

# Hand-back · 001-radar-pA · D′ 真 rerun 结果 → 触发 forge v3(gate 形态重裁 · 第四次「推演≠真跑」)

**Severity**: high
**Tags**: prd-revision-trigger
**Created**: 2026-07-17T05:24:16Z
**Baseline**: main `9cb881f`

> **一句话**:D′ 真 rerun 跑通了(`deepseek-v4-flash` · exit 0)。**a1 机制内存层验证生效**;但真跑暴露一个结构盲区 —— **真跑 pipeline 无法端到端验证 a1**,因为 a1 产物只在被 operator 否掉的 gate 形态后面可见。这是 001 主线**第四次「forge/推演 verdict ≠ 真跑可行」**。**触发 forge v3 重裁 gate 形态** —— T010/T011 解锁、KG-B10、SHARED-CONTRACT 修订全 gated 在它后面。

## §1 做了什么(D′ 真 rerun 首跑)

**真跑命令**(agent 自跑 · C8 合规 · 见 §4):
```
RADAR_LLM_MODEL=deepseek-v4-flash PYTHONPATH=src python -m radar.cli.reconstruct \
  --direction "diffusion models" --slices 3 --from-date 2021-01-01 --to-date 2023-12-31
```
真 OpenAlex 检索 + 真 DeepSeek `deepseek-v4-flash` 提炼 · exit 0 · 产物落 `out/`(gitignored):
盲测叙事 `out/reconstruct/diffusion-models-20260717T051551Z.md` + answer-key(密封 · agent 未读)+ 签字模板。

## §2 结果:a1 机制 solid,但真跑无法验证它(核心发现)

**a1 内存层验证 = 生效**(不烧 key · slicer 直验):6 篇带 `source_ref` 的 Paper → `slice_timeline` →
全 6 条 source_refs **真挂到 TimeSlice**(slice-1:3 · slice-2:3 · 无丢失)· `model_copy` 精化面往返**保留**。
**结论:a1 机制正确 · forge 不必再审 a1 对错。**

**⚠ 但真跑 pipeline 无任何可见落盘产物能验 a1**(结构盲区):
- CLI 只落**盲测面** `render_blind_markdown` · 按 operator 决策**故意不渲染 source_refs**(保识伪难度)。
- **审计面** `render_markdown`(渲染 refs 的那面)· 真跑**不落盘**。
- **audit.json**(机器可读 source 映射)· `generate_audit_channel_postgate` 在 **gate PASS 后**才产(现无 PASS)。
→ 三条能看到 a1 产物的路径,真跑**一条都没走**。a1 挂 refs 是内存里的事,真跑落盘看不到。

**「跑 rerun 得 PASS/FAIL」这个二值预设本身也被真跑推翻** —— PASS 需盲测签字 gate 判定,而该 gate 形态
operator 2026-07-15 已当面否 + selfcheck/独立核验无 CLI 入口 → 真跑**走不到判定**,给出第三种结果:
「gate 形态不定,连验证 a1 都做不到」。**= 001 主线第四次「推演≠真跑」**(前三:首跑 FAIL / operator 拒
gate 形态 / KG-B9 循环依赖)。

## §3 建议 IDS 处置(forge v3 · gate 形态重裁 = 关键路径)

1. **起 forge v3 重裁 gate 形态** —— 这是当前唯一关键路径。真跑证据现已齐(前三次都缺"真跑过"证据 ·
   本次补上)。forge v3 该裁:
   - **gate 存废/形态**:盲测二值签字(operator 已否)vs 留痕可审计验收 vs 降为非阻断 smoke signal vs
     信任移 O7 后验(承接 2026-07-15 二次 STOP 的 §3 四方向 + moderator injection 77a5176)。
   - **⚠ 新增维度(本次真跑暴露)**:「a1 产物(per-slice source 映射)何时/如何对 operator/审计可见」。
     现设计 = post-gate 才产;但若 gate 去二值化改留痕可审计,**a1 产物正是留痕的底料,可能反该始终
     可见** —— 非 post-gate 才产。gate 形态一变,a1 产物可见性时机跟着变。
2. **KG-B10 / SHARED-CONTRACT 修订 · 继续暂缓(等 gate 形态定)**:KG-B10 是「post-gate 产物绑 gate PASS」
   协议;但本次真跑证明 **gate 形态本身要变 → "post-gate" 这个时机前提会塌** → 现在设计 KG-B10 = 在要
   被推翻的前提上盖楼 = **KG-B7 过度投资**。IDS 上次已判"等 spine 裁决" · 本次真跑给此判断硬证据:
   **KG-B10 的设计前提取决于 gate 形态,必须 gate 形态先定。**
3. **T010/T011 解锁 · 仍不得起**:gate 未 PASS(且 gate 形态待重裁)· HANDOFF §4-OQ1 铁约束防 V4。
   **项目内 gate/pipeline 代码保留不动**(不静默改)。

## §4 附:model 名 + C8 自动化(两个可复用发现)

- **model 名教训**:agent 依赖的 DeepSeek 公开文档(`api-docs.deepseek.com`)**过时** —— 只列
  `deepseek-chat`/`deepseek-reasoner`,但账号 `/models` 端点真实返回含 `deepseek-v4-flash`/`deepseek-v4-pro`。
  **operator 是对的**。教训:model 名以账号 `/models` 端点为真相源,非公开文档。
- **C8 自动化突破(可复用)**:agent 现能**自跑烧 key 的 reconstruct** 而不泄露 —— 子进程内
  `eval "$(grep -E 'export DEEPSEEK_API_KEY' ~/.bashrc | head -1)"` 把 key **eval 进子进程内存**(值不进
  stdout/context)· 与上次泄露(2026-07-11 把含 key 行**当输出打印**)的本质区别 = 「当代码执行」vs
  「当文本打印」。C8 满足。**⚠ 该 key 仍建议轮换**(有前科)。

## §5 未决 / 状态

1. **未 push**:worktree-001-radar-pA 领先 main(含本次 followup commit `7d0a64c`)· push 需 operator 确认。
2. **签字模板未污染**:rerun 覆盖了 `verification/P0.2-…-diffusion-models.md`(新 run_id + 清空历史判定)·
   已 `git checkout` 恢复(保留 run-877f1c1e 历史 FAIL 判定审计价值 + 它是被否 gate 形态产物)。
3. ⚠ **DEEPSEEK key 轮换**(008/001 同 key · 有泄露前科)。

## §6 provenance

- **真跑产物**:`out/reconstruct/diffusion-models-20260717T051551Z.md`(盲测面 · gitignored)+ answer-key(密封)。
- **followup 全档**:XenoDev `specs/001-radar-pA/followup-log.md` **FU-D-rerun-a1-verify-blindspot**(commit `7d0a64c`)。
- **教训档**:第四次「推演≠真跑」· 承接三次 STOP(首跑 FAIL 2026-07-11 / operator 拒 gate 2026-07-15 /
  KG-B9 循环依赖 2026-07-16)· 见 XenoDev `DECISIONS-forge-v2-landing.md` + dogfood-backlog KG-B8/B9/B10。
- **上一包关联**:`001-radar-pA-20260717T032424Z`(DAG 结构改收口 · #1 BLOCKING DONE)· 本包是其后续真跑证据。
