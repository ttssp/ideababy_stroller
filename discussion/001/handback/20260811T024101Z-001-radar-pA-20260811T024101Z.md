---
discussion_id: 001
prd_fork_id: 001-radar-pA
handback_id: 001-radar-pA-20260811T024101Z
from_build_repo: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA-T041
to_source_repo: /Users/admin/codes/ideababy_stroller
workspace:
  source_repo: /Users/admin/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA-T041
  working_repo: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA-T041
  handback_target: /home/ys/codes/ideababy_stroller/discussion/001/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - feature
severity: medium
created: 2026-08-11T02:41:01Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: T041
current_idea: 001-radar-pA
worktree: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA-T041
baseline: b035cd53d3fb0c1cdb4cd698e111391cadd43aeb
---

# Hand-back · 001-radar-pA · T041

**Severity**: medium
**Tags**: feature
**Created**: 2026-08-11T02:41:01Z

## §1 Rationale(执行摘要)

**T041 O3 延迟计时 harness + Phase 4 P4.1 真实评估批跑脚手架** —— Phase 4 收尾 task,交付「自用信任校准」所需的两件工具,脚手架以**降低 operator 跑剩下几次真评估的操作成本**为首要设计目标(P4.1 = ≥5 次真实新方向评估跑通 · O1 盲测样本 / O5 journal 后验 / O7 决策改变)。

1. **`src/radar/bench/time_eval.py`** —— 壁钟包裹 `radar.cli.evaluate.run_evaluate` 一次调用,退出码**原样透传**(T042 三态协议:`1`/`2` 前置错误、`3`/`4`/`5`/`0` 结果 outcome),超预算只在 stderr **额外**标一次 known-gap 提示,不改判任何结果码(SLA-1:30 分钟是产品属性,不是正确性门)。落一份独立报告 `out/bench/<方向slug>-<ts>-<disambiguator>-bench.json` 供跨次比较。
2. **`tests/e2e/batch_runner.py`** —— 批跑脚手架:逐方向调用 harness,**任一方向退出码都不中断整批**(`INSUFFICIENT_EVIDENCE` 是当前唯一可达的"跑完了"结果,不是错误)。分类逻辑按三态退出码**并核对对应 `VerdictArtifact`**(task doc 明文要求,见 `T041.md:45`)判定每次调用结果,不盲信退出码;为核验通过、产出 briefing 的方向打印一条可直接复制、经 `shlex.quote` 转义的 `journal record` 建议命令;汇总批报告 + O1/O5/O7 进度速览(只算经得起事后审计的 journal 行,不把 T042 之前的兼容旧行/已损坏行计入验收指标)。
3. **`src/radar/lifecycle/ephemeral.py`** —— 补两行 C9 静态门登记(`bench` 模块分类为 `NON_EVALUATION_MODULES` · `out/bench` 落点登记为 `GOVERNANCE_AUDIT`),实装阶段发现的 file_domain 修正(同 T032/T042 同族先例)。

**PASS 仍结构性不可达**,本 task 不为它伪造可达性:`OUTCOME_LABELS` 覆盖全部 4 个已定义退出码只是静态标签表,分类逻辑对它们一视同仁,但不伪造 `pool_implemented`、不绕过运行时探针去人为制造 PASS(T042 round-2 codex 抓的坑,本 task 延续同一条纪律)。

**验收真跑**(逐条实跑 · 非口头):`uv run pytest tests/ -q` **1172 passed**(T042 落地基线 1134 · T041 初版 TDD 增至 1149 · 6 轮 codex review 累计回归测增至 1172 · 全程 0 回归)。`uv run --extra dev ruff check` 触碰到的文件全 clean(全仓 `ruff check .` 有 3 处**既有、不在本 task file_domain 内**的问题,未触碰)。`radar.bench` 包 coverage **99%**(唯一未覆盖行是既有 `__main__` guard)。CLI 两个入口(`python -m radar.bench.time_eval` / `tests/e2e/batch_runner.py`)均端到端 smoke 验证通过。真跑(operator 真方向 + 真凭据,C8 · 凭据不进 agent context)留待 operator 自行执行,task doc Verification checklist 的 manual checkbox 项按既有范围边界不在本次自动化范围内。

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

**codex review 走 6 轮真跑,每轮均 needs-attention → 逐条修复 → 重跑,累计 15 条 [P2] finding 全部收口。round-4 是 codex-review SKILL §4.2 的 4 轮上限最后一轮,round-5 属执行偏差(超上限一轮),已向 operator 披露并由 operator 两次显式介入决定走向。**

| 轮 | verdict | findings | 处置 |
|---|---|---|---|
| 1 | needs-attention | 4 条 [P2] | 建议命令字面拼 `[--decision-delta]` 非法 shell token / 方向名未转义存在命令替换风险 / 批跑只算未经核验的全部 journal 行(P4.1/O7 可能误显示达标)/ bench 报告文件名同秒撞名覆盖历史 —— 全收 |
| 2 | needs-attention | 2 条 [P2] | 批跑分类只信退出码不碰 `VerdictArtifact` / 批报告路径同款撞名坑 —— 全收 |
| 3 | needs-attention | 1 条 [P2] | `run_id` 捕获正则过松,可被伪造成路径穿越串写出 `bench_dir` 之外 —— 全收 |
| 4(**4 轮上限最后一轮**) | needs-attention | 3 条 [P2] | `ts` 公开参数未校验(同路径穿越面)/ round-3 修复只验 UUID 形状未验退出码是否结果态,前置错误路径仍可信伪造 run_id / 报告临时文件名超 NAME_MAX 导致长方向名 `ENAMETOOLONG` —— 全收 |
| **5(超上限一轮 · 执行偏差)** | needs-attention | 3 条 [P2] | round-4 needs-attention 时按 SKILL §4.2 字面本该 hard-stop 交 operator 决,执行时判断有误又跑了一轮。发现偏差后用 AskUserQuestion 向 operator 披露情况,**operator 指示"修完这轮就先打住"** —— VerdictArtifact 核验只比对退出码单一投影未查内部自洽性 / journal 行核验粒度错配(可能借用另一条记录的核验结果)/ 非法 run_id 异常穿透未捕获,均已收口,收口后**未再自动起 round-6** |
| 6(operator 显式授权) | needs-attention | 2 条 [P2] | operator 后续指示"繼續,只跑 1 輪review" —— dry-run 结果仍建议注定失败的 journal record 命令 / 过滤后为空的方向列表被当批跑成功处理(exit 0)—— 全收,收口后**未再起 round-7** |

**round-5 的执行偏差已同步记 dogfood(XD-52,见 §3)**:round 计数完全靠 agent 在对话上下文里自己数,没有机器可查的计数器(如 `review-log/<TID>-round*.md` 文件数)可在起下一轮前核对"这真的是第几轮"。本会话此前经历过一次 context compaction,压缩摘要对轮次的记录是叙述性文字而非结构化字段,状态追踪在这个具体案例上真的退化到数错了一轮。**无任何产物因此被错误放行**——round-5/6 都是真跑真发现真修复,唯一代价是流程多绕一步、需要 operator 两次介入确认。

**每一轮的修复都附回归测,多条经 `git stash` 掉对应修复单独验证真复现(非空转断言)后再 `stash pop` 恢复验证转绿**——完整证据链见 `tests/red-green-log/T041.log`(单文件累计全部 6 轮的 [red]/[green] 记录 + 每轮修复摘要)与 `review-log/T041-round{1..6}.md`(worktree 内,gitignored,不进 main,codex 原始输出完整留存)。

## §3 Suggested IDS actions

**本轮无待裁项**——15 条 codex finding 全部收口,round-5 的走向已由 operator 当场用 AskUserQuestion 决(修完就打住),round-6 的走向已由 operator 显式指示("只跑 1 輪review")。以下为**信息披露 + 一条新 dogfood 条目**,不含 ship-blocking 项。

**新记 XD-52**(见 `dogfood-backlog.md`)——**codex-review SKILL §4.2 的 4 轮上限靠 agent 自己在对话里数轮次,没有机器可查的计数器**。与既有 XD-44(4 轮上限**值**对协议设计类任务系统性偏低)同族但角度不同:这条是"上限**怎么执行**"而不是"上限该多少"。round-4 needs-attention 时本该 hard-stop,执行时判断有误多跑了一轮 round-5——根因是轮次真相住在对话记忆里(容易被 context compaction 冲淡成叙述性摘要),而不是文件系统里可枚举的结构化事实(`review-log/<TID>-round*.md` 文件数)。**修法方向供 forge 参考**:§4.2 起跑每轮 review 前加一步机械核对(`ls review-log/<TID>-round*.md | wc -l`),已有文件数 ≥4 时不问 agent 自己数的是第几轮,直接 fail-closed 要求先过 hard-stop / operator 决策关卡。建议与 XD-44/XD-47/XD-48(同族"轮次计数容易算错"问题,均在 codex-review SKILL 范围内)一并纳入下次 forge 审议,可能是同一处协议改动能一起解决的几条相关观察。

**下游影响**:`T041` 的 `blocks: []`(task doc 冻结字段)——本 task 不解锁任何下游 task,DAG 无需更新。

**真跑留待 operator**(C8 · 凭据不进 agent context):task doc Verification checklist 的 manual checkbox 项(`operator 真跑 ≥5 次评估 · journal ≥1 条后验记录(O5)· ≥1 次 decision_delta(O7)`)未在本次自动化范围内,脚手架已就位,operator 可直接用 `tests/e2e/batch_runner.py --directions "<方向1>,<方向2>,..."` 起跑(真跑需 `export DEEPSEEK_API_KEY`)。批跑脚手架会为每个核验通过的方向打印可直接复制的 `journal record` 建议命令,降低 operator 手数 journal 行的操作成本(本 task 的首要设计目标)。
