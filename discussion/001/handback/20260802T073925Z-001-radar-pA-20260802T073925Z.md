---
discussion_id: 001
prd_fork_id: 001-radar-pA
handback_id: 001-radar-pA-20260802T073925Z
from_build_repo: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA
to_source_repo: /home/ys/codes/ideababy_stroller
workspace:
  source_repo: /home/ys/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA
  working_repo: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA
  handback_target: /home/ys/codes/ideababy_stroller/discussion/001/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - spec-gap
  - practice-stats
severity: medium
created: 2026-08-02T07:39:25Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: O7-first-real-evaluate
---

# Hand-back · 001-radar-pA · O7-first-real-evaluate

**Severity**: medium
**Tags**: spec-gap,practice-stats
**Created**: 2026-08-02T07:39:25Z

## §1 Rationale(执行摘要)

**O7 时钟第 1 次真 evaluate 跑通** —— IDS 第 35 条 (D) + 第 36 条第 4 项授权。

- **O7 计数:0/5 → 1/5**;**`decision_delta=true` 首次即达成**(spec §1-O7 的「≥1 次实际改变
  operator 投入决策」**这一半已满足**,剩下的只是次数)。
- **⏰ 时钟起算日 = `2026-08-02`** —— spec §1-O7 只写「3 个月内 ≥5 次」,**从未定义从哪天起算**。
  本次真跑是事实起点。**请 IDS 在 forge 001 上把这个日期写进 spec**,否则「3 个月」永远无法判定。
  ⇒ 截止日 = **2026-11-02**。
- **报回窗口决策点**(第 36 条要求「烧 key 前先报」):**窗口本身不冲突**,但查出**另一个更实的
  冲突**(§3.2)—— operator 已裁 **(a)**,本次不跑 B′+D′ 自证,当 spec 缺口回流。
- **本包内嵌真跑产物全文**(§6)—— 两个产物都被 `.gitignore:83 out/` 吃掉,不嵌就是
  **R1-F5(T022 真跑无可复核物证)原样复发**。

## §2 真跑实况

### 2.1 数字

| 项 | 值 |
|---|---|
| 命令 | `python -m radar.cli.evaluate --direction "coding LLMs"`(**不加** `--dry-run` · 不传窗参数) |
| 窗口 | 缺省 = 近期窗(today − 24 月 · `src/radar/retrieval/openalex.py:43`) |
| 退出码 | **0** |
| 检索语料 | **200 篇**(真 OpenAlex) |
| 全链壁钟 | **41.8 秒**(未触 O3 预算告警) |
| 判断 | **1 条**(⚠ 见 §2.3) |
| 产物 | `out/briefing/coding-llms-20260802T022608Z.md` · 6064 B · sha256 `2d23fae27915eed33aa73b520a9e2c88e4886bb8e3c52e649061893abe102726` |
| journal | `out/journal/evaluations.jsonl` · 1 条 · 7060 B · sha256 `f7cd5b968dd6884148710c91e5d68cbcf652ae5ceb746030caeae81f481895ed` |

**operator 决策**:方向 `coding LLMs` · 结论 **观望** · 日期 `2026-08-02` · **`decision_delta=true`**。
`journal list` 读回验证 PASS(= spec §7-P2 裁「读法 B」后的验收读回,同时实证整条链路)。

### 2.2 第一次尝试被环境阻断(如实记 · 非代码缺陷)

首跑在**第 2 环**炸:

```
File .../radar/llm/deepseek_client.py, line 55, in _http_post
RuntimeError: DeepSeek API 不可达(网络/DNS/TLS 错):
  [SSL: UNEXPECTED_EOF_WHILE_READING] EOF occurred in violation of protocol (_ssl.c:1006)
=== EXIT: 1 ===
```

**根因查实**(非猜测):operator 机器的 mihomo 规则表里有
`index 11 · DomainSuffix deepseek.com → DIRECT`(且重复写了两遍,index 11/17),
把 DeepSeek 单独踢出代理走直连,而直连路径上 `api.deepseek.com` 被 **SNI 阻断**
(`curl -v` 证:CONNECT 隧道 200 → 发出 `Client hello` → 无响应)。同一代理走 GitHub/OpenAlex 均 200
(命中兜底 `index 18 · Match → Proxy`)。operator 改规则后 `401`(= 连通,仅无认证)→ 重跑成功。

**三件如实**:① **key 未烧**(TLS 握手就断,请求未送达);② **零产物落盘**
(fail-closed 正确工作 —— 这正是 R1-F1 那条修法要的行为);③ **不记 dogfood**
(环境问题,非框架缺陷)。

⚠ 但它是**第二次**「代码全绿 ship,第一次真跑就炸在第一个真 provider 环」——
上次(T022)是代码(JSON 被 markdown 围栏包住 + 随机截断),这次是网络。成因不同,
但**共同印证已在册的 KG-30**(ship 门不含真 provider 冒烟)。不重复记条目。

### 2.3 ⚠ 「非豁免判断 1 条 < 下界 3」—— **第二次独立复现,坐实是结构缺陷**

产物 §抽样计数 自己写明:非豁免 **1** 条(下界 3)· 豁免 **0** 条 ⇒
「本份 briefing **不得据以判 O2 / §7-P2 PASS**」。

**这不是新发现,但这次的证据强度不一样**:

| | T022 那次(2026-07-23) | 本次(2026-08-02) |
|---|---|---|
| 方向 | 另一方向 | `coding LLMs` |
| 语料 | —— | **200 篇**(不稀疏) |
| 非豁免判断 | 1 | **1** |
| 可用锚 | 44 个 | 有(证据链 1 击命中) |

T022 那次已查清成因 = **T020 的 judge prompt 里没有「至少产 N 条判断」的下界**,
LLM 原始就只返 1 条。**本次用完全不同的方向 + 200 篇语料复现同一结果 ⇒ 排除
「方向稀疏」与「偶然」两种解释,坐实是 prompt 的结构性缺陷。**

⇒ **与 009 KG-43 同族(判据可达性先于判据冻结)**:§7-P2 的 PASS 判据在 T020 补上判断数下界之前
**结构性不可达**。归 T020 域 · 待 IDS/operator 决 · 本包不扩范围。

**对 O7 的影响:无。** O7 数的是 operator 的真实决策,不依赖证据链抽样判据。
本次 briefing 在 **O5/O7 计数用途有效**,在 **P2 用途无效**(产物自己已声明)。

## §3 报回:窗口决策点的结论

> IDS 第 36 条要求「先核实 selfcheck 缺省窗现状,把冲突(或不冲突)的结论报回,**再烧 key**」。
> 已照办 —— 下面的结论在烧 key **之前**产出。

### 3.1 结论 A:窗口本身**不冲突**(可以放心用近期窗)

`scripts/selfcheck-07-17.sh` 的 `2021-01-01→2023-12-31` **不是语义要求**,是位置参数 `$3/$4` 的
**向后兼容缺省值**。脚本头部原话:forge v4 参数化 ——「旧硬编码 07-17 值**降为默认**」
(`scripts/selfcheck-07-17.sh:5-8` + `:30-38`)。

窗在自证里的真实作用:传给 `OpenAlexRetriever` 两次(序列内确定性重生成 audit.json +
独立派生 corpus_refs)。⇒ 约束是「**与被自证的那个 run 同窗**」,**不是**「必须 2021→2023」。
O7 用近期窗完全合法。

脚本自己也写着:用**默认参数**跑 = 复现 STOP 证据(07-17 是校准产物,①b 正确 fail-closed);
用**新评估 run 的 direction/ts** 跑 = 真生效自证(`scripts/selfcheck-07-17.sh:20-24`)。

### 3.2 结论 B:⚠ **真正的冲突在产物族 —— 这是 spec 缺口,不是实现问题**

**`radar.cli.evaluate` 产不出 B′+D′ 自证能消费的 evidence bundle。**

| | `radar.cli.evaluate`(O7 跑的) | `radar.cli.reconstruct`(自证消费的) |
|---|---|---|
| 产物 | `out/briefing/<slug>-<ts>.md` | `out/reconstruct/<slug>-<ts>.md` **+** `out/credibility/<slug>-<ts>.audit.json` |
| 渲染器 | T022 `MarkdownBriefingRenderer`(判断 + Tech Radar 环) | `reconstruct/writer.render_markdown`(per-slice + 来源块) |

**三条机器可核的硬证据**:

1. `unlock_preflight` 要 `--audit <audit.json>`,而 `run_evaluate`
   (`src/radar/cli/evaluate.py:145-165`)**只写 briefing,不产 audit.json**。
2. `scripts/selfcheck-07-17.sh:63` 的 briefing 缺省派生就是 `out/reconstruct/{slug}-{ts}.md`。
3. `src/radar/credibility/selfcheck_sequence.py:174` 的 `_require_eval_briefing_shape` 是
   **正向 allowlist**:按 `^###\s+(\S+)\s+·`(`src/radar/credibility/unlock_preflight.py:34`)分段 ·
   **逐段**要求块级 `**来源(source refs...)**:`。T022 渲染的是 `### {i} · {ring}`
   (`src/radar/briefing/renderer.py:272`)—— heading 正则**会匹配**(slice_id 被解析成判断序号 `1`),
   但**逐段来源块必然缺** ⇒ **fail-closed STOP**;即便绕过,B′ coverage 也会拿判断序号去对
   audit.json 的真 slice_id ⇒ 再 STOP。

⇒ **「同一验收事件内以同 stem evidence bundle 跑 B′+D′ 自证」用 evaluate 产物无法满足**;
要满足必须**另跑一次 `radar.cli.reconstruct`**(同方向同窗、**另一个 ts ⇒ 另一个 stem**、**再烧一次 key**)。

**根因是词汇撞车**:forge v3/v4 语境里的「**评估 run**」= `radar.cli.reconstruct --calibration=False`
(a1 解锁链那一族),**不是** `radar.cli.evaluate`。两个名字撞了,底下是两族产物。

**operator 裁定(2026-08-02)= (a)**:本次**只跑 evaluate + journal record**,**自证不跑**,
把这条当 spec 缺口回流。理由:O7 数的是 operator 真实决策,不依赖 a1 解锁链;
硬把 evaluate 产物塞进自证会撞上一道**本来就该拒它的门**,那就是**第五次「冻结不可达判据」**。

⇒ **这是语义问题不是实现问题,按 OQ-A1-1 纪律不在 XenoDev 当场定。**

## §4 Suggested IDS actions

> ⚠ **XD-37 自我标注**:本节是全包唯一会让 IDS 花钱的部分,却**不在任何评审的视野里**
> (本 session 零 codex 评审 —— 见 §5.3)。每条带 `file:line`,请逐行复核。

### 4.1 归 forge 001(spec amendment · 建议与既定那批一起落)

1. **把 O7 时钟起算日写进 spec** —— 现状 `specs/001-radar-pA/spec.md:94` 只写「3 个月内完成 **≥5 次**」,
   **没有起点定义**,时钟在本次真跑前根本没启动。建议措辞:「起算日 = 首条真实评估 journal 行的 `date`」
   ⇒ 本次 = **2026-08-02**,截止 **2026-11-02**。
   ⚠ 不定义起点 = O7 **永远无法判定通过或失败**,与「判据可达性」同族。
2. **裁清「per-run 自证义务」的定义域**(§3.2)。两个可能读法,IDS 二选一:
   - **(i)** 自证义务只覆盖 `reconstruct` 评估 run(a1 解锁链)⇒ O7 的 `evaluate` run **无自证义务**,
     现状即正确,只需在 spec 里把两个「评估 run」**改名消歧**;
   - **(ii)** 自证义务也覆盖 `evaluate` run ⇒ 必须给 `evaluate` 补 audit.json 产出 +
     让 `_require_eval_briefing_shape`(`src/radar/credibility/selfcheck_sequence.py:174`)认第二种产物形状
     = **改 C″ 分流契约**,工作量不小且触 KG-B10 面。
   我的建议是 **(i)** —— 但这是 IDS 的裁定,本包不预设。
3. **T020 judge 补判断数下界**(§2.3)—— 在此之前 §7-P2 的 PASS **结构性不可达**。
   相关落点:`src/radar/judge/judge.py`(prompt 构造)· 产物侧计数与警示已就位
   (`src/radar/briefing/renderer.py:244-253`)。
   ⚠ 本条与 IDS 已在册的 T022 结论同源,**本包提供第二次独立复现证据**(不同方向 + 200 篇语料)。

### 4.2 归 `/expert-forge 006`(框架件)

4. **真跑产物被 `.gitignore` 吃掉 = R1-F5 的复发条件仍在**(`.gitignore:83`)。
   本包用「全文内嵌 hand-back」这个办法**单次**兜住(§6),但那是人工纪律不是机器门。
   ⇒ 与已在册的 **KG-31/32(PPV 产物随 ship 自毁)** 并案:建议 ship 门要求
   「烧过 key 的产物必须有**仓外持久落点** + sha256 绑定」,否则烧钱买来的证据随 worktree 蒸发。
   ⚠ 本条**不新开 backlog 条目**(KG-31/32 已覆盖),只请 forge 并案时把本次实例算进证据。

### 4.3 本轮**不**要求 IDS 做的(免误排期)

- **不要求现在裁 O7 节奏** —— 已定「每周 1 个新方向」,**禁止批量补跑**(那会把 O7 从产品成功
  信号降成刷计数)。按此节奏 5 次约需 4 周,远早于 11-02 截止。
- **不碰 spec / 不起 T040 T041 / 不改 parallel-builder SKILL / 不修 XD-42 错引用** —— 维持既定处置。

## §5 已知缺口(如实记 · 非 blocking)

### 5.1 ⚠ R5 四条修法未经 codex 复核 —— 两条正落在本次产物的可信度面上

`df5ce24` 的四条修法**只有本地 pytest + mutation 背书**(详见前一包
`001-radar-pA-20260802T012127Z` §4.1)。其中两条**直接决定本次 briefing 的可信度**:

| 修法 | 落在哪 | 若被第 6 轮推翻的后果 |
|---|---|---|
| **R5-F2** 真跑横幅绑定 `lines[2]` | `src/radar/journal/cli.py:181-187` | 本次 journal 行的「快照确为真跑产物」这一保证失效 |
| **R5-F4** URL canonicalizer 只折叠 scheme/host | `src/radar/credibility/gate.py:152-180` | 影响 `src/radar/graph/builder.py` 别名回退锚点 ⇒ **本次那条唯一证据锚可能指向语料里的另一篇文档** |

⇒ **本次 briefing 在 O5/O7 计数用途有效**(journal 行记的是 operator 的决策,不依赖证据链抽样判据);
**若后续起第 6 轮 codex 推翻 R5 修法,P2 用途需重判**。

已在 journal 的 `rationale` 里**逐字写进这条风险**,让三个月后的 O5 后验看得见当时的判断基础有多薄。

### 5.2 本次那条唯一证据锚**未经人工语义核验**

锚 = `https://doi.org/10.1038/s41586-025-09422-z`(1 击)。机器只保证「该 ref 真在本次检索的
200 篇语料里、锚真存在于本 run 的图/叙事、链 ≤3 击」;**所引原文是否真支撑那句判断,仍需 operator 人审**
—— 这是 spec §6 明写的边界,产物自己也声明了。**本包不冒称已核验。**

### 5.3 ⚠ 本 session **零 codex 评审**

本 session 无代码改动(两包均为 docs + 真跑),故未起 codex。但 §4 的四条建议与 §3.2 的
spec 缺口判断**都没有被任何第二方复核过** —— 按 XD-37 的教训如实标注。

### 5.4 B′+D′ 自证**未跑**(operator 裁 (a) · 不是漏做)

理由见 §3.2。⚠ 连带后果:本次 evaluate run **不产生任何 a1 解锁链证据**,
`out/journal/a1-selfcheck.jsonl` 未新增记录。T010/T011 的解锁状态**不受本次影响**
(它们早已由 `diffusion-models-20260718T231343Z` 那次 B′+D′ PASS 解锁 · HANDBACK-LOG 第 22 条)。

### 5.5 KG-33 第 9 次

`gen-handback.sh` 的 `to_source_repo` / `workspace.source_repo` 仍是 mac 字面量,已手改;
`--repo-root .` 致 `from_build_repo` / `build_repo` / `working_repo` 变字面量 `.`,一并手改。
6 约束 validator producer 模式对两者均不报错(无机器门)。

### 5.6 产物可读性:叙事被消毒转义弄乱

`§位移叙事` 里满是 `\*\*` 与 `\(u+201c\)` —— 这是 T021/T022 那两层安全消毒
(欺骗性码点剥离 + markdown 中和)的**正确行为但副作用**:内容完整,可读性明显受损。
**刻意不修**(修它等于放宽注入防御面)。若 operator 认为影响使用,应作为独立 task 走
「渲染时反转义 / 分离展示层」的正规改法,不在此当场动。

---

## §6 附:真跑产物全文(证据保全 · 因 `.gitignore:83` 不进 git)

### 6.1 briefing —— `out/briefing/coding-llms-20260802T022608Z.md`

sha256 `2d23fae27915eed33aa73b520a9e2c88e4886bb8e3c52e649061893abe102726` · 6064 B

````markdown
# 方向评估 briefing · coding LLMs

> 本产物由 `radar.cli.evaluate` **全链真跑**产出(真检索 → 真图 → 真切片 → 真判断 → 真证据链强制)· spec §7 PPV **P2 的终点产物**。
>
> ⚠ **本产物证明什么 / 不证明什么**:证据链的**结构 provenance**(锚真存在于本 run 的图/叙事、ref 真命中本次检索语料、链 ≤3 击)由机器强制;所引原文的**内容语义是否支撑该判断**仍需 operator 人审(spec §6 明写的边界 · 不冒充语义核验)。

## 本次评估

| 项 | 值 |
|---|---|
| 方向 | coding LLMs |
| 时间戳(UTC) | 20260802T022608Z |
| 检索语料 | 200 篇 |
| 全链壁钟 | 41.8 秒 |

## 位移叙事

基于您提供的时序位移叙事与图关系数据，我将聚焦于“coding LLMs”方向，精化出一段突出\*\*结构连接\*\*的位移叙事。---自2024年8月至2026年5月的切片序列中，该方向经历了从\*\*全周期自动化方法族\*\*向\*\*混合安全范式\*\*的结构位移。早期切片呈现以LLM为核心、覆盖软件工程全链条的横向扩张，而2026年2\(u+2013\)3月的切片则收窄至对抗攻击、测试反馈增强与可观测性适配的交汇点，标志着方法族从\(u+201c\)生成主导\(u+201d\)向\(u+201c\)攻防对抗主导\(u+201d\)的接合部迁移。至2026年5月，结构进一步转向以综述框架为锚点，将仿生防护机制与零样本分析并置，形成一种\(u+201c\)理论梳理+特定场景防护+泛化应用\(u+201d\)的三元张力形态。这一连串变化并非单纯的领域细分，而是研究范式从\(u+201c\)能力探索\(u+201d\)逐步过渡到\(u+201c\)安全审视\(u+201d\)与\(u+201c\)方法杂交\(u+201d\)的位移路径。在具体的结构连接层面，该位移路径由多条此前未点明的\*\*接合链条\*\*驱动：1.  \*\*从“执行反馈”到“推理强化”的纵向替代链\*\*：早期切片中，“Reinforcement learning with execution feedback for code LLMs \(RLEF\)”通过\`extends\`连接至“Reinforcement learning for reasoning in LLMs \(DeepSeek-R1\)”，随后该关系被\`supersedes\`反转为后者替代前者。这一链条揭示了该方向从\*\*代码专用奖励模型\*\*向\*\*通用推理策略\*\*的范式升维，成为后续所有方法迁移的底层逻辑。2.  \*\*“零样本分析”作为横向粘合枢纽\*\*：多个早期方法（如“On-the-fly code visualization \(WaitGPT\)”与“Contextualizing code LLMs \(Codellm-Devkit\)”）均通过\`combines\`关系汇聚至“Zero-shot code analysis with LLMs for syntax and semantics”。这一节点并非孤立存在，而是与后续“Zero-shot synthesis of compilable code \(ZS4C\)”形成\`combines\`链，使得零样本能力从\*\*静态分析\*\*延伸至\*\*动态合成\*\*，为2026年切片中的“零样本分析”锚点提供了结构先声。3.  \*\*“漏洞检测”作为安全转向的承重墙\*\*：早期切片中，“LLM-based vulnerability detection benchmarking”通过\`combines\`接入“LLM-based autonomous program improvement \(AutoCodeRover\)”，同时“Backdoor attack assessment for LLM-generated HDL code \(RTL-Breaker\)”通过\`extends\`挂接至该漏洞检测节点。这一\*\*三节点接合\*\*（漏洞检测 ↔ 自主改进 ↔ 后门评估）构成了从“生成验证”到“攻防对抗”的关键转轴，直接支撑了2026年切片中“对抗攻击”与“测试反馈增强”的交汇。4.  \*\*“提示工程”与“自动重构”的双向桥接\*\*：早期“Prompt engineering vs fine-tuning \(empirical assessment\)”通过\`combines\`关联“Legacy code modernization”，而“Comparative analysis of design pattern implementation validity”则通过\`extends\`强化“Code smell detection \(iSMELL\)”。这两条平行线在后续切片中并未消失，而是通过“安全审视”框架下“仿生防护机制”的引入，间接与“零样本分析”形成新的三元张力——即\*\*经验调优\*\*与\*\*结构性重构\*\*在安全约束下的重新并置。综上，该方向的位移并非线性细分，而是由“执行反馈→推理强化”的替代、“零样本分析→合成”的扩展、“漏洞检测→后门评估”的加固，以及“提示工程→重构”的桥接，四重\*\*结构接合\*\*共同编织而成。这些链条将早期“生成主导”的横向方法族，逐步焊接为晚期“攻防对抗主导”的纵向安全范式，最终在2026年5月的综述框架下，以“理论梳理+特定场景防护+泛化应用”的三元形态完成结构性收敛。

## 抽样计数(spec §1-O2 (c))

- **非豁免判断**:1 条(抽样下界:3)
- **豁免判断(免证据链)**:0 条

> ⚠ **非豁免判断 1 条 < 抽样下界 3** —— 按 spec §1-O2 (c),本份 briefing **不得据以判 O2 / §7-P2 PASS**。
>
> 计数不足**不推断成因**:judge 少产判断 / provenance 丢失 / 公开脉络确实稀疏都会命中同一格。「该判什么」的结果值语义待 spec **OQ-A1-1** 由 IDS 决议,本产物**不作结果分类**。

## 判断

### 1 · 观察 · ⚠ 低置信

coding LLMs方向已从全周期自动化方法族的横向扩张,经安全审视与攻防对抗主导的混合范式,收敛为以零样本分析为粘合枢纽、安全防护为承重墙的可复用接合结构;但2026年5月切片出现'综述框架+仿生防护+零样本'的三元松散并置,缺乏纵深接合链,结构从收敛转向分化,成型度处于退潮与补丁堆积的临界点

<details><summary>证据链(≤3 击到真原文)</summary>

- 锚 `e9` · 1 击 · https://doi.org/10.1038/s41586-025-09422-z

</details>

---

⚠ **P2 判据边界**:`evaluate --direction ... && ls out/briefing/*.md` 命令成功 **≠** spec §7-P2 PASS。

- 抽样下界见上方计数(§1-O2 (c):非豁免判断不足下界时不得判 PASS);
- spec §7 的 P2 终点还含 **journal 真行**(= T030,尚未到位)—— 本产物只兑现 briefing 那一半,**不宣称 P2 整体已绿**。
````

### 6.2 journal 行 —— `out/journal/evaluations.jsonl`

sha256 `f7cd5b968dd6884148710c91e5d68cbcf652ae5ceb746030caeae81f481895ed` · 7060 B · 1 条

| 字段 | 值 |
|---|---|
| `direction_name` | `coding LLMs` |
| `conclusion` | `观望` |
| `date` | `2026-08-02` |
| `decision_delta` | **`true`** |
| `briefing_snapshot` | 3251 字符 · = §6.1 全文(此处不重复) |
| `rationale` | 见下 |

**`rationale` 逐字**:

> 简报判该方向已从横向扩张收敛为「零样本分析为粘合枢纽、安全防护为承重墙」的接合结构,但
> 2026-05 切片出现「综述框架+仿生防护+零样本」三元松散并置、缺纵深接合链,结构从收敛转向分化,
> 处于退潮与补丁堆积的临界点 ⇒ 判定当前不是下注时点,先观望,再看几个月是否重新收敛。
> ⚠ 三个月后回看时须知「判断基础很薄」:本次非豁免判断仅 1 条(抽样下界 3),该唯一判断自标低置信;
> 其证据锚 doi.org/10.1038/s41586-025-09422-z 的语义支撑未经人工核验;且 R5-F4(URL 归一化)
> 那条修法未经 codex 复核,锚点归属正确性存疑。

`journal list` 读回输出:

```
评估 journal: out/journal/evaluations.jsonl · 1 条
日期           结论     decision_delta  方向
2026-08-02   观望     true            coding LLMs

其中 decision_delta=true: 1 条(O7 · 真改变投入决策的次数)
```
