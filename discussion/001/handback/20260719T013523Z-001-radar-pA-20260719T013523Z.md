---
discussion_id: 001
prd_fork_id: 001-radar-pA
handback_id: 001-radar-pA-20260719T013523Z
from_build_repo: /home/ys/codes/XenoDev
to_source_repo: /home/ys/codes/ideababy_stroller
baseline: 9cb881f70508488f406a494ac57e87db08d725ba
worktree: .claude/worktrees/001-radar-pA
workspace:
  source_repo: /home/ys/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev
  working_repo: /home/ys/codes/ideababy_stroller
  handback_target: /home/ys/codes/ideababy_stroller/discussion/001/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - feature
severity: medium
created: 2026-07-19T01:35:23Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: T011
---

# Hand-back · 001-radar-pA · T011

**Severity**: medium
**Tags**: feature
**Created**: 2026-07-19T01:35:23Z

## §1 Rationale(执行摘要)

T011 方法/claim 级图构建 ship:LlmGraphBuilder 实装 GraphBuilder Protocol · 论文集→MethodClaimGraph(节点=方法/claim 非论文级 C6 · 边=关系/继承/接合)· GRAPH_MAX_NODES=50 有界 OUT-5 · 节点诚实锚只锚真检索/采样 ref 反幻觉 · 悬挂边丢弃 · 复用 reconstruct 注入防御+采样 · C9 run-ephemeral · codex adversarial-review round-2 · 23 test cov94% · 零回归。

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

**tag: `feature`** —— T011 方法/claim 级图构建 ship(Phase 1 · spec §5 P1.2 · 差异化根 C6 · 交付里程碑)。

### 决议链(上游 → 本次 ship)

1. **前置**:forge v4 §c 条款生效 + T004-A2/A4 满足(worktree 分支基线)· T010 已 ship(同批)· T011 depends_on [T004-A2,T004-A4] 满足 → 起跑。集成基线 = worktree 分支 `worktree-001-radar-pA`(operator 定 · 非 main)。

2. **实现(spec §5 P1.2 + C6/OUT-5/C9/C8)**:`LlmGraphBuilder` 实装 GraphBuilder Protocol —— 论文集 → `MethodClaimGraph`(节点=方法/claim `kind∈{method,claim}` **非论文级** C6 防退化 · 边=方法族关系/继承/接合)。**复用 reconstruct 单一真相**:`_serialize_untrusted_field`(注入防御 · codex 9 轮硬化)+ `_sample_window`(endpoint-inclusive 采样)· 不重造。GRAPH_MAX_NODES=50 + GRAPH_MAX_EDGES=200 有界(OUT-5)· C9 run-ephemeral · C8 key 注入。

3. **codex adversarial-review 迭代(4 轮 · risk_level=medium + spine + 复用注入防御 → §2.1 adversarial)**:
   - **round-1** needs-attention 3 finding:#2 缺 nodes/edges 键 silent 空图 → 修(显式键校验 + 零节点 raise)· #3 `papers[:24]` 前缀截断偏老漏最新 → 修(复用 `_sample_window` endpoint-inclusive + 只锚采样 ref)· #1 边无关系级 source 锚。
   - **round-2** needs-attention:收紧**节点侧 fail-closed** —— 无真锚节点从「降 source_ref=None 留图」改「**整节点丢弃**」(spec §O2:图只留可 O2 三击核验的节点 · 不让 LLM 发明节点当结构证据 · 反 OUT-8 真理机)。
   - **round-3** needs-attention:**新发现 finding #2** [medium] 边规模无界(`_parse_edges` 无上限/去重 → 退化 LLM 全连接炸下游)→ 修(GRAPH_MAX_EDGES=200 有界截断 + `(src,dst,relation)` 去重 · 逻辑保留)· finding #1 边 provenance 复现。
   - **round-4**:仅复现边 provenance(round-3 边有界/去重已收不复现)。
   - **confirm-review(第 5 轮)**:reviewer **反对边禁用本身**(节点-only 图无法兑现方法关系图目标 · T012/T020 依赖边判 structural_connection)· 且**新发现 finding #2** [medium] source_ref exact-match 会误拒 DOI/arxiv 合法变体(仓内 gate 已有 canonicalization)。**operator 决(reviewer 首选路径)= T011 gated 到 FU 先做 · 合并为一个完整方法关系图 ship**(非节点-only stub)。

4. **finding #1(边关系级 provenance)= 真 spec/冻结接口矛盾 · operator 决合并 ship(FU 折入 T011)**:
   - **矛盾**:spec §O2 + T011 task Outputs 明写「节点/**边**挂 source_ref 锚 · 供 T021 三击」· 但 `GraphEdge`(T001 **冻结** domain · model_config=_FROZEN)**无 source 字段** · 且 `domain/` 在 T011 file_domain(`graph/**`)**外**。
   - **decision(operator)**:授权改冻结 T001 domain(同 KG-B5「结构改 frozen 授权」先例)· `FU-T011-edge-provenance` **折入 T011 branch**(commit `a8cd87d`)· T011 以**完整方法关系图**交付(非节点-only stub)。file_domain 扩到含 `src/radar/domain/GraphEdge`(授权在册 · task doc `7d4c441` + 状态更新)。
   - **实装**:① `GraphEdge` 加 `source_ref: SourceRef | None`(关系级证据锚 · 默认 None 向后兼容 · T003 空图/现有构造不破 · 385 passed 验)· ② `_parse_edges` **重开边 + 要求每边有命中真检索 ref 的关系锚**(两真锚节点间幻觉关系/缺锚边 fail-closed 丢弃 · 守 §O2 反幻觉)· ③ **finding #2 修**:节点/边锚 membership 复用 gate `_canonicalize_doc_ref`(消 DOI/arxiv 变体假拒 · 锚回原始 ref)。

**验收指标**:31 test green(边 provenance fail-closed / 幻觉关系拒建 / DOI 变体命中 / _parse_edges 逻辑直测)· graph cov 95% · ruff clean · OUT-5 grep(全量/paper_level/all_arxiv 于 graph/)空 · **385 passed 零回归**(GraphEdge 加字段向后兼容验过)· §4.4 verifier(file_domain 扩含 domain/ · operator 授权)。**注**:真 LLM 集成 build(task V2 `uv run python -c`)deferred · 未授权烧 key(C8 · 同 `_build_real_llm` 惯例 · 单测注入 fake LLM 全覆盖逻辑)。**T011 以完整方法关系图交付**(节点+边均带真 source 锚 · 可 O2 三击 · 反幻觉)。

## §3 Suggested IDS actions

### 给 IDS 的建议动作

1. **`/handback-review 001` 决议本 hand-back(T011 feature ship · FU 折入)** —— 确认方法/claim 级图构建落地(Phase 1 P1.2 · C6 差异化根)· **完整方法关系图交付**(节点+边均带真 source 锚 · 可 O2 三击 · 反幻觉)· FU-T011-edge-provenance 已折入。

2. **⚠ 冻结 domain 改(operator 授权 · 记录在册 · 请 IDS review 确认)`GraphEdge` 加 `source_ref`**:codex adversarial-review **5 轮**收口 —— 边无关系级 source 锚(spec §O2 要求边三击 · 但 GraphEdge 冻结 domain 无字段)= 真 spec/冻结接口矛盾。operator 决:**T011 gated 到 FU 先做 · 合并为一个完整 ship**(拒节点-only stub)· 授权改冻结 T001 domain 加 `source_ref: SourceRef | None`(默认 None 向后兼容 · task doc `7d4c441` + 状态更新 commit)。**这是 spec 与冻结接口结构矛盾的收口**(同 KG-B5「结构改 frozen 授权」先例 · 非语义重写 · 385 passed 验向后兼容)。**⚠ 请 IDS 在 review 确认此冻结接口改动符合 T001 契约治理**(若认为触及 PRD O2 语义或需 forge 追认可提)。**T021 依赖链**:T021(证据链强制)现可 depends_on T011(边 provenance 已就位 · O2 三击到边的物理前提已满足)。

3. **残留框架级 dogfood(KG-B5 族 · 在册 · 归 IDS forge · 本次不当场改)**:
   - **gen-handback template macOS 硬编码**(同 T010 hand-back §3 · producer 手工修 5 字段 + 补 baseline)。
   - **codex-review SKILL §0.1 codex 路径版本漂移**:硬编码 `codex/1.0.3/` · 实际 Ubuntu 是 `codex/1.0.4/`。
   - **eval-event-log writer.sh 接口漂移**:真接 JSON via stdin · 非 parallel-builder SKILL §6.4 文档的 `--type` 等 flag。
   建议 IDS:三者随框架批次 env 化 / glob 版本 / 刷新 SKILL 文档。

4. **T012(依赖 T010+T011)**:两者 ship 后可起(切片重建收口)。**T021**(证据链强制)depends_on FU-T011-edge-provenance(边 provenance 是 O2 三击到边的前提)。
