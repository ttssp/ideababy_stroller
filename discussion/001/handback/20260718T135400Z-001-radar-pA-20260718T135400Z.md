---
discussion_id: 001
prd_fork_id: 001-radar-pA
handback_id: 001-radar-pA-20260718T135400Z
from_build_repo: /home/ys/codes/XenoDev
to_source_repo: /home/ys/codes/ideababy_stroller
baseline: 9cb881f70508488f406a494ac57e87db08d725ba
worktree: .claude/worktrees/001-radar-pA
workspace:
  source_repo: /home/ys/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev
  working_repo: /home/ys/codes/ideababy_stroller
  handback_target: /home/ys/codes/ideababy_stroller/discussion/001/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - feature
severity: medium
created: 2026-07-18T13:54:00Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: forge-v4-landing
---

# Hand-back · 001-radar-pA · forge-v4-landing

**Severity**: medium
**Tags**: feature
**Created**: 2026-07-18T13:54:00Z

## §1 Rationale(执行摘要)

**forge 001 v4 verdict(采 γ)落地收编完成** —— 「机制 landed」里程碑固化回流 IDS。本 hand-back 记录 XenoDev build session(2026-07-18 · autonomous)对 IDS forge 001 v4 verdict(`discussion/001/forge/v4/stage-forge-001-v4.md` · PRD v1.6 O4)§a/§b 待办的落地结果。

**核心状态(两态显式区分 · forge v4 新命名)**:
- ✅ **机制 landed**:forge v3 gate 形态重裁四模块(A″/B″/C″/D″)全 landed —— g1 人肉盲测二值签字**永久退役出解锁链** · 评估 run(默认)全机器核验人不在判定回路 · 校准 run(`--calibration`)保留识伪盲测零解锁语义。
- ⏳ **条款生效(landed-not-yet-effective)**:未生效 —— 待**首个评估 run 产物**在同一验收事件内跑 B′+D′ 自证 PASS = 条款生效事件(canary blocked-until-verified 正统 · 诚实无 deadline 有判据)。

**§a 收编(4 commit · Conventional Commits · 未 push)**:forge v3 worktree 未 commit 实装(13 改 + 9 未跟踪)按变更类型拆 3 + §b 独立 1 收编:
- `11e81dd feat(radar)`:8 src 机制 landed(reconstruct 评估/校准分流 + unlock_preflight evidence bundle + selfcheck_sequence 新 + journal digest/sealed_key + writer slice_id 标题 + rerun_selfcheck 留痕 + cli + 脚本)。
- `6d442af test(radar)`:4 新 test + 2 改 test(+55 · **354 passed** · 零 regression)。
- `4c626a7 docs`:spec amended_3(v0.4→v0.5)+ DAG 注释 + codex 9 轮 approve immutable 记录 + dogfood 回填(含 forge v4 处置段)+ 2 .work hand-back 落地档留痕。

**§b selfcheck 脚本 S 级参数化(`a43da36 refactor`)**:`scripts/selfcheck-07-17.sh` 硬编码 direction/ts → 参数化 5 透传参(旧 07-17 值降默认 · 向后兼容 · briefing 缺省由源码 `_slugify` 派生不在 shell 重造 slug)。**forge v4 §b 铁律全守**:只复用现有 `run_selfcheck_sequence` 入口 + 三道守卫 · **无新增判据/旁路 flag/provider-proof spine** · S 级达标未膨胀未回落手工模板。**codex adversarial-review = approve · 0 material findings**(四点 challenge 逐条确认)。

**§c 停手(未做 · operator 门控)**:首个评估 run + 同事件自证 = 条款生效事件 · gated on ① DEEPSEEK key 轮换(operator 手动 · 泄露前科 key 不再烧)② operator 选方向 ③ 显式授权烧 key。**未自跑评估 run**(C8)。

**验收指标**:354 passed · ruff clean(改动文件)· codex adversarial approve(§b · 0 finding)· 工作树干净 · 31 commit 领先 main。

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

**tag: `build-complete`** —— forge 001 v4 verdict §a/§b 待办落地完成(机制 landed 里程碑)。

### 决议链(上游 → 本次落地)

1. **IDS forge 001 v4 verdict(采 γ · Decision menu [A])** —— 生效自证标的从「07-17 真产物」以**正式 amendment(非 waiver)**改写为「**首个评估 run 产物**」:每个评估 run 产物落地后在同一验收事件内以同 stem evidence bundle 跑 B′+D′ 自证;首个自证 PASS = 条款生效事件(T010/T011 随之机器解锁);FAIL → STOP + hand-back 回流,下一评估 run 重试。07-17 降**校准活动历史样本**(exclusion 非豁免:不销毁不豁免不作 PASS 证据 · answer-key 维持密封)。**β cut**(ML eval hygiene 零豁免 + 临床 protocol「waivers not permitted」跨域合围)。

2. **本次 XenoDev 消费 = §a 收编 + §b 参数化**(operator 批准 a/b · c 停手):
   - **§a**:9 轮 codex review 成果不悬 working tree → 4 commit 收编 · 状态命名「机制 landed」(非「条款生效」)。
   - **§b**:参数化只透传给现有入口 + 三道守卫零削弱(β cut 首要理由即护它们 · codex 9 轮成果是资产)· codex re-review approve 0 finding。

3. **仓根 HANDOFF 修复(附带 · commit 731b685)** —— 发现仓根 HANDOFF.md 是 stale 008-pB 身份(gen-handback 硬读它产 hand-back 身份 · 会投错 discussion),字节 mirror IDS SSOT 001 HANDOFF 修复(sha256 一致 · check-3 identity mode-1 PASS)。**本 hand-back 即验证此修复生效**(正确产 001 身份)。

### ⚠ v4 §underweights 兑现监测(诚实标注 · 未来 v5 触发前哨)

forge v4 §underweights 明写:「脚本参数化 S 级」+「同一验收事件靠 runbook 纪律未机器强制」仍是**推演**(回声室连续第四次高危)。本次落地对其中可验证部分给出真跑证据:
- ✅ **参数化确 S 级**(§b codex approve 佐证 · 未膨胀)——缓解 v5 触发条件②。
- ⏳ **同一验收事件纪律 = runbook 纪律**(未机器强制)——v5 触发条件④(产物先用后证/隔日补证)仍靠 operator 纪律守 · 本次未触及(尚未跑评估 run)。
- ⏳ **首个评估 run 真跑自证 = 本 amendment 最后防线**——待 c 步(key 轮换 + operator 授权)· 若再 STOP = 第六次「推演≠真跑」= v5 触发条件①。

## §3 Suggested IDS actions

### 给 IDS 的建议动作

1. **`/handback-review 001` 决议本 hand-back(build-complete)** —— 确认 forge v4 §a/§b 落地 = 机制 landed 里程碑达成;条款生效(§c)显式 deferred 到首个评估 run 自证事件。

2. **残留框架级 dogfood(在册 · 归 IDS forge · 本次不当场改)**:
   - **KG-B10**(spine 不可伪造绑定)——forge v3 已收窄至**校准面**(评估 run 无 post-gate 产物概念后 · 协议面仅剩校准 answer-key 密封完整性 + 自证记录防伪);audit_channel 防伪 spine 面**冻结不动**(防 KG-B7 第三次过度投资)。D″ digest/sealed_key 只留痕不做绑定校验。**留 forge 定校准面防伪方向**(受控 writer / 密码学绑定 cosign/SLSA 同构)。
   - **KG-B11**(spec-writer SKILL §3.2.1 裸 cat 覆盖 singleton 不产 immutable = R-Q7 协议债)——SKILL 层不对称 · 随框架批次攒批走 006 forge · 原判维持。
   - **KG-B5 族(本次新观察)**:仓根 HANDOFF `git_common_dir_hash` = IDS SSOT 值 `11c3749cc9465c4d`,但 IDS 仓当前实际 hash 已漂移至 `bbf1b1889cd7dcb6`。check-3 是 3-mode OR 短路(mode-1 remote-URL 已 PASS)故不阻断,但 HANDOFF hash 陈旧 = mirror/HANDOFF hash 同步缺口(同 T101 mirror-sha 漂移族)。**建议 IDS 决**:是否随 forge 顺清 HANDOFF hash 刷新机制(产源时重算 · 或校验时容忍 mode-1 短路后不比对 hash)。

3. **条款生效路径(operator 侧 · 非 IDS 动作)**:key 轮换 → 选方向 → 授权烧 key → 首个评估 run + 同 stem 自证。PASS = 条款生效 + T010/T011 机器解锁;FAIL → prd-revision-trigger hand-back 回 IDS(触发 forge v5)。

4. **不阻断项(记录 · 非 blocking)**:`tests/test_reconstruct.py:658-659` 两条 pre-existing F541 ruff 债(T003-A2 commit 引入 · 非本批次)· 供 operator 决是否顺清。

5. **⚠ gen-handback tag enum 漂移(本次发现 · KG-B5 族)**:本 hand-back 是「机制 landed 里程碑」,语义等同上个已投递的 build-complete 里程碑(`20260716T170153Z` · tag=`build-complete`)。但**当前 gen-handback tag enum 已不含 `build-complete`**(合法值 = `{drift, prd-revision-trigger, practice-stats, spec-gap-fix, feature, spec-gap, self-test, redesign}`)—— 已投递的 build-complete hand-back 证该 tag 曾合法,现被从 enum 剔除 = 产者工具 enum 与已投递实践漂移(同 mirror-sha / HANDOFF hash 漂移族)。本次改用合法值 `feature`(交付里程碑最贴)。**建议 IDS 决**:enum 是否补回 `build-complete`(与历史 hand-back 对齐)· 或统一把里程碑类归 `feature`(则历史 build-complete hand-back 语义需追溯映射)。

6. **⚠ gen-handback template 路径缺陷(本次发现 · KG-B5 族)**:`templates/handback.template.md` 的 `to_source_repo` + `workspace.source_repo` 是**硬编码 macOS 字面** `/Users/admin/codes/ideababy_stroller`,**无占位符**,gen 不替换(sed 替换表无这两字段)· 且默认 `REPO_ROOT=$SCRIPT_DIR/../..` 在 worktree 内跑时解出 worktree 路径(非 canonical `/home/ys/codes/XenoDev`)· `--repo-root` 又与 `@<section>` 文件解析基目录耦合(无法只改 build_repo 不动 section 基)。**6 约束验证器不校这些字段值**(check-5 只校 handback_id/filename/discussion_id 一致性 · check-3 校 3 identity 字段 · 均不碰 source_repo/build_repo 值),故不阻断 · 但会 ship 错内容(macOS 路径 + worktree build_repo)误导 consumer。本 hand-back 已**手工修正**这 3 字段为 Ubuntu/canonical 值(对齐已投递实践 `20260716T170153Z`)。真产源自 **worktree** `.claude/worktrees/001-radar-pA`(git worktree · 分支 main · baseline 见下)。**建议 IDS 决**:template 加 `{{TO_SOURCE_REPO}}`/`{{SOURCE_REPO}}` 占位 + env 化(对齐 handback_target 的 IDS_ROOT 口径)· REPO_ROOT 与 section-base 解耦(worktree 内产 hand-back 时 build_repo 应为 canonical XenoDev 根)。
