---
discussion_id: "001"
prd_fork_id: 001-radar-pA
handback_id: 001-radar-pA-20260717T092547Z
from_build_repo: /home/ys/codes/XenoDev
to_source_repo: /home/ys/codes/ideababy_stroller
current_idea: 001-radar-pA
worktree: .claude/worktrees/001-radar-pA
baseline: 9cb881f70508488f406a494ac57e87db08d725ba
workspace:
  source_repo: /home/ys/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev
  working_repo: /home/ys/codes/ideababy_stroller
  handback_target: /home/ys/codes/ideababy_stroller/discussion/001/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - build-complete
  - prd-revision-trigger
severity: high
created: 2026-07-17T09:25:47Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: T003-A3
---

# Hand-back · 001-radar-pA · forge 001 v3 落地(gate 形态重裁)+ ⚠ v4 触发(第五次「推演≠真跑」)

**Severity**: high
**Tags**: build-complete · **prd-revision-trigger**
**Created**: 2026-07-17T09:25:47Z
**Baseline**: main `9cb881f`(forge-v3 landing 叠于 worktree-001-radar-pA · 领先 main 26+ commits)

> **⚠ 一句话**:四模块机制全 DONE(354 passed · codex 9 轮 adversarial · round-2 抓到 1 真生产 bug 已修 · 护栏有界 finding 全修 · 唯一残留收敛到 KG-B10 spine 冻结),**但 D″ 生效自证真跑暴露真架构张力 = 第五次「推演≠真跑」**:07-17 真产物本身是**校准/盲测产物**(pre-v3 形态产 · 含负控 + answer-key),forge v3 分流后评估自证**不该消费它**,故正确 fail-closed STOP → **v4 触发条件①兑现** → 本 hand-back 携 `prd-revision-trigger` 回 IDS 触发 forge v4。**不硬改·不宣称假 PASS**(operator 已决 Option A)。

## §1 做了什么(forge v3 Decision menu [A] 落地)

消费 IDS forge 001 v3 verdict(`discussion/001/forge/v3/stage-forge-001-v3.md` · operator 已批准 ·
PRD v1.5 O4 · HANDBACK-LOG 第 18 条 follow-up),在 XenoDev worktree-001-radar-pA 落地 gate 形态重裁四模块。
全程 TDD + codex adversarial-review。**核心裁决**:g1(人肉盲测二值签字)**永久退役出解锁链**;新 gate
形态 = 按 run 类型分流 + 两层 + 信号。

### 模块 A″ · 评估 run 生成时落 audit.json + human-readable 审计面 —— **DONE**
- `cli/reconstruct.py` `run_reconstruct` 分流:评估 run(默认)在 narrative 生成时**直接** `write_audit_channel`
  落 `out/credibility/<slug>-<ts>.audit.json`(**去 gate PASS 前置** · 证据是管线副产物)+ `write_narrative_markdown`
  落 human-readable 审计面(render_markdown 含 source_refs · operator 可读留痕入口)。
- **复用现有函数**(函数体不改):`write_audit_channel`(audit_channel.py:72)+ `write_narrative_markdown`
  (writer.py:115)。**不走** `generate_audit_channel_postgate`(含 `_verify_gate_pass_artifact` KG-B10 冻结面)。
- **评估 run 无负控泄露论证**:评估 run **不注入负控** → audit.json 与审计 markdown 是同一真 narrative
  两面,无「答案」可泄(post-gate 路径存在的唯一理由 = run_reconstruct 注入负控 · DECISIONS D3 根因)。
- **KG-36 消费面枚举**(实装前置):grep 确认整条 CLI→gate→audit→unlock 链生产侧**尚未 wire**
  (`generate_audit_channel_postgate`/`write_rerun_pass_artifact`/`run_a1_selfcheck`/`evaluate_gate` 全部只有
  tests caller)· 故 A″ 落 audit.json 大部分是**新 wire** 非 rewire · S 级估计更可信。唯三下游消费面
  (`read_audit_channel`/`check_unlock_predicate`/`verify_selfcheck_journal`)改前逐一核对 · 无 silent 丢。

### 模块 B″ · preflight 消费对象换 evidence bundle · 去 gate-PASS 残留 —— **DONE**
- `check_unlock_predicate`(unlock_preflight.py:56)**已**消费 evidence bundle(audit.json + briefing + corpus)·
  **不消费 RERUN-PASS**(v2 已是 evidence 面)· cli PASS 后叠 D′ 硬门 `verify_selfcheck_journal`。故 B″ src 侧
  = **docstring 语义澄清 + 去 gate-PASS 残留核查 + spec/DAG 文案**(评估 run 语境无需改谓词逻辑)。
- **去双轨核查**(不得留旁路 · 「条款落定不悬空」):评估解锁链(unlock_preflight.py / cli/unlock_preflight.py /
  wire 脚本)**零 RERUN-PASS/gate_pass 消费**(grep + tokenize 剥注释后可执行代码验证)· 无「RERUN-PASS 存在即
  解锁」fallback。docstring 更新为 forge v3 语义(g1 退役 · evidence bundle · 人不在判定回路)。

### 模块 C″ · `--calibration` 分流 flag + 分流泄露面对抗验证 —— **DONE**
- `cli/reconstruct.py` 加 `--calibration` flag(默认评估 run)。评估 run:不注入负控 · 落 audit.json +
  render_markdown · 不产 answer-key/签字模板/run-binding。校准 run:注入负控 + answer-key 密封 + 盲测面 +
  run-binding + 签字模板 · **不落 audit.json**(选项 A · operator 决)· 零解锁语义。
- **⚠ 分流泄露面对抗验证(forge v3 §underweights 未完成此对抗审查 · 本批次补齐)**:盲测 briefing
  `<slug>-<ts>.md` 与 audit `<slug>-<ts>.audit.json` 共享 stem(B′ ③ naming 基准)· operator 签字前可从盲测
  文件名推 audit 路径 diff 出 WRONG_DATE 负控(RT-1 旁路)。**选项 A 直接收窄**:校准 run 零解锁语义 →
  不需 audit 解锁面 → **物理不产** → 无可 diff 兄弟文件 → 泄露面=∅。对抗测试
  `test_calibration_negative_control_not_diffable_via_shared_ts` 断言校准 run 无同 stem audit 兄弟(收窄=物理不存在)。
- **端到端验证**:评估 run 落 audit.json + 审计 md(guidance 指向 B′/D′ 机器解锁);校准 run 落盲测面 +
  answer-key + run-binding · **无 audit.json**。两路径行为逐面核实。

### 模块 D″ · 07-17 真产物自证序列 + A1SelfCheckRecord 字段 —— **机制 DONE · 生效自证 STOP（见 §4 v4）**
- `journal/schema.py` `A1SelfCheckRecord` **加两字段**（纯加法 · 默认 None · 向后兼容）:`audit_digest`
  (audit.json sha256 · 篡改证据留痕)+ `sealed_key_ref`（sealed answer-key 指针 · 校准 run 才有）。
  **牵动即停铁律**:只作留痕 · **不进** `verify_selfcheck_journal` 绑定判定（那是 KG-B10 spine 冻结面）。
  `run_a1_selfcheck` 加两透传参（不改判定逻辑）。向后兼容锁测证旧 journal 记录仍可读。
- `credibility/selfcheck_sequence.py`（新增）：`generate_eval_audit_for_selfcheck`（确定性 retrieval+切窗
  生成 audit.json · **无 LLM · 不烧 key · 不要求 gate PASS** · 评估 run 语义 · **不复用** postgate 函数
  避免牵动冻结绑定面）+ `run_selfcheck_sequence`（audit → B′ 谓词 → D′ 双路径核验入 journal · 带 audit_digest 留痕）。
- `scripts/selfcheck-07-17.sh`（operator 手跑 · 真 OpenAlex 免费检索 · 不烧 DEEPSEEK key）。
- **离线序列机制已验可跑**：`tests/test_selfcheck_sequence_forge_v3.py`（5 测全绿）证自证序列可跑通、B′ 四件 +
  D′ 双路径逻辑正确、coverage 漂移 fail-closed、fake retriever 强制 fixture、校准 stem 拒消费。
- **⚠⚠ 真产物生效自证 STOP（诚实 · 未 PASS · 见 §4 v4 触发）**：07-17 真产物本身是**校准产物**
  （`control_slice_ids=['slice-3']` · `mode=fake_citation` · 有同 stem answer-key/runbinding）→ 评估自证
  正确 fail-closed（不宣称假 PASS）。**这是本 hand-back 的 prd-revision-trigger 核心。**

## §2 v4 触发条件监测（forge v3 §underweights）

- ① **改接远超 S 级** → **未命中**。全部改动 = 谓词逻辑复用（B″ docstring）+ 字段级加法（D″ 两字段）+ 评估
  分支落盘（A″ 复用现有函数）+ 两处 fail-closed 守卫（codex 修）。未改 `check_unlock_predicate`/
  `verify_selfcheck_journal`/`evaluate_gate` 函数体逻辑。
- ② **泄露面未收窄** → **未命中**。C″ 对抗测试证校准 run 物理不产 audit.json（收窄=∅）· codex finding #1 修
  补齐同 stem 校准标记 fail-closed（评估自证不消费校准 briefing）。
- ③ **抽查范式失效** → **未命中**。corpus_refs 仍强制**独立真检索语料**（`--corpus-refs` 必填 · 非自 corpus）·
  codex finding #2 修封住「注入 retriever 铸合成解锁」。
- ④ **KG-B10 校准面防伪方向不受控** → **未命中**（本轮冻结不动 spine · D″ digest 只留痕不做绑定校验）。
- 🔴 **v4 触发命中（实为「生效条款前提在真跑下不成立」· 见 §4）**：forge v3「生效条件写死 = 对 07-17 真产物
  自证」的**前提在真跑下不成立** —— 07-17 产物是 **pre-split 校准 artifact**,非评估 evidence bundle。**这是第五
  次「推演≠真跑」**（v3 的评估/校准 split 与 v3 自己钦点的生效标的抵触）· operator 决 Option A → 产本 hand-back。

## §3 边界与铁律（严守）

- ✅ **KG-B10 audit_channel 防伪 spine 面冻结**（`_verify_gate_pass_artifact` 可伪造检查本轮**不修不加固** ·
  forge v3 收窄至校准面 · spine 细节留后续 · 防 KG-B7 第三次过度投资）。
- ✅ **未改 gate 判据 / evaluate_gate 逻辑**（gate.py 未动 · 校准活动的识伪盲测形态保留）。
- ✅ **未自行改 IDS PRD**（PRD v1.5 O4 是 IDS 已灌回的 SSOT · 本仓只消费）。
- ✅ **spec §5/DAG frozen 结构改走 spec-writer amendment 通道**（frontmatter amended_3 · amendment log
  v0.4→v0.5 · DAG 注释更新 · 机器验:无环 + 边端点全声明 + T010/T011 入边 [T004A2,T004A4]）。
- ✅ **REVIEW-LOG 用 `write_review_log_immutable` lib**（双写 singleton + immutable · **防 KG-B11** spec-writer
  §3.2.1 裸 cat 覆盖丢 body 的框架不对称 · 见 §5）。

## §4 codex adversarial-review 结果 + ⚠ v4 触发核心

**codex adversarial-review · 9 轮 · 最终 approve 收敛**（working-tree · forge v3 src · 全 finding 真实 = review 真起作用 · **round-9 approve · 0 material findings** · REVIEW-LOG immutable `working-tree-forge-v3-gate-reshape--2026-07-17T102746Z`）：

**Round 1 · needs-attention · 2 [high] 全修**：

| # | Finding | 处置 |
|---|---|---|
| 1 [high] | `run_selfcheck_sequence` 未拒同 stem 校准标记 → 校准 briefing 可被回填 audit 铸非 fixture PASS journal → **校准负控面流入评估解锁面（破 C″ 分流）** | **修**：`_reject_if_calibration_stem`（同 stem `_answer-key-*.json`/`*.runbinding.json` → fail-closed raise）。回归测覆盖。 |
| 2 [high] | 非 fixture 自证只靠 caller 布尔 + 可注入 retriever → 合成数据可铸真 rerun 解锁 journal | **修**：`effective_is_fixture = is_fixture or (retriever is not None)`（注入 retriever 降 fixture · 只默认真 OpenAlex 路径可铸非 fixture）。回归测覆盖。 |

**Round 2 · needs-attention · 3 [high]**（round-1 修的精度迭代 + 1 真生产 bug）：

| # | Finding | 处置 |
|---|---|---|
| 1 [high · **真生产 bug**] | `render_markdown` 标题 `### <period> ~ <period>` · 但 B′ 只解析 `### <slice_id> ·` → **评估 run 真产物进 B′ coverage 解析空 slice 集 STOP · 走不到 D′ 判定**（我的测用手写 briefing 掩盖了这个生产形状 bug） | **修**：`render_markdown` 标题改 `### <slice_id> · <period> ~ <period>`（对齐盲测面 · 评估产物现 B′ 可解析）· 补端到端测 `test_eval_run_product_is_b_prime_parseable_briefing`（从 `run_reconstruct(calibration=False)` 真产物直跑 B′）。 |
| 2 [high] | `_reject_if_calibration_stem` 忽略 briefing_path · 只查 cwd 相对 `out/credibility` → cwd 可绕过 | **修**：接 `briefing_path` · 锚定 briefing 所在 `<root>/out/reconstruct` 的 sibling `<root>/out/credibility` · root 不可判定 fail-closed。补测 `test_...undeterminable_root`。 |
| 3 [high] | retriever→fixture 降级只保护 wrapper · 直调 `generate_eval_audit_for_selfcheck` + 公开 `run_a1_selfcheck(is_fixture=False)` 可绕 | **= KG-B10 D′ 面 spine 子协议**（不可伪造 provider-proof · 同 caller 布尔面）· **本轮显式冻结不建**（防 KG-B7 过度投资 + 防 V4 当场动 spine）· docstring 诚实标注边界（非机器强制 · 靠程序纪律 · 归 IDS forge）。 |

**Round 3 · needs-attention · 1 [high · 有界]**：`_reject_if_calibration_stem` 只查 sibling marker · 校准 briefing 复制到无 marker 干净 root → 放行。**修**：新增正文形状 denylist（盲测专属标记）。**副益**：抓出我自己测 helper 用了校准标题 · 已改回评估形状（反 mock-pass 教训）。

**Round 4 · needs-attention · 1 [high · 有界]**（codex 明确「不是 KG-B10 · 是形状校验没正向验证」）：正文 denylist 是精确字符串匹配 · **删掉 3 条 marker 行**即绕过 → 校准负控内容进评估自证。**修**：denylist → **正向 allowlist**（`_require_eval_briefing_shape`）—— 核心 = **每 slice 段须有来源块 `**来源(source refs...)**:`**（评估面 `render_markdown` 独有 · 盲测面从不产）· 缺则 STOP。marker-stripped 校准 briefing 仍无来源块 → STOP。补回归测（删 marker 行的校准 briefing 放干净 root 仍 STOP）。

**Round 5 · needs-attention · 1 [high · 有界]**（codex 显式在 approve↔needs-attention 间摇摆 · 最终定为实现细节）：`_require_eval_briefing_shape` 用**全局 `text.count()`** 数来源块 · 不逐段 → 前言/首段塞 N 个来源 marker 即过形状门（我的代码注释声明「每 slice 段须有来源块」但没兑现）。**修**：改**逐段校验** —— 按 slice heading 分段 · 每段须有来源块行落在本 heading 与下一 heading 之间。删旧全局计数死代码。

**Round 6 · needs-attention · 1 [high · 有界]**（codex 明确「不需伪造可解析 provenance · 边界复用 marker」= 非 KG-B10）：逐段用 substring 搜 marker → marker 塞 heading 行 / 空 marker 块 / fenced code 内 marker 均可绕。**修**：`_segment_has_block_source` **严格块级校验**（marker 须独立行 + 排除 heading/引用/代码围栏 + 后须有 bullet）。补 4 回归测。

**Round 7 · needs-attention · 1 [high · 有界 · 我 r6 修的真实现 bug]**（codex 明确「有界形状实现 bug · 不是 KG-B10 · 直接违背 round-6 不变量」）：r6 的 fence 状态**每段重置** + heading 收集不排除 fence 内 heading → fence 在 heading 前打开时看不到 opener · fence 内 marker 被当真块级来源。**修**：`_compute_fence_flags` **单 pass 连续 fence-aware** parser（fence 状态跨 heading 连续传递）· heading 收集排除 fence 内 heading。**这是我自己 r6 修引入的真实现 bug · codex 正确抓到**（反-mock-pass 教训:自己的护栏也可能实现不到位）。

**Round 8 · needs-attention · 1 [high · 有界 · 跨-parser 一致性 bug · codex 明确非 KG-B10]**：D″ 形状门 fence-aware 但 **B′ `_parse_briefing_slice_ids` 非 fence-aware** → 同 evidence bundle 内两 parser 对「什么是 slice heading」定义漂移（slice-2/3 heading 藏 fence 内 · D″ 只校 slice-1 通过 · B′ coverage 却计 3 片可匹配 audit）。**修**：抽 `parse_fence_aware_slice_ids` 共享 helper 到 `unlock_preflight` · **B′ `_parse_briefing_slice_ids` 改 fence-aware** · D″ 形状门复用同一 `_BRIEFING_SLICE_LINE_RE`（单一真相 · 逐字节一致）。**副益**：连带硬化 B′ coverage(现 fence-aware · 拦 fence padding)。补回归测(两 parser 均只见 fence 外 slice-1 · coverage 1≠3 STOP)。

**收口判据（codex-review SKILL KG-8 收敛尾部纪律 · 9 轮 · 呼应 backlog KG-8「反-mock-pass 护栏 12 轮」同型）**：round-2 #1（真生产 bug · 评估产物 B′ 可解析）+ round-2 #2（cwd 锚定）+ round-3 #1（denylist）+ round-4 #1（denylist→正向 allowlist）+ round-5 #1（全局计数→逐段校验）**均有界已修**（每轮都是真改进 · 我的实现逐轮贴近声明的不变量）。**护栏收敛曲线单调收窄**：round-5 codex 已显式 approve↔needs-attention 摇摆并点名「下一层 = 手搓真形状 briefing 骗过形状+B′ resolvable = 用户已冻结的 KG-B10 面」。**唯一残留 = KG-B10 provider-proof 面**（不可伪造 provenance 密码学绑定 · 同 wrapper-only fixture + caller 布尔 = KG-B10 D′ 面 · 已升级 IDS · 本轮不建 · 防 KG-B7 过度投资）。**不深挖 spine**（KG-8 收口纪律:反-mock-pass 护栏近无穷攻击面 · 有界全修 + spine 冻结 + v4 触发承载实质 blocker）。**round-9 re-review = approve · 0 material findings**（codex 明确「未找到可支持阻断的非 KG-B10、有界 shape/parser 真 bug · B′ parser 已 fence-aware · D″ 复用同一 regex · round-8 fence padding 漂移已闭合 · 剩余属更刁 markdown 变体或 KG-B10 面」）。REVIEW-LOG immutable `working-tree-forge-v3-gate-reshape--2026-07-17T102746Z`（写via `write_review_log_immutable` lib · 防 KG-B11）。

**⚠⚠ finding #1 修的过程暴露真架构张力（v4 触发核心 · 第五次「推演≠真跑」）**：

- fail-closed 守卫 #1 落地后，对 07-17 真产物跑 `scripts/selfcheck-07-17.sh` **正确 STOP**（命中同 stem 校准标记）。
- 诊断：07-17 真产物（`out/reconstruct/diffusion-models-20260717T051551Z.md`）**本身是校准/盲测产物** ——
  briefing 顶部「盲测说明…混入阴性对照切片」· 同 stem `_answer-key-*.json`（`control_slice_ids=['slice-3']` ·
  `mode=fake_citation`）+ `*.runbinding.json`（同 mtime 13:16 · 同批产）· 由 **pre-v3 run 形态**产（那时
  run_reconstruct 总注入负控 = 校准语义）。
- **结论（诚实 · 不宣称假 PASS）**：forge v3 引入评估/校准分流后，评估自证**绝不该消费校准 briefing**（否则
  校准负控面流入评估解锁面 = 破 C″ 分流边界 · codex #1 正是此威胁）。故 **forge v3「生效条件写死 = 对 07-17
  真产物立即自证」的前提在真跑下不成立** —— 07-17 产物是 pre-split 校准 artifact，不是评估 evidence bundle。
- **这是第五次「推演≠真跑」**：v3 的评估/校准 split 与 v3 自己钦点的生效标的（07-17 产物）语义抵触。前四次
  =首跑 FAIL / operator 拒 gate 形态 / KG-B9 循环依赖 / D′ rerun 暴露 a1 验证盲区 · 本次 = 生效条款标的错配。
- **operator 决 Option A（2026-07-17）**：不硬改·不宣称假 PASS·保留 codex 2 修（守卫正确·收窄分流）·
  保留四模块机制（离线序列测全绿）· **产本 prd-revision-trigger hand-back 回 IDS 触发 forge v4**。

**请 forge v4 重裁（生效条款标的）**：
- 选项 α：生效自证改对**评估 run 新产物**跑（自证序列先跑一次评估 run · 但那烧 DEEPSEEK key · 且偏离 forge
  「对 07-17 产物自证」钦点）；
- 选项 β：07-17 产物**特批为 pre-split 遗留**·允许一次性豁免消费（但豁免=旁路·违「条款落定不悬空」·codex #1 正挡此）；
- 选项 γ：承认「对既有 pre-split 产物自证」在 split 下**不可达**·生效条款改为「首个评估 run 产物落地即自证」·
  07-17 产物降为校准活动历史样本（与识伪 retire-as-gate keep-as-calibration 一致化）。
- **XenoDev 侧倾向 γ**（最诚实：split 后「校准产物走评估自证」本就语义错·生效自证的自然标的是首个真评估 run 产物）·
  但**这是 forge 治理域**（改 PRD 生效条款）· 不在 XenoDev 当场定。

## §5 框架级 dogfood 观察（记 dogfood-backlog · 归 IDS forge）

1. **KG-B10 spine 子协议残留**（在册 · 本轮冻结不动）：「下游 provenance 机器不可伪造地绑上游 gate PASS」·
   评估 run 无 post-gate 产物后协议面收窄至**校准 run answer-key 密封完整性 + 自证记录防伪**两处 · 解法方向 =
   受控 writer / 密码学绑定（cosign/SLSA 同构）· 归 IDS forge 裁 spine 细节。
2. **pre-existing 框架 lint 债**（新观察）：`tests/test_reconstruct.py:658-659` 两条 F541 ruff 错误
   （f-string without placeholder）· **非本批次引入**（git blame = T003-A2 commit 81ef4d6）· 本 session 未改该
   文件故未触。属既有测试 lint 债 · 记此供 operator 决是否顺手清（不在本批次 scope）。

## §6 operator 下一步

1. IDS 跑 `/handback-review 001` 决议本 hand-back（**build-complete + prd-revision-trigger 双 tag**）：
   采纳 gate 形态重裁四模块机制 + codex 2 修 · **并起 forge v4 重裁生效条款标的**（§4 选项 α/β/γ · XenoDev 倾向 γ）。
2. **forge v4**：重裁「生效自证该对评估 run 新产物跑，还是 07-17 特批 pre-split 遗留，还是生效条款改『首个评估 run
   产物落地即自证』（07-17 降校准历史样本）」。这是本 hand-back 的 prd-revision-trigger 触发点。
3. KG-B10 spine 子协议：仍在册（本轮冻结）· operator 决 forge / 拆 task / SHARED-CONTRACT 通用层（收窄至校准面后）。
4. spec `amendment_3_reviewed_by` **已收口** = `codex@2026-07-17`（9 轮 adversarial-review approve 收敛 · REVIEW-LOG immutable）。

## §7 未 push（领先 origin）

本 landing 叠于 worktree-001-radar-pA（领先 main 9cb881f · 26+ commits）· **未 push**（per CLAUDE.md · push
前需 operator 确认）。⚠ DEEPSEEK key 本批次**未烧**（D″ 用确定性 retrieval+切窗 · 无 LLM）· 仍建议轮换（C8）。

**测试总计**：354 passed（318 baseline + 36 新增/re-scope · 零 regression）· 我的改动文件 ruff clean。
**codex review**：adversarial-review · **9 轮**（round-2 抓到 1 真生产 bug = 评估产物非 B′ 可解析 · 已修 ·
round-3~8 抓到复制/删-marker/前言塞/边界复用/fence跨heading/跨-parser 旁路 = 单pass连续fence-aware严格块级 + B′/D″ parser 一致 · 已修）· 有界 finding 全修 · 唯一残留归 KG-B10 spine 冻结面
（护栏近无穷攻击面 · KG-8 收敛尾部收口纪律 · 不深挖）。
**D″ 真产物生效自证**：**STOP（诚实 · 未 PASS）** —— 07-17 是校准产物 · 评估自证正确 fail-closed = 第五次
「推演≠真跑」→ prd-revision-trigger 回 IDS 触发 forge v4。离线序列机制已验可跑（fake retriever 强制 fixture）。
