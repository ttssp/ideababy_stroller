---
discussion_id: 001
prd_fork_id: 001-radar-pA
handback_id: 001-radar-pA-20260810T034651Z
from_build_repo: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA
to_source_repo: /home/ys/codes/ideababy_stroller
workspace:
  source_repo: /home/ys/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA
  working_repo: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA
  handback_target: /home/ys/codes/ideababy_stroller/discussion/001/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - feature
severity: medium
created: 2026-08-10T03:46:51Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
current_idea: 001-radar-pA
worktree: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA
baseline: 505eaac
related_task: T040
---

# Hand-back · 001-radar-pA · T040

**Severity**: medium
**Tags**: feature
**Created**: 2026-08-10T03:46:51Z

## §1 Rationale(执行摘要)

T040(契约测集 · spec §6 verification 固化)剩余 5/6 交付已 ship —— `test_source_ref_resolves.py`(O2)/
`test_radar_ring_and_low_confidence.py`(O4 · 含渲染到最终 briefing markdown 的断言)/
`test_decision_delta.py`(O7)/`test_credibility_gate_structure.py`(O1 结构半支)/
`test_o2_exemption_contract.py`(O2 豁口分层契约:frozen-spec (a)(b) block + (c) warn-only +
T021 task-level 契约)。第 6 项 `test_no_aggregate_authoritative_verdict.py`(OUT-8)已在此前
commit `869872d` 单独 ship,本次不重做。

44 条新契约测,22+ 条 mutation-proof-teeth 循环全部证据在案(改 src 对应分支 → 转红 → 复原 →
转绿),全量回归 `uv run pytest tests/ -q` 1061 passed 零回归,task 规定的 gate 命令
`uv run pytest tests/contract/ -q` 148 passed,ruff clean。

codex review 六轮(round 1-3 全部当轮收口共 8 finding;round 4 触 4 轮硬上限 hard-stop,原样
报告 operator;operator 裁定直接修复 round-4 的 2 条 [P2] 而非重开轮次计数,修后再跑 round 5
确认——round 5 又抓到 1 条既有测试(非本次新增)的结构弱点已修;round 6 再抓 3 条 [P2],
operator 判定均属"契约测试可以更严谨"而非"生产代码有真实漏洞",裁定停止迭代,ship 当前状态,
3 条转 follow-up,详见 §3)。

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

**触发**:IDS 第 42 条 (2)(2026-08-04T00:18Z)授权起跑批次之一,授权的是"起跑",不预判验收。

**决议链(codex review 6 轮)**:

- round 1(--scope working-tree):2 [P2] —— 泄露检测反黑名单绕过(改用不变量断言,同内容仅
  `is_negative_control` 不同的两份叙事必须渲染逐字节相同)+ 缺原始输入 fail-closed 未接入 gate
  命令范围。均已修(commit `d9347b1`)。
- round 2(--scope working-tree):3 [P2] —— 净化底线(反空转守卫)/ no-provenance 正向豁免分支
  未覆盖 / 重算门只测了单方向不一致。均已修(commit `68cd745`)。
- round 3:working-tree scope 在 clean 树上一度"空转"判 0 finding(与同 task round 1/2 行为不
  一致),诊断为 KG-23 同族的非确定性复现(已记 dogfood),改用 `--base worktree-001-radar-pA
  --scope branch` 重跑后得到 3 个真实 [P2](示例块假绿 / claim 身份单条负例 / 三击边界)。均已修
  (commit `0dcb188`)。
- round 4(--base --scope branch):2 [P2] —— 签字模板对 `is_negative_control` 未做同等不变性测
  (`build_signoff_template` 只在全员未标记叙事上测过)+ O4 断言只查内存 `Judgment` 对象未验证
  渲染进最终 briefing markdown。触 4 轮硬上限,原样 hard-stop 报告 operator(commit `695274a`
  记录声明)。
- **operator 裁决(round-4 之后)**:选择"直接修这 2 条,再跑一轮 codex 复核后 ship"(非重开
  codex 轮次计数,由本会话直接实施修复)。两条均已修 + mutation-proof(commit `586545e`)。
- round 5(--base --scope branch,复核修复):1 [P2] —— 新发现既有(非本次新增)
  `test_should_have_signoff_template_schema_with_three_verdict_fields` 只做子串检查,未验证
  `type`/`statement`/`o2_traceable_ref` 真嵌套在 `new_evidence_chain` 之下。已修(改用按行+缩进
  的结构校验,不引入 PyYAML 新依赖,commit `6ea4105`)。
- round 6(--base --scope branch,复核修复):3 [P2] —— 详见 §3(operator 决定不再迭代,转
  follow-up)。

**收尾裁决**:operator 复核 round-6 三条 finding 性质后判定"均非重大 bug,是测试严谨度的边际
改进",决定停止继续跑 codex 轮次,接受当前状态 ship,三条转 follow-up 记录待未来视需要处理。

## §3 Suggested IDS actions

**round-6 codex review 的 3 条 [P2] follow-up(operator 判定非 ship-blocking,如实披露)**:

1. `test_credibility_gate_structure.py` 的负控注入 mutation-proof 测试全程只用 `seed=1`
   (触发 `FAKE_CITATION` 分支),`NegativeControlInjector` 的另一分支 `WRONG_DATE`(偶数 seed)
   从未被验证"腐化内容确实到达可见渲染输出"这一不变量——若 `WRONG_DATE` 分支只打标记不真的
   偏移日期,现有断言不会发现。
2. `test_credibility_gate_structure.py` 的 `new_evidence_chain` 嵌套结构校验(本轮 round-5 刚
   补的按行+缩进检查)本身仍不够严谨:能证明字段"在 `new_evidence_chain:` 之后且有缩进",但
   不能排除被放进另一个顶层映射,或嵌套进 `new_evidence_chain.metadata` 这类中间层级。codex
   建议改为真解析 YAML 或同时校验相对缩进+中间层级。
3. `test_o2_exemption_contract.py`(既有,非本次新增)"无论 low_confidence"字样的 enforcement
   契约,实际只把低置信空证据判断送进了 `enforce_evidence_chain`;高置信空证据分支只验证了
   Pydantic 构造器会报错,未构造绕过类型校验的实例(如 `model_construct`)送入
   `enforce_evidence_chain` 验证 enforcement 层本身是否也会拦。

**建议处理**:上述 3 条建为独立 follow-up task(或并入未来 T040 系列的收尾 task),不阻塞
T041(spec.md 定义 T041 `depends_on: [T040, T042]`,本次 T040 5/6 交付 + 既有 OUT-8 已覆盖
task 文档 `## Outputs` 全部六项,`## Verification` checklist 全过)。

**IDS 侧无需即时决议**——本 hand-back 为 informational + 记录 follow-up,不含需要 chair 裁决的
争议项。
