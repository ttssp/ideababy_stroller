---
discussion_id: 001
prd_fork_id: 001-radar-pA
handback_id: 001-radar-pA-20260802T162313Z
from_build_repo: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA
to_source_repo: /home/ys/codes/ideababy_stroller
workspace:
  source_repo: /home/ys/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA
  working_repo: /home/ys/codes/XenoDev/.claude/worktrees/001-radar-pA
  handback_target: /home/ys/codes/ideababy_stroller/discussion/001/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - drift
  - practice-stats
severity: medium
created: 2026-08-02T16:23:13Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: v0.7-amendment-forge-v5
---

# Hand-back · 001-radar-pA · v0.7-amendment-forge-v5

**Severity**: medium
**Tags**: drift,practice-stats
**Created**: 2026-08-02T16:23:13Z

## §1 Rationale(执行摘要)

**v0.6 → v0.7 一次性 amendment 落地完成** —— `/expert-forge 001` **v5** verdict 12 项决策表中的
**9 项**(1/2/3/4/5/6/7/9/11)已落进 `specs/001-radar-pA/spec.md` 正文,含 operator 明确追加的
item 4(O7 longstop —— stage doc「同批耦合」清单遗漏但 IDS 已确认应含,非刻意排除)。

- **item 8**(T040 起跑)/ **item 10**(`ephemeral.py`/`writer.py` 代码面)/ **item 12**(展示投影新
  task)/ **PRD v1.7 三点修订**(IDS 侧) —— 均按 forge v5 verdict 明文「独立 · 不在本次」处理,
  本仓本轮**零改动**。
- **落地前置已满足**:HANDBACK-LOG 第 39 条「R5 逐条裁决」先行完成,四条修法(F1-F4)全部
  accepted、无 rejected/deferred。
- **R5 承重关系已如实落入 spec 正文**:item 11 具名 provenance 链依赖 R5-F2、item 2 锚点身份判据
  依赖 R5-F4,两条修法均**尚未经 codex 复核** ⇒ spec 明文标注「允许现在落成规范文本,但其保证
  不得被援引为 O2/P2 PASS 依据,直到第 6 轮 codex 复核过 R5-F2/R5-F4」(§1-O2(c) + §0.2 各一处)。

**codex adversarial-review 共跑 5 轮**(`--base 9444e09` 固定 · 每轮复核累积 diff,非增量 diff ——
让每轮都验证到迄今全部改动的自洽性,不只验证本轮新增部分):

| round | finding 数 | 处置 |
|---|---|---|
| 1 | 3(2×high + 1×medium) | 全收(AND/OR 逻辑错主句 · T041 owner 断层诚实标注 · claim 去重算法未定义诚实残留) |
| 2 | 3(2×high + 1×medium) | 全收(round-1 修的主句之外,§1-O2(c)/§6 残留旧"且"表述矛盾 · T021/T040/T041 已 ship 但仍是 v0.6 语义 + 引用已删 `OQ-A1-1` · 池化判据未按新增 §0.1 自查转正条件) |
| 3 | 3(全 high) | 2 条收(`FAIL`/`INSUFFICIENT_EVIDENCE` 判定顺序未定 · 操作化"证明不可比"触发时点),1 条(池未按方向分区)确认为 forge v5 verdict 决策表原文逐字决定 —— 改分区键属"架构转向",超本次 landing 授权,不修,转为显式披露 |
| 4 | 3(全 high) | 2 条收(转正条件补合取:codex 复核 + claim 去重落地缺一不可 · O7 longstop 公式与 fallback 措辞并存产生互斥读法,改写为显式二分支消歧),1 条(同 round-3 池分区,round-3 已定性)不重复动作 |
| 5 | 2(全 high) | **verdict 收敛为仅这 2 条**(池未按方向分区 · CLI/journal 结果值接口协议无 owner)——两条均已在 spec 正文显式披露为"超本 amendment 授权、留供 IDS/forge 决议"的残留风险,round 5 = **hard-stop**(codex-review SKILL §4.2:round 4+ 仍 needs-attention 需 operator 决策;本轮多跑一轮到确认"仅剩超权限项"才停,而非在仍有本轮可收口 finding 时提前停) |

**最终 verdict = `needs-attention`(非 `approve`)**,但收敛到的 2 条残留**均已被识别为需要
operator/IDS 决策的架构 / task-assignment 问题,而非本轮可自行收口的缺陷** —— 详见 §3。

Version `0.6 → 0.7`,`amendment_5_reviewed_by` 已从占位 `pending` 更新为完整 5 轮记录
(spec.md frontmatter + Amendment log 第 10 条)。全程 0 src/tests 改动,仅 `spec.md`。

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

按 frontmatter `tags` 逐条说明:

### 若 `drift`:spec 与实际(task 实装 / forge 决策)不符

**Drift 1 —— spec 新语义与已 ship 的 task 文件不同步(预期 vs 实际)**

- **预期**(spec.md §1-O2(c) 字面):O2/P2 PASS 判据 = 跨 run 累积池(≥3 非豁免判断)+ 借池条件 +
  `INSUFFICIENT_EVIDENCE`/`FAIL` 两值语义。
- **实际**:`tasks/T021.md`/`tasks/T040.md`(均已 ship · squash 完成)现行内容仍实装 **v0.6 单 run
  自足阈值**(非豁免 0/1/2 → 不判 PASS · 恰 3 → 可 PASS,无池化 / 无借池条件 / 无对应 pytest)且
  **仍引用本 amendment(item 7)已从 spec 整条删除的 `OQ-A1-1`**(dangling 引用)。`tasks/T041.md`
  的 `file_domain`(`bench/**` + `tests/e2e/**`)不覆盖 CLI/journal 层,故 `INSUFFICIENT_EVIDENCE`
  的 CLI 退出码 / 产物状态字段协议**无 owner**。
- **证据**:`grep -n "非豁免\|INSUFFICIENT_EVIDENCE\|OQ-A1-1" specs/001-radar-pA/tasks/T021.md` 第
  31/44/60 行 · `tasks/T040.md` 第 40/42 行,均字面命中"单 run ≥3"旧措辞与 `OQ-A1-1` 死链。
- **后果**:在对应 task-level 补丁 ship 之前,机器侧实际运行的仍是 v0.6 行为 ——
  `evaluate` 命中样本不足时机器侧仍可能 exit 0 且正常落盘,v0.7 池化/结果值语义**目前只在 spec
  规范层生效**,尚无验收测试覆盖。
- **已在本轮如何处理**:spec.md §1-O2(c) 新增"spec-task 同步缺口"条 + §6 O2 行交叉引用,如实
  声明这是**未指派的 follow-up task-level 工作**,不在本 amendment 授权范围(本轮只改
  `spec.md`)。

**Drift 2 —— 池化判据的分区键与"证明不可比"拆池流程,是 forge v5 verdict 原文决策而非本次起草**

- codex adversarial-review round-3/4/5 三轮持续指出:池仅按 `decision epoch + rubric` 分区、不按
  评估方向分区(方向只用于去重),理论上单 run 可借跨不相干方向的历史判断凑满 ≥3 判 PASS,且
  "被证不可比"才事后拆池、拆池**不追溯**已判的 PASS,存在"跨方向假 PASS 被永久保留"的风险。
- 核实:这**逐字匹配** `ideababy_stroller/discussion/001/forge/v5/stage-forge-001-v5.md` 决策表
  第 2 行原文("池…仅限同一 decision epoch 与 rubric…按「规范化 claim + direction」去重…方向被证
  不可比 → 拆池,而非退回强迫单 run 产 3 条")—— **不是本次 spec-writer 起草引入的缺陷,是 forge
  v5 已裁定的设计**。
- **本轮处理**:未修改分区键(改分区维度 = "架构转向",超本次 landing 授权,见本仓 CLAUDE.md
  决策矩阵"重大架构转向 → 必须先 forge,不静默停")。已在 §1-O2(c) 池定义的 fallback 条下补充
  操作化"证明不可比"的触发时点(首次抽样核验时 operator 就地判定、不追溯),并显式记入残留
  设计风险 + 引用 forge v5 决策表原文出处,留供下一轮 forge(v6)裁决是否需按方向预先分区。

### 若 `practice-stats`:统计区间 + 数字

- **区间**:2026-08-02(v0.6 → v0.7 起草)~ 2026-08-02(本包产出),单 session 内完成。
- **spec.md 行数**:328(v0.6)→ 462(v0.7,含 5 轮 codex 修复后)。
- **codex adversarial-review**:5 轮,共 14 条 finding(6×round1-2 + 3×round3 + 3×round4 +
  2×round5,含跨轮重复计数的同一残留项),12 条在授权范围内直接收口,2 条转为显式披露 + 留供
  IDS 决议(占比 ~14%,低于本仓历史平均 —— 本次多数 finding 是文本内部逻辑一致性问题,非
  设计缺陷,收敛速度快于 T020 那轮的 12 轮)。
- **commit 数**:本次 amendment 共 6 个 commit(1 落地 + 4 轮 fix + 1 补记录),全部 `docs`/`fix`
  scope,0 行 `src/`/`tests/` 改动。

## §3 Suggested IDS actions

1. **裁 Drift 2(池分区键)——弱建议路由到下一轮 forge(v6)**:是否需要在池定义里把"评估方向"
   (或预先声明的可比较类 identifier)纳入强制分区键,而不是仅用于去重、靠事后"证明不可比"再
   拆池?两个选项:
   - **(i)** 维持现状(forge v5 verdict 原文决策)——理由:池累积的初衷就是应对"单方向单 run
     产不出 ≥3 条"的稀疏场景,首个 epoch 内跨方向借用属可接受的过渡态,真正造成"永久假 PASS"
     需要多个条件同时成立(跨方向借用 + 从未被人工核验发现不可比 + 该 PASS 结论被下游实际使用)。
   - **(ii)** 分区键加方向 + 拆池后重判并撤销受影响的既有 PASS —— 更严格,但工作量不小,且
     "撤销既有 PASS"本身是新的架构决策(如何通知已依赖该 PASS 的下游 · 是否需要版本化 PASS
     结论)。
   我的建议是 **(i)**(维持现状 + 已有的显式风险披露作为过渡),但这是 IDS/operator 的裁定,
   本包不预设。

2. **指派 task-level 同步工作(Drift 1)**:`tasks/T021.md`/`tasks/T040.md`/`tasks/T041.md` 需要
   补丁使其与 v0.7 语义一致 —— 删除死链 `OQ-A1-1` 引用、补池化版 pytest(⑤⑥)、补 CLI 退出码/
   产物状态字段协议(归属未定,当前 T041 的 `file_domain` 不覆盖)。这是**新的独立 task 范围**
   (可能是 T021/T040 的 follow-up,或新开一个 task),建议随下一批 task-decomposer 起跑时一并
   排入,而非阻塞本次 spec amendment 的落地(spec 先行 + task 补齐是本仓既定模式,同 R5-F2/F4
   "先落规范文本 + 显式标注保证暂不成立"的处置精度一致)。

3. **item 8(T040 起跑)现已解锁**:forge v5 verdict 的 [A] 执行顺序是「1. R5 裁决 → 2. spec 落地
   + codex review(本包)→ 3. T040 起跑 → 4. PRD v1.6→v1.7」。本包完成第 2 步,第 3/4 步是否
   立即推进由 operator/IDS 决定;若推进 T040,建议一并纳入 Suggested action 1/2 的裁定结果(池
   分区键 / task 同步范围会直接影响 T040 的 contract suite 设计)。

4. **PRD v1.7 三点修订**(O7 时间锚 / 呈现面是 O1 风险 / 判断数量是集合验收采样非单次 KPI)—— 按
   forge v5 verdict 决议 D,这是 IDS 侧 `/scope-inject` 动作,不在本仓授权范围,提醒 operator
   在裁定上述两项后一并处理。

5. **建议 operator 跑 `/handback-review 001`** 对本包做决议,重点核对:(a) 9 项落地是否符合 forge
   v5 verdict 原意(§2 逐条对照);(b) 2 条残留 finding 的路由建议(Suggested action 1/2)是否
   采纳;(c) R5-F2/F4 何时安排第 6 轮 codex 复核(item 2/11 的 PASS 依据目前被挂起,不安排复核
   则会长期停留在"允许落规范文本但不可用于判 PASS"的过渡态)。
