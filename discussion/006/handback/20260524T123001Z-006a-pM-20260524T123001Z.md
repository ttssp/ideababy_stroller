---
discussion_id: 006
prd_fork_id: 006a-pM
handback_id: 006a-pM-20260524T123001Z
from_build_repo: /Users/admin/codes/XenoDev
to_source_repo: /Users/admin/codes/ideababy_stroller
workspace:
  source_repo: /Users/admin/codes/ideababy_stroller
  build_repo: /Users/admin/codes/XenoDev
  working_repo: /Users/admin/codes/XenoDev
  handback_target: /Users/admin/codes/ideababy_stroller/discussion/006/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "647b0db7b4d47318"
tags:
  - feature
severity: low
created: 2026-05-24T12:30:01Z
related_task: T010
---

# Hand-back · 006a-pM · T010

**Severity**: low
**Tags**: feature
**Created**: 2026-05-24T12:30:01Z

## §1 Rationale(执行摘要)

T010 把 spec §6 O3 行末"追加 bootstrap verify"段(一长串 `bash -c 'set -e; ...'`)沉淀为 `tests/integration/bootstrap-verify.sh` 单文件 test,operator 可单跑或被 T012 SHIP GATE 聚合。共 7 块完整 check:spec O3 原命令逐字 + T002 parallel-builder + T003 writer.sh(SSOT 真名)+ framework mirror + T001 dangerous-24 + Safety Floor 件 1/3。

ship gate 状态:
- TDD red→green:positive PASS(BOOTSTRAP-OK)+ negative mv AGENTS.md exit 1 + negative mock Safety Floor 件 1 缺失 exit 1
- spec §V 4/4 PASS
- Cross-model review:codex 2 rounds(round 1 P2 漏 Safety Floor 件 1/3 → round 2 真修;round 2 P2 "应真跑 scan-credentials.sh 不仅 test -x" → **operator A 决议 accept-as-backlog**)
- ship commit:ccfc8c9(squash from worktree-T010 · 3 worktree commit)

## §2 触发原因 + 决议链

### 触发原因

T002 + T003 retroactive amendment 2026-05-24 标 status: shipped 后,T010 dependency-graph 入度归零(T001 ✅ + T002 ✅ + T003 ✅)。operator 起 T010 作为 phase 3 第一个 task(SHIP GATE T012 的 setup precondition 之一)。

### 决议链

1. **2026-05-24 起前 3 决议**(operator 拍板):
   - `writer` 文件名用 SSOT 真名 `writer.sh`(非 spec §40 原写的 `write-event.sh`)
   - T002/T003 不补正式 codex review(retroactive amendment pattern 等价 ship)
   - 主 session opus 直接跑(本 task 纯样板 ls/test · 模型选择无意义)
2. **round 1 codex needs-attention(1 P2)**:漏检 Safety Floor 件 1(credential-isolation)+ 件 3(backup-detection)。
3. **round 2 修法**:加 §6 件 1 check + §7 件 3 check。负向 test mock 件 1 缺失 真退 1。
4. **round 2 codex needs-attention(1 P2)**:应真跑 scan-credentials.sh 不仅 test -x。
5. **operator A 决议 accept-as-backlog**:
   - finding 自身有 bug:codex 说 "scan exit 1" 实测 exit 0(脚本 spec/impl gap)
   - 执行 finding 引 cascade:14 false positive(spec/review-log/handback/AGENTS.md/CLAUDE.md 讨论性 `prod://`),修需 patch scan + 加 .gitignore + 重打规则,横向超 T010 scope(1h · low)
   - 跟 004-pB T021/T024/T025/T040-B precedent 同 · operator A 决议突破 cap
   - finding 留 hand-back §3 A1/A2

## §3 Suggested IDS actions

| # | 类型 | 描述 | priority | spec ref |
|---|---|---|---|---|
| **A1** | **bug + governance**(主要诉求) | `scan-credentials.sh` spec/impl gap 修:spec 注释明确 "exit 1 if credentials found" 但实测 echo "ERROR" 后 **exit 0**;影响 T012 SHIP GATE 真聚合 Safety Floor 件 1 的能力。同 pattern 可能潜伏 lib/handback-validator/check-*.sh(跟 FU-T003-stderr-fix root cause 一类) | **medium** | .claude/safety-floor/credential-isolation/scan-credentials.sh |
| **A2** | governance · 凭据扫描规则治理 | `scan-credentials.sh .` 在本仓真跑命中 14 false positive(spec/review-log/handback/AGENTS.md/CLAUDE.md 讨论性 `prod://`)。需要:(a) 加 `.scan-credentials-ignore` 白名单 (b) 区分"内容含字符" vs "真凭据"两类 (c) 加 `--exclude-paths` 参数 | medium | .claude/safety-floor/credential-isolation/ |
| **A3** | follow-up · bootstrap-verify v0.2 | A1+A2 修完后,T010 bootstrap-verify 升级:第 6/7 块从 `test -x` 升级到真跑 `scan-credentials.sh` + `diff-snapshot.sh smoke`,真校 Safety Floor 件 1/3 运行时可用性(非仅文件存在) | low | tests/integration/bootstrap-verify.sh §6/§7 |
| A4 | tracking · gen-handback.sh UX | gen-handback.sh 默认 `--out` 不含 prd_fork_id 前缀,导致 004-pB T010 + 006a-pM T010 同名冲突(本 ship 跑期间触发 · git restore 恢复)。建议 `--out` 默认改 `<prd_fork_id>-<task-id>-handback.md` 或 producer 端加文件存在性 fail-closed | low | lib/handback-validator/gen-handback.sh |
| A5 | tracking · IDS 端 followup | 上一 ship FU-T003-stderr-fix hand-back 包(006a-pM-20260524T114651Z · tags=[drift])IDS forge 端是否决议反向同步 bootstrap-kit writer.sh?状态确认 | medium | /Users/admin/codes/ideababy_stroller/discussion/006/handback/ |

## §4 PRD-revision-trigger 自检

本 ship **不触 PRD-revision-trigger**。

理由:
- 不动 spec scope(spec §6 O3 量化 / Outcome 集合 不变 · 本 task 把 spec inline 命令搬家)
- 不动 production code(仅加 1 个 test 文件)
- 不动 Safety Floor 件 1/2/3 实装(round 2 修仅加 check 它们存在)
- 不动 IDS SSOT(不像 FU-T003-stderr-fix 那次 tags=[drift])
- round 2 P2 finding 是改进建议性质,operator 决议留 A1/A2/A3 backlog;不属 spec scope 改动

仅 §3 A4 一项 gen-handback.sh UX 改进(本仓内 follow-up),非 spec scope。

## §5 后续 task 建议

| # | task | 类型 | 估时 | 依赖 |
|---|---|---|---|---|
| 1 | A1 scan-credentials.sh exit code bug fix(独立 FU) | bugfix | 1h sonnet | 独立 |
| 2 | A2 scan-credentials.sh false positive 治理(白名单 + exclude) | governance | 2-3h sonnet | A1 |
| 3 | A4 gen-handback.sh `--out` 默认加 prd_fork_id 前缀 | UX | 30 min sonnet | 独立 |
| 4 | 起 006a-pM T004(hand-back template + gen · 3h · sonnet) | feature | 3h sonnet | 入度 0 |
| 5 | 起 006a-pM T005(spec-writer review hook link test · 2h · sonnet) | feature | 2h sonnet | 入度 0 |
| 6 | 起 006a-pM FU-producer-2(SKILL §6.3 cross-device fallback · 2h · sonnet) | feature | 2h sonnet | FU-producer-1 ✅ |
| 7 | 起 006a-pM T006(等 T001 ✅ + T003 ✅ → 入度归零) | feature | 2h sonnet | T001 + T003 |
| 8 | 起 006a-pM T011(等 T003 ✅ + T008 + T006 → 等 T006/T008) | feature | 2h sonnet | T003 + T006 + T008 |

## §6 File changes

squash commit `ccfc8c9` on main · 3 files / +176 / -0:

| 路径 | 改动类型 |
|---|---|
| `tests/integration/bootstrap-verify.sh` | **新增** 66 line · 7 块 check(O3 原命令 + T002 + T003 + framework mirror + dangerous-24 + Safety Floor 件 1 + 件 3) |
| `review-log/T010/round1-codex.md` | **新增** 51 line · round 1 codex 全输出 + P2 finding(漏检 Safety Floor 件 1/3) |
| `review-log/T010/round2-codex.md` | **新增** 59 line · round 2 codex 全输出 + P2 finding + operator A 决议 accept-backlog 链 |

LOC 分布:
- test(integration):66 LOC(纯 ls/test 校验件 · set -e 切 repo root)
- doc(review-log):110 LOC(2 round codex 全输出 + 决议链)

worktree commit 链(squash 前 3 commits):
- `69a8ed9` feat round 1 初实装(5 块 check)
- `88f6861` fix round 2 加 Safety Floor 件 1+3 check(回应 codex round 1 P2)
- `15302d4` docs(review-log) round 2 operator A 决议 accept-backlog

## §7 Known gotchas

1. **`writer` 文件名 SSOT vs spec gap**:T010 spec §40 原写 `lib/eval-event-log/write-event.sh` 但 SSOT 实装是 `writer.sh`。本 ship 用 SSOT 真名(跟 T003 A1 amendment 一致 · operator 决议 A2 已 accept)。bootstrap-verify §3 注释明确 root cause 防未来 cherry-pick spec 误改回。

2. **Safety Floor 件 1/3 check 限制**:本 ship 仅 `test -x` 校文件存在,不真跑 scan-credentials.sh / diff-snapshot.sh。round 2 codex 推 push 这点 — operator 决议 accept-as-backlog 留 A1/A2/A3 在 IDS 端跨仓治理(scan 自身 bug 修 → bootstrap-verify v0.2 真扫)。

3. **scan-credentials.sh false positive 14 文件**:本仓 spec/review-log/handback/AGENTS.md/CLAUDE.md 都含讨论性 `prod://`,真跑 scan 会全命中。这是 v0.1 governance 缺口(凭据扫规则区分"内容含字符" vs "真凭据"),不是 T010 范围 → A2 留 IDS forge 决议。

4. **gen-handback.sh 同名冲突**:本 ship 首次 gen 用默认 `--out .work/handback/T010-handback.md` 覆盖了 commit 295ae39 的 004-pB T010 hand-back。已 git restore 恢复 + 改用 `006a-pM-T010-handback.md` 前缀名。A4 留 gen-handback.sh `--out` 默认改 `<prd_fork_id>-<task-id>-handback.md` 防未来再撞。

5. **HANDOFF.md 临时 cp 范式**:gen-handback.sh 要求 `HANDOFF.md` 真存在(非 `HANDOFF-006a-pM.bak`)· 本 ship cp .bak → HANDOFF.md 跑完立即 `git restore HANDOFF.md`(非 rm · 上次 FU-T003-stderr-fix 用 rm 真删了 HANDOFF.md,这次用 restore 范式更安全)。
