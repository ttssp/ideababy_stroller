# Forge v3 · 009-pM3prime · P3R2 · Opus 5 · finalize(strong-converge)

**Timestamp**: 2026-08-13T06:25:00Z
**Searches used**: NONE this round.
**Visibility**: 已读对方 P1 / P2 / P3R1。
**Convergence mode**: strong-converge。

## 1. 我对每条分歧的最终立场 + 让步

### 分歧 4(B 侧新提)· 探针能不能报告「边界**真信号**留存率」—— 🔴 **我全盘让步,B 侧对**

我 P3R1 §2 (c) 写的是「边界**真信号**留存率」。**这是一个范畴错误,而且是 forge v2 已经命名过的那一个。**
没有独立 validity 锚时,我们只知道**哪些候选被抽出来了**,不知道哪些是**真的**;
把「模型重复输出的一致性」当成「真值留存」,正是 v2 判过的「stability ≠ validity」换了层皮。
41 条盲标路已 CLOSED ⇒ v0.1 内**不存在**可用的真值锚。
**改为:预注册边界候选留存率(candidate retention)。** B 侧引句成立:代理量不得改名为真值效度。

> ⚠ 自记一笔:这是我本轮**第三次**被自己的证据推翻(P2 推翻 P1 两条,R2 这里第三条)。
> 前两次是我自己发现的,**这次不是** —— 说明单侧自检不足以挡住范畴替换,B 侧的独立裁决是必要的。

### 分歧 1 · 探针 vs 两个铰链 —— **收敛**

双方均同意立即执行,且 hinge 作为输出。差别已被分歧 4 吸收:**信度铰链探针能测,效度铰链探针不能测**。
⇒ 探针的输出是**两个**:跨次信度(可测)+ 候选留存(可测);**效度铰链在 v0.1 内保持公开未解**,写进 v0.2 note。

### 分歧 2 · C4 形态 —— **收敛**,采 B 侧措辞

`pass_semantic: none`。强制的是**运行 / 申报 / 落库 / 身份绑定**,不是用未校准数字新建门槛。
我接受 B 侧的补充:**report-only ≠ optional**。未来若要设门禁,必须**升 `caliber_version`**,
并在**看相关结果之前**预注册口径、阈值、适用模型与校准样本。

### 分歧 3 · CN/HK —— **收敛**,采 B 侧的双态表述(比我的更准)

**执行态 `STOP` 不撤销;认识态改为 `UNKNOWN`(measurement-invalid)。**
旧「真稀疏」结论**作废**。B 侧的 ledger 双记要求(同存执行态与认识态 + 旧理由失效证据 + 科学重判触发条件)我采纳 —— 它比我"改性质"的说法可落地。

### 我的 R2 债务 —— **偿付**(B 侧准入条件:四类上限 + 唯一命令 + 三态判据,缺一即反对)

**🔴 前置依赖(硬)**:探针必须在 **(a) 有界语义重试落地之后**跑。
证据:p≈0.17%/条,102 条一跑 P(完成)≈84%,6 跑全完成 ≈**35%** —— **不先修 (a),探针自己就跑不完**。
而 (a) 有效的证据已在场:PREREG §10.4 问题 1 —— 那条失败记录**重试 3 次一次都没复现**(失败是随机的,不是脏数据)。

**四类上限(全部由实测外推,非拍脑袋)**

| 上限 | 值 | 依据 |
|---|---|---|
| **N(重复次数)** | **6**(分成两个**不相交**的 3 次子集,用于比较"聚合 vs 单次") | 需要 ≥2 个独立聚合结果才能测"聚合的可复现性";3+3 是最小可行 |
| **记录数** | **102 条**(= `--sample-size 40` + 原始 `collection.db` + `caliber_version=v1` 的确定性抽样结果) | **与已有三次读数是同一批记录**(抽样确定性,PREREG §10.5 已验证 records=102 两跑一致)⇒ 新旧可直接比对,不额外花钱 |
| **单跑墙钟** | **≤ 35 min**(超时即 abort 该跑) | 实测 8 s 固定 + 0.0006 s/字符;该 102 条含 qa 短文,取上界 ~20 min,留 75% 余量 |
| **总成本** | **≤ 6 × 35 min = 3.5 h** · **≤ $10** | 钱不是约束(PREREG §6:400 万 input token 约个位数美元;本探针约 1/3 量) |

**唯一命令**(诚实交代边界):**跑数据**可以是一条 —— 一个 6 次循环的 shell 命令,复用现有 CLI,每跑写独立 `--db`。
**但算指标需要一个新脚本**(现 CLI 无重复/聚合/比对能力)⇒ **探针是 XenoDev 侧一个 task,不是 IDS 里敲一行**。
这一点不掩饰:「一条命令可验证」在当前代码下**做不到零改动满足**。

**逐项 pass / fail / indeterminate 三态判据**(**全部是比较型,不含事前拍的绝对阈值** —— 因为我们已经看过 0.44)

| 指标 | pass | fail | indeterminate |
|---|---|---|---|
| **M1 · 聚合是否提高可复现性**<br>`J_agg` = 两个 3 次聚合结果间的 Jaccard;`J_1` = 单次读数两两 Jaccard 的中位数 | `J_agg` 的 bootstrap 95% CI **下界 > `J_1`** | CI **上界 ≤ `J_1`** | CI **跨过 `J_1`** ⇒ **无区分力**(K 点名要求这一态可判) |
| **M2 · 候选留存率**<br>k-of-3 多数票后候选数 / 单次候选数中位数 | — | — | **无 pass 语义,只申报**(无 validity 锚 ⇒ 任何阈值都在假装知道哪些是真的) |
| **M3 · 留存的分层结构**<br>候选按"出现在几次跑里"分层的分布 | — | — | **无 pass 语义,只申报**;若留存几乎全是高频候选 ⇒ 按 arXiv 2404.05088 预期,**显式记录该风险** |

**预先承诺的解读**(反 p-hacking · 事后不得改):
- M1 = fail 或 indeterminate ⇒ **聚合这条路在 v0.1 内不成立** ⇒ 触发**路线改判议题**(不是自动改判,是必须起 forge v4)
- M1 = pass 且 **M2 < 50%** ⇒ 聚合可用,**但稳定性是靠丢掉一半候选换来的**;该代价必须写进 verdict,
  且 E2 若消费聚合语料,其密度读数**不得与历史 41 / 73 / 71 直接比较**(不同测量对象)

**停止规则**:**6 跑跑完即停**,不因结果难看加跑。任一跑 `census_invalid` ⇒ 按机械规则重跑该次,
**最多 2 次补跑**;补跑仍失败 ⇒ 整个探针判 **indeterminate**(不是 fail)。

## 2. 联合 verdict(单一)

**E1 提取器在 v0.1 内可继续使用,但其单次输出即刻失去"事实"地位;扩样与 E2 继续 blocked。**

四条同时生效:

**(a) 有界语义重试** —— 在宣告 record 失败前补应用层重试(覆盖空 content / 坏 JSON / `usage` 缺失,SDK 传输层重试不覆盖这三类)。这是**落实既有 SLA**「调用失败 → 重试/退避 · **连续**失败 hard-fail」,**零冻结项改动**;重试耗尽仍 hard-fail,KG-40 不动。

**(b) C4 改形态,`pass_semantic: none`** —— spec C4 / SLA E1「同文同版 100% 同输出」在 API 消费者位置上**原理不可满足**(修复位点在推理引擎 kernel 的 batch invariance;DeepSeek 官方无 `seed`、无可复现性承诺)。改为**强制测量 + 申报 + 落库 + 身份绑定**(`system_fingerprint` / `service_tier` / 语料内容哈希)。**report-only ≠ optional**。设门禁须升 `caliber_version` 并在看结果前预注册。

**(c) 一次有界预注册聚合探针** —— 上限 N=6 / 102 条 / 单跑 ≤35 min / 总 ≤3.5 h、≤$10;三态判据与解读见 §1;**必须在 (a) 之后跑**。它**只测**跨次信度与**候选**留存,**不得**自称测得真信号留存。

**(d) CN/HK 双态改判** —— 执行态 `STOP` 保留(fail-closed),认识态改 `UNKNOWN`(measurement-invalid);
旧「真稀疏」判断**作废**;ledger 双记 + 记明旧理由失效证据与科学重判触发条件。理由只许来自科学效度,不得诉诸市场价值。

**路线不改判** —— 但改判的触发条件已被 (c) 的 M1 判据**事前冻结**:M1 = fail 或 indeterminate ⇒ 必须起 forge v4 议路线。

## 3. 残余分歧 → v0.2 note

1. **效度铰链在 v0.1 内公开未解**。41 条盲标路 CLOSED ⇒ 无真值锚。聚合能不能保住构念效度,**v0.1 内无法回答**。任何文档不得把 M1=pass 表述为"效度已验"。
2. **周期校准的周期与升版责任未定**。双方 §4 都判 ⚠。测量**义务**已定形,但"多久跑一次、谁负责、变到什么程度触发升版"未定。归 (b) 落地时补。
3. **trial ledger 的 grain 需要新维度**(execution/run identity + 模型身份 + 语料内容身份),双方已同意病因,但**新 grain 的具体形态本轮未设计**。它会改 DSR/PBO 的分母语义,**不宜顺手改**。
4. **`_N_MAX = 12` 顶满的因果未测**。它是"内容不稳定"的候选机制之一(顶满 ⇒ 留哪 12 条任意),但本轮**没有实测归因**。放宽 `_N_MAX` 看提案数分布是否仍顶满是**零成本可证伪**的,可作为 (c) 的顺带观测,**但不作为 (c) 的 pass 条件**。

## 4. W 形态产出的初步草稿建议(给 synthesizer)

**verdict-only** —— 取 §2 全文;开头一句必须是「单次输出即刻失去事实地位」,因为那是 operator 唯一必须立刻改变的行为。

**decision-list(4 列矩阵)** —— 建议行:
`census.py 应用层重试`(新增)· `spec C4 / SLA E1 提取确定性行`(调整 · 形态改)·
`test_same_text_same_version_same_output`(调整 · 现为净负资产,改成申报型断言或标记失效)·
`system_fingerprint / service_tier 落库`(新增)· `语料内容哈希`(新增)·
`trial_ledger grain`(调整 · **但只登记议题,本轮不设计**)· `PRD §7 红线 3 措辞`(调整)·
`PRD §6 CN/HK STOP 注记`(调整 · 双态)· `聚合探针 task`(新增)·
`扩样 / E2`(保留 blocked · **不动**)· `41 条盲标路`(保留 CLOSED · **不动**)。
每行第 6 列 obligation path 必须填;`扩样` 与 `41 条` 两行填 `N/A(维持原状)`。

**refactor-plan** —— 按模块分三组,**顺序是硬的**:
① `census.py` 重试 + 身份落库(解 ② 与 ①);
② spec/SLA/PRD 三处措辞同步改形态(解 C4 假绿);
③ 聚合探针 task(**依赖 ①**)。
③ 之前不得动扩样。`trial_ledger` grain 与 `_N_MAX` 归 v0.2 note,不进本次 refactor。
