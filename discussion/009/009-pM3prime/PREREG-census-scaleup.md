# 预注册 · E1 普查扩样 + 收益检验规格(草稿 · 待 operator 签署)

**Status**: 🟡 **DRAFT — 未签署 · 未授权任何执行**
**Drafted**: 2026-08-12
**Drafted by**: IDS session(`/handback-review 009` 决议第 33 条之后的延伸讨论)
**Fork**: 009-pM3prime
**关联**: PRD v0.9 · HANDBACK-LOG 第 33 条 · forge v5「两级证据制」· PRD §7 trial ledger 记账义务

---

## 0. 这份文件是什么 / 不是什么

**是什么**:在**看到任何收益数字之前**,把「要提取多大范围的语料」和「拿到数据之后怎么检验」两件事**先钉死**。
签署之后,执行方没有解释空间;检验结果无论好坏都必须按本文的规格解读。

**不是什么**:
- ❌ **不授权 E2**(历史窗回测)。E2 在 PRD §5 是显式 Scope OUT,授权是独立的 operator 决定。
  本文只是**预先把 E2 真被授权时要用的规格冻结下来**,免得到时候看着数据现定规则。
- ❌ **不改任何已冻结的口径**(什么算信号、rejection taxonomy、密度阈值 `30/stratum`、
  `caliber_version=v1`、`caliber_hash=46fbd4b7b46d397b`)—— 全部原样沿用。
- ❌ **不重开 41 条盲标路**(那条已在 v0.1 CLOSED,见 PRD §4-E1 块 (p))。本文与金标验收**无关**,
  走的是完全不同的证据路线(市场结果,不是人工标注)。

**为什么现在写**:现在是**唯一干净的时点**。密度数字已经见过了,但**任何收益数字都还没产生过**。
过了这个点再定规则,就无法排除「看着结果定标准」。写这份文件的成本为零,拖过去的成本不可逆。

---

## 1. 触发本文的事实(2026-08-12 实算)

E1 现有的 41 条信号,**统计效力不足以回答任何收益问题**:

**命中率检验(对硬币基线 · 单尾 α=0.05)**

| 项 | 值 |
|---|---|
| n=41 拒绝硬币需要 | **≥27/41 = 65.9%** |
| 真实命中率 60% 时的检出力 | **27.5%**(七成概率漏检一个真有本事的人) |
| 真实命中率 65% 时的检出力 | **52.6%**(掷硬币) |
| 真实命中率 70% 时的检出力 | 77.6% |
| 可靠检出 60% 所需样本量 | **n = 158** |

**而且有效样本远小于 41**(下方 §4 的检验规格必须处理这一点):

- 半导体/科技集中度 **25/41 = 61%**,NVDA 单票 **10/41 = 24%**
- 方向集中度 **long 32/41 = 78%**
- **只有 13 个不同时点**、14 个 source 簇(加权平均簇大小 4.61)
- 同源相关 ICC=0.2 时,`n_eff ≈ 24`;叠加因子集中度后,真实独立下注数**大概率是个位数**

**顺带结论(可闭合一个挂起项)**:groundedness 那条挂起 lane 的效力问题现在有答案了。
门槛 5%,n=41 上观察到 2/41 的 95% CI 是 **[1.3%, 16.1%]** —— **「5% 合格」与「16% 不合格」
在这个样本量下区分不了**。⇒ **无区分力**。PRD (e) 早已写明该结论完全可接受,建议据此正式收口。

---

## 2. 根因:41 不是天花板,是抽样的产物

`sampling.py` 里 `_N_PER_STRATUM = 40`(每 form 抽 40 条),3 form × 40 = 120,
扣掉 OOS 窗内与空文本后实际处理 **102 条记录 → 41 条信号**。

**语料库里有 9081 条记录。普查覆盖率 = 1.1%。**

各 form 的实测产出率(来自那次真跑,按 OOS 比例折算处理数):

| form | 处理数 | 产出信号 | 产出率 | 可用池(已扣 OOS) | 全量外推 |
|---|---|---|---|---|---|
| **article** | ≈36 | **27** | ≈0.75/条 | **1055** | **≈790** |
| **replay** | ≈35 | 13 | ≈0.37/条 | **310** | **≈115** |
| qa | ≈32 | **1** | ≈0.03/条 | 6000 | ≈187 |

⚠ **产出率的不确定性很大**(每 form 只有 40 条的基数),外推值应当作数量级参考,不是承诺。

---

## 3. 冻结项 A · 提取范围

### 3.1 纳入(article + replay 全量)

```
form ∈ {article, replay}
AND capture_status = 'captured'
AND NOT is_in_oos_window(published_at)        # C12 冻结窗 [2025-12-01, 2026-02-01)
AND text 非空
```

预期规模:**≈1365 条记录 → ≈900 条信号**(其中 US 约占 98%,按 E1 实测 US 40 / CN 1 / HK 0)。

### 3.2 排除 qa,及理由

**qa 不纳入本批。** 6000 条可用记录换约 187 条信号 —— **成本是 article 路线的 32 倍,增量只有 20%**。

⚠ **这是效率取舍,不是「qa 没有信号」的结论。** 如果日后要纳入 qa,**必须作为独立的新批次
重新预注册**,不得中途追加进本批(中途追加 = 变相的 optional stopping)。

### 3.3 与既有 41 条的关系

`select_source_ids` 是**确定性**的(按 `sha256(seed_key + source_id)` 排序取前 n),所以把 n 从 40
扩到全池时,**原来那 40 条仍在新集合的前 40 位** ⇒ **现有 41 条信号是新批次的子集,不丢弃、不重跑冲突**。

⚠ 已知且已接受:这 41 条的**机器答案**已泄漏约 19 条(见 PRD 块 (p))。**对本检验无影响** ——
本检验的裁判是市场收益,而**任何收益数字从未被计算过**。泄漏的是「机器认为这是哪只票」,
不是「这只票后来涨没涨」。

### 3.4 ⚠ 治理说明:这是在改一个已冻结的常量

`_N_PER_STRATUM = 40` 的源码注释写着「冻结候选 · census 消费此方案 · **不临时缩**」。本文是在**扩**它,
必须说清为什么这不是「移动球门」(PRD §4-E1 (f) 判据):

| (f) 判据 | 本次是否触发 |
|---|---|
| 看数后**下调**既有阈值数字 = 移动球门(禁) | ❌ 不触发 —— 是**上调**样本量,且**没有任何阈值被改动**(密度阈值 30/stratum、`caliber_version`、`caliber_hash` 全部不动) |
| 目的是让某个**已有的**门槛通过 | ❌ 不触发 —— 密度关早已按 advisory 语义**终裁完毕**(US go),扩样**不重开该结论** |
| 目的是让一个**从未跑过**的检验具备效力 | ✅ 就是这个 —— 且**该检验的任何结果都尚未存在** |

**判定:不构成移动球门。** 但本条必须写进 trial ledger,由后来者复核。

---

## 4. 冻结项 B · 检验规格(E2 真被授权时按此执行,不得临场改)

> ⚠ 本节的具体选择**需要 operator 拍板**(见 §7 待决项)。下列是建议值,签署时逐条确认。

### 4.1 数据集切分(利用已冻结的 OOS 窗)

| 集合 | 定义 | 用途 | 可看几次 |
|---|---|---|---|
| **开发集** | §3.1 全部(OOS 窗外) | 调口径、看失败模式、做诊断 | 不限 |
| **确认集** | **OOS 窗 `[2025-12-01, 2026-02-01)` 内的 article + replay**(≈171 条记录 → 约 110 条信号) | **最终检验** | **仅一次** |

**这块确认集是现成的**:C12 从设计之初就把它排除在 census 之外,**至今零次接触**。
它正好解决「定时校准提取器会不会导致过拟合」那个问题 —— **在开发集上随便调,在确认集上只开一枪。**

### 4.2 「命中」的定义

**必须是超额收益,不能是绝对涨跌。** 理由:61% 集中在半导体、78% 是 long —— 用绝对涨跌做判据,
大盘涨的时候几乎全中,测到的是 beta 不是本事。

建议定义:

```
hit = (direction == long  且 excess_return > 0)
   或 (direction == short 且 excess_return < 0)
excess_return = 标的在 horizon 内的收益 − 基准在同窗口的收益
```

**基准(待定 · §7-1)**:候选 SPY / QQQ / 同板块 ETF。⚠ 鉴于半导体集中度,用 SPY 会把
「押对了半导体」记成分析师的本事;用 SMH 则会把它扣光。**这个选择会实质改变结论,必须事前定死。**

### 4.3 Horizon(待定 · §7-2)

`extracted_signals` **没有 horizon 列**(机器该维恒 null,是 D-1 已确认的结构事实),所以 horizon
**必须在本文事前指定**,不能逐条读。

`sampling.py` 已冻结 `_HORIZONS = (5, 20, 60)` 个交易日。

⚠ **三个 horizon 全测 = 三次检验 = 多重比较**。建议:**指定 1 个为主检验**(建议 **20 日**),
另两个标为**探索性**,其结果**不得**单独用于结论、且必须与主检验一同披露。

### 4.4 主检验统计量与相关性处理

- **主检验**:命中率 vs 50%,**单尾 α=0.05**
- **⚠ 必须做相关性修正** —— §1 已实测出严重的簇结构。指定用 **按 `source_id` 与按日期的
  block bootstrap**(而非把 n 条当独立样本)。**不做这个修正,p 值是假的。**
- **同时报告**(不作为主结论):平均超额收益 + 其 clustered SE、按信号等权的组合收益曲线
- **DSR / PBO**:按 M2 既有实现跑,`trial_count` 取 trial ledger 的真实累计值(见 §5)

### 4.5 停止规则(反 p-hacking)

1. **一次性承诺提取范围**(§3),跑完为止。**不允许**「先跑一部分 → 看结果 → 决定要不要继续」
2. 主检验在确认集上**只跑一次**,结果无论好坏都写进 hand-back
3. 任何在看到结果**之后**对 §4 规格的修改,**一律作废本次检验**,必须换新 `caliber_version` 重来

### 4.6 结果的预先承诺解读

| 结果 | 事先承诺的解读 |
|---|---|
| 主检验显著 | **「这套流程(提取器+分析师)在历史窗上有超额收益」** —— 按 forge v5 两级证据制,这是**开发证据**,**不是** alpha 确证。确证仍须 forward 线 |
| 主检验不显著 | **「n≈900 上测不出超额收益」** —— **不得**改口径重测同一批数据。可以做诊断(哪类信号差),但那属于 ②-a lane,结论不得表述为「其实是有的」 |
| 开发集好、确认集差 | **以确认集为准**。这正是确认集存在的意义 |

---

## 5. Trial ledger 记账(PRD §7 强制)

本批次执行时须写入 `trial_ledger`,至少含:

- `caliber_version = v1`(不变)· `caliber_hash = 46fbd4b7b46d397b`(不变)
- `extractor_variant` = 本次实际使用的变体(温度 0 · 模型 ID · prompt 版本钉死)
- `sampled_source_ids` = §3.1 解出的完整 id 列表(**这是扩样的证据,必须落库**)
- `horizon` = §4.3 指定的主 horizon
- 一条注记指向本文件 + 签署日期

⚠ **后续每一轮提取器校准/优化都要各记一条 trial**。这是 DSR/PBO 惩罚多重检验的输入 ——
不记账 = 让防过拟合机制失效 = 自欺。

---

## 6. 成本(未实测 · 必须先做小规模计时)

**我不知道真实吞吐和花费**,不能凭估计签署。**建议签署前先跑一次计时试验**:

```
在 article 池里取 20 条(用同一确定性抽样,取前 20)→ 计时 + 记 token 消耗 → 外推 1365 条
```

已知参照(不足以外推):07-17 那次失败跑是 61 分钟 / 58 条,但那次几乎全部截断+重试,不代表正常速率。

---

## 7. 待 operator 决定的项(签署时逐条填)

| # | 待决项 | 建议 | 影响 |
|---|---|---|---|
| **7-1** | **超额收益的基准** | 需要讨论 | **会实质改变结论**。SPY = 把「押对半导体」算作本事;SMH = 扣光板块收益只留选股。两者答的是不同问题 |
| **7-2** | **主 horizon** | 20 交易日 | 另两个降为探索性 |
| **7-3** | 是否纳入 qa | 否(本批) | 32 倍成本换 20% 增量 |
| **7-4** | 确认集是否就用 OOS 窗 | 是 | 它是现成的、零接触的 |
| **7-5** | 先跑计时试验再签署? | 是 | 成本未知不宜承诺 |

---

## 8. 签署

签署即冻结 §3 与 §4,此后不得修改(修改 = 作废本次检验)。

- **Operator 签署**: ⬜ 未签署
- **签署日期**:
- **签署时的 git HEAD**:
- **§7 各项最终取值**:

---

## 9. 本文不覆盖的(明确留白)

- **E2 授权本身** —— 独立决定
- **forward 线(E3)设计** —— 仍在 Scope OUT,要动须走 forge
- **定时校准机制的具体设计** —— 已讨论出四条候选(拒绝率监控 / 双模型分歧率 / 引文支撑度 /
  市场结果反标),但**尚未预注册**,须另文
- **pre-cutoff 对照模型实验** —— 建议降级为校准工具(②-a lane),不作为本检验的前置
- **41 条盲标路** —— 已 CLOSED,与本文无关
