---
discussion_id: 009
prd_fork_id: 009-pForge
handback_id: 009-pForge-20260703T002635Z
from_build_repo: /home/ys/codes/XenoDev
to_source_repo: /Users/admin/codes/ideababy_stroller
workspace:
  source_repo: /Users/admin/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev
  working_repo: /home/ys/codes/XenoDev
  handback_target: /home/ys/codes/ideababy_stroller/discussion/009/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - feature
severity: medium
created: 2026-07-03T00:26:35Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: T006
---

# Hand-back · 009-pForge · T006

**Severity**: medium
**Tags**: feature
**Created**: 2026-07-03T00:26:35Z

## §1 Rationale(执行摘要)

T006 · 009-pForge M1 ingest CLI(P1 PPV 真路径 owner)ship:实装 python -m decision_ledger.price.ingest 真入口 → source 分流(tiingo/baostock/hk)→ 真源抓取 → 落 price_daily/ticker_status 真行,PIT 可查。M1 价格地基闭环。review:codex 额度用尽(至 Jul 7)→ subagent fallback 双跑(red-team+code-reviewer 一致锁定 B1 persist_status 吞错 + B2 baostock 会话泄漏 2 ship-blocking + N1 market silent-mismatch)→ 3 轮修复收敛(R2 复审抓 L1/L2 · R3 收敛确认 approve)→ 0 ship-blocking。退出码语义完整(0/1/2)· 凭据隔离(token env 指针)· PPV 终点真 disk sqlite 可 SELECT。

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

**tag = feature（clean task ship）**

按 frozen spec（reviewed-by codex@2026-07-02）实装 P1 PPV 真路径入口，无新 PRD 修订触发。

**交付内容**：
- `price/ingest.py`（新）：`ingest(source_name, ticker, *, market, start, end, conn, source_factory=None) -> IngestResult` 编排层。
  · source 分流（tiingo/baostock/hk）· 入参 fail-closed（非法 source / 空 ticker → ValueError）· hk 走 HKSourceDeferred 上抛 NotImplementedError（NG-12 不静默）。
  · **market fail-closed 校验**：源返回的 market 必须与 --market 一致，不一致 raise（防 --market US 静默落 CN 撞 T003 PIT 查询键）。
  · **status known-gap 精确处理**：fetch_status 只窄捕获源声明的 CoverageError（TiingoCoverageError/BaoStockCoverageError）记 status_gap；persist_status 在 try 外（真写库错如 append-only trigger IntegrityError 自然上抛，不被误当 gap）。
  · **源生命周期收口**：try/finally 调 source.close()（若有）· baostock factory login 后即检 error_code fail-closed + 附 close(logout) · banner 导 stderr（不污染 stdout PPV 契约）。
  · 持久化复用各 source 幂等 persist（不自造写库）· default_source_factory 延迟 import（顶层不拉 httpx/baostock 重依赖）。
- `price/__main__.py`（新）：argparse CLI `python -m decision_ledger.price.ingest`（P1 真入口）。
  · `--source {tiingo,baostock,hk}` `--ticker` `--market {US,HK,CN}` `--start` `--end` `--db` `--token-env`。
  · `--db` 显式路径避开 load_settings 的无关 key（ANTHROPIC_API_KEY 等）· 缺省才回落 config.db_path。
  · **退出码语义完整**：0 成功（含 rows=0 打 WARN）/ 2 用户入参或配置错（hk deferred / 非法 source / 空 ticker / market 不匹配 / token 文件缺 = NotImplementedError·ValueError·FileNotFoundError）/ 1 未预期内部失败（persist IntegrityError / DB locked / OperationalError）· 不放 raw traceback。
  · token 走 --token-env 指针（名不读值 · 凭据隔离）· DI 缝 `_open_conn`/`_build_source_factory` 供集成测试注入。
- `tests/integration/test_ingest_cli.py`（新）：24 集成测试（真 0014 迁移建表 + DI-mock 源 + tmp_path disk SELECT）。

**PPV P1 验证（真终点）**：`python -m decision_ledger.price.ingest` 真入口活（--help + hk deferred rc=2 真跑）· 真 CLI → 真 disk sqlite → 独立进程 SELECT 出真行（close_raw=180.0/adj_factor_fwd=0.9/source=baostock + status=active）· 终点是真持久化非 stdout。

**review 链（codex 额度用尽至 Jul 7 → subagent fallback 双跑 + 对抗复审 · 3 轮修复收敛）**：
- R1：codex adversarial-review 报 usage limit → 启用 fallback（CLAUDE.md 规则 · high-risk = red-team + code-reviewer 双跑）。
- R1-fb 双跑**一致锁定** 2 ship-blocking + N1：**B1**（except Exception 吞 persist_status 真写库错误标 status_gap+rc=0，red-team 用真 0014 迁移复现 IntegrityError 被吞）· **B2**（bs.login 无 logout → 会话泄漏 M1-R4 + banner 污染 stdout · 违 baostock.py 文档"login/logout 由 orchestrator T006 管"）· **N1**（--market 收了不用静默落错市场）· red-team 额外 **M2**（退出码语义不完整 · 未预期错裸 traceback+rc=1）+ N2（空 fetch 静默成功）。
- R2 修 B1/B2/N1/N2/M2/N3 → red-team 对抗复审：0 ship-blocking，但结构化 verdict 抓 2 低危 **L1**（M2 catch-all 把 token 文件缺 FileNotFoundError 误归 rc=1 · 本次引入回归）+ **L2**（bs.login 不检 error_code · pre-existing）。
- R3 修 L1（FileNotFoundError→rc=2）/L2（login error_code fail-closed）→ red-team 收敛确认 **approve**：mutation testing 证新测试非假绿（revert 真 fail）+ 真查装机 baostock 0.9.2 源确认 error_code 语义 · 提 1 pre-existing low（_open_conn 在 try 外）→ 顺手收口（纳入 try · 坏 db 路径走 rc=1 契约）。
- **最终 0 ship-blocking · reviewed-by = subagent-fallback@2026-07-03**（codex 恢复 Jul 7 后补跑权威确认 · fallback 是过桥非替代 · 不冒充 codex）。

**契约细化（impl-driven · KG-22）**：无签名变更；ingest 编排按 T003/T004/T005 已定契约装配。新增 IngestResult（source/ticker/rows_written/status_written/status_gap_reason）供 CLI 打印 + 测试断言。

## §3 Suggested IDS actions

**无需 IDS 决议动作**（tag=feature · clean ship · 非 prd-revision-trigger · 无 PRD 矛盾）。以下为知会 + 下游 task 前置 + 框架级 dogfood：

1. **知会：T006 ship 完成**，M1 ingest CLI（P1 PPV 真路径入口）就位。至此 **M1 价格地基闭环**：T001/T002 建表 → T003 PIT 查询引擎 → T004 Tiingo 美股源 → T005 BaoStock A股源 + 港股留位 → **T006 ingest CLI 把真源接进真库**。P1 PPV 真路径（CLI 触发 → 真源抓取 → 落 price_daily/ticker_status 真行 → PIT 可查）已由集成测试端到端验证。下游解锁：T007（M1 contract tests · depends T006）。

2. **T007 前置（记录 · 非 IDS 动作）**：T006 blocks T007（contract tests）。ingest CLI 入口 = `python -m decision_ledger.price.ingest --source {tiingo,baostock,hk} --ticker <t> --market {US,HK,CN} --start --end --db [--token-env]`。T007 可用此真入口 + 真 sqlite 写 contract test 断言 P1 全链（拆分样例正确 + 退市样例非静默空）。

3. **P1 PPV 真源实测 deferred（同 T004/T005 · M1-R1）**：集成测试用 DI-mock 源 + 真 disk sqlite 覆盖 CLI/编排/持久化全链（真 0014 迁移建表 + 真 persist 幂等 + append-only trigger + 独立进程 SELECT）；真 baostock login+网络 / 真 tiingo token 的端到端标 pending（环境就绪解）。CLI 真入口本身已真跑验证（--help + hk deferred rc=2）。

4. **框架级 dogfood 兑现记录（KG-32 实战 · 第三次）**：本 task 是 CLAUDE.md「cross-model review fallback」+ KG-32 的**第三次实战**（T003=首次 codex 补跑抓深 bug · T005=red-team 抓 codex 漏的重复日 · T006=本次）。本次**再次验证双跑价值**：code-reviewer 与 red-team **独立一致**锁定 B1/B2/N1 两个 ship-blocking + market silent-mismatch（两视角互证，非单一 reviewer 偶得）；且 red-team 对抗复审的**结构化 verdict** 比其 prose 摘要更严（抓出 prose 没强调的 L1 本次引入回归），说明对抗复审 + mutation testing 的必要性。**codex 恢复（Jul 7）后应对 T006 补跑 adversarial-review 作权威确认**（spine · PPV owner · fallback 是过桥非替代 · reviewed-by 已标 subagent-fallback@2026-07-03 不冒充 codex）。建议 `/expert-forge` 纳入 KG-32（codex-review SKILL 加 fallback 分流 · SKILL 是 bootstrap-kit mirror 件，真正入 SKILL/SHARED-CONTRACT 回 IDS forge 定）。

5. **impl-driven 契约细化知会（KG-22 · 非 scope 变更）**：T006 编排层新增两处 fail-closed 硬约束（spec 未显式要求但符合"不静默"哲学）：① market 一致性校验（--market 与源返回不符 → raise · 防撞 T003 PIT (ticker,market) 查询键）；② status 处理拆分（fetch_status 的源 CoverageError = known-gap 记原因，persist_status 的写库冲突 = 真错上抛）。均为安全加固，若 operator 认为需更新 spec 文字可在 review 时标注。
