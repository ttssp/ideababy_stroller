---
discussion_id: 009
prd_fork_id: 009-pM3prime
handback_id: 009-pM3prime-20260813T093952Z
from_build_repo: /home/ys/codes/XenoDev-009
to_source_repo: /home/ys/codes/ideababy_stroller
workspace:
  source_repo: /home/ys/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev-009
  working_repo: /home/ys/codes/XenoDev-009
  handback_target: /home/ys/codes/ideababy_stroller/discussion/009/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "0de4fb398ede9db1"
tags:
  - practice-stats
severity: medium
created: 2026-08-13T09:39:52Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: T014
related_spec_section: spec.md O10/C4 · SLA.md 提取稳定性行 · migration 0022
---

# Hand-back · 009-pM3prime · T014

**Severity**: medium
**Tags**: practice-stats
**Created**: 2026-08-13T09:39:52Z

## §1 Rationale(执行摘要)

**T014**(`specs/009-pM3prime/tasks/T014.md`)落地 forge `009-pM3prime` v3 verdict 的实装部分,消化
obligation-ledger 四条待决义务(`OB-009-pM3prime-v3-01`/`-02`/`-03`/`-05`,均 `due_gate: xenodev:task-review`)。

- **OB-01**(`census.py` 应用层语义重试):按 `RETRY-CONTRACT-20260813.md`「A + 判据 + 证不出则降级 B」设计,
  只对 `api_failure`/`parse_failure`(空 content / 坏 JSON / usage 缺失)重试,`truncation_failure` 不重试;
  k=3(1 次初跑 + 2 次重试),指数退避 1s→4s;重试耗尽后仍走原 hard-fail 路径,R10 早停语义、`stats` 计数
  语义(耗尽只计一次非逐次累加)均不变。B 分支(三态 + 区间密度)本轮未实装,按义务原文留给真跑证明后触发。
- **OB-02**(`system_fingerprint`/`service_tier` 落库):API 响应里已有但从未读取的两个身份字段,现在随每次
  attempt 写入新表 `extraction_attempt_log`(migration `0022`)。
- **OB-03**(语料内容身份四元组):`trial_ledger` 新增 5 可空列(`run_id` + `content_hash_{algorithm,value,
  computed_at,target}`),取代原「只落一个哈希」设计——动机见同批第二条 hand-back(`20260813T072930Z`)记录的
  PREREG `checksum 00e69d77` 真实事故(只记值不记算法,事后 9 种摘要复算无一命中)。哈希覆盖面 = 影响抽取器
  输出/资格判定的全部输入(raw file text + `published_at` + `record_tickers`),单次读取(无 TOCTOU)、流式
  增量(内存有界)、哈希输入与实际送入 proposer 的 `record_tickers` 顺序一致。
- **OB-05**(C4 假绿测试改标):`test_same_text_same_version_same_output` 改名
  `test_literal_proposer_is_deterministic_but_not_evidence_for_c4`,docstring 明确其只覆盖
  `literal_ticker_proposer`(构造上确定),不为 LLM 路径的 C4 契约(`pass_semantic: none`)提供证据。

**未触碰任何冻结项**(hard constraint · 均未改动):R10 早停语义 · `caliber_hash=46fbd4b7b46d397b` ·
`threshold_hash=97c58440426f8770` · `density_threshold` · OOS window · `_N_PER_STRATUM` · `_N_MAX=12` ·
`trial_ledger` 现有列的 grain 语义 · RETRY-CONTRACT B 分支 · scale-up/E2/E3 · 41 条盲标路径 ·
已 ship 的 `tasks/T011.md`。`OB-009-pM3prime-v3-07`(聚合探针)依赖本条,本轮未起。

**验证**:新增 3 个 commit(`3acee51`/`0d9f5cb`/`82ef616`),1231 passed(基线 1209 + 22 新测试,0 回归),
ruff clean。codex adversarial-review 走 9 轮(6 轮自发起本地收敛 · round 7-9 为正式 `--wait --base --scope branch`
真路径,均已按 commit history 记入 body):round 1-6 修正核心重试/哈希逻辑的 5 类真缺陷(哈希范围/TOCTOU/内存
无界/覆盖不全/哈希-抽取表征不一致)+ 1 处测试自身假绿;round 7-9 修正 2 处诊断链路完整性(记账落库失败可编程
判断 + CLI 独立 exit code)+ 1 处文档口径不一致(SLA 字段可空性声明);round 8/9 收敛出 2 条明确超出本轮 scope
的残留 finding(见 §2)。

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

**Tag: practice-stats**(统计区间 + 数字)

- 本次 XenoDev session 单 task(T014)的完整构建活动统计:
  - 3 个直接提交到 `feat/009-spec` 的 commit(该分支已建立的直提交惯例,非 worktree+squash 流程)。
  - codex adversarial-review 共 9 轮(6 轮自发起本地迭代收敛 + 3 轮正式 `--wait --base <ref> --scope branch`
    真路径调用);前两次正式调用因 scope 选错(`--base main` 把整条 78-commit/214-file 分支历史都审了一遍,
    带出 2 条与 T014 无关的历史代码 finding)重跑,之后统一改用 `--base HEAD~N` 精确锁定本 task 的 commit 范围。
  - 无 operator 中途干预;全部 9 轮 finding 均为 codex 自主抓出,自主修复或自主判定 out-of-scope。
  - 最终 codex verdict 仍是 `needs-attention`(round 9),剩 2 条残留 finding——见下方「触发理由」。本 task
    未达到 codex `approve`,是有意识地在 4 轮软上限之上继续跑到第 9 轮后主动停止(判断为已收敛:round 8→9
    的 2 条 finding 内容不变,说明真正可动的 in-scope 问题已耗尽,剩下的是需要 scope 决策的架构问题)。

**为什么 round 9 仍是 needs-attention,但判断可以停止迭代**

round 7 起,codex 找到的 finding 分两类:
1. **诊断链路自身的完整性问题**(可低成本修复、未越 T014 scope):round 7 的「记账失败无编程信号」、round 8 的
   「CLI 未读取该信号」——均已修复(见 §1「验证」)。
2. **诊断链路自身的健壮性问题**(需要新的架构决策、明显越 T014 scope):round 8/9 稳定重复的 2 条:
   - **[high] 进程崩溃窗口丢审计**:`extraction_attempt_log` 现设计是 run 末尾批量落库(独立于主事务,详见
     `_persist_attempt_log` docstring),若进程被 SIGKILL/OOM/宿主重启打断,内存中未落库的 attempt 会丢失。
     要做到真正崩溃可恢复,需要「每次 API 响应立即落库(写穿透)」——是比本任务大得多的架构决策(连接管理/
     幂等约束/恢复检查),且这类基础设施服务的对象正是 `OB-009-pM3prime-v3-07`(聚合探针)。
     operator 已在原始授权中明确「OB-07 依赖 OB-01,本轮不起」——本 finding 属于 OB-07 的前置基础设施,因此
     判定超出本轮 scope。此外,这与 census 主数据本身既有的「单原子事务·中途崩溃丢整 run 进度·可重跑」设计
     哲学一致(该设计已有的代码注释明确将其列为可接受 tradeoff),不是本次改动引入的新退化。
   - **[medium] 失败批次无法绑定语料内容身份**:`extraction_attempt_log` 目前只记 `source_id`,不记内容哈希;
     语料四元组只落 `trial_ledger`,而 `census_invalid` 会回滚这些行——所以跨批次比对「同一条 record 是否
     反复失败」时,理论上无法排除是语料本身(raw text/published_at/record_tickers)在两次失败之间发生了变化。
     修复需要给 `extraction_attempt_log` 加 per-record 内容哈希列并穿透 proposer 调用链——明显超出 OB-01
     字面记账要求(attempts/失败类型/终态/source_id/system_fingerprint/service_tier),同样是服务 OB-07
     跨批次比对的前置基础设施。

两条均记入 T014.md「Known gotchas / residual findings」,不在本轮代码中修复。

## §3 Suggested IDS actions

| # | Action | 类型 | 优先级 | 备注 |
|---|---|---|---|---|
| 1 | 将 `OB-009-pM3prime-v3-01`/`-02`/`-03`/`-05` 标记 `satisfied`(append 新 ledger 行,不改历史行) | ledger 更新 | 高 | 本 hand-back §1 已列各条的具体落地位置(migration `0022` + `census.py`)· 可机械核对 |
| 2 | 决定「进程崩溃窗口丢审计」+「失败批次无法绑定语料内容身份」两条残留 finding 是否并入 `OB-009-pM3prime-v3-07`(聚合探针)前置基础设施范围,还是独立记新 obligation | scope 决策 | 中 | 两条均是 codex round 8/9 稳定复现的 finding(见 §2),XenoDev 侧已判定超出 T014 授权 scope,需 IDS 显式决议归属,不建议 XenoDev 侧自行拍板起 OB-07 相关工作 |
| 3 | codex-review SKILL 硬编码 macOS 路径 + 钉死 `1.0.3`(本机 Ubuntu 实际 `1.0.4`)导致本次全程手动指定真实路径绕过 | 框架级 drift(非本包 tag,仅供 IDS 参考) | 低 | **非新发现**——`dogfood-backlog.md` 已有 `KG-27` 记录同一问题(spec-writer/parallel-builder/codex-review 三 SKILL 均硬编码);本次只是又一次真实复现,不需再记新条目,不需 IDS 本次动作 |

[ ] 收悉,`OB-01`/`-02`/`-03`/`-05` 记 `satisfied`,作为 practice-stats 入库
[ ] 就残留 finding 的 OB-07 归属给出决议
[ ] 无操作
