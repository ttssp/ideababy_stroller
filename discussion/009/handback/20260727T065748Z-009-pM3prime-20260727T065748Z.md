---
discussion_id: 009
prd_fork_id: 009-pM3prime
handback_id: 009-pM3prime-20260727T065748Z
from_build_repo: /home/ys/codes/XenoDev-009
to_source_repo: /Users/admin/codes/ideababy_stroller
workspace:
  source_repo: /Users/admin/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev-009
  working_repo: /home/ys/codes/XenoDev-009
  handback_target: /home/ys/codes/ideababy_stroller/discussion/009/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "0de4fb398ede9db1"
tags:
  - drift
  - prd-revision-trigger
severity: high
created: 2026-07-27T06:57:48Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: FU-KG43
---

# Hand-back · 009-pM3prime · FU-KG43

**Severity**: high
**Tags**: drift,prd-revision-trigger
**Created**: 2026-07-27T06:57:48Z

## §1 Rationale(执行摘要)

**两层金标 Layer-2 的冻结门槛组合在数学上不可达 —— 真跑前的零成本可达性验证推翻 LOG 第 27 条「harness 全就位·待真跑」结论。operator 41 条盲标劳动在门槛解锁前产不出有效 verdict。需 IDS 决议修 SLA 门槛表 / Layer-2 验收定义。**

## 背景与触发

LOG 第 27 条(D-7 改写 build 侧落地)决议残留 defer ① = 「operator 真盲标产真数字」。本 session 按该 defer 起 Layer-2 真跑,环境复核全绿(alembic `0018` · `extracted_signals` 41 条 / 14 distinct source · `gold_layer2_result`/`_dimension`/`watchlist` 三表已建且 0 行未污染 · 双备份在位 · baseline `1054 passed`)。

在向 operator 发出「请交 human/gold/watchlist 三份文件」的 judgment gate **之前**,先做了一次机械可达性验证(合成数据 · 内存 DB · 未碰真盘 · 未向 operator 展示任何机器答案)。结果:**在「operator 盲标 100% 完美 + watchlist 全命中 + 时序合规」的理想上界下,`passed` 仍恒为 0**。

发现三处结构不自洽,第一处数学封死。

## D-1(致命 · 数学不可达)· 粗一致率关 × sector/horizon 覆盖率关 互斥

**根因**:`extracted_signals` 无 sector/horizon 列 → machine 这两维恒 `null`(实测 `machine.json` 该两维 0/41 非 null)。而 `gold_layer2.py:60` 的 `mapping_agreement_rate` 把「machine `null` vs human 有值」计为**不命中**。

**严格推导**(设 human 中 sector 弃权 x 条、horizon 弃权 y 条 · n=41):

- 粗一致率关(`gold_layer2.py:369` 合取首项 `rate >= threshold`),ticker/direction/timepoint 三维 100% 全对的上界:
  `rate = (3×41 + x + y) / (5×41) = (123 + x + y)/205 >= 0.70` → `x + y >= 20.5` → **x + y >= 21**
- 覆盖率关(`gold_layer2.py:350-353` · SLA 冻结 sector/horizon 均 0.75):
  `coverage(sector) = coverage_denominator/41 >= 0.75` → `coverage_denominator >= 31` → 而 `coverage_denominator <= 41 - x` → `x <= 10`;同理 `y <= 10` → **x + y <= 20**

**矛盾,恰好差 1。** 理论上界 `143/205 = 0.69756 < 0.70`。**与 operator 标注质量完全无关 —— 不存在任何一组 human 标注能使 passed=1。**

**探针实测**(三组合成实验 · 全部 `passed=False`):

| 实验 | 粗一致率 | 逐维门槛 | passed |
|---|---|---|---|
| A1 · human 填 sector/horizon | **0.600** < 0.70 ✗ | 五维双错率全 **0.0** · 覆盖率全 OK · ticker 锚 **1.0** ✓ | **False** |
| A2 · human 弃权 sector/horizon | 1.000 ✓ | sector/horizon 覆盖 **0.000** < 0.75 ✗ | **False** |
| B · 填 + 仅 1 条 direction 分歧 | 0.595 ✗ | 五维全绿,但 `unresolved_conflict=1` | **False** |

A1 尤其说明问题:**逐维双错率全 0.0(完美)、覆盖率全达标、ticker 外证据锚 1.0 —— 即 D-7 三分离契约(错误独立性 / 领域能力 / 仲裁权)全部满分 —— 仍被 legacy 粗一致率关一票否决。**

**可复现证据**:`projects/004-pB/scripts/layer2-feasibility-probe.py`(本 hand-back 随附 · 零成本 · 无 LLM · 只读抄真 schema 建内存库不碰真盘 · 跑完 <1s)。

    uv run python scripts/layer2-feasibility-probe.py    # exit 1 = 门槛不可达(当前状态)· exit 0 = 可达

门槛**逐维等于** SLA 冻结值(未传任何偏离值 · C6)· 该脚本可直接用于验证任何修订方案是否真的解开死锁。

## D-2(高概率 · 无出口)· CLI 未暴露 dimension_arbitration

`blind_annotate.py:305` 的 `_run_reconcile` 调 `reconcile()` 时**不传** `dimension_arbitration`(CLI 无对应 flag)→ 恒为 `{}`。而 `gold_layer2.py:223` `_has_unresolved_conflict` 规则:direction/timepoint 维只要有**任意一条** human 非弃权且 ≠ machine → `unresolved_conflict=True` → `passed=0`。

叠加覆盖率门槛(timepoint 0.90 / direction 0.80)后的实际要求:**timepoint ≥37 条盲标日期须与提取器 `published_at` 逐条一字不差,direction ≥33 条逐条一致。**

方法论自反:**第二源验收的价值在于独立标注可能与机器不同,而当前通过条件是零分歧且无仲裁出口。** 库层 `reconcile()` 已有 `dimension_arbitration` 参数(C9 仲裁留痕设计存在),只是 CLI 未暴露 —— 能力在、入口缺。

## D-3(定义缺口 · 非死锁但须写进 spec)· sector/horizon 双错率测的不是 machine

machine 这两维恒 `null` ≠ gold 有值 → machine 在这两维 **100% 错**。而「双错」定义为 machine 错 ∧ human 错 → 该两维的 `double_error_rate` **恒等于 human ≠ gold 的比例**,即 operator 盲标与自身考据的自洽性。

SLA 表已注「sector 外证据锚:无(B6)」,但**未说明 machine 结构性弃权导致该维测量对象漂移** —— 数字会被误读为「机器板块判得准」。这是 B5 诚实边界的加强版,须在定义补全里写明。

## 为什么回 IDS 而非 XenoDev 自决

D-1 的三个解法(改 `--threshold` / 改 `mapping_agreement_rate` 分母语义 / 新契约路弃用 legacy agreement 关)**都触及 spec §O8 与 SLA 冻结验收定义**;D-2 触及 C9 仲裁契约的暴露面。CLAUDE.md 决策表「PRD 约束不可达 / spec 与 PRD 矛盾 → 切回 IDS 修」。

尤其:**在看到「过不了」之后由 build 侧自行下调门槛 = 移动球门 = C6 自欺 = V4 失败模式**,即使 `--threshold` 严格讲不在 SLA 冻结三组门槛内(SLA §Layer-2 冻结的是逐维双错率 / 覆盖率 / ticker 锚),XenoDev 也不自决。本 fork **零代码改动**,仅产本 hand-back。

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

本 hand-back 携 `drift` + `prd-revision-trigger` 双 tag,触发原因与决议链:

## 1. `drift` — 预期 vs 实际(spec/SLA 与 harness 实际不符)

**预期**(LOG 第 27 条决议 + SLA §Layer-2 门槛表状态注):
> 「Layer-2 门槛已从『blocked-on 领域真人』升级为『已预注册 · 待 operator 真盲标跑』—— harness 扩契约已实装经测」
> 「解锁路径(operator 真盲标 · 零代码):`annotate` → `reconcile` → 写结果行 → `gate._both_layers_passed` 自动转真」

**实际**:解锁路径在当前门槛组合下**数学封死**。`gate._both_layers_passed` 无论 operator 标得多准都不会转真。「零代码解锁」不成立 —— 必须先改门槛定义或改 `mapping_agreement_rate` 语义。

**证据**:
- 数学推导(§1 D-1):理论上界 `143/205 = 0.69756 < 0.70`,矛盾差额恰好 1 个弃权名额。
- 探针实测:三组理想上界合成实验全 `passed=False`,其中 A1 组 **D-7 三分离契约全部满分**(逐维双错率 0.0 / 覆盖率全达标 / ticker 锚 1.0)仍被 legacy 粗一致率关否决。
- 结构实测(未泄露任何机器答案):`machine.json` 41 条,`sector` 0/41 非 null,`horizon` 0/41 非 null,`ticker`/`direction`/`timepoint` 均 41/41。
- 环境复核全绿:alembic `0018` · `extracted_signals` 41 / 14 distinct source · 三表 0 行未污染 · baseline `1054 passed`。

**drift 的性质**:这**不是** build 实装偏离 spec —— `gold_layer2.py` / `blind_annotate.py` 严格实现了 spec 与 SLA 写下的规则,4 轮 subagent-fallback 对抗审也 approve。drift 在于 **spec/SLA 写下的规则组合本身内部不自洽**,而这个不自洽在冻结时无人验算(forge v1 推演 + build review 均未算可达性)。

**这是「forge 推演 ≠ 真跑可行」的第五次实证**(前四次:001 首跑 FAIL / operator 拒 gate 形态 / KG-B9 循环依赖 / D' rerun 暴露 a1 验证盲区)。本次特殊之处:**只花零成本合成探针就在 operator 投入劳动前抓到**,未烧任何 token、未消耗任何不可逆的盲标机会。

## 2. `prd-revision-trigger` — 哪条约束不可达 + 建议修订方向

**不可达的约束**:E2 解锁三条件之 ② 「两层金标过」。Layer-2 在当前 SLA 冻结门槛 + `--threshold 0.70` 组合下 `passed` 恒 0。

**三个候选修订方向**(IDS / forge 拍板 · XenoDev 不自决):

- **(a) 新契约路弃用 legacy 粗一致率关** —— 理据最强。`gold_layer2.py:297` 的 `mapping_agreement_rate` 是 D-7 改写前的旧指标,SLA §Layer-2 已明确「第二源资格从『输出一致率』refactor 为『逐维双错率 + abstain 覆盖率 + 外证据锚 + 盲标隔离』」。但 `evaluate_layer2:369` 的合取**仍保留 `rate >= threshold` 作首项** —— refactor 未清干净,新旧两套判据叠加。建议:`gold` 给出的新契约路不再叠加 agreement 关(legacy 路保留兼容旧测)。
- **(b) 修 `mapping_agreement_rate` 分母语义** —— 「machine 结构性不产该维」不应计为「与 human 不一致」。可改为该维双方均 `null` 或 machine 侧结构性缺维时排除出分母。影响面:4 个既有 legacy 测。
- **(c) 下调 `--threshold`** —— 最快但最不推荐。它是「看到过不了才改数字」,即使技术上 `--threshold` 不在 SLA 冻结三组内,观感与 C6 精神冲突;且改到 ≤0.6976 才可能过,擦边过说明该指标已无区分力,保留它没有意义。

**D-2 的修订方向**:给 `blind_annotate.py` CLI 补 `--dimension-arbitration-json` 入口(库层 `reconcile()` 已有该参数 · C9 仲裁契约设计存在)。否则「第二源要通过验收只能与机器完全一致」的自反状态无解。

**D-3 的修订方向**:spec §O8 / SLA §Layer-2 定义补全 —— 写明 machine 在 sector/horizon 结构性弃权,故该两维 `double_error_rate` 实测的是 operator 盲标 vs 自身考据的自洽性,**不构成对 machine 映射正确性的验收**。同时补 gold provenance 协议(gold=operator 考据版 / human=operator 盲标版 / machine=`extracted_signals`)与 B5 诚实边界(gold 与 human 同为 operator → 共享盲点;ticker 维靠 watchlist 外锚 + abstain 担独立性;解释维本质是盲标 vs 考据的自一致性 —— 这是 v0.1 降级验收,不是技术保证)。

## 3. XenoDev 侧本 session 的处置(零代码 · 未自决)

- **未改**任何 spec / SLA 冻结门槛(C6);**未传** `--*-threshold-json`(缺省用 SLA 冻结逐维门槛,传偏离值会被 `_assert_matches_frozen_sla` raise —— 该 fail-closed 机制本 session 复核有效)。
- **未代做**三个 operator judgment gate([OP-1] watchlist curate / [OP-2] 盲标 / [OP-3] 考据)—— 代做即破坏 D-7 第二源独立性。
- **未向 operator 展示任何机器答案** —— `machine.json` 生成后只验长度与各维非 null 计数,worksheet 只含 `id/source_id/raw_ref/span_start/span_end`(无 ticker/direction/timepoint),盲标机会完好未损。
- **已产机械脚手架**(可复用 · 解门槛后零改动接上):`blind/machine.json`(41 条)+ `blind/worksheet.txt`(41 行按 id 序)。
- **已记 dogfood-backlog KG-43**(框架级 · 见 §3-2)。

## 4. 红线复核(本 session 逐条确认未破)

- `gold_layer2.py:290` 第二 LLM / 跨 vendor 未校准模型当 Layer-2 终审 → raise:**在,未动**。
- `gate.py:44` fail-closed 默认挡未验收信号:**在,未动**。
- 严禁用第二 LLM 凑 Layer-2 第二源:**未做,且本 hand-back 不建议此方向**。
- C6 门槛预注册不可回改:**未改任何冻结门槛**,D-1 的解法交 IDS 决议。

## §3 Suggested IDS actions

operator 在 IDS 跑 `/handback-review 009` 决议,建议动作(按优先级):

| # | 建议动作 | 类型 | 影响面 | 紧急度 |
|---|---|---|---|---|
| 1 | 决 D-1 修订方向((a) 新契约路弃用 legacy agreement 关 / (b) 修分母语义 / (c) 下调 threshold) | spec §O8 + SLA §Layer-2 | E2 解锁条件 ② 全部 | **阻断** |
| 2 | 决 D-2:是否给 CLI 补 `--dimension-arbitration-json` 入口 | blind_annotate CLI + C9 仲裁契约 | Layer-2 能否容纳真实分歧 | 高 |
| 3 | 决 D-3:spec/SLA 定义补全(gold provenance 协议 + machine 结构性弃权 + B5 边界) | spec §O8 / SLA 注 | 数字可读性 · 防误读 | 中 |
| 4 | 记 KG-43 框架治理 defer 归 IDS forge(冻结门槛前置可达性验证门) | framework | 所有预注册门槛场景 | 中 |
| 5 | E2 授权判据复核:条件 ② 仍未满足 | 治理 | E2/E3 起跑 | 信息式 |

## 1.【核心 · 阻断】决 D-1 修订方向

三个方向见 §2-2。**build 侧倾向 (a)** —— 理据:SLA §Layer-2 已白纸黑字写「第二源资格从『输出一致率』refactor 为『逐维双错率 + abstain 覆盖率 + 外证据锚 + 盲标隔离』」,但 `evaluate_layer2:369` 的合取仍把旧 `rate >= threshold` 留作首项,**refactor 未清干净是本次死锁的直接成因**。探针 A1 组给了最干净的证据:D-7 三分离契约全部满分,却被一个已被 refactor 声明淘汰的旧指标一票否决。

(a) 若被采纳,XenoDev 侧改动很小(新契约路合取去掉 agreement 项 + 补测),legacy 路与 4 个既有旧测不受影响。

**不建议 (c)**:在看到「过不了」之后下调门槛,即使 `--threshold` 严格讲不在 SLA 冻结三组内,也与 C6「预注册不可回改」精神冲突,且擦边过(0.6976)说明该指标已无区分力。

## 2.【高】决 D-2:CLI 仲裁入口

当前状态「第二源要通过验收只能与机器逐条完全一致」是方法论自反 —— 独立第二源的价值恰在于可能不同。库层 `reconcile()` 已有 `dimension_arbitration` 参数(C9 设计存在),缺的只是 CLI 暴露。若 IDS 认可,XenoDev 侧补一个 flag 即可;若 IDS 认为「零分歧」是刻意的严格设计,则需在 spec 写明该判据的方法论理据(build 侧目前找不到)。

## 3.【中】决 D-3:定义补全

三项,均为文档层:
- **gold provenance 协议**:gold = operator 考据版(读全文 + 外锚查证)/ human = operator 盲标版(不看机器输出的第一反应)/ machine = `extracted_signals`。当前 spec 未定义这三者关系,是 D-7 未闭合的定义缺口。
- **machine 结构性弃权**:`extracted_signals` 无 sector/horizon 列 → 该两维 `double_error_rate` 实测 operator 自洽性而非 machine 正确性(§1 D-3)。
- **B5 诚实边界**:gold 与 human 同为 operator → 共享盲点;ticker 维靠 watchlist 外锚 + abstain 担独立性;解释维本质是盲标 vs 考据的自一致性。**这是 v0.1 降级验收,不是技术保证** —— 须在 spec 明写,防被读成「已有独立第二源验收」。

## 4.【中 · 框架治理 · KG-43】冻结门槛前置可达性验证门

**框架缺口**:这套 framework 支持「门槛预注册 · C6 不可回改」(强治理,对),但**没有任何一道门要求在冻结前验证门槛可达** —— 即「是否存在一组合法输入使 passed=1」。009 Layer-2 的五维门槛表经 forge v1 推演 + operator 批 plan 签字 + build 四轮对抗审,全程无人验算,结果冻结了一个数学上不可能通过的门槛组合。

**建议**:spec-writer / task-decomposer SKILL 加一道 **feasibility probe 前置**:任何预注册验收门槛(阈值组合 / 合取判据)在冻结前,须用理想上界合成数据跑一次「存在性证明」——构造最优输入,断言 `passed=1` 可达。零成本(合成数据 · 无 LLM · 无真跑),但能挡住整类「冻结了不可能通过的门」的失败。

**受益面**:所有用这套 framework 且有预注册门槛的项目(证伪链 / 金标验收 / SLA gate 全部命中)。

**已按 dogfood 铁律记 XenoDev `dogfood-backlog.md` KG-43,未当场改 framework**,请随本 hand-back 带回 forge 议。

## 5.【信息式】E2 解锁三条件状态复核

- ① 真密度显著:**US ✅**(决议 25 · strong 40 > advisory 30 · 最保守弱锚折扣 34 > 30)· CN/HK ❌ STOP
- ② 两层金标过:**❌ blocked** —— 状态从 LOG 第 27 条的「harness 就位 · 待真跑」**降级**为「门槛组合不自洽 · 待 IDS 决议 D-1」。Layer-1 门槛仍空(blocked-on Layer-1 真盲标跑)。
- ③ operator 授权:**❌**

→ 条件 ② 不解 → **E2 不授权**(E2 历史窗回测 / E3 forward 线做进 XenoDev = 越界 BLOCK)。US 密度 go **不改变**金标这条 gate(金标是信号质量硬门 · 与密度 advisory 正交)。

## 6.【残留 defer · 非本 hand-back close】

- **operator 真盲标产真数字**:机械脚手架已就位(`blind/machine.json` 41 条 + `blind/worksheet.txt` 41 行),**盲标机会完好未损**(全程未向 operator 展示任何机器答案)。D-1 决议后可立即起跑,盲标产物跨修订方向可复用。
- **watchlist 补权威数据(B1)**:未变。红线不动 —— 严禁本 fork 现填交易所清单(可能引 look-ahead + 越界)。另注:若 watchlist 照 gold ticker 反推 curate,外证据锚会退化为同义反复,该清单须来自 operator 外部权威认知。
- **blind 隔离技术不可证性(B5)**:未变(honor-system + 时序可证伪已是最强可得)。
