---
discussion_id: 009
prd_fork_id: 009-pForge
handback_id: 009-pForge-20260702T125820Z
from_build_repo: /home/ys/codes/XenoDev
to_source_repo: /Users/admin/codes/ideababy_stroller
workspace:
  source_repo: /Users/admin/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev
  working_repo: /home/ys/codes/XenoDev
  handback_target: /home/ys/codes/ideababy_stroller/discussion/009/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - feature
severity: medium
created: 2026-07-02T12:58:20Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: T003
---

# Hand-back · 009-pForge · T003

**Severity**: medium
**Tags**: feature
**Created**: 2026-07-02T12:58:20Z

## §1 Rationale(执行摘要)

T003 · 009-pForge M1 PIT 查询引擎 ship：实装 SqlitePITPriceProvider 双时间轴 point-in-time 复权价查询引擎 + AdjustedPricePoint 结构化返回 model + T002 接口返回类型收口（决议 A）。这是 M1→M2 主线的价格地基（as-of 不泄漏未来 = 红线）。本轮不含 004 adapter（决议 B · CurrentStatePriceProvider 拆 T008 · 触发#2 gate）。review 链 codex R1→fallback R2→codex R3/R4/R5 权威 approve，9 findings 全闭合（含 codex 补跑抓出 fallback 漏的 2 个深 as-of 泄漏）。

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

**tag = feature（clean task ship · 非 prd-revision-trigger）**

T003 是 spec-rev-1 决议 A+B 落地后的首个 build task，按 frozen spec（reviewed-by codex@2026-07-02）实装，无新 PRD 修订触发。

**交付内容**：
- `price/provider.py`（新）：`SqlitePITPriceProvider` — 双时间轴 PIT 查询引擎。SQL `WHERE ticker=? AND market=? AND trade_date<=? AND as_of<=? AND as_of GLOB '<canonical>' ORDER BY trade_date DESC, as_of DESC, source ASC LIMIT 1`（spec C2）；复权查询期动态算 `close_raw × factor`（C1/D-M1-1）；`get_status` 双轴退市查询（C3）。
- `price/models.py`：新增 `AdjustedPricePoint`（6 字段 · 触发#1 结构化返回 · frozen）。
- `price/interface.py`：`get_adjusted_close` 返回类型 `float|None → AdjustedPricePoint|None` 收口（决议 A）；adapter 引用改指 T008（决议 B）。

**as-of 泄漏防线三层（地基红线）**：
1. query 参数固定宽度 canonical UTC 校验（无小数秒 · R1-F3/R3）+ datetime 组件校验；
2. `target_date` 对称 canonical-date 校验（fallback）；
3. 读侧 `as_of GLOB` 排除表内脏小数秒行参与 PIT 比较（R4）。

**review 链（codex R1 → fallback R2 → codex R3/R4/R5 权威）**：
- codex R1：4 findings（多源 tie 非确定选源 / 因子缺静默当1.0 / offset 泄漏 / 非法 adjust 静默走 back）全修。
- codex 额度用尽 → 按 CLAUDE.md fallback 规则启用 subagent（red-team + code-reviewer）过桥 R2（3 findings 修：docstring 同步 / target_date 对称 / None 歧义标注）。
- codex 恢复补跑 R3/R4：**抓出 fallback 两 reviewer 都漏的 2 个更深 as-of 泄漏**（小数秒字典序泄漏 / 表内脏行泄漏）—— 全修。
- **codex R5 = approve（SHIP · 0 material findings · 9 findings 全闭合）**。

**决议 B 边界**：本轮不含 004 adapter（`CurrentStatePriceProvider` → T008 · ⛔BLOCKED · 触发#2 gate `PricePoint close-only vs 004 PriceBar OHLCV` 未决）。M1/M2 主线不依赖 T008，本 ship 不阻塞。

## §3 Suggested IDS actions

**无需 IDS 决议动作**（tag=feature · clean ship · 非 prd-revision-trigger · 无 PRD 矛盾）。以下为知会 + 后续 task 前置：

1. **知会：T003 ship 完成**，M1 PIT 查询引擎地基就位。下游解锁：T006（M1 ingest CLI · P1 PPV owner）、T008（004 adapter · 仍 ⛔BLOCKED 待触发#2）。

2. **触发#2 仍待 IDS 决议**（park/open）：`PricePoint close-only vs 004 PriceBar OHLCV/volume`。T008（`CurrentStatePriceProvider`）撞此未决点，保持 BLOCKED。M1/M2 主线不依赖 T008，不阻塞。触发#2 定后再起 T008。

3. **T012 集成前置（记录 · 非 IDS 动作）**：`get_adjusted_close` 的「无行」与「因子缺」当前都返裸 None，T012 M2 回测集成时定是否需 typed error/sentinel 区分（现不预设 · 已在 provider/interface docstring 标注）。

4. **T006 写侧前置（记录）**：本 task 读侧 `as_of GLOB` 是 defense-in-depth 治标（防表内脏小数秒数据泄漏）；T006 ingest 应在写入侧强制 as_of 固定宽度 canonical（治本）。

5. **框架级 dogfood（本轮随包回流）**：KG-32 = codex-review SKILL 无 fallback 分流（codex 额度用尽时 ship 流程 review 门死锁）。已在 XenoDev CLAUDE.md 落地过桥版（operator 授权），但 SKILL 本体是 IDS bootstrap-kit mirror 件，真正入 SKILL/SHARED-CONTRACT 的分流需回 IDS forge 定。建议 `/expert-forge` 时纳入。
