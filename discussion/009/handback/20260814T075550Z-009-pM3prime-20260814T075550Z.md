---
discussion_id: 009
prd_fork_id: 009-pM3prime
handback_id: 009-pM3prime-20260814T075550Z
from_build_repo: /home/ys/codes/XenoDev-009
to_source_repo: /home/ys/codes/ideababy_stroller
workspace:
  source_repo: /home/ys/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev-009
  working_repo: /home/ys/codes/XenoDev-009
  handback_target: /home/ys/codes/ideababy_stroller/discussion/009/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "0de4fb398ede9db1"
current_idea: "009"
worktree: feat/009-spec(XenoDev-009 直提交惯例 · 无独立 worktree · 本仓自 T010 起 spine task 均直提交)
baseline: T015 hand-back commit 4e39b85(codex round-9 · M1=pass)→ 本 task(T016)落地后 HEAD 191cb51
tags:
  - practice-stats
severity: medium
created: 2026-08-14T07:55:50Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: T016
related_spec_section: specs/009-pM3prime/tasks/T016.md · spec.md §3(C4)· §4(D-13)· §6.3 · SLA.md E1「提取稳定性」行
ids_verdict_evidence:
  verdict: needs-attention
  findings_count: 1
  review_log_path: .claude/skills/codex-review/real-review/T016-branch-diff-vs-4e39b85-2026-08-14T072905Z.md
  review_log_sha256: cad718d4911b1af74bea42635f0c0cbca05519cd9d8e6c763fa9356ba1600ac3
  target_file: T016-branch-diff-vs-4e39b85
  ts: 2026-08-14T07:29:05Z
  codex_model: gpt-5.6-sol
---

# Hand-back · 009-pM3prime · T016

**Severity**: medium
**Tags**: practice-stats
**Created**: 2026-08-14T07:55:50Z

## §1 Rationale(执行摘要)

**T016**(`specs/009-pM3prime/tasks/T016.md`)落地 forge `009-pM3prime` v4 verdict 决策矩阵 **B 组(记账/可观测性)+ C 组(C4 三处同步)** 的 XenoDev 侧四项交付物,消费 `framework/obligations/ledger.jsonl` 五条到期义务 `OB-009-pM3prime-v4-02/03/06/07/08`。

**四项交付物**:①`extraction_attempt_log`/新表 `census_run` 四前提(预注册观察边界 · 逐 attempt exposure 可计 · 精确时间+删失可识别 · 失败批次内容身份可比)逐条证明/证否 —— 结论:四前提均有真实缺口,按契约触发 fallback(migration `0023` 新增 `census_run`/`aggregation_probe_result` 两张独立审计表,不碰既有 `trial_ledger`/`extraction_attempt_log` grain)。②`aggregation_probe_result` 落库补 `SLA.md:136` 缺失的"跨次一致性读数存在性"判据。③C4 `convert` 判据(过程证据+结果证据+具名批准+版本化回退四者合取)+ 版本化回退语义逐字同步进 `spec.md` C4 行与 `SLA.md` E1 行(IDS `PRD.md` §7 红线3 已落地对应内容)。④核查并同步 T015 `M1=pass` 裁决进 XenoDev 侧正式文档面(`spec.md` §4 新增 D-13)。

**实现期材料偏差**:原计划设想 census_run 完成态写入可在主事务持锁期间用另一独立连接并发写入,实测证伪(SQLite 单文件同一时刻仅允许一个写者,含 WAL 模式)。改为完成态写入挪到主 `conn.close()` 之后,operator 经 AskUserQuestion 明确选择"接受残留风险"档 —— 前提②(SIGKILL 场景下逐 attempt exposure 可计)未被完全闭合,诚实记入 §2。

**codex adversarial-review 4 轮真跑**(`--base 4e39b85 --scope branch`):round 1-3 每轮均抓到真实缺陷(落库失败静默返回 0 / 生命周期写不查 rowcount / `INSERT OR IGNORE` 误吞约束违规 / 子跑诊断信号丢失 / DB-JSON 非原子孤儿行 / 早停截断内容哈希前缀 / JSON 转态失败未更正),全部代码修复。round-4 命中 codex-review SKILL §4.2 的 4 轮硬顶(仍 needs-attention · 1 high),operator 经 AskUserQuestion 明确选择"修复后直接 ship,不再等第 5 轮 codex 复核"——finding 已代码修复(commit `9b5bbff`),未经第 5 轮 codex 权威复核,如实记入 §3。

全量回归 1304 passed / 7 skipped / 1 xfailed(基线 1287 passed,净增 17 = 0 回归),ruff clean。四项交付物验收判据(SQL/pytest)全部记入 `T016.md` Verification 表,可独立复核。

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

**Tag: practice-stats**

- **统计区间**:本 task(T016)单 task 全周期,直接提交到 `feat/009-spec`(该分支已建立的直提交惯例,非 worktree+squash 流程)。
- **构建活动**:6 个直接提交(migration `0023` + census.py/aggregation_probe.py 核心改动 + 4 轮 codex 修复 commit + REVIEW-LOG 机器可读记录)· 覆盖两张新表 schema 设计、`census.py`/`aggregation_probe.py` 双模块记账机制、16 处新增/更新测试(净增 17 = 1287→1304 passed,一处测试因不同 caliber_version 复用而在 round 内被计入 round-4 净增)、`spec.md`/`SLA.md`/`T016.md` 三处文档同步。
- **operator 干预次数**:2 次(均通过 AskUserQuestion 主动征询,非被动打断)——①实现期发现 SQLite 单写者约束material deviation,operator 选定"接受残留风险"档;②codex round-4 命中 4 轮硬顶,operator 选定"修复后直接 ship,不再等第 5 轮 codex 复核"。
- **codex adversarial-review**:4 轮真跑(`node companion.mjs adversarial-review --wait --base 4e39b85 --scope branch`,全部真路径,非 fallback)。round 1-3 每轮均抓到"正常使用路径下会真实触发"的缺陷(3 high / 1 high+1 medium / 1 high+1 medium),全部代码修复;round-4(1 high)修复后未再补跑第 5 轮(命中 SKILL §4.2 4 轮硬顶,operator 授权跳过)。累计 4 轮 5 类 finding,4 轮全部实质性(无环境级/沙箱级空转),单调收敛(每轮暴露的缺口范围逐轮收窄,同一物理位置的问题从"逻辑失败未处理"精确到"进程终止窗口未处理")。
- **完成耗时**:单 session 内完成(设计→实装→3 轮 codex 修复→material deviation 决策→round-4 修复→REVIEW-LOG 机器可读记录→本 hand-back),未跨多个 session。

## §3 Suggested IDS actions

### 需 IDS 决议

1. **`extraction_attempt_log` 四前提 fallback 的两处已知不完整点是否可接受为长期状态,还是应记新 obligation 交未来专门 task 收口**:
   - 前提①(预注册观察/停止边界):重试停止规则(k=3 · 指数退避)有 frozen 文档基础,但观察窗口大小(`--sample-size`)本轮只做**运行级预注册**(声明即落库),不像 C6 式跨运行硬冻结——operator 可在看过小样本结果后换参数重跑而无机制拦截或留痕。
   - 前提②(逐 attempt exposure 可计):SQLite 单写者约束实测证伪原计划的"边跑边写",最终设计下若进程在主事务 commit/rollback 之前被硬杀,本轮 `extraction_attempt_log` 记账仍会整轮丢失(与本 task 之前行为相同,未变差,但也未被彻底闭合)。operator 本轮已明确选择接受此残留风险,完整推理见 `T016.md`「诚实残留」段。彻底闭合需要进程外 durable receipt(本地 append-only 文件 + reconcile,或独立 sqlite 文件),本轮判断超出 fallback 的"等价耐久回执"最小闭合范围。
   - IDS 只需决定:这两点是否需要另立 obligation(交未来架构级 task),还是维持现状(census 是人工触发一次性 CLI,非无人值守常驻服务,风险窗口有限)。

### 信息式收悉(无需 IDS 决议)

2. **codex round-4 finding 已修复但未经第 5 轮 codex 权威复核**:round-4(`--base 4e39b85 --scope branch`)finding([high] JSON `persistence_status` 在 `completed` 转态调用前进程终止的窗口内仍可能永久错误自称 `"ok"`)已代码修复(commit `9b5bbff`,新增回归测试覆盖),但命中 codex-review SKILL §4.2 的 4 轮硬顶,operator 经 AskUserQuestion 明确授权"修复后直接 ship,不再等第 5 轮 codex 复核"。这是 XenoDev 侧 codex-review 治理内部的过程性决定(依据 CLAUDE.md「cross-model review fallback」同精神的本仓治理条款),不构成 PRD/spec 冲突,仅供 IDS 归档知悉。完整 4 轮记录见 `.claude/skills/codex-review/REVIEW-LOG.md`(singleton 已更新)+ `real-review/T016-branch-diff-vs-4e39b85-2026-08-14T072905Z.md`(immutable)。
3. **交付物②③④(SLA.md 判据补齐 + C4 三处同步 + M1=pass 文档同步)均已机械落地**,内容逐字来自已冻结的 IDS `PRD.md` §7 红线3 与 T015 既有裁决,无新增判断空间,无需决议。
4. **已核实**:本仓 `spec.md`/`SLA.md` 均未引用 `WARN-c4-extraction-stability-report-only` 的 `due_event` 旧值(`forge:009-pM3prime:phase0`),v4 指令"若引用了旧值一并更正"的前提条件不成立,无需额外更正动作。

### Suggested actions

| # | Action | 类型 | 优先级 | 备注 |
|---|---|---|---|---|
| 1 | 就前提①/②两处已知不完整点决定是否记新 obligation 交未来架构级 task | scope 决议 | 中 | 完整推理见本节「需 IDS 决议」#1 + `T016.md`「诚实残留」段 |
| 2 | 收悉 codex round-4 finding 已修复但未经第 5 轮复核(SKILL §4.2 4 轮硬顶 · operator 已授权) | 信息式 | 低 | 完整记录见 REVIEW-LOG.md + real-review immutable 记录 |
| 3 | 将 `OB-009-pM3prime-v4-02/03/06/07/08` 标记 `satisfied`(append 新 ledger 行,不改历史行) | ledger 更新 | 高 | 四项交付物均已完成,验收判据(SQL/pytest)全部可独立复核,见 `T016.md` Verification 表 |
| 4 | 收悉交付物②③④均为机械判据补齐/逐字同步/事实核查,无新增判断空间 | 信息式 | 低 | 内容逐字来自已冻结的 PRD.md §7 红线3 与 T015 既有裁决 |

[ ] 就前提①/②残留风险决定是否记新 obligation
[ ] 收悉 codex round-4 未经第 5 轮复核(信息式)
[ ] `OB-009-pM3prime-v4-02/03/06/07/08` 记 `satisfied`
[ ] 无操作(收悉,作为 practice-stats 入库)
