---
discussion_id: 009
prd_fork_id: 009-pForge
handback_id: 009-pForge-20260703T034538Z
from_build_repo: /home/ys/codes/XenoDev
to_source_repo: /Users/admin/codes/ideababy_stroller
workspace:
  source_repo: /Users/admin/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev
  working_repo: /home/ys/codes/XenoDev
  handback_target: /home/ys/codes/ideababy_stroller/discussion/009/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - feature
severity: high
created: 2026-07-03T03:45:38Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: T010
---

# Hand-back · 009-pForge · T010

**Severity**: high
**Tags**: feature
**Created**: 2026-07-03T03:45:38Z

## §1 Rationale(执行摘要)

T010 · 009-pForge M2 analyst_alpha 两表 migration(0015)ship:建 analyst_alpha_eval(accepted)+analyst_alpha_rejections(rejected)两评估表(D-M2-2)。codex 恢复 R1-R5 权威审抓7真finding:范围内5个(F1 direction/horizon CHECK·F2 FK→advisor_reports·F3 rejections幂等·F4 provenance FK→price_daily+已算行完整性·F7 结果列域约束)全修;越界2个(F5 未来as_of=T012责任·F6 FK撞004 INSERT OR REPLACE需operator决)分流。范围仲裁(subagent+spec+operator)一致:T010范围内满分闭合·ship合理·F5/F6记hand-back。26迁移单测+540全004套绿+真alembic upgrade→0015。发现即修 :字 bind-param陷阱(KG-34)。severity=high 因 F6 是待触发真回归须T012前operator决。

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

**tag = feature（M2 建表 · 但含 2 个越界 finding 分流 · 见 §3）**

按 frozen spec（reviewed-by codex@2026-07-02）建 M2 alpha 头两张评估表（D-M2-2），无新 PRD 修订触发（但 F6 触及 004 既有 upsert 写路径，需 operator 决策 · 见 §3）。

**交付内容**（alembic 0015 · 只新增两表不 alter 004 · OUT-10）：
- `analyst_alpha_eval`（accepted 评估行）：
  · 报告回指：`advisor_id/source_id/week_id` **复合 FK → advisor_reports**（0011 PK · codex F2）
  · 信号：`signal_published_at`（权威时间基准）/`ticker`/`market` NOT NULL + **CHECK(US/HK/CN)**（rev-1）/`direction` **CHECK(long/short)**（codex F1 · neutral/wait/no_view 不产样本不进 eval · SLA C16）/`horizon` **CHECK(5,20,60)**（codex F1 · C16 固化集合）/`join_source` CHECK/`parse_confidence`
  · **provenance 六列**（entry_/exit_ 各 hit_trade_date/hit_as_of/source）+ **entry/exit 各复合 FK → price_daily**（codex F4 · 防不可复现假引用）+ **半填 CHECK**（每组三列全空或全非空）+ **已算行必带 provenance CHECK**（codex R4 · hit/excess/dsr/pbo 任一非空 → entry+exit provenance 必齐）
  · 结果列：`hit` CHECK(0,1) / `excess_return` / `dsr`,`pbo` CHECK[0,1] / `trial_count` CHECK(≥0)（codex R5-F7）+ dsr/pbo 非空则 trial_count 必非空
  · **UNIQUE**(advisor,source,week,ticker,market,horizon,signal_published_at)含 market（rev-1）
- `analyst_alpha_rejections`（rejected）：同报告 FK + `ticker`（可空）+ `rejection_reason` **CHECK 7 枚举**（含 unknown_market · rev-1）+ **COALESCE(ticker,'') 幂等 UNIQUE INDEX**（codex F3 + 自查 R2：堵 multi_ticker 场景 ticker=NULL 漏去重）
- forward-only downgrade raise · 评估表非 bitemporal 不加 append-only trigger

**验证**：26 迁移单测（含所有 codex finding 的负例回归）+ 540 全 004 套绿 + 真 `alembic upgrade head → 0015` + `.schema` 断言（FK/CHECK/UNIQUE/COALESCE INDEX 全在）。

**impl 内发现即修的真 bug（KG-34 框架级）**：DDL 字符串内 `--` 注释的 `:字` 模式（如 `:同`）被 `op.execute` 的 SQLAlchemy `text()` 误判为 bind parameter → 真 alembic upgrade 报 `A value is required for bind parameter '同'`（单测 executescript 不解析 :param 故没暴露，只 §2.4 真跑 alembic 才炸）→ 改注释去 `:字`。已记 dogfood KG-34。

**review 链（codex 恢复 · R1-R5 权威审 · 7 finding）**：
- R1：F1(direction/horizon CHECK)+F2(FK→advisor_reports)+F3(rejections 幂等)【全 T010 范围内 · 修】
- R2：F4(provenance FK→price_daily + 半填 CHECK)【范围内 · 修】+ 自查 R2(ticker=NULL 漏去重 → COALESCE INDEX)
- R4：F4 残留(已算行 provenance 全空)【范围内 · 修】
- R5：F5+F6【**越 T010 范围 · 分流** · 见 §3】+ F7(结果列域约束)【范围内 · 修】
- **范围仲裁**（code-reviewer subagent + spec 事实 + operator 决策一致）：T010 范围内 finding(F1-F4/F7)全闭合 · ship 合理 · F5/F6 越界分流。
- **reviewed-by = codex@2026-07-03**（R1-R5 · 范围内 approve）。

## §3 Suggested IDS actions

**需 IDS/operator 决策动作**（F6 = 真回归 · T010 的 FK 引入 · 但修复点在 004 既有代码越 T010 范围）。以下为知会 + 2 个越界 finding 分流（operator 已决 ship T010 + F6 记此分流）：

1. **知会：T010 ship**，M2 alpha 两表就位。至此 M2 建表根节点完成 → 下游解锁 T011（冻结 join 合约 · depends T010）。

2. **⚠ F6 [high · operator 待决 · 加 FK 的次生真回归]**：T010 给 analyst_alpha_eval/rejections 加的复合 FK
   `(advisor_id,source_id,week_id) → advisor_reports`（codex F2 修复 · **无 ON DELETE 子句 = 默认 RESTRICT**）会撞
   **004 既有 `repository/advisor_repo.py:38` 的 `INSERT OR REPLACE INTO advisor_reports`**（upsert_weekly）。
   - **真实复现路径**（仲裁 subagent 坐实 · foreign_keys=ON 在 base.py:68 + env.py:62 双开）：一旦某
     `(advisor_id,source_id,week_id)` 已有 eval/rejection 子行，`upsert_weekly` 覆盖同周报告时 SQLite REPLACE
     = 先 DELETE 冲突父行 → child FK RESTRICT → **`FOREIGN KEY constraint failed`**（upsert 直接失败）。
   - **当前无触发**：analyst_alpha 子行要 T012+ 才写（现全 004 套 540 绿）· 但 **T010 埋了待触发地雷**，T012 起写子行后 upsert_weekly 会炸。
   - **修复点越 T010 范围**：改 `advisor_repo.py` 的 upsert = 004 既有代码（spec **OUT-10** 不 alter 004 + parallel-builder §2.5 不修 out-of-scope）→ T010 无权当场改。
   - **operator 决策选项**（须 T012 起写子行前解决）：(a) 改 004 `upsert_weekly` 的 `INSERT OR REPLACE` → `ON CONFLICT DO UPDATE`（不删父行 · 最干净 · 但改 004 归属需定）；(b) 给 FK 补 `ON DELETE CASCADE`（REPLACE 时级联删子行 · 但会静默删已算 alpha 样本 = 数据事故 · **不推荐**）/`ON DELETE SET NULL`（撞 NOT NULL）；(c) 确认 upsert 路径不会命中已引用父行。**建议 (a)**，新开 FU-task 改 004 repo 层。

3. **F5 [high · 分流 T012 + 可选 spec 决议]**：codex 指 provenance FK 不阻止未来 as_of 进入已算样本。
   - **判定为 T012 责任**（仲裁 + spec 事实一致）：as-of look-ahead 防护 spec **D-M2-4** 明定为 T012 realized-return
     引擎运行时责任（`get_adjusted_close(query_as_of=entry_date/exit_date)` 双侧显式 as_of · 测试
     `should_query_entry_at_entry_date_and_exit_at_exit_date` 在 T012 verification）。eval 表按 **D-M2-2** 只存
     `hit_as_of`（命中行 as_of）· **无 query_as_of 列** · DDL 层 CHECK "hit_as_of ≤ 查询口径" 需先加 query_as_of 列 = 改 spec 冻结字段集。
   - **T012 前置**：T012 实装取价时用正确 query_as_of 天然锁死可见边界（provenance FK 已保证指向真 price_daily 行）。
   - **可选 spec 决议**：若 operator 认为需审计层二次校验（eval 存 query_as_of 列 + DDL CHECK），须回 IDS 决议加 D-M2-2 字段。**否则不加**（T012 运行时防护已足）。

4. **框架级 dogfood（本轮随包回流）**：
   - **KG-34**：迁移单测 `executescript` 与生产 `op.execute`（SQLAlchemy text）行为不一致 → DDL 内 `:字` bind-param 陷阱单测测不出（本 task 真踩 · 靠 §2.4 真 alembic upgrade 才抓）。
   - **KG-35**：codex-review companion `--wait` 的最终结构化 verdict/findings 不稳定落 stdout log（R2-R5 都没落）· 需 `status --all --json` 的 job summary + job 独立 logFile 兜底才拿到 verdict。影响 codex-review SKILL §4 verdict 解析可靠性。
   - 建议 `/expert-forge` 纳入 KG-32/KG-33/KG-34/KG-35。

5. **契约细化（impl-driven · KG-22）**：T010 建表在 spec D-M2-2 字段基础上，按 codex 权威审加固了多个 DDL 硬约束
   （direction/horizon/结果列 CHECK · provenance/advisor FK · rejections 幂等 · 已算行 provenance 完整性）—— 均为
   spec"防错配收益/防选择偏差/as-of 可复现"意图的 DDL 落地，非 scope 变更。T011/T012/T013/T014 写入 eval/rejections 时须满足这些约束。
