---
discussion_id: 009
prd_fork_id: 009-pForge
handback_id: 009-pForge-20260706T073428Z
from_build_repo: /home/ys/codes/XenoDev
to_source_repo: /Users/admin/codes/ideababy_stroller
workspace:
  source_repo: /Users/admin/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev
  working_repo: /home/ys/codes/XenoDev
  handback_target: /home/ys/codes/ideababy_stroller/discussion/009/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - feature
  - spec-gap-fix
severity: medium
created: 2026-07-06T07:34:28Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: O8-downstream
---

# Hand-back · 009-pForge · O8-downstream

**Severity**: medium
**Tags**: feature,spec-gap-fix
**Created**: 2026-07-06T07:34:28Z

## §1 Rationale(执行摘要)

O8 下游集成 = 兑现 T015 的 O8「alpha 平权上台」在 004 现有下游的落地(IDS forge v4 授权改 004 spine · handback-review 009 T015 决议)。T015 codex R3 揭示 O8 在 004 现有下游只是「假达成」(缓存键 silent-wrong + 双渲染 signals[:3] 截断 · 004 为「恰好 3 路方向票」设计承载不了「第四路非方向 evidence」)。本 task 让 alpha 的「非方向质量旁证」正确上台,含 alpha_lane 能力层(T015)一并 ship。

**结论:O8 真达成 · status: shipped(merge c2e8347 到 feat/009-spec)· codex R1-R4 单调收敛 → operator 收口 approve · 全 004 套 725 passed 无回归 · mutation 全 kill · §4.4 verifier 全过。T016 解锁。**

三模块 + 盲区追平:
- **(a) 承载**:StrategySignal 加一等字段 `is_directional`/`signal_kind`/`evidence_strength`(有默认 · 向后兼容)+ ConflictReport 加 `evidence_lanes` 旁路(默认空 · **不升级 discriminated union** · binding② 最小冲击)+ assembler 装配分流(`is_directional=False`→evidence_lanes · 方向票→signals · 守护方向票≥3 不变量#2)。alpha_lane 填一等字段(有效 evidence + **所有 fail-closed 分支**都 `is_directional=False` 走旁路)。
- **(b) 缓存(解 R3-F1 silent-wrong)**:`_compute_signals_hash` 方向票段+evidence 段**各纳入 rationale_digest**(codex R1-F1+R4-F1:进 prompt 的内容必进 hash · 与 `_build_evidence_block` 同源 `_sanitize_signal_text` 归一)+ evidence 显著性分桶(strong≥0.75/moderate/weak/none)· `has_divergence` **确定性推导**(方向票 direction 集合 · evidence 无法改 · codex R2-F1)+ `_reconcile_root_cause` 文案-布尔一致(无分歧类 denylist 归一 · codex R3-F1+R4-F2)· 方向票 **canonical 排序贯穿** prompt/hash/seed/report.signals(codex R2-F2+R3-F2)。
- **(c) 渲染(解 R3-F2 截断)**:Telegram `build_narrative` evidence 区(方向票 permutation R2 D22 保留)+ 共享 `_evidence_section.html` partial(conflicts detail + draft_preview 两面 · `data-not-directional=true` + 显 strong/moderate/weak+命中率/样本/显著性 + **「不计入多空」标注**)+ decisions.css `1fr 1fr 1fr`→auto-fit 自适应。
- **序列化(B 硬伤)**:0016 migration(`evidence_lanes_json TEXT` · forward-only)+ conflict_repo insert/反序列化两端(旧 signals_json 无新字段 + evidence_lanes_json NULL → 默认兜底不 KeyError)。

**codex 4 轮单调收敛(全 needs-attention · 每轮实质 finding 越来越窄 · 都是前轮修复的对称遗漏 · operator 收口 · 同 T014 9 轮 precedent)**:R1(evidence prompt/hash 未完整 + fail-closed UI 500)→ R2(has_divergence 可被 LLM 改 + evidence 顺序不同源)→ R3(文案-布尔不一致 + 方向票顺序不同源)→ R4(方向票 rationale 未进 hash + 无分歧类只 exact match)。全修 + 回归测 + mutation 锁死。

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

**§underweights 兑现 · 回流 forge 推演 3 盲区(spec-gap-fix · forge-lite 只审 Telegram+conflicts-UI 漏了 · 起手 grep 抓出)**:

forge v4 §underweights 硬要求「动手前先 grep 全仓 report.signals/[:3]/_THREE_PERMUTATIONS」防第三渲染面漏。起手 `rg` 抓出 forge 只审了 Telegram+conflicts-UI 两面,**漏了 3 个额外消费 report.signals 的面**(全已追平):
1. **`ui/router_decisions.py:155` draft_preview 第 4 渲染面**:forge 未提 · evidence 旁路后不会自动显示 → 已加 evidence 分区(复用 `_evidence_section.html`)。
2. **`repository/conflict_repo.py:32/81` 序列化两端(硬伤)**:forge 未提 · insert 只 dump signals + `_row_to_conflict_report` 逐字段重建 6 字段 → evidence_lanes 落库即丢 = 新 silent-wrong → 已加 0016 migration + 序列化两端 + 抽 `_dict_to_signal` 一等字段默认兜底。
3. **`ui/static/decisions.css:13` `1fr 1fr 1fr` 三列硬编码**:forge 未提 → 已改 auto-fit 自适应 + evidence-section 分区样式。

**两个 §underweights 未验点结论(forge 点名要验)**:
- **`all_no_view` 短路**:确认原「全 no_view→暂无分歧」短路只看**方向票**(evidence 恒 NO_VIEW 不参与短路判定)· 方向票全 NO_VIEW + 1 alpha evidence 时 evidence **不被吞**(仍进 evidence_lanes · 回归测 `should_not_swallow_evidence_when_all_directional_signals_no_view`)。
- **evidence 进 prompt 与 cache 键同源**:确认 evidence 进 cache 键(signals_hash)必**同进 prompt**(`_build_evidence_block`)· 否则 hash 变输出不变=新不一致。且 codex R1-F1/R4-F1 进一步揭示 prompt 里所有影响输出的内容(evidence + 方向票 rationale)都须进 hash · 已用 rationale_digest 覆盖。

**forge 推演盲区的实证价值(MEMORY 教训:009 一周期 F6 幻影 bug + two-SELECT 原子性两次都是推演漏被 build-time 杀)**:codex R1-R4 逐轮在 build-time 揭示 forge/我推演漏的 silent-wrong(has_divergence 可被 evidence 改 / prompt-hash 不同源 / 方向票 rationale 未进 hash / 无分歧类变体)· 全是「推演看着对但真跑/真审才暴露」的对称遗漏 · 印证 forge v5 教训「审没真跑过的容易跑偏」· build-time 对抗审是硬证据。

**归属自检(dogfood 铁律)**:改 004 是 **IDS forge v4 授权**的下游集成(OUT-10 裁决 · 非当场绕治理)→ XenoDev 内做;但 **grep 发现的 3 盲区 = forge-lite 推演覆盖不全的框架级发现** → 本 hand-back 回流(不当场吞 · §underweights 兑现)。

## §3 Suggested IDS actions

**交付**:改 domain(strategy_signal/conflict_report)+ assembler(装配分流/缓存指纹/prompt/has_divergence 确定性/文案一致)+ conflict_repo 序列化 + 0016 migration + telegram/conflict_narrative + ui(_evidence_section partial/conflicts detail/draft_preview/decisions.css)+ alpha_lane 填一等字段 · 26 新下游集成测(承载/缓存 mutation-killer/序列化往返/渲染/边界)+ alpha_lane 28 测 · 全 004 套 725 passed 无回归 · ruff clean(本 task 文件)· mutation 全 kill · §4.4 verifier 全过。reviewed-by=codex@2026-07-06(R1-R4 单调收敛 → operator 收口 approve)。

**Suggested IDS actions(operator 在 IDS 跑 /handback-review 009)**:

1. **回流 forge-lite §underweights 盲区(forge 改进)**:forge v4 §underweights 已要求起手 grep,但 forge-lite 自身推演只审了 Telegram+conflicts-UI 两渲染面 —— **起手 grep 是对的(抓到了),但 forge 推演阶段漏了 router_decisions/conflict_repo/decisions.css 3 面**。建议 forge 流程改进:凡「给共享域对象加旁路字段」的下游集成,forge 推演阶段应强制枚举该对象的**所有消费面**(渲染/序列化/导出/搜索),不只主渲染面。这是 forge-lite 推演覆盖度的框架级教训。

2. **has_divergence 语义正式化(既有弱设计 · O8 顺带确定性化)**:O8 把 has_divergence 从「完全信任 LLM」改为「方向票 direction 确定性推导 · LLM 只产文案」(codex R2-F1 逼出)。这是既有弱设计的正确化(现有测无一断 has_divergence 具体值 · 向后兼容)· 但**判据(≥2 不同实质方向 · 排除 no_view)是 XenoDev 实装时定的**,建议 IDS 确认此判据对齐 PRD/spec 对「分歧」的定义(尤其 LONG vs NEUTRAL / WAIT 是否算分歧)· 若 spec 有更细口径则回 forge。

3. **T016 就绪**:O8 真达成解锁 T016(evaluate CLI 落库 analyst_alpha_eval + 真 DB-backed alpha_provider + build_default 生产接线 · Option b)。T016 起跑时 alpha_provider 真接通后,evidence_lanes 从「Mock 能力可达」变「真生产数据」。

4. **carry-forward known-gap**:真 DB-backed alpha_provider(读 0015)+ build_default alpha_provider DI 参数 = T016(本 task Mock + assembler 分流证能力可达)· evidence_bucket 阈值(strong≥0.75 等)+ 无分歧类 denylist 短语是可迭代的(forge 定原则 · 文案/阈值实现可调)。
