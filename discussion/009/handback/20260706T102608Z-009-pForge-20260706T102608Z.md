---
discussion_id: 009
prd_fork_id: 009-pForge
handback_id: 009-pForge-20260706T102608Z
from_build_repo: /home/ys/codes/XenoDev
to_source_repo: /Users/admin/codes/ideababy_stroller
workspace:
  source_repo: /Users/admin/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev
  working_repo: /home/ys/codes/XenoDev
  handback_target: /home/ys/codes/ideababy_stroller/discussion/009/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - feature
  - spec-gap
severity: medium
created: 2026-07-06T10:26:08Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: T016
---

# Hand-back · 009-pForge · T016

**Severity**: medium
**Tags**: feature,spec-gap
**Created**: 2026-07-06T10:26:08Z

## §1 Rationale(执行摘要)

T016 M2 evaluate CLI shipped (P2 PPV real-path owner). python -m decision_ledger.alpha.evaluate --advisor <id> orchestrates resolve(T011) then compute_realized(T012, per signal x {5,20,60} horizon, real PIT price no-mock R2-F2) then triplet(T013) then deflate(T014), and upserts analyst_alpha_eval real rows (market + six provenance columns + hit/excess per-row, dsr/pbo/trial_count per-horizon). DbAlphaResultProvider (the DB-backed provider T015 deferred to T016) reads 0015 back into TripletMetrics for AlphaLane. P2 PPV terminus = data.sqlite SELECT-able real rows (verified end-to-end on disk). Review: codex adversarial-review branch-mode failed on sandbox bwrap loopback (env failure, not code finding; recorded dogfood KG-37), so per CLAUDE.md fallback rule ran red-team-reviewer + code-reviewer double-pass (write-boundary/spine); 3 red-team rounds monotonically converged to approve; red-team caught F1 idempotency ship-blocker (re-run for an advisor crashed with IntegrityError and dropped new rows) which codex missed by not running, fixed with INSERT ON CONFLICT DO UPDATE (0015 recomputable-table semantics; refreshes stale dsr/pbo/trial_count; provenance co-refreshes to stay row-consistent under backfilled price revisions). 10 integration tests (real PIT, no mock price) + full 004 suite 735 passed no regression, ruff clean. Section-4.4 verifier clean. OPERATOR DECISION NEEDED (spec-gap): T015 alpha_lane docstring assigns production registry wiring (build_default alpha_provider DI) to T016, but registry.py is NOT in T016 frozen file_domain (only evaluate/main/test), so editing it violates file_domain + the verifier. So M2 alpha data reaches analyst_alpha_eval (delivered) but does NOT yet reach conflict_report in production because build_default does not register the alpha lane. This registry-wiring gap fell between T015 (deferred it) and T016 (file_domain excludes it). Decide: fold into T017-pre, or open a separate task (T016b) to add the build_default alpha_provider DI wiring. Capability is proven reachable (DbAlphaResultProvider to AlphaLane to evidence signal, integration-tested), same posture as T015 O8 manual register.

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

(待补 · per T007 amendment · operator 跑 gen-handback 时未传 --section2)

## §3 Suggested IDS actions

(待补 · per T007 amendment · operator 跑 gen-handback 时未传 --section3)
