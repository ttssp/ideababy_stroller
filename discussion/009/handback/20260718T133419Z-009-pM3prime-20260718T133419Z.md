---
discussion_id: 009
prd_fork_id: 009-pM3prime
handback_id: 009-pM3prime-20260718T133419Z
from_build_repo: /home/ys/codes/XenoDev-009
to_source_repo: /Users/admin/codes/ideababy_stroller
workspace:
  source_repo: /Users/admin/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev-009
  working_repo: /home/ys/codes/XenoDev-009
  handback_target: /home/ys/codes/ideababy_stroller/discussion/009/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "0de4fb398ede9db1"
tags:
  - spec-gap-fix
severity: high
created: 2026-07-18T13:34:19Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: KG40-anchoring-dp
---

# Hand-back · 009-pM3prime · KG40-anchoring-dp

**Severity**: high
**Tags**: spec-gap-fix
**Created**: 2026-07-18T13:34:19Z

## §1 Rationale(执行摘要)

**接续 KG-40 root-cause hand-back(`009-pM3prime-20260717T081757Z` · prd-revision-trigger)· 结论:引文回锚制契约改造完成并经 codex 18 轮 adversarial-review R18 approve · 解除 E1 真密度普查的 spine 阻断(KG-40)· 待 operator 授权重跑真普查。**

上一轮 hand-back 报告:E1 真普查 200 条 58/58 finish_reason=length 截断 ≈100% 失败,根因 = census prompt 让推理型 LLM 产原文字符偏移(span_start/span_end),长语料下烧尽 4096-token 预算 JSON 未闭合。IDS forge v6 verdict [A] 授权改为**引文回锚制**(LangExtract 同构):LLM 只产逐字引文 quote,偏移由确定性代码分层回锚。本 hand-back 报告该改造的落地 + review 收口。

**交付事实(commit 76036af · 13 files · +2515/-231)**:
- **模块 B(新)`anchoring.py`**:确定性分层回锚层(exact→normalized→fuzzy→not_found/ambiguous)。核心 = **occurrence 消解 DP 重写**:`_enumerate_strong_candidates` 单一归一坐标候选表(exact/normalized 只作 tier 标签 · 位置序排序)· `resolve_anchor` 取位置最早未占用候选(不再 exact 优先)· 三口径(distinct 唯一性 / capacity 可行容量 / 分配)全从候选表派生 → raw-vs-normalized 口径不对称构造性移除。
- **模块 A `extractor.py`**:ProposedSpan 加 quote 载体 · extract() 接回锚 · ExtractResult(strong-only signals + fuzzy_signals 审计旁路 + tiers)· 同 quote 多(ticker,direction)冲突全判 ambiguous。
- **模块 A `census.py`**:prompt_v2(LLM 只产 quote)· census_invalid 硬门(失败计数>0 → 非零 exit + rollback + 清零 persisted 指标 · CLI 不声称落库)· 三类失败计数(api/parse/truncation · fail-closed)· 模型无关预算 · variant 身份 · stats delta · 早停省 key。
- **模块 D**:E1 runbook tier 读数 + E2 match-tier 维度。

**验证**:993 passed · 7 skipped · lint clean(触碰件全过)· codex **18 轮真跑 adversarial-review**(非 fallback · 本 worktree codex 环境恢复 · 见 dogfood KG-41)· **R18 approve / No material findings**。硬边界全守:caliber v1 / ExtractionRejectionReason 枚举 / AnchorTier / 0017 migration / gold_layer / 004 已 ship 件 均未碰 · provenance 用 detail 承载不落库。

**这解除了什么**:KG-40 是 E2/E3 解锁三条件"真密度显著"的前置阻断。引文回锚制修好后,E1 真密度普查在契约层不再被系统性截断阻断。**但本 FU 不重跑真普查**(烧 key · 需 operator 凭据 + 经 `/handback-review 009` 另行授权)—— 请 operator 决议是否授权重跑。

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

**tag = spec-gap-fix**:本 hand-back 交付上一轮 `prd-revision-trigger`(KG-40 · E1 密度被 proposer 契约缺陷阻断)所请求的 forge 定契约后的**修复落地**。IDS forge v6 verdict [A] 定引文回锚制契约 → 本 FU 落地 + review 收口。

**occurrence 消解演进(为什么最后走 DP · 这是 hand-back 的关键决策记录)**:
- codex 对抗审 18 轮单调收敛(R1 4-high → R18 approve · ~29 findings correctness 全修 + 回归测)。
- **R13→R15** 是贪心「raw exact 优先 + normalized 回填」的口径不对称变体(supply 计数 → 唯一性并集 → demand 归一 · 逐个修耦合点)。
- **R16**(混合全半角 citation 顺序锚反 · 实证 `看多ＡＡＰＬ。看多AAPL。` 两条半角 quote 顺序反)= 连续第四轮同轴 → 触发**熔断报 operator**。**operator 裁决 [2]:换 DP 重写 occurrence 消解**(执行 v6 契约「贪心 claim-set 证不出四语义 → 用 DP」分支 · 非新决策)。
- **DP 重写**:单一归一坐标候选表 · 位置序分配 · 三口径共源 → 口径不对称构造性移除(消 R13-R16 的根)。删 4 个旧独立口径函数。
- **R17**(DP 后 codex 在更清晰结构下暴露两 latent 缺陷 · 旧贪心码同样漏 · 非 DP 引入):F1[high] NFKC 多字符展开(℡→TEL)片段命中被撑成整字符 false strong → 回映边界校验(`_normalize_text(切片)==norm_quote`)一次闭合整类;F2[medium] distinct==demand 不保证非重叠可行(重叠候选 `ＡAA`/`AA`×2)→ 区间调度 `_max_nonoverlap_capacity` · ambiguity 用「distinct>demand ① 或 capacity<demand 且 <distinct ②」· 与耗尽(capacity<demand 但 ==distinct 无重叠)显式区分。**F2 = option[2] DP 分配 mandate 的补齐(非再 litigate 熔断轴)**。
- **R18 approve**:codex 确认 F1 回映校验挡住 NFKC 片段 · F2 distinct/capacity 双口径与耗尽区分逻辑自洽 · 0 material。

**熔断机制实证有效**:R16 连续四轮同轴触发熔断 → operator 用 [2] DP 裁决终结循环 → R17/R18 收敛。这印证 spec 的「贪心证不出用 DP」契约分支是对的兜底 · 熔断防了 R17/R18/... 无限贪心补丁。

**codex 权威地位 + 环境恢复**:本 FU 全 18 轮真 codex(companion 1.0.4 · adversarial-review --wait --base aa4112d · 非 fallback)· 推翻 KG-38「本 worktree codex 必挂 · fallback 首选」判断(codex 挂死非永久 · 跨天恢复 · 详见 dogfood KG-41 · 建议 forge 给 fallback 判据加时效)。R18 codex 受 sandbox bwrap 限制未本地复跑 pytest(静态审)· 执行面由 build 侧 993 passed 补证。

## §3 Suggested IDS actions

**2 个 defer(operator 决 · 非缺陷 · 须 operator review 时确认)**:

1. **R8 弱锚计入 verdict = operator 冻结口径(非缺陷)**:M3' 前提是隐性信号(分析师不显说 ticker 字面)· 故 `is_weakly_anchored`(extractor.py)只拒空白 span · 不硬要求 ticker 字面出现在引文内(强字面匹配会误毙真隐性信号)。弱锚率记 census report 供 operator 审引文质量。codex R8 提此为 medium · R9 已认同这是 operator 冻结口径不再提。**defer:真跑读数时 operator 结合 weak_anchor 率人工终裁**(不是代码 bug)。

2. **R10 并发复用 proposer 污染 = 假设场景(docstring 约束)**:census 当前全单进程串行(无并发路径)· proposer stats 用 run-scoped delta(stats_at_start 快照差值)防重试污染。若未来引入并发复用同一 proposer 实例会有 stats 竞态 · 但当前无此路径。已 docstring 约束「单 run 作用域」。**defer:并发化时须重审 stats 隔离**(当前非缺陷)。

**下一步(operator 决议入口)**:
- **本 FU 不重跑真普查**(烧 key · 需 operator 凭据 + `/handback-review 009` 另行授权)。KG-40 契约阻断已解 · 但 E1 真密度读数须 operator 授权重跑才能产出。**请 operator 决:是否授权用引文回锚制重跑 sample-size 真普查**(阈值候选 threshold_hash 已 preregister 签字 · 重跑前须确认 DEEPSEEK key 就位)。
- **仍在 out-of-scope(未动)**:E2/E3 仍零代码(gated on E1 真密度显著)· gold_layer1/caliber/0017/004 已 ship 件仍不碰。
- **框架回流**:dogfood KG-41(codex 恢复 · fallback 判据时效性)随本 hand-back 回流 · 建议 operator 攒批回 `/expert-forge 009` 议 CLAUDE.md fallback 段判据。

**baseline**:aa4112d(DeepSeek 接线批次)· 本 FU commit 76036af(feat/009-spec · 未 push · 待 operator 确认)。
