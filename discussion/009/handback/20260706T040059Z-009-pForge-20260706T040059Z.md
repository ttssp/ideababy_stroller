---
discussion_id: 009
prd_fork_id: 009-pForge
handback_id: 009-pForge-20260706T040059Z
from_build_repo: /home/ys/codes/XenoDev
to_source_repo: /Users/admin/codes/ideababy_stroller
workspace:
  source_repo: /Users/admin/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev
  working_repo: /home/ys/codes/XenoDev
  handback_target: /home/ys/codes/ideababy_stroller/discussion/009/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - prd-revision-trigger
  - drift
severity: high
created: 2026-07-06T04:00:59Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: T015
---

# Hand-back · 009-pForge · T015

**Severity**: high
**Tags**: prd-revision-trigger,drift
**Created**: 2026-07-06T04:00:59Z

## §1 Rationale(执行摘要)

T015 = M2 alpha 头的「上台」层:把 T013 三件套(TripletMetrics)包成新 StrategyModule lane(D-M2-1),经 004 registry get_all() **平权**进 conflict_reports(O8)。新增 `strategy/alpha_lane.py`(AlphaLane + AlphaResultProvider Protocol + Mock)+ 单测 · 全 009 fork 内 · 不碰 004 已 ship(不违 OUT-10)。

**结论:alpha_lane.py 本身作为「能力层」已稳(28 测 / 100% coverage · 全 004 套 684 passed 无回归 · ruff clean),但 codex 3 轮 adversarial-review 逐层暴露了一个 T015 spec 没预见的下游集成架构缺口 —— O8「平权上台」在 004 现有下游(缓存键 / 双渲染 / ConflictReport 结构)承载不了「第四路非方向 evidence」。operator 决:切回 IDS 走 prd-revision-trigger,不在 XenoDev 硬修拖多个 spine 文件。T015 未 merge 到 feat/009-spec(下游澄清前 O8 不算真达成)· worktree + 8-commit 完整审计链保留。**

**codex 3 轮单调下钻(R1→R2→R3 · 全 needs-attention · 每轮实质 finding · 越钻越深进下游)**:
- **R1**(2 high + 1 medium):(F1)direction 系统性反向 —— 原从 mean_excess 符号产 LONG/SHORT,但上游 realized_return.py:231 excess 已按历史信号方向 signed(long→+ / short→−),成功历史 short call 亦产正 mean_excess,mean_excess 符号**不含当前多空方向** → 修为只产 NEUTRAL 质量旁证;(F3)TripletMetrics 矛盾值静默信任 → 加 _metrics_inconsistency 自洽校验(有限数/n>0/significant 与 |z|≥1.96 一致/mean 与 z 同号)矛盾则 fail-closed;(F2)默认 registry 未接入 → operator 决维持 Option b 生产接线留 T016。
- **R2**(1 high + 1 medium):(F1)R1 修后把非方向 alpha 塞进 Direction.NEUTRAL,但 004 NEUTRAL 语义是「中性/持仓观点」→ 显著正/显著负/真中性观点塌缩成同一高置信 NEUTRAL,下游当第四路方向票 → 改 direction=NO_VIEW + confidence 恒 0.0 + 显著性强度移到 inputs_used["evidence_strength"] + is_directional=False + signal_kind="alpha_quality_evidence";(F2)provider 异常击穿 fail-closed → try/except 降级 NO_VIEW。
- **R3**(1 high + 2 medium):(F3)provider 宽捕获吞编程错 → 修为异常分层(可恢复 OSError/TimeoutError/ValueError 降级 · 编程错 AttributeError/TypeError fail-loud 重抛 + error_type 结构化可观测)· **已在 XenoDev 修完**;(F1)+(F2)= **架构缺口 · 切回 IDS**(见 §2)。

alpha_lane.py 内部能修的(R1 反向 / R1 自洽 / R2 NO_VIEW 语义 / R2+R3 provider 异常分层)全部已修 + 回归测锁死 + mutation 红绿证(NEUTRAL→LONG 7 测 kill · F3 三门各 1 kill · conf floor 4 kill)。

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

**prd-revision-trigger 核心(codex R3-F1 + R3-F2 · 均已核 004 真代码铁证)**:D-M2-1「alpha 平权上台」在 spec 里只定义了「经 registry get_all() 平权进 conflict_reports」(O8),但**没想清 004 下游是否承载得了「第四路 / 非方向 evidence」这个新概念**。004 现有下游是为「恰好 3 路方向票」设计的,加第四路(尤其「非方向 evidence」语义)在两处击穿:

**① R3-F1 [high] · alpha 强度未进 conflict_report 缓存语义键(silent-wrong)**:AlphaLane 有效结果全是 `direction=no_view · confidence=0.0`(R2-F1 决策的必然结果 —— 非方向证据不能占 direction 方向槽)· 强度只在 inputs_used["evidence_strength"]。但 `conflict_report_assembler.py:159-164` 的 signals_hash 只 hash `source_id={direction}:{confidence_bucket}` → **强 alpha / 弱 alpha / provider error / no provider 全都是 `analyst_alpha=no_view:low`,缓存键完全相同** → 同 ticker 下复用旧 divergence_root_cause/has_divergence。用户看到 signals 里有新的 evidence_strength,但报告根因是旧的 = silent-wrong。**修法需改 assembler 缓存键语义(把 reason/signal_kind/is_directional/evidence_strength/z_excess 纳入 signals_hash,或结构化输入前禁该路径缓存)· 在 T015 file_domain 外(assembler = spine)**。

**② R3-F2 [medium] · 第四路被双渲染层硬编码 signals[:3] 截断**:O8 要 analyst_alpha 平权进 conflict_report,assemble 层确实达成(report.signals 含 4 路)· 但用户可见路径**两处**硬编码三路:`telegram/conflict_narrative.py:54` `signals = report.signals[:3]` + `ui/router_conflicts.py:169` `first_three = signals[:3]`。AlphaLane 作为第四路在 assemble 后进 report.signals,却在 Telegram + UI 两个用户可见路径**完全丢失**。O8 测只断 assemble 结果,覆盖不到渲染层。**修法需改双渲染为 N 路 / 显式区分方向票 vs 非方向 evidence · 均在 T015 file_domain 外(UI 层)· 且「非方向 evidence 怎么展示」本身是产品决策**。

**为什么切回 IDS(CLAUDE.md 决策矩阵「spec 与实现深层矛盾 / 重大架构转向」)**:R3-F1/F2 不是 alpha_lane.py 内部 bug,是 D-M2-1 的 O8 定义与 004 实际下游架构之间的缺口。继续在 XenoDev 硬修会把 T015 从「单文件 lane」膨胀成「改 assembler 缓存 + 双渲染层 + 定义非方向 evidence 展示语义」的跨 spine 改动 —— 越 file_domain + 越 Option b(operator R1-F2 已批)+ 可能牵动其他 task + 且「非方向 evidence 怎么承载/展示」是需要 forge 的产品/架构决策,不该 XenoDev 当场定(V4 失败模式:静默扩范围绕治理)。R3-F3(异常分层)是 alpha_lane.py 内部收窄 · 已在 XenoDev 修完不越界。

## §3 Suggested IDS actions

**交付(未 merge · 能力层已稳)**:1 新 production(alpha_lane.py · AlphaLane + AlphaResultProvider Protocol + MockAlphaResultProvider)+ 1 新测(28 测:结构一致/C8/C9/R9/三层 fail-closed/NO_VIEW 非方向证据映射/自洽校验/provider 异常分层)· alpha_lane.py 100% coverage · 全 004 套 684 passed 无回归 · ruff clean · mutation 红绿证全 kill · §4.4 verifier 全过。8-commit 完整审计链(test/feat/fix 交替 · R1→R2→R3 全轨迹)在 branch `task/009-pForge-T015-alpha-lane`(worktree 保留 · 未 merge)。**reviewed-by 未标 approve(codex R1-R3 全 needs-attention · R3-F1/F2 切回 IDS)**。

**Suggested IDS actions(operator 在 IDS 跑 /handback-review 009 决议)**:

1. **澄清 D-M2-1 的 O8 下游集成语义(prd-revision 核心)**:spec O8「alpha 平权进 conflict_reports」需补下游承载定义 —— (a)ConflictReport 是否扩展支持「非方向 evidence」一等公民(vs 复用 direction 5 值)· (b)缓存键语义是否纳入 evidence 强度(否则 R3-F1 silent-wrong)· (c)Telegram/UI 渲染是否从「固定 3 路方向票」改「N 路 + 方向票/非方向 evidence 分区展示」。这三条是「非方向 evidence 怎么上台」的产品+架构决策,建议 forge。

2. **重估 alpha 上台的信号形态(承 R1→R2 教训)**:R1 证实 alpha 不能产方向(上游 signed),R2 证实不能借 NEUTRAL —— alpha 的本质是「分析师历史 edge 质量旁证」而非「第四路方向票」。若 IDS 决议 alpha 应影响决策的**方向**,需 provider 扩返当前 resolved signal direction + follow/fade 语义(T016+ · 需 spec 明确);若决议 alpha 只作**质量权重/旁证**,需 ConflictReport/UI 支持非方向 evidence 展示。这是 M2 alpha 头「怎么落进决策台」的根本设计,建议随 §1 一起 forge。

3. **T015 后续路径**:待 IDS 澄清后,(a)若扩 ConflictReport/缓存/渲染 → 可能拆新 task(T015 下游集成)或并入 T016;(b)alpha_lane.py 能力层已稳,按澄清结果调 signal 形态(NO_VIEW+evidence vs 扩展 direction)后再 merge。**当前不 merge 是刻意的** —— 避免把「O8 假达成(assemble 层进了但用户看不到 + 缓存 silent-wrong)」ship 进 feat/009-spec。

4. **已在 XenoDev 修完不需 IDS 动作**:R1-F1 反向语义 / R1-F3 自洽校验 / R2-F1 NO_VIEW 语义 / R2-F2+R3-F3 provider 异常分层(可恢复降级 + 编程错 fail-loud + error_type 可观测)· 全部 alpha_lane.py 内部 · 回归测 + mutation 锁死。

**carry-forward known-gap**:AlphaResultProvider 真 DB-backed(读 0015 analyst_alpha_eval)+ build_default 生产接线 = T016(Option b · R1-F2 operator 已批)· 本 task Mock + 手动 register 证 O8 能力可达。
