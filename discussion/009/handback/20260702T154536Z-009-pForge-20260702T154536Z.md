---
discussion_id: 009
prd_fork_id: 009-pForge
handback_id: 009-pForge-20260702T154536Z
from_build_repo: /home/ys/codes/XenoDev
to_source_repo: /Users/admin/codes/ideababy_stroller
workspace:
  source_repo: /Users/admin/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev
  working_repo: /home/ys/codes/XenoDev
  handback_target: /home/ys/codes/ideababy_stroller/discussion/009/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - feature
severity: medium
created: 2026-07-02T15:45:36Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: T004
---

# Hand-back · 009-pForge · T004

**Severity**: medium
**Tags**: feature
**Created**: 2026-07-02T15:45:36Z

## §1 Rationale(执行摘要)

T004 · 009-pForge M1 Tiingo(美股)价格源 ship：实装 TiingoSource — 抓 Tiingo 日线 → 映射 PricePoint(close_raw + adj_factor_fwd=adjClose/close + source='tiingo')→ 落 price_daily；token 走 env 指针（凭据隔离硬约束1）。codex adversarial-review 6 轮 R6 authoritative approve（0 material findings）。与 T005 BaoStock 文件域不重叠，并行开发。

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

**tag = feature（clean task ship）**

按 frozen spec（reviewed-by codex@2026-07-02）实装，无新 PRD 修订触发。

**交付内容**：
- `price/sources/tiingo.py`（新）：`TiingoSource`
  · `fetch(ticker, *, start, end) -> list[PricePoint]`：httpx（已在栈）经 DI 注入的 http_getter 抓 Tiingo daily REST；`adj_factor_fwd = adjClose/close`（Tiingo 未直接给因子，反算）；close=0/无 adjClose → 因子 None（不静默当 1.0）。
  · `fetch_status(ticker)`：**恒 raise TiingoCoverageError**（见下"契约细化"）。
  · `persist/persist_status`：同步 sqlite3（对齐 T003）· 事务原子 + full-PK 全值比对幂等（含 ingested_at + tuple 规范化兼容 sqlite3.Row）。
  · `load_token_from_env_pointer(env_name)`：env 存文件路径，读文件内容（凭据隔离硬约束1 · 名不读值）。
- `price/sources/__init__.py`（新 · 包标记）。

**as-of/身份/凭据红线**：
- as_of/ingested_at 写固定宽度 canonical UTC（YYYY-MM-DDTHH:MM:SSZ）· 对齐 T003 读侧 GLOB（否则写进去查不到）。
- trade_date 经严格 date.fromisoformat 解析（非静默切片 · 防格式漂移污染 T003 TEXT 排序）。
- token 走 Authorization header（不进 URL query · 防 access log/APM/异常泄漏）。

**review 链（codex 全程 6 轮 · 逐轮加深）**：R1(date切片/token URL/persist事务)→R2(幂等漏ingested_at+row_factory)→R3(空metadata静默active=survivorship)→R4(从endDate推delisted错·endDate是覆盖边界非退市日)→R5(有覆盖写active又成false-active)→**R6 approve**。

**契约细化（impl-driven · KG-22 · 需 T006/operator 知）**：
spec 原设想 `fetch_status → delisted/active`。但 codex 核实 Tiingo EOD 官方文档：startDate/endDate 是**价格覆盖区间**（endDate=最新可用价格日），**不是退市信号**；活跃股（AAPL）也带 endDate。且 Tiingo 免费档**无独立可靠 active/delisted 信号**（正是 M1-R1/PRD §11 缺口①预判）。故 `fetch_status` **恒 raise TiingoCoverageError**（状态列 = 诚实 known-gap，不静默造 active/delisted 双向 survivorship bias）。**价格路径 fetch() 不受影响**（Tiingo 真正价值 = 日线+复权近 CRSP · O5 美股硬门）。退市覆盖走 deferred 实测（delisting-coverage-pending · 需付费 EODHD 或手工补）。

## §3 Suggested IDS actions

**无需 IDS 决议动作**（tag=feature · clean ship · 无 PRD 矛盾）。以下为知会 + T006 集成前置：

1. **知会：T004 ship**，美股 Tiingo 价格源就位。T006（M1 ingest CLI · P1 PPV owner）可调 `TiingoSource.fetch/persist` 落美股日线。

2. **T006 集成前置（契约细化 · impl-driven）**：
   - `fetch_status` 对 Tiingo 恒 raise `TiingoCoverageError` —— T006 路由美股状态查询时须知 Tiingo 状态列 = known-gap，不要期望它返 active/delisted。美股退市状态走 deferred 实测或另源。
   - token 经 `load_token_from_env_pointer("XENODEV_009_TIINGO_TOKEN_FILE")` 解出后传 `TiingoSource(token=...)`；env 指向 token 文件路径（不是值）。T006 需在 env 就位后调。

3. **退市覆盖实测 deferred（M1-R1）**：`test_should_verify_real_delisting_coverage_when_live_token` 标 skip（delisting-coverage-pending）· 需真 Tiingo token + 网络；免费档退市覆盖不足是已知风险（PRD §11 缺口①），实测不足则标 known-gap，不假装全覆盖。

4. **spec 契约细化知会（KG-22）**：本 task 把 spec 原 `fetch_status → delisted/active` 收敛为"Tiingo 免费档状态不可判定 → 恒 raise"。这是 impl 阶段核实 Tiingo API 真实语义后的诚实修正（与 M1-R1"不信文档·免费档退市覆盖不足"一致），非 scope 变更。若 operator 认为需更新 spec 文字，可在 handback-review 时标注。
