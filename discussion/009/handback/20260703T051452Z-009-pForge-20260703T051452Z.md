---
discussion_id: 009
prd_fork_id: 009-pForge
handback_id: 009-pForge-20260703T051452Z
from_build_repo: /home/ys/codes/XenoDev
to_source_repo: /Users/admin/codes/ideababy_stroller
workspace:
  source_repo: /Users/admin/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev
  working_repo: /home/ys/codes/XenoDev
  handback_target: /home/ys/codes/ideababy_stroller/discussion/009/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - feature
severity: medium
created: 2026-07-03T05:14:52Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: T011
---

# Hand-back · 009-pForge · T011

**Severity**: medium
**Tags**: feature
**Created**: 2026-07-03T05:14:52Z

## §1 Rationale(执行摘要)

T011 · 009-pForge M2 join 合约(alpha/join.py)ship:实装唯一 join 路径 resolve_signals(advisor_id, conn) → (ResolvedSignal[], Rejection[]) —— 把"分析师信号 → (ticker, market, direction, signal_published_at)"解析成唯一无歧义信号,七类歧义**默认拒绝不猜**(D-M2-2 · M2-R4)落 analyst_alpha_rejections。market 只从 004 watchlist 权威查表(rev-1 · specrev-R3 · 严禁 ticker 字符串猜);signal_published_at 是 T012 取价 entry_date 的唯一权威时间基准。

codex 额度用尽(恢复 12:57)→ 走 subagent-fallback 双跑(red-team 对抗 + code-reviewer 五维)· high-risk correctness-spine 全程双跑 · **4 轮全走完 → R4 APPROVE · 0 ship-blocking**。fallback 抓真缺陷极多(比单轮深):R1 两 blocking(跨周 scope 泄漏 + distinct-date 该拒不拒)· R2 两 critical(json_extract 崩整批 + 跨 advisor 泄漏)· R3 一 high(savepoint 最外层强制 commit)· R4 confirm + 一 medium(commit 契约文档化)。范围内全闭合 · 越界 1 个(#B 跨 advisor)operator 决分流。

20 单测绿 · coverage 95% · 全 004 套 560 passed 无回归 · ruff clean · 6 复现脚本证每个 blocking 消除。severity=medium 因 #B 跨 advisor 泄漏是 v0.2 前置真约束 + R4 commit 契约是 T012 集成前置(见 §3)。

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

**tag = feature（M2 join 合约实装 · 含 2 个 carry-forward 分流 · 见 §3）**

按 frozen spec（reviewed-by codex@2026-07-02）实装 D-M2-2 唯一 join 路径,无新 PRD 修订触发（但 #B 触及 004 strategy_signals schema 约束 + R4 commit 契约触及 T012 集成,均需 carry-forward · 见 §3）。

**交付内容**（新增 alpha/ 包 · 只 ADD 不改 004 · OUT-10）:
- `RejectionReason` StrEnum:严格对齐 0015 rejections CHECK 7 枚举(multi_ticker/no_signal_date/duplicate_signal/multi_direction/sector_only/source_conflict/unknown_market)
- `ResolvedSignal` / `Rejection` pydantic frozen model:ResolvedSignal 含权威 market(Market enum)+ signal_published_at + join_source
- `resolve_signals(advisor_id, *, conn)`:真读 004 advisor_reports.structured_json + strategy_signals + watchlist(不 mock)· 七类歧义判定 · market 权威查表 · rejections 幂等 savepoint 原子落库
- `_signal_week` / `_resolve_market` / `_reject` / `_persist_rejections` 内部助手

**核心红线实装**:
- 歧义默认拒绝(M2-R4):七类各落对应 reason enum · 绝不 best-effort 猜
- market 权威路径(rev-1):只 watchlist 查表 · 查不到 unknown_market 拒 · 零 ticker 字符串启发式
- signal_published_at 唯一时间基准 · 缺则 no_signal_date 拒
- accepted 只收 long/short(SLA C16 · 0015 eval CHECK)· neutral/wait/no_view 静默跳过非歧义

**review 链（subagent-fallback 4 轮 · codex 恢复后优先补跑权威）**:
- R1(双跑 BLOCK · 2 blocking 同根):red-team Case A(distinct-date 该拒不拒 · sorted[0] 猜最早为权威)+ code-reviewer Critical(strategy_signals 查询 WHERE ticker=? 未按 advisor-week scope · 跨周池化泄漏 → T012 取错周 entry 价)→ **operator 决方案一**:join 加 advisor_week_id scope · distinct-date 一律 duplicate_signal 拒
- R2(分歧:red-team BLOCK / 五维 APPROVE · red-team 2 新 critical):#A json_extract 遇 malformed inputs_used_json 崩整批(R1 引入)→ Python 侧 json.loads guard(_signal_week)· #3 _persist 裸 commit 抢占共享连接 → SAVEPOINT · **#B 跨 advisor 同 week 泄漏**(operator 决分流 · 见 §3)
- R3(red-team NEEDS-ATTENTION · 1 high):SAVEPOINT 作最外层时 Python sqlite3 不拦 RELEASE → 强制 commit 落盘 · 调用方 rollback 撤不掉(违契约)→ in_transaction 检查 + 自开 BEGIN
- R4(red-team APPROVE · 0 blocking + 1 medium):6 场景直执行验证 R3 fix 全闭合 · medium(commit 契约文档化)当轮补 resolve_signals public docstring

## §3 Suggested IDS actions

本 task ship 内全闭合;**2 个 carry-forward** 需 operator/后续 task 处理（均 v0.2 或 T012 前置 · 类比 T010 F5/F6 分流范式）:

### carry-forward #1 · 跨 advisor 同 week 泄漏（R2 red-team #B · v0.2 前置 · operator 已决分流）

**现象**（已复现）:v0.2+ 多 advisor 时,两 advisor 各自在同一 week_id 对同 ticker 出报告 → `resolve_signals("advA")` 会捞到 advB 的同 week_id+ticker 的 strategy_signal → 同一 signal 行被**双重归属**（伪 accepted / 伪 multi_direction 拒）。

**根因**:strategy_signals 表**无 advisor_id 列**,inputs_used_json 只有 advisor_week_id（= week_id 日历串,不带 advisor 身份）· advisor_reports 的 UNIQUE(week_id, source_id) 允许多 advisor_id 共享 week_id。join scope key 只能到 `week_id + ticker`,不足区分 advisor。

**为什么不当场修**:strategy_signals 无 advisor 定位键是 **004 已 ship schema 固有约束**。彻底修需（选项 A）改 `strategy/advisor_strategy.py` 写 advisor_id 进 inputs_used_json + 历史数据回填 → **越 T011 file_domain（只 alpha/）+ 违 spec OUT-10（不改已 ship 004 表/信号产生逻辑）**;（选项 B）join 侧经 signal_id 反查 advisor_report → **物理不可行**（strategy_signals 无外键回指 · inputs_used 无 advisor 键）。

**为什么 v0.1 ship 合理**:**v0.1 明确单 advisor**（domain/advisor.py:28 · source_id 仅 schema 预留 · v0.2+ 才多源）· 单 advisor 下只有一个 advisor 的报告 + 信号 → **不触发**。join.py module docstring 已显式标此已知边界 + v0.1 假设。

**operator 决策项（v0.2 多 advisor 前必决）**:是否给 004 strategy_signals 加 advisor 定位键（改 signal 产生 + 数据迁移 · 需 spec 决议 · 类比 T010 F6 触及 004 既有写路径）。

### carry-forward #2 · resolve_signals 连接 commit 契约（R4 red-team medium · T012 集成前置）

**现象**（已复现 · file-based DB）:resolve_signals 作连接第一个写时自开 BEGIN 承载 rejection 落库,返回时事务保持 open（持久化决策留调用方）· 若调用方（T012）忘 commit/rollback → 持 SQLite 文件级写锁,阻塞该库其它 writer（"database is locked"）。

**处置**:非阻断（T011 无实际调用方）· join.py resolve_signals **public docstring 已显式补 commit 契约**（调用方须及时 commit/rollback · 组合更大 unit-of-work 用同 conn BEGIN 包 · rejection 批经 SAVEPOINT 嵌套隔离不误伤调用方写）。

**T012 前置建议**:T012 task spec 宜加 bullet 明示此 commit 契约,防实现者只从私有 helper 注释发现。T012 realized-return 引擎消费 ResolvedSignal 时,在同一 conn 上把 resolve + eval 写包成一个原子事务,末尾统一 commit。

### 框架级 dogfood（本轮无新增）

本 task 无新框架级缺陷（KG-30 red-green log gitignore 已知 · 沿用既有处置 · log 物理存在但不进 commit）· codex 额度用尽走 fallback 是 CLAUDE.md 已治理机制。codex 恢复后**优先补跑 codex 权威确认**（spine task · 若 codex 翻案按 codex 为准）。
