---
discussion_id: 009
prd_fork_id: 009-pForge
handback_id: 009-pForge-20260702T154812Z
from_build_repo: /home/ys/codes/XenoDev
to_source_repo: /Users/admin/codes/ideababy_stroller
workspace:
  source_repo: /Users/admin/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev
  working_repo: /home/ys/codes/XenoDev
  handback_target: /home/ys/codes/ideababy_stroller/discussion/009/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - feature
severity: medium
created: 2026-07-02T15:48:12Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: T005
---

# Hand-back · 009-pForge · T005

**Severity**: medium
**Tags**: feature
**Created**: 2026-07-02T15:48:12Z

## §1 Rationale(执行摘要)

T005 · 009-pForge M1 BaoStock(A股)价格源 + 港股 adapter 留位 ship：实装 BaoStockSource — 自拉不复权/qfq/hfq 三个 resultset（每行校验 code 身份）→ 映射 PricePoint（close_raw + qfq/hfq 复权因子 + source='baostock'）→ 落 price_daily；退市显式（status=0，A股覆盖最全）。港股 = HKSourceDeferred 四件套 raise NotImplementedError（NG-12 · 免费源接不了）。review：codex R1-R3（3 high 修）→ 额度用尽（至 Jul 7）→ subagent fallback（red-team + code-reviewer）· red-team 抓出 codex 漏的重复日 [high] · 修后确认 0 ship-blocking。与 T004 Tiingo 文件域不重叠，并行开发。

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

**tag = feature（clean task ship）**

按 frozen spec（reviewed-by codex@2026-07-02）实装，无新 PRD 修订触发。

**交付内容**：
- `price/sources/baostock.py`（新）：`BaoStockSource`
  · `fetch(ticker, *, start, end) -> list[PricePoint]`：**自己**拉三个 resultset（adjustflag=3/2/1 = 不复权/qfq/hfq），每个 resultset 每行都校验 code 身份（因子输入绑定到已证明 ticker，不接受外部裸 map）；adj_factor_fwd=qfq_close/close_raw · back=hfq_close/close_raw；某日缺复权 close / close_raw=0 → 因子 None（不静默当 1.0）。**同 trade_date 重复行 → fail-closed raise**（歧义不猜）。
  · `fetch_status(ticker)`：query_stock_basic，status=0 退市（须有 canonical outDate）· status=1 上市。**五路 fail-closed**：无记录/多行/code 不匹配/status 非 0-1/status=0 缺 outDate → raise BaoStockCoverageError（反 survivorship bias · M1-R1）。
  · `persist/persist_status`：同步 sqlite3 事务原子 + full-PK 全值幂等（含 ingested_at + tuple 规范化）。
- `price/sources/hk_deferred.py`（新）：`HKSourceDeferred` 四件套（fetch/fetch_status/persist/persist_status）均 raise NotImplementedError（NG-12 留位 · 不静默返空/假装接了）。
- `price/sources/__init__.py`（包标记 · 与 T004 同 · 已由 T004 合入）。
- 依赖：+baostock>=0.8,<1.0（pyproject `[project.dependencies]` + tech-stack.md §2 · 非 broker/options/crypto 不撞红线 · uv.lock 同步）。

**as-of/身份红线**：as_of 固定宽度 canonical UTC（对齐 T003 读侧 GLOB）· trade_date 严格 ISO 解析（三路 date + outDate）· _rows_of 行宽校验（schema 漂移 fail-closed）。

**review 链**：
- codex R1（fetch_status 信任 rows[0] → 五路 fail-closed）→ R2（fetch 忽略 k线 code + fetch_status 缺空 code → _require_row_code 抽取 · fetch+fetch_status 都调）→ R3（qfq/hfq 因子外部裸 map 无身份 → fetch 自拉三 resultset 每行校验 code）。
- codex 额度用尽（至 Jul 7）→ subagent fallback（CLAUDE.md 规则 · high-risk = red-team + code-reviewer 双跑）。
- **red-team 抓出 codex R1-R3 都漏的 [high]**：`_fetch_close_map` 无重复 trade_date 守卫 → 同日两行静默保留最后一行 = 错复权因子静默污染（复现 qfq [9.0,1.0] → 因子 0.1 应 0.9）→ 修：重复日 fail-closed（qfq/hfq/raw 三路）。code-reviewer 端到端复权 round-trip 验证通过 + 3 非阻断建议（行宽/HK persist_status/preclose）全采纳。
- **fallback 确认轮：0 ship-blocking · CLOSED**。

**契约细化（impl-driven · KG-22 · 需 T006 知）**：spec 原 `fetch` 由调用方预取 qfq/hfq 传入；codex R3 指出裸 map 无身份会串错标的污染复权价 → 改为 BaoStockSource **自拉**三个 resultset（每行 code 校验）。fetch 签名去 qfq_close/hfq_close kwargs —— T006 调用方更简单更安全（不必自己预取）。

## §3 Suggested IDS actions

**无需 IDS 决议动作**（tag=feature · clean ship）。以下为知会 + T006 集成前置 + 框架级 dogfood：

1. **知会：T005 ship**，A股 BaoStock 价格源 + 港股留位就位。T006（M1 ingest CLI · P1 PPV owner）可调 `BaoStockSource.fetch/persist` 落 A股日线（含 qfq/hfq 复权因子）。

2. **T006 集成前置（契约细化 · impl-driven）**：`BaoStockSource.fetch(ticker, *, start, end)` 已改为**自拉**三个 resultset（不再需调用方传 qfq_close/hfq_close）—— T006 直接调即可，因子由 source 内部按身份校验拉齐。港股 ticker 路由到 `HKSourceDeferred` 会在 fetch/fetch_status/persist/persist_status 任一 raise NotImplementedError（NG-12）—— T006 须显式处理此 deferred（不当"接了"）。

3. **退市覆盖实测 deferred（M1-R1）**：`test_should_verify_real_delisting_coverage_when_live` 标 skip（delisting-coverage-pending）· 需真 baostock login + 网络；A股 BaoStock 覆盖历史相对最全，实测时对已知 A股退市标的验 delisted 显式。

4. **框架级 dogfood 兑现记录（KG-32 实战）**：本 task 是 CLAUDE.md「cross-model review fallback」+ KG-32 的**第二次实战**（首次 = T003），且**更强验证了规则价值**：codex R1-R3 三轮都没抓到 `_fetch_close_map` 重复日 [high]，是 fallback 的 red-team 抓到的（code-reviewer 也没抓 —— 它的 round-trip 测试用干净单行数据）。这印证了 CLAUDE.md 规则「high-risk = red-team + code-reviewer 双跑」的必要性（单一 reviewer 会漏）。**codex 恢复（Jul 7）后应对 T005 补跑 adversarial-review 作权威确认**（fallback 是过桥非替代 · reviewed-by 已标 subagent-fallback@2026-07-02 不冒充 codex）。建议 `/expert-forge` 纳入 KG-32（codex-review SKILL 加 fallback 分流 · SKILL 是 mirror 件回 IDS 定）。

5. **spec 契约细化知会（KG-22）**：`fetch` 签名去 qfq_close/hfq_close kwargs（自拉更安全）· 非 scope 变更（安全加固）。若 operator 认为需更新 spec 文字可在 review 时标注。
