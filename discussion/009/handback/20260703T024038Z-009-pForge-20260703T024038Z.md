---
discussion_id: 009
prd_fork_id: 009-pForge
handback_id: 009-pForge-20260703T024038Z
from_build_repo: /home/ys/codes/XenoDev
to_source_repo: /Users/admin/codes/ideababy_stroller
workspace:
  source_repo: /Users/admin/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev
  working_repo: /home/ys/codes/XenoDev
  handback_target: /home/ys/codes/ideababy_stroller/discussion/009/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - feature
severity: medium
created: 2026-07-03T02:40:38Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: T007
---

# Hand-back · 009-pForge · T007

**Severity**: medium
**Tags**: feature
**Created**: 2026-07-03T02:40:38Z

## §1 Rationale(执行摘要)

T007 · 009-pForge M1 contract tests ship:把 spec §8 M1 四条契约(as-of双轴防未来+防backfill / 复权拆分 / 退市显式+防泄漏 / 唯一源)固化成回测可测 BLOCK gate。4 contract 文件 + conftest · 纯测试无 production。验证:契约测试验已ship T001-T006契约→写完即绿 · mutation testing 证 gate 非假绿(破坏provider双轴/复权数值→负例变红·恢复回绿)。review:codex 额度用尽(至Jul7)→subagent fallback 双跑(red-team+code-reviewer 一致0 ship-blocking·各自独立mutation确认双轴gate真有效)→修F1/F2/F4/F5(O4 AST盲区加固+抽conftest+skip)→red-team对抗复审approve·0 blocking。至此M1全task(T001-T007)ship毕。

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

**tag = feature（clean task ship · M1 验收 gate）**

按 frozen spec（reviewed-by codex@2026-07-02）把 spec §8 M1 四条契约固化成回测可测 pytest gate（违任一 = M1 BLOCK），无新 PRD 修订触发。

**交付内容**（4 contract test 文件 + conftest · 纯测试无 production code）：
- `tests/contract/test_m1_asof.py`（O1 as-of 双轴）：
  · `should_return_no_future_factor_when_as_of_before_factor_date`（防未来 trade_date）
  · `should_reject_backfilled_revision_when_as_of_after_query`（防 backfill 泄漏 · 同旧 trade_date 晚到修订 as_of>query 查不到 · codex R1-F1：单 trade_date<= 挡不住）+ 两条对照正例（防假绿）。
- `tests/contract/test_m1_adjust.py`（O2 复权）：
  · `should_compute_adjusted_close_correctly_when_split_occurs`（拆分样例 adjusted_close=close_raw×factor 数值精确 · 拆分不当暴跌）+ back 复权 + 因子缺返 None（不静默当 1.0 · C1）+ 因子 backfill 负例。
- `tests/contract/test_m1_delisting.py`（O3 退市 bitemporal）：
  · `should_mark_delisted_not_silent_empty_when_ticker_delisted`（退市显式 · survivorship-free）
  · `should_not_leak_delist_status_when_as_of_before_delist_event`（防退市状态泄漏 · codex R1-F1）+ 两条对照正例。
- `tests/contract/test_m1_unique_source.py`（O4 唯一源 · rev-1 决议 B 缩范围只审 PIT 直取路径）：
  · isinstance PITPriceProvider Protocol（动态）+ interface `__all__` 唯一暴露 + AST 审 get_adjusted_close 真实现唯一在 provider.py（Protocol stub 区分）+ close×factor 旁路扫描（加固含 ast.Subscript 下标）+ import/SQL 审查（price_daily 直读旁路）+ O4 消费侧 pytest.skip defer T008。
- `tests/contract/conftest.py` + `__init__.py`（新 · 共享 _run_upgrade/insert_price/insert_status/conn/provider fixture · session 缓存迁移模块 · T017 M2 contract 可复用）。

**验证方式（gate 真挡违约的证据）**：契约测试验证已 ship 的 T001-T006 契约 → 写完即绿。用 **mutation testing 证 gate 非假绿**：破坏 provider 双轴守卫（去 as_of<=query）→ asof/delisting 负例变红；破坏复权数值（不乘因子）→ adjust 负例变红；恢复后回绿。每个 negative test 配 positive control。

**review 链（codex 额度用尽至 Jul 7 → subagent fallback 双跑 + 对抗复审 · 2 轮收敛）**：
- R1：codex adversarial-review 不可用（超时/usage limit）→ 启用 fallback（CLAUDE.md 规则 · M1 gate correctness 关键 = red-team + code-reviewer 双跑）。
- R1-fb 两 reviewer **一致判 0 ship-blocking**（各自独立 mutation testing 确认 O1/O2/O3 双轴 gate 真有效）+ 4 改进：F1（O4 AST 旁路检测盲区 · 下标形态逃检）· F2（second-impl 只匹配字面名）· F4（helper 四份复制）· F5（adapter defer 空断言）· F6（provider._conn 私有耦合）。
- R2 修 F1（补 ast.Subscript + import/SQL 审查 · 验证下标旁路真抓红）/F2（docstring 声明局限）/F4（抽 conftest + __init__.py）/F5（pytest.skip）/F6（conn fixture 播种不碰私有）。
- R2-fb red-team 对抗复审 **approve · 0 blocking**：4 修复真跑验证（F1 注入下标旁路真抓 + F4 provider._conn is conn 同连接 + 播种 commit + 无迁移缓存污染 + mutation 抗性保持 + F5 报 SKIPPED 非 PASSED）· 1 low 残留（O4 getattr/变量 key 旁路仍逃 · **已披露非隐藏** · 追 T012）。
- **最终 0 ship-blocking · reviewed-by = subagent-fallback@2026-07-03**（codex 恢复 Jul 7 补跑权威确认）。

## §3 Suggested IDS actions

**无需 IDS 决议动作**（tag=feature · clean ship · 非 prd-revision-trigger · 无 PRD 矛盾）。以下为知会 + 下游 task 前置（含关键 defer 追踪项）+ 框架级 dogfood：

1. **知会：T007 ship 完成**，M1 四条契约 gate（as-of 双轴防未来+防backfill / 复权拆分 / 退市显式+防泄漏 / 唯一源）就位。至此 **M1 全部 task（T001-T007）ship 完毕** —— M1「唯一行情地基」的独立可用性验收门齐备。下游解锁：T012（M2 回测集成 · depends T007）。

2. **⚠ 关键 defer 追踪项（O4 消费侧 + 完备旁路检测 = T012 必须补 · 不可静默 drop）**：
   - 本轮 O4 唯一源**只交付供给侧**（price 包内无旁路复权实现 · rev-1 决议 B 缩范围）。**消费侧**（回测层只用 PIT 层这一个源）因 M2 回测层未实装（T012+）**显式 defer**，测试用 `pytest.skip` 标明（报告可见"这条被 defer"非假 PASS）。
   - fallback red-team 复审确认 O4 静态 AST 检测有**已披露的残留盲区**：`getattr(row,'close')*getattr(row,'adj')` / 变量 key 下标 `row[k1]*row[k2]` 仍能逃过全部三层检测（AST 乘法/名匹配/import-SQL 审查）。这是**启发式 tripwire 非完备证明**，已在 test docstring 诚实声明。
   - **T012 必须做**：M2 回测层实装时，做**消费侧唯一源审**（回测取价点只调 `get_adjusted_close` · isinstance/AST 审真实调用点）作为 O4 的完备验收，覆盖静态检测的盲区。**此项须在 T012 task 显式列为验收条件，不让 defer 静默丢失**（red-team R2-fb 明确建议）。

3. **T008 前置（rev-1 决议 B · 触发#2 gate）**：O4 的 004 兼容半（`CurrentStatePriceProvider` isinstance PriceDataProvider + xgboost lane 换真 provider 仍绿）= T008 验收，撞未决触发#2（PricePoint close-only vs 004 PriceBar OHLCV/volume）· 保持 ⛔BLOCKED。T007 已用 `test_o4_consumer_side_unique_source_deferred_to_t008` 显式标边界（防误判 T007 漏测 O4）。

4. **框架级 dogfood 兑现记录（KG-32 实战 · 第四次）**：本 task 是 CLAUDE.md「cross-model review fallback」+ KG-32 的**第四次实战**（T003 codex补跑抓深bug · T005 red-team抓codex漏的重复日 · T006 双跑一致锁定2 ship-blocking · T007 本次）。本次价值：两 reviewer 各自**独立 mutation testing** 确认 gate 真有效（不只看代码，真破坏 provider 验证负例变红）—— 印证对 contract test 这类"验证已实装契约"的 gate，fallback 的对抗 + mutation 是必要的假绿防线。**codex 恢复（Jul 7）后应对 T007 补跑 adversarial-review 作权威确认**（M1 验收 gate · fallback 是过桥非替代 · reviewed-by 已标 subagent-fallback@2026-07-03 不冒充 codex）。建议 `/expert-forge` 纳入 KG-32 + KG-33（worktree remove --force 丢 stash）。

5. **契约测试无 impl-driven 契约细化**：T007 是纯测试 task，验证 T001-T006 已定契约，未产生新签名/契约变更。测试名严格对齐 spec §6.1 指定名（should_..._when_...）。
