---
discussion_id: 009
prd_fork_id: 009-pForge
handback_id: 009-pForge-20260706T131309Z
from_build_repo: /home/ys/codes/XenoDev
to_source_repo: /Users/admin/codes/ideababy_stroller
workspace:
  source_repo: /Users/admin/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev
  working_repo: /home/ys/codes/XenoDev
  handback_target: /home/ys/codes/ideababy_stroller/discussion/009/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - feature
severity: low
created: 2026-07-06T13:13:09Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: T017
---

# Hand-back · 009-pForge · T017

**Severity**: low
**Tags**: feature
**Created**: 2026-07-06T13:13:09Z

## §1 Rationale(执行摘要)

T017 M2 contract tests shipped — the M2 acceptance gate. Turns spec section-8 M2 three contracts into runnable pytest (violating any one = BLOCK): (1) test_m2_source_id_unique.py asserts C8 (AlphaLane __init__ injects no registry / other lane — inspect signature audit against forbidden signal words registry/lane/assembler) + R9 (source_id = analyst_alpha unique, duplicate registry.register raises); (2) test_m2_no_aggregate_in_004.py asserts C10/R10 red-line (ConflictReport + StrategySignal pydantic schemas have NO winner/recommended/aggregate/priority/composite fields — schema field audit + AST field-definition audit of domain source, the latter walking only ClassDef->AnnAssign so comments naming the red-line are not misread as fields); (3) test_m2_no_cross_source.py asserts AC-2/C10 end-to-end (real ConflictReportAssembler.assemble with mock LLM: alpha routes to evidence_lanes as a single source_id analyst_alpha equal-standing lane, the three directional votes stay independent in signals un-merged, and hasattr(report, winner/aggregate/...) is False at runtime). CONTRACT-TEST TDD POSTURE: these assert EXISTING invariants (green from birth, no natural red phase), so teeth were proven by MUTATION verification (tests/red-green-log/T017.log): four mutations each break a distinct invariant (add a registry ctor param -> C8; make registry silently overwrite -> R9; add a winner field to ConflictReport -> R10; make the assembler drop alpha evidence -> cross-source merge) and each drives the targeting test red (rc=1); after revert all three go green, conflict_report existing 2 tests unchanged still green (R10 red-line held), full 004 suite 743 passed no regression, ruff clean. C8 HONEST READING (documented, not scope drift): spec section-8 C8 literally says it accepts only (LLMClient, SourceDataProvider), but T016b (per handback-review 009 decision) added an alpha_provider DI param mirroring xgboost price_provider DI (a data-result provider, NOT a registry/lane); the test therefore encodes C8 as its true intent of no registry/lane-typed injection rather than a rigid arity==2, and the docstring documents this divergence from spec literal text transparently. REVIEW: first ran codex review (medium test-only -> review tier), but the codex background job hit early Reconnecting 2/5, 3/5 session-stream drops, read all five invariant source files, then went fully silent for 7+ minutes producing no verdict (status still running, pid alive, reasoning stream dead) — a session-DROP not a quota exhaustion; cancelled it and fell back per CLAUDE.md policy to a code-reviewer subagent (medium single-layer test-only -> single-pass). The fallback reviewer independently re-ran assemble outside the harness and reproduced the routing, cross-checked every assertion against the five ground-truth sources + spec section-8 C8/C10, confirmed scope (4 files all additions, ZERO production code), and returned approve with 0 blocking + 3 non-blocking coverage-completeness nuances that the tests themselves already disclose as defense-in-depth and delegate to the 004 R3 architecture backstop (signal-word list misses non-lane-named sibling-lane injection; name-based aggregate detection misses semantic renames though the runtime hasattr check is stronger than model_fields; pytest.raises tuple is broader than the ValueError-only contract but harmless). REVIEW-LOG marked reviewed-by: subagent-fallback@2026-07-06 (not impersonating codex), with a note that codex may optionally be re-run when it recovers (this task is non-spine, blocks empty, so re-run priority is low). OUT OF SCOPE / NOT A DEFECT: the three non-blocking items are completeness nuances delegated to architecture, not correctness gaps; production wiring is unchanged (this is a test-only task). With T017 green the M2 layer acceptance gate is in place: the alpha chain data path (T016) and factory wiring capability (T016b) are now both guarded by runnable contract tests. M2 is complete (T017 blocks nothing).

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

(待补 · per T007 amendment · operator 跑 gen-handback 时未传 --section2)

## §3 Suggested IDS actions

(待补 · per T007 amendment · operator 跑 gen-handback 时未传 --section3)
