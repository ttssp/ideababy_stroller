---
discussion_id: 009
prd_fork_id: 009-pM3prime
handback_id: 009-pM3prime-20260716T162427Z
from_build_repo: /home/ys/codes/XenoDev-009
to_source_repo: /Users/admin/codes/ideababy_stroller
workspace:
  source_repo: /Users/admin/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev-009
  working_repo: /home/ys/codes/XenoDev-009
  handback_target: /home/ys/codes/ideababy_stroller/discussion/009/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "0de4fb398ede9db1"
tags:
  - prd-revision-trigger
severity: medium
created: 2026-07-16T16:24:27Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: T013
---

# Hand-back · 009-pM3prime · T013

**Severity**: medium
**Tags**: prd-revision-trigger
**Created**: 2026-07-16T16:24:27Z

## §1 Rationale(执行摘要)

**证伪链前两站 E0(口径预注册)+ E1(密度普查/两层金标)在 XenoDev-009 全站实装完成**(T001-T012 · 869 passed
/7 skip/1 xfail · ruff clean)· 两条 PPV(P1 E0 建表+预注册 · P2 E1 密度普查)真库端到端验过。**核心 E1 数字与判定
如下,交 operator 决 E2/E3 授权**(证伪链哲学:每站带 STOP · STOP=诚实结论非失败)。

**⚠ 首要结论(需 operator 拍板)**:E1 密度普查产**诚实 STOP(below_threshold_stop / insufficient_sample)**,
**但这个 0-accepted 数字来自确定性 `literal_ticker_proposer` 替身,不是真密度**。真密度测量需真 LLM 提取器
(`--proposer llm` + `--token-env`),本会话无 LLM 凭据故未跑真 LLM 路径。**因此本 hand-back 的密度结论是
"下界/占位",不是可据以否定 M3' 的最终密度**。operator 需决定:(a) 配 LLM 凭据补跑真密度普查再定 E2/E3;
(b) 先签预注册阈值(附录 B 候选未签字);(c) 是否回 IDS forge 重估。

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

本 hand-back 携 `prd-revision-trigger` tag,触发原因与决议链逐条:

### 1. E1 密度普查 → below_threshold_stop(诚实 STOP · 但需真 LLM 复核)

- **实测**(literal_ticker_proposer · sample 80 → 208 record 参与 · 避 OOS 窗后):accepted **0** · rejected **29**
  (全 `no_direction`)· per-market 密度 `{US:0, CN:0, HK:0}` · 全 < 预注册候选阈值 30 → verdict=below_threshold_stop。
- **⚠ 关键**:此 0-accepted **来自确定性替身 proposer 的弱表达能力**(英文 ASR 转写里中文情绪词/隐性荐股无法字面命中),
  **不是真密度**。record_tickers 只覆盖 164 个 replay record(1.8%)· 真密度须真 LLM 提取器读中文原文语义抽。
- **决议链**:密度真数字 = M3' ROI 闸门(forge v5 §underweights「没人读语料正文」)→ **需 operator 配 LLM 凭据
  (`--proposer llm --token-env <名>`)补跑真密度普查**,再定 below_threshold 是真结论还是替身假象。若真 LLM 也低 →
  诚实 STOP 成立(insufficient_sample · 回 IDS forge 重估闭环前提);若真 LLM 显著 → 请授权 E2。

### 2. 密度口径 spec 张力(per-horizon 阈值 vs horizon-less 信号)· review 双跑独立指出

- **张力**:spec 附录 B / O3 设计 **per (market, horizon∈{5,20,60})** 密度阈值,但一条疑似信号**结构上无 horizon**
  (horizon 是 E2 回测窗选择 · 提取时不产生)· 原实现把一条信号 fan 进 3 个 horizon 桶 = 假的 per-horizon 独立证据。
- **本仓已修**(review HIGH#1):密度改 **per-market horizon-independent** 计数 · verdict 比 per-market 阈值(诚实)。
- **需 operator 决**:这是**代码修 vs PRD 口径修**的边界 —— 若 PRD 坚持 per-horizon 密度,须定义"信号如何分配 horizon"
  (scope 决策 · 非纯 bug)· 否则确认 per-market 密度口径为准(建议 · 更贴信号本质)。→ `prd-revision-trigger`。

### 3. record_tickers 语义判定(O6)= weak_association / verify

- **实测**:抽样 60 record 字面比对 literal_rate ≈ 0.325 · record_tickers 来自 008 采集期 LLM 从 key_points 推的候选 ·
  DDL 无消歧元数据 · 只覆盖 replay 164 record → **weak_association**(弱关联)· 决策 **verify**(当候选集校验,不当 ground-truth)。
- **决议**:census ticker 解析走 verify 路径(不直接 reuse record_tickers · 否则把 ~67% 噪声底带进密度)· 已落 record_tickers_judgment 表。

### 4. 两层金标 harness 就绪但未跑真标注(需真人第二源)

- **Layer-1**(α + span P/R/F1 · pure-math 验过 perfect=1.0/half=0.125/inverse=-0.667)+ **Layer-2**(映射一致率 ·
  **D-7 强制 human 第二源** · 非 LLM)+ **gate**(两层未过不流下游 · mutation-killer 证 teeth)全实装 + 测过。
- **未跑真验收**:金标真数字需真标注数据(Layer-2 硬要真人第二标注源 · 决策 2)· 本会话无真人标注 → harness 就绪待
  operator 提供真人标注 + 配 LLM 凭据后跑真金标。gate 默认关(fail-closed · 未过不放行)。

### 5. gate extractor_variant known-gap(review MED#4)

- gold_layer*_result 表无 extractor_variant 列 · gate 只按 caliber_version 过 → 同 caliber 下新 variant 信号可蹭旧 variant 的过。
- **本 fork 不修**(E2 消费方不存在 · 不投机加列)· gate docstring 显式记 · **E2 设计时须**给 gold 表加 variant 列 + gate WHERE 加此维。

## §3 Suggested IDS actions

operator 在 IDS 跑 `/handback-review 009` 决议,建议动作(按优先级):

1. **【阻断 · 必先做】配 LLM 凭据补跑真密度普查**:E1 密度 STOP 结论目前基于弱替身 proposer,**不可据此否定 M3'**。
   须 `export <LLM_TOKEN_ENV>=<token 文件路径>` 后跑
   `python -m decision_ledger.extraction.census --collection-db /home/ys/codes/XenoDev/out/collection.db
   --caliber-version v1 --extractor-variant v1 --proposer llm --token-env <名>`(温度 0 · 读中文原文语义抽)。
   注:llm proposer 端到端接线本 fork 留 NotImplementedError 接口(build_llm_proposer)· 需接 decision_ledger.llm 客户端
   (温度 0 · 输出 schema=ProposedSpan[])—— 这步是 E1 真密度的最后一公里,建议 operator 授权后由 XenoDev 补接线再跑。

2. **【口径决策】确认密度口径 = per-market horizon-independent**(建议)· 或若坚持 per-horizon 则定义信号→horizon 分配规则
   (scope 决策 · 改 PRD 附录 B)。本仓已按 per-market 修正实装。

3. **【签预注册阈值】** spec 附录 B 密度阈值候选(每 stratum 30 · threshold_hash_candidate=97c58440426f8770)**未签字**
   (OQ-3 · C6)· 我不能代签。operator 审推算(反推自 M2 walk-forward 18/3/3 + min_trials<20)后签 → 钉 threshold_hash →
   **签字前不得开真普查**(先注册后普查纪律)。

4. **【金标真跑】** 配真人第二标注源 + 真 LLM 后跑两层金标真验收(Layer-2 硬要 human 第二源 · D-7)· gate 默认关待两层过。

5. **【E2/E3 授权判据】** = 真密度普查显著 + 两层金标过 + operator 授权。**当前三者均未满足 → E2/E3 不授权**
   (证伪链诚实停 · E2 历史窗回测 / E3 forward 线做进 XenoDev = 越界 BLOCK)。

6. **【gate variant-gap】** 记入 E2 设计输入:gold_layer*_result 加 extractor_variant 列 + gate WHERE 加此维。

**codex 补审**:本 fork 全程 codex adversarial-review 双挂死(KG-38#2)· 走 subagent fallback(red-team+code-reviewer 双跑 ·
8 findings 全修)。codex 恢复后建议对 census 密度口径 + PIT 时区 + Krippendorff α + 0017 migration **优先补跑 codex** 作权威确认。
