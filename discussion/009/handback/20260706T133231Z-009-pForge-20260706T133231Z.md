---
discussion_id: 009
prd_fork_id: 009-pForge
handback_id: 009-pForge-20260706T133231Z
from_build_repo: /home/ys/codes/XenoDev
to_source_repo: /Users/admin/codes/ideababy_stroller
workspace:
  source_repo: /Users/admin/codes/ideababy_stroller
  build_repo: /home/ys/codes/XenoDev
  working_repo: /home/ys/codes/XenoDev
  handback_target: /home/ys/codes/ideababy_stroller/discussion/009/handback/
source_repo_identity:
  expected_remote_url: "git@github.com:ttssp/ideababy_stroller.git"
  repo_marker: "# Idea Incubator — Project C"
  git_common_dir_hash: "11c3749cc9465c4d"
tags:
  - feature
severity: low
created: 2026-07-06T13:32:31Z
operator_score: null
operator_score_at: null
operator_score_rationale: null
related_task: M-fork-complete
---

# Hand-back · 009-pForge · M-fork-complete

**Severity**: low
**Tags**: feature
**Created**: 2026-07-06T13:32:31Z

## §1 Rationale(执行摘要)

009-pForge FORK-COMPLETE closeout — all authorized scope shipped, handing the M3 go/no-go decision back to IDS governance. This fork was scoped (PRD-form phased, Phases [M1, M2]) to deliver only the first two Strangler-Fig phases around the already-shipped 004 (decision shell) and 008 (collection): M1 the point-in-time bitemporal price history layer (the one critical new organ), and M2 the alpha head (first verifiable value anchor answering can-this-analyst-actually-pick). M3 (calibration head, unified shell, temporal heterogeneous graph lane, distillation lane, the two feedback loops) is EXPLICITLY out of scope for this fork per spec line 23 and OUT-1/2/5/6/7 (crossing = BLOCK), deferred outside the fork pending an operator decision that itself gates on whether M2 sample size is DSR-significant. Per CLAUDE.md decision matrix, starting M3 would be a scope expansion / major architectural turn whose SSOT is IDS (idea L1 / PRD L3 / forge governance) — it must be forged in IDS, not started in XenoDev (this is exactly the V4 failure mode the constitution guards against). So rather than build M3, this closeout returns control to IDS.

SHIPPED (all merged to feat/009-spec integration branch): M1 = T001 (price_daily + ticker_status bitemporal tables, alembic 0014, PK includes as_of, delisting CHECK), T002 (PIT domain models + get_adjusted_close interface, interface-before-impl), T003 (PITPriceProvider bitemporal query engine + AdjustedPricePoint structured return, trigger-1), T004 (Tiingo US source + adjustment factors + delisting coverage), T005 (BaoStock A-share source qfq/hfq + HK adapter placeholder deferred), T006 (M1 ingest CLI = P1 PPV owner), T007 (M1 contract tests: as_of dual-axis anti-future/backfill, unique source, adjustment, delisting), T008 (CurrentStatePriceProvider 004 adapter + xgboost lane real-source swap — was BLOCKED trigger-2 gate, resolved). M2 = T010 (analyst_alpha_eval + analyst_alpha_rejections migration 0015), T011 (frozen join contract: signal parse to ticker/market/direction/signal_date, ambiguity-rejects-by-default, authoritative market resolution), T012 (realized-return engine, C16 formula, dual-side per-signal explicit as_of, direction to signed, costs), T013 (TipRanks triplet: Hit Ratio + mean excess + Z significance), T014 (walk-forward/OOS + trial-count accounting + DSR/PBO deflate + insufficient-sample honest reporting), T015 (AlphaLane StrategyModule, equal-standing in 004 registry, R3 isolation), T016 (M2 evaluate CLI = P2 PPV owner), T016b (alpha lane production wiring: build_default alpha_provider DI, per handback-review 009 decision), T017 (M2 contract tests: no cross-source merge, source_id unique, 004 no aggregate). Every task shipped through the full parallel-builder flow (TDD, cross-model review, per-task hand-back to IDS discussion/009/handback/ — 18 immutable records through 20260706T131309Z). Final state: working tree clean, 004-pB full suite 743 passed / 4 skipped / 1 xfailed, no regression.

PPV (both real-path termini owned and structurally verified — no mock in the path): P1 (M1) real CLI decision_ledger.price.ingest --source baostock --ticker <t> -> real alembic upgrade -> real source client -> price_daily/ticker_status SELECT-able rows with close_raw/adj_factor/trade_date/as_of (T006 owns). P2 (M2) real CLI decision_ledger.alpha.evaluate --advisor <id> -> frozen join -> per-signal dual-side explicit as_of PIT-native get_adjusted_close (no mock price, no as_of=today adapter) -> C16 metrics -> DSR/PBO deflate -> analyst_alpha_eval SELECT-able rows + analyst_alpha_rejections + conflict_report with alpha source_id and NO winner/aggregate (T016 owns). Both anti-PPV lines (no synthetic/current-only price for historical realized return; terminus must be data.sqlite SELECT-able not stdout) held.

DEFERRED / KNOWN-GAPS carried forward (documented, none is a defect in authorized scope): (1) HK-market clean free-source data = schema placeholder only, real ingest needs paid EODHD + operator authorization (OUT-11, spec Rollback); (2) wire.py production real-integration = the 004-pB registry->assembler->conflict_report chain is currently a _StubAssembler and nobody calls build_default in the running app, so T016b made the factory CAPABLE of registering the alpha lane (DI-only, same posture as xgboost price_provider) but real prod wiring is deferred to whoever constructs the provider at integration time (same defer as xgboost; also the DbAlphaResultProvider-sync-conn vs app-async-pool tension is left to that point); (3) whether M3 goes at all depends on M2 sample-size DSR significance (O9 insufficient-sample path exists and is honest about judging-nothing) — this is the operator gate.

FRAMEWORK DOGFOOD (batched for IDS forge, NOT fixed in place per dogfood iron rule): 009-pForge surfaced KG-30 through KG-38 (9 framework-level items) in dogfood-backlog.md — notably KG-30 (red-green log gitignored under mirror subproject), KG-31 (gen-handback mac-literal path leak), KG-32 (codex-review no-fallback deadlock — since mitigated by the CLAUDE.md fallback governance clause), KG-37 (codex branch-mode bwrap loopback env failure), KG-38 (codex review background job session-stream drop / silent hang). These await operator review and batch back to IDS /expert-forge as the X targets; none was changed in XenoDev (SHARED-CONTRACT / SKILL / mirror are read-only here).

NEXT STEP (operator): run /handback-review 009 in IDS to accept the fork-complete closeout, then decide the M3 go/no-go (forge M3 into a new PRD/fork in IDS if greenlit, gated on M2 DSR-significance evidence). This hand-back is a governance/closeout summary, not a code change — no production diff, no review gate needed (retroactive summary; same class as a docs-only hand-back).

## §2 触发原因 + 决议链(按 frontmatter `tags` 逐条说明)

(待补 · per T007 amendment · operator 跑 gen-handback 时未传 --section2)

## §3 Suggested IDS actions

(待补 · per T007 amendment · operator 跑 gen-handback 时未传 --section3)
