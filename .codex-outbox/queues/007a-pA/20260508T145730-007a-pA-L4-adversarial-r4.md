# Adversarial Review · 007a-pA · R4 (FINAL)

## R3 finding closing status (post-R4 narrow fix)

| R3 finding | Status | Verification evidence |
|---|---|---|
| B-R3-1 C14 contract drift | CLOSED | `spec.md:80` now defines review session as independent `state.review_mode=true`, leaves `state.enabled` unchanged, gates hook by enabled AND not review_mode, and keeps O3 enabled-only. Grep: exact `review-mode.*alias\|equivalent\|= --off` returned 5 hits (`spec.md:5,104,122,146,201`), all revision/history or explicit anti-alias context; expanded blocker grep returned 6 with `spec.md:18` as a safe false-positive from `--off` and review_mode appearing in one O3 paragraph. |
| H-R3-1 `--on` 不清 review_mode | CLOSED | D21 exists at `spec.md:108`; T012 implements pre-state load, `enabled=true`, conditional `set_review_mode(False)`, and conditional stdout at `tasks/T012.md:48-54`; T031 adds `TestOnClearsReviewMode` at `tasks/T031.md:82-100`; T032 teaches full restore vs partial review exit at `tasks/T032.md:53-60`; SLA mirrors the two exit paths at `SLA.md:35-43`. |
| H-R3-2 trust_summary 不显示 review_mode | CLOSED with one new implementation-contract concern below | Architecture adds schema and template/status behavior at `architecture.md:259-280` and `architecture.md:294-324`; T020 adds the section, warning calculation, and drift assert at `tasks/T020.md:74-78` and `tasks/T020.md:109-118`; T031 adds `TestTrustSummaryShowsReviewMode` at `tasks/T031.md:102-115`; backward compat is explicit at `architecture.md:273,278`. |
| Partial-1 PROD-3 health_check | CLOSED | `risks.md:71` now recommends `friction_tap.grep --count-agent-emit --since=72h` plus `trust_summary --check-enabled`. Full `rg health_check specs/007a-pA` returned 18 hits, not the self-reported 16, but all are historical, negative, or replacement notes; no prescriptive PROD-3 health_check remains. |
| M-R3-1 SCH-1 数字 | CLOSED | SCH-1 row now says 22.75h within PRD v1.1 23h cap at `risks.md:78`; spec schedule table totals 22.75h at `spec.md:154-164`. Low residual summary drift noted below. |
| M-R3-2 architecture v0.3 | CLOSED | `architecture.md:3` version is 0.3; `architecture.md:6` source spec is `spec.md v0.3`. |
| M-R3-3 SLA.md v0.3 | CLOSED | `SLA.md:3` version is 0.3; `SLA.md:7` source spec is `spec.md v0.3`. |
| M-R3-4 spec.md task summary alias | CLOSED | `spec.md:122` now says independent state field and explicitly no alias; `spec.md:146` says T012 wording no longer labels alias. |

## R4 修订引入的 edge cases

- Self-reported schema-bump edge: ACCEPTED. `review_mode_set_at` is a nullable backward-compatible field, with missing/null handled as None at `architecture.md:273,278`; version is already v0.3. No BLOCK.
- Self-reported empty warning placeholder: LOW. T020 renders `{review_mode_warning}` as an empty string at `tasks/T020.md:77,114`; worst case is a blank line, not contract failure.
- edge a, `--on` when review_mode already false: CLOSED. T012 prints cleared only when previous review_mode was true (`tasks/T012.md:49-52`), and T031 tests the no-noise path (`tasks/T031.md:89-92`).
- edge b, 24h basis: ACCEPTED BY DESIGN. The warning is relative to current trust_summary runtime minus `review_mode_set_at` (`architecture.md:273,294`; `tasks/T020.md:111-114`). A day-5 review_mode still true at day 14 should warn.
- edge c, timestamp clearing: MOSTLY CLOSED, LOW TEST GAP. Architecture requires `set_review_mode(False)` to clear `review_mode_set_at` (`architecture.md:273,279`), and T012 routes `--on`/`--review-mode-off` through that helper (`tasks/T012.md:51,57`). T031 verifies `review_mode=false` but does not explicitly assert timestamp null after clear (`tasks/T031.md:97-100`).

## New blockers (R4 引入或漏抓的)

None.

## New high-severity concerns

- **H-R4-1 · trust_summary review_mode render string mismatch.** The behavioral contract says the report shows `state.review_mode = <true|false>` (`architecture.md:294`, `spec.md:188`, `tasks/T031.md:108,112,140`, `tasks/T020.md:156`), but the actual T020/architecture templates use a colon form (`architecture.md:322`, `tasks/T020.md:75`) and T020's drift assert checks colon (`tasks/T020.md:118`). A build worker copying T020 literally can fail T031 exact assertions. This is not a product-design blocker, but it is a high-risk test-contract mismatch before implementation.

## Medium / Low

- **M-R4-1 · health_check grep count self-report inaccurate.** R4 task claimed 16 hits; fresh grep found 18. Content is still compliant, so this is traceability drift, not behavior drift.
- **L-R4-2 · risks summary stale schedule wording.** SCH-1 row is fixed, but `risks.md:83` still says the high-high summary includes SCH-1 and `risks.md:93` still mentions accepting 22.5h over 20h for an R2 future path. This can confuse later readers, but does not reopen M-R3-1 because the actual SCH-1 row is correct.
- **L-R4-3 · timestamp clear lacks direct integration assertion.** Add or expect a unit/integration assertion that `review_mode_set_at is None` after `--review-mode-off` and after `--on` from a true review_mode state.

## PRD faithfulness check (post-R4)

- [x] Spec C14 与 D17/D20/D21 一致(独立 review_mode 字段)
- [x] §6 verification 含 `--on` 清 review_mode + trust_summary 显示 review_mode 两条
- [x] D8 whitelist = {Bash, Edit, Write, Claude Code lifecycle hook block}(无 Read)
- [x] D21 显式记录 `--on` 完整恢复语义
- [x] schema field `review_mode_set_at` 标 backward-compat

## Top 3 things the operator personally must verify

1. Pick one trust_summary rendering contract before build: change template/assert to the equals form, or relax T031/T020 checks to accept the colon form.
2. Ensure implementation tests assert `review_mode_set_at` clears to null on both `--review-mode-off` and `--on` from review_mode=true.
3. Decide whether to clean the stale `risks.md:83,93` schedule summary now, or accept it as low doc drift before build.

## Verdict

**CONCERNS**

R3's blocker and high concerns are closed: C14 no longer instructs alias semantics, `--on` has full-restore semantics, and trust_summary now surfaces review_mode state with stuck-true warning logic.

I do not recommend BLOCK because the remaining issue is not a PRD or state-machine contradiction. But I also would not call this CLEAN: H-R4-1 can produce a binary implementation/test mismatch in the first build pass. Since R4 is the hard cap, the pragmatic path is either:

- **Accept risk and build** with an explicit operator note: implement Hook Operational Status so it satisfies T031's visible `state.review_mode = true|false` assertion, despite the T020 template's colon wording.
- **One-line L4 spec cleanup before build**: align `tasks/T020.md` and `architecture.md` template/assert wording to the same delimiter used by spec §6 and T031. No L3 rescope is needed.

---
**Quote ≤15 words verbatim**.
