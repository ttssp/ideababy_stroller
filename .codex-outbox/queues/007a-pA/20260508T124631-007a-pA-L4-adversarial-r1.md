# Adversarial Review · 007a-pA · R1

## Blockers (must fix before proceeding)

| # | Location | Issue | Why it blocks | Fix direction |
|---|---|---|---|---|
| B1 | `specs/007a-pA/architecture.md:181`, `specs/007a-pA/architecture.md:190`, `specs/007a-pA/tasks/T010.md:41`, `specs/007a-pA/risks.md:17` | `--threshold high` is not a filter. The decision table emits the same whitelisted failures at every threshold and only relabels confidence as H/M/L. | TECH-2 is the only H/H risk, and its primary mitigation is nonfunctional. A false-positive flood will still emit at high threshold, just marked as high confidence, so O2 and trust calibration can fail while tests pass. | Decouple confidence classification from threshold. First classify event evidence as H/M/L using event features independent of threshold; then threshold filters minimum confidence. Add tests proving `threshold=high` suppresses M/L events. Revisit the H boundary; generic whitelisted nonzero exits should not automatically be H. |
| B2 | `specs/007a-pA/spec.md:132`, `specs/007a-pA/spec.md:134`, `specs/007a-pA/tasks/T020.md:76` | Verification references runnable modules that the task package never builds: `friction_tap.tools.count_entries` and `friction_tap.tools.health_check`. T020 explicitly leaves a health-check CLI out of scope. | O1 and O3 are core outcomes. If their verification commands do not exist, the build can reach "done" without a runnable acceptance path. This violates the spec contract's verification requirement. | Either add explicit tasks and file domains for count/health modules, or rewrite O1/O3 verification to use implemented entry points such as `trust_summary` plus `grep.py` helpers. Make the exact commands appear in tasks and tests. |
| B3 | `discussion/007/007a-agent-emit/007a-pA/PRD.md:78`, `specs/007a-pA/spec.md:17`, `specs/007a-pA/spec.md:134`, `specs/007a-pA/risks.md:61` | O3 is no longer faithful to the PRD. PRD O3 is "hook still enabled / not turned off"; spec O3 adds "recent 24h has agent entry". BUS-1 already names operator absence as plausible. | A trusted, enabled hook can fail O3 solely because the operator had no dogfood activity in the last 24h. That creates a false adoption failure and corrupts the day-14 verdict. | Change O3 to measure enabled state / not-off since last `--on`. Track activity separately as a freshness or dogfood-activity metric. If an activity window is needed, make it "since last dogfood session" or 7 days, not unconditional 24h. |

## High-severity concerns (should fix; ok as follow-up with owner+deadline)

| # | Location | Issue | Suggested action |
|---|---|---|---|
| H1 | `specs/007a-pA/spec.md:20`, `specs/007a-pA/spec.md:137`, `specs/007a-pA/tasks/T020.md:71` | O6 outcome requires a why, but verification only checks non-empty text. A one-word "both" can pass while adding no adoption evidence. | Require one of relieved/watched/both plus a reason with a minimum evidence threshold, e.g. ≥20 CJK chars or ≥8 English words, and include this in T020/T031 checks. |
| H2 | `specs/007a-pA/spec.md:34`, `specs/007a-pA/spec.md:153`, `specs/007a-pA/tasks/T031.md:46` | Adjudication tags bind by physical next line, but review happens by manually editing a file while the hook may still append. T031 tests concurrent appends, not human editor save races or tag misbinding. | Make review workflow explicit: either `friction --off` during review, or add a tag command / append-only review section. At minimum add README warning and an integration test for stale-save/misbound tag scenarios. |
| H3 | `specs/007a-pA/architecture.md:168`, `specs/007a-pA/architecture.md:178`, `specs/007a-pA/tasks/T011.md:53` | Payload schema drift silently noops except for `last_run.log`. If Claude Code changes field names, capture can go to zero without an in-flow warning. | Add a visible schema-error counter in trust summary / status, and a post-install real-payload smoke test. Keep hook exit 0, but fail loud to operator through health output. |
| H4 | `discussion/007/007a-agent-emit/007a-pA/PRD.md:137`, `specs/007a-pA/spec.md:31`, `specs/007a-pA/spec.md:90` | Spec expands the whitelist to `Read`, while PRD's unresolved OQ-E candidate lists Bash/Edit/Write plus lifecycle block. `Read` failures are likely noisier and not human-approved in the PRD text. | Remove `Read` from v0.1 or record explicit operator sign-off. If kept, add separate examples proving it is H/M/L classified conservatively. |
| H5 | `specs/007a-pA/HANDOFF.md:19`, `specs/007a-pA/HANDOFF.md:25`, `specs/007a-pA/HANDOFF.md:32` | HANDOFF Step 3 gives ADP a short feature description, then Step 4 expects manual reconstruction of O1-O6, IN/OUT, constraints, and verification. The conversion table omits Problem/Context, UX principles, and biggest product risk. | Replace the short prompt with a structured mini-PRD handoff block: problem, user, O1-O6, scope IN/OUT, constraints, UX principles, biggest risk, and verification. Add those omitted fields to the IDS→ADP mapping. |
| H6 | `specs/007a-pA/spec.md:118`, `specs/007a-pA/dependency-graph.mmd:14`, `specs/007a-pA/dependency-graph.mmd:69` | Spec lists T022 for OQ-C, but dependency graph and file manifest have no T022. The sampled task estimates sum to ~19h before review fixes, not the advertised 11.5-16.5h. | Reconcile task IDs, dependency graph, and estimates. Either create T022 or fold it explicitly into T020 everywhere. Recompute total and critical path after blocker fixes; likely cut scope or mark schedule risk high. |

## Medium / Low findings

- `architecture.md:207` uses PIPE_BUF reasoning for regular file appends. Keep O_APPEND, but rewrite the guarantee precisely for local APFS/POSIX regular files and stop implying pipe semantics.
- `tasks/T012.md:36` adds a per-call `-f` path escape hatch while `spec.md:51` says configurable path is out of scope. This is probably acceptable as D6 escape hatch, but call it "explicit one-shot override" everywhere to avoid scope leakage.
- `state.py` corruption recovery is under-specified. `StateError` hints at deleting state, but trust summary can hard-fail; add a documented repair command or fallback report mode.
- Operational entry-volume math is missing. TECH-2 assumes flood risk, but no spec row estimates max entries/day or when grep-friendly breaks.
- SEC-2 accepts secret leakage into stderr logs as manual-edit only. Given production credential isolation is a repo-wide safety floor, add a v0.1 minimal redact pattern for obvious token shapes or explicitly accept this as a red risk.

## PRD faithfulness check

- [ ] Every PRD outcome (O1-O6) has corresponding spec verification  
  Fails: O1/O3 verification commands are missing; O3 changes the PRD meaning; O6 verification is weaker than the outcome.
- [ ] No spec feature outside PRD scope IN  
  Not clean: `Read` in the whitelist appears added beyond the PRD's stated OQ-E candidate.
- [ ] No spec feature in PRD scope OUT (cross-check non-goals.md)  
  Mostly clean, but `-f` path override must be consistently framed as D6 one-shot escape hatch, not configurable path.
- [ ] HANDOFF.md schema covers all IDS → ADP fields per SHARED-CONTRACT v1.1.0  
  Fails: Problem/Context, UX principles, and Biggest product risk are not mapped; short feature description is too lossy.

## Top 3 things the operator personally must verify (human judgment required)

1. Define concrete H/M/L examples for the static judge. Without this, `--threshold high` cannot be trusted as TECH-2 mitigation.
2. Decide O3 semantics: is adoption "not turned off", "recently active", or both as separate metrics?
3. Decide whether one self-interview answer needs a real reason threshold. If "both" alone is acceptable, O6 should stop pretending to validate adoption insight.

## Verdict

**BLOCK**

The package is not ready for build. The threshold design invalidates the primary mitigation for the highest risk, and the verification contract references commands that do not exist in the task package. Those are not documentation nits; they mean the implementation can pass local task checks while failing the product's central trust premise.

Fix B1-B3 before round 2. H1-H6 can be triaged after blockers, but H1 and H2 are close to blockers because they attack the same trust/adjudication spine that made Candidate A worth selecting.

---
**Quote ≤15 words verbatim from any file**.
