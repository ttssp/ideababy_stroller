# Codex Completion · 009-pM3prime · forge v3 · P2

**Completed**: `2026-08-13T05:52:56Z`

**Files written**:
- `discussion/009/009-pM3prime/forge/v3/P2-GPT55xHigh.md` — 321 words (`wc -w`) / 3,007 characters (`wc -m`)
- `.codex-outbox/queues/009-pM3prime/20260813T054358Z-009-pM3prime-forge-v3-p2.md` — 146 words (`wc -w`)

**Searches run**: 12。主题：DeepSeek `seed` / fingerprint / determinism；OpenAI Python SDK 默认重试边界；确定性设置下的 LLM 多跑一致性（TARr@N/TARa@N）；zero-shot NER 两阶段多数票与采样次数；LLM annotation 测量框架；稀有实体/边界项鲁棒性。

**Headline**: A 侧“实现比 SLA 更严”在应用层语义失败上成立；但 DeepSeek 无 seed/确定性保证，内容不稳定仍只能作为测量与聚合问题处理，且 N/阈值须由 009 自行标定并预注册。

**Verdict**: `CONCERNS`

**任务 A 结论**: `A 侧修正 2 成立（需限定）`——SLA 的 retry/backoff 覆盖 census；SDK 只重试传输/特定 HTTP 错误，空 content、坏 JSON、缺 `usage` 仍被实现首次即判失败。落实有界语义重试无需先改冻结 SLA，但不能解决 Jaccard 0.44。

**任务 B 结论**: DeepSeek 当前官方 Chat API 请求 schema **无 `seed`，无可复现性保证**；`system_fingerprint` 只表示运行模型的后端配置。[官方出处](https://api-docs.deepseek.com/api/create-chat-completion)

**任务 C 结论**: `同意`——回滚是事实但不是病因；C1/O11 ledger grain 是 `variant×caliber×horizon`，真正缺口是同一 grain 的多次运行及其模型/语料/输出身份不可区分。

**任务 D 结论**: `文献不给`——检索到 TARr/TARa、集合 Jaccard、元素频率和两阶段多数票等形态，但没有适用于本任务的成熟阈值或最少 N；须以本任务实跑标定后预注册。

**Notes for Claude Code**:
- 未触发 BLOCK；`moderator-notes.md` 不存在。
- 已读对方 P1，未读对方 P2；独立完成指定核实。
- 只采用 DeepSeek/OpenAI 官方材料和原始论文；无 tech-stack、定价或详细 refactor plan。
- 我明确撤回 P1 中“failed trial 不入账是根病”的推论；应区分分析 trial ledger 与 execution/run identity。
- P3 仍需协商两个独立铰链：聚合后的重复信度，以及聚合是否保留边界真信号的构念效度。
