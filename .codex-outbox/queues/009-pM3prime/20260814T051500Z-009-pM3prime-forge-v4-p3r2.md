# Completed: 2026-08-14T12:47:23+08:00

## Files written

- `discussion/009/009-pM3prime/forge/v4/P3R2-GPT55xHigh.md` — 297 whitespace-delimited words；749 Han characters
- `.codex-outbox/queues/009-pM3prime/20260814T051500Z-009-pM3prime-forge-v4-p3r2.md` — 本确认文件

## Headline

双方以 0 unresolved 收敛：四项义务统一改成具名动作绑定，遗留能力缺口全部登记 `due_gate`，不再生成无消费点的 note。

## Verdict

**CONCERNS**

M1 仅接受为固定批可比性证据，不解锁 E2；当前跨批比较不可信，须以 `OB-15` 四前提加 fallback 修正；校准与 C4 convert 的动作、责任、升版和回退语义均已定形。

## Convergence

**converged** — `0 unresolved`。

## Ruling on A-side held position

1. **具名敞口 vs 优化**：采“具名敞口”。身份稳定时的缓慢漂移目前缺少检测与重校准能力，不是已有能力的优化。
2. **v0.2 note 必带 `due_gate`**：同意。否则 verdict 会亲自新增“机制有、动作无”的第五处实例。
3. **`due_gate` 取值**：采用 `forge:009-pM3prime:phase0`，并限定 `due_event` 为义务登记后的**下一次 firing**；不能把本次已经发生的 phase0 计为履约。

## v0.2 notes

1. **统计型重校准间隔**：待足量 control history 后预注册方法与 N。`due_gate: forge:009-pM3prime:phase0`；`due_event: next firing after obligation registration`。
2. **per-market 跨次分解**：非 v4 硬产出；当前未满足须先落 `PRD.md` §6 条件 1。`due_gate: forge:009-pM3prime:phase0`。
3. **`xenodev:*` 消费点接线**：下次 phase0 复核；接线前相关 obligation 一律 `consumer_wired: false` 且 `exposure` 必填。`due_gate: forge:009-pM3prime:phase0`。

## Notes for Claude Code

- synthesizer 的 W 必须逐项产生 `verdict-only`、五列 decision-list（保留/调整/删除/新增/obligation path）及三组 refactor-plan。
- `OB-15` 采用四前提：预注册观察/停止边界、逐 attempt exposure、失败结果及精确时间/区间并可识别删失、失败批内容身份；证不出则 fallback。
- 版本化回退统一写“历史不可变；授权被后续 `caliber_version` supersede”，不得写“旧结论作废”。
- per-market 未满足说明必须落 `PRD.md` §6 条件 1，不得只留在 stage doc。
- 凡 obligation path 指向未接线的 `xenodev:handoff-intake`、`xenodev:task-review` 或 `xenodev:ship-gate`，Step 6.5 必须登记 `consumer_wired: false` 和具体 `exposure`。
- 不得解锁 E2、翻转 41 条 CLOSED 或修改 C6 数字；不要传播 P3R1 将议题⑤原句误引为 per-market 立场的表述。

## Next step

Claude Code synthesizer 可据双方 P3R2 生成 forge v4 最终 stage 文档及 obligation registry 条目。
