# P3R1 completion receipt

- **Completed**: 2026-08-13T06:11:14Z
- **Files written + word count**: `discussion/009/009-pM3prime/forge/v3/P3R1-GPT55xHigh.md` — 941 Chinese characters; 184 whitespace-delimited words
- **Headline**: 接受有界探针，但禁止在无独立 validity 锚时把候选留存冒充真信号留存。
- **Verdict**: CONCERNS

## Initial verdict draft（exact）

E1 路线暂不改判，但单次语料立即失去事实地位，扩样与 E2 继续 blocked。
先按现有 SLA 补有界语义重试；C4 v0.1 改为无 pass 语义的强制测量、申报、落库义务，并绑定模型与语料身份。
允许一个有界、预注册的聚合探针进入 verdict，但 R2 必须钉死四类上限、单命令和 pass／fail／indeterminate 三态判据；它只能测信度与候选留存，不能在无 validity 锚时自称测得“真信号留存”。
CN/HK 改判为 measurement-invalid／unknown 下的 fail-closed STOP，不再称“真稀疏”。

- **diff1**: 同意立即执行探针，但无独立真值锚时只能报告候选留存，不能报告真信号留存。
- **diff2**: C4 v0.1 明确 `pass_semantic: none`；强制 report/ledger，不新建 pass/fail 门禁。
- **diff3**: CN/HK 不撤销执行态 STOP，但认识态改为 measurement-invalid/UNKNOWN，旧“真稀疏”结论作废。
- **Probe experimental debt ruling**: accept into verdict with boundedness requirements — R2 必须固定 N、记录数、单次总时长、总成本四上限，唯一命令，以及逐项 pass/fail/indeterminate 判据；缺一即反对纳入最终 verdict。
- **New disagreements**: 探针在缺独立 validity 锚时是否有资格输出“边界真信号留存率”；我方裁决为无资格，只能输出预注册边界候选留存率。
- **Notes for Claude Code**: R2 请把探针写成一次性实验契约并补齐上述硬边界；保留 `pass_semantic: none`，不要重开 41 条盲标路线，也不要用商业理由解释 CN/HK STOP。
