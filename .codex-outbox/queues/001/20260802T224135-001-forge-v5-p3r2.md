# Forge v5 P3R2 completion · 001 · GPT55xHigh

**Completed**: 2026-08-02T22:41:35+08:00

**Files written**:
- `discussion/001/forge/v5/P3R2-GPT55xHigh.md` — 1482 CJK 字符；§2 联合 verdict 202 CJK 字符
- `.codex-outbox/queues/001/20260802T224135-001-forge-v5-p3r2.md` — 本回执

**Headline**: 采定向 refactor；T040 立即 ratchet 起跑，但累积池防空过、例外到期生命周期、安全展示投影与 R5 裁决闭环是落单硬条件。

**Verdict**: CONCERNS

**Convergence outcome**: `converged`

**12 项表态汇总**:
- item 1 → **改判据、改 B**：warn 候选补取证计划、复核日和升 block 条件；无证据自动续期即触发 v6。
- item 2 → **改 A、改判据、改 B**：池限同一 epoch/rubric，预定关闭日、去重和 anchor 有效性；当前全豁免 run 不得借旧池 PASS；不可比则拆池。
- item 3 → **改判据**：稳定 requirement ID、唯一 expected outcome、验证命令一对一引用；语义歧义交有资质者复核。
- item 4 → **改判据**：明确 `start=min(first_journal_date,effective_date+30d)`，截止为 `start+3 months`。
- item 5 → **同意**：usage 三类互斥，跨用途拆条。
- item 6 → **改 B**：删除无依据的 `≥5 run`；首个预声明 epoch 关闭仍不足才起上游 task。
- item 7 → **同意**：第三值语义现在落，CLI/字段协议留 T041，不追溯。
- item 8 → **改判据**：保存 versioned baseline，并证明存量只 warn、新增才 block。
- item 9 → **同意**：另一门兜底必须有测试或 mutation 的实际转红证据。
- item 10 → **改判据、改 B**：新增 `review_by < expires_on`；先 warn、到期 fail closed，固定 N 不冒充 NIST 明文。
- item 11 → **同意**：限定词 + 术语章，不动 C″；裸词扫描限规范性 spec。
- item 12 → **改 A、改判据、改 B**：采用上下文安全展示投影；恶意输入必须 inert，证不出则展示版不 ship。

**遗漏检查结论**: 修订后 12 项穷尽四簇实质问题；但施工包漏了过程闭环：R5 尚未正式裁决。须在落单前逐条标 `accepted/rejected/deferred` 并映射到 12 项。decision-list 还应显式保留 selfcheck 正向 allowlist、门②声明机制和 C″ 分流契约。

**反自欺收口**:
- 新发现 item 12 用“不作证据用途”标签解释掉展示面注入风险；已改为安全证不出则不 ship。
- 新发现 item 10 把 expiry 突然转红降成实现代价；已改为 `review_by` 预警 + 到期必红。
- `≥5 run` 是无来源阈值，已删除；R5 未裁是证据闭环遗漏，已设为落地前置。
- 既有双向登记继续有效：强制多样本有效性面未检索；累积规则属 post-hoc，不追溯。

**Notes for Claude Code**:
- synthesizer 可直接产最终 12 项施工单，但必须采用本稿对 item 2、10、12 的修订；任一降级为 note，联合 verdict 改为 BLOCK。
- item 2 与 item 6 必须共享同一预声明 epoch，禁止另造 `5 run` 魔数。
- item 1 的更早可证伪点是 warn 候选过 `review_by` 后无证据仍被续期，不等“几乎总是证不出”。
- 本轮无 unresolved、无新增检索；`moderator-notes.md` 不存在。
