# Adversarial Review · 004-pB · R3

**Reviewed**: 2026-04-26T20:16:35+08:00
**Reviewer**: GPT-5.4 xhigh
**Spec version**: v0.3

## R2 BLOCK 验证矩阵 (5 条)

| R2 ID | R3 修订声明 | 实际修了吗? | 行号佐证 | 备注 |
|-------|------------|-------------|---------|------|
| B-R2-1 | spec 改 PRD O5/C11 度量口径 → 回退 | ⚠️ | `spec.md:39-44,59,128,163,169,171,233`; `architecture.md:435-494` | 核心段落已经回到 GET `/decisions/new` → commit 200 OK 的全程口径, 这部分是对的。但 **当前 package 仍有 R2 残留**: `architecture.md:47` 还写 miss `≤ 10s`; `architecture.md:635` 还写 miss 上限 `10s`; `architecture.md:711` 还写“超时 10s 后 spinner 升级”; `architecture.md:758` 还把 invariant #1 写成 draft LLM `≤ 10s`; `architecture.md:782-788` 的 `release_log.jsonl` 仍按“commit 阶段 wall-clock”记。R3 的头号目标是把整个 spec 包统一回 PRD 原口径, 现在还没完全收口。 |
| B-R2-2 | T008 placeholder fallback → 删 | ✅ | `T008.md:33-35,47-55,96-119,147-149,155-157,176-177,186-189`; `architecture.md:438,480-483,503,770` | placeholder-then-commit 路径已经删干净; 超时是 503 + 不放行 commit, 不再伪装成“暂无分歧”。 |
| B-R2-3 | D21 latency 论证 → 诚实 + fast-path | ✅ | `spec.md:163,169,171`; `architecture.md:576-616`; `T010.md:35-40,66-69,157-161`; `T013.md:29-31,42-49,72-80,96-103,112-113` | R3 已诚实承认 Rebuttal 不可缓存, cache warmer 只覆盖 ConflictReport, fast-path 配置也明确写进 task。工程估算仍有风险, 但“论证是否诚实”这一条已修正。 |
| B-R2-4 | task pack → 真砍 ≤ 160h | ✅ | `spec.md:191-219`; `risks.md:37-38`; `T011.md:16,28`; `T015.md:15,29-30`; `T016.md:16,28`; `T018.md:15,27`; `T019.md:23,35`; `T021.md:17,31`; `T022.md:13,27` | 我独立重算 `T001-T022 estimated_hours` = **160h**。spec §5 上限写到 `160h`, risks OP-2 也写 `160h`, 三处算术终于对齐。 |
| B-R2-5 | cache miss 例外 → Q11 删 | ⚠️ | `open-questions.md:17-18,215-223,237-241`; `spec.md:279,312-319` | `open-questions.md` 已明确把 Q11 标成 “R3 删除”。但 `spec.md §8` 里仍保留了一个带删除线的 `Q11` 小节, 所以“真删除”只做到了半套。 |

## R1 4 ⚠️ + 1 HIGH 真 close 验证

| R1 ID | R3 修订声明 | 实际修了吗? | 行号佐证 |
|-------|------------|-------------|---------|
| ⚠️ B1 | O5 + T008 联合 close | ⚠️ | `spec.md:39-44,59,128,163,169,171,233`; `architecture.md:435-494`; `T008.md:33-35,47-55,147-149` |
| ⚠️ B3 | contract-level lane 隔离 | ✅ | `architecture.md:238-257,327-331,771`; `T007.md:39-40,75-113,141-145,157-163` |
| ⚠️ H1 | T015/T017 metadata + strict mode | ⚠️ | `T015.md:5-6,29-30,112`; `T017.md:5-7,31-32`; `T020.md:5-6,34-35,83-112,120-124`; `dependency-graph.mmd:3-4,22,109-111` |
| ⚠️ M2 | T022 CLI + Glossary 一致 | ⚠️ | `spec.md:267`; `T022.md:27-30,49-53,109-141`; `T020.md:49`; `architecture.md:684-687` |

补充说明:
- ⚠️ B1 没真 close, 原因不是 T008, 而是 **B-R2-1 仍有 package 级残留**。
- ⚠️ H1 的 task metadata 已基本修正, 但 `strict` 什么时候在 CI 默认打开, 仍没有落到 package 规则里。
- ⚠️ M2 的 T022 自身是 CLI-only, 但 T020/architecture 还在讲 banner 点击 / meta_decisions UI, 故不算真正统一。

## 新引入问题 (R3 衍生)

| # | Location | Issue | Severity | Fix direction |
|---|----------|-------|----------|---------------|
| 1 | `architecture.md:47,635,711,758,782-788` | **R3 的核心口径没有在 package 全文收口**。当前正文仍残留 R2 的 `≤10s` / “commit 阶段 wall-clock” 叙述。builder 若按这些段落实现, 仍可能把 draft 超时预算和 O5 日志口径做错。 | BLOCK | 把所有当前正文里与 O5 相关的 `10s` / `commit-only` 文案统一改回 `≤5s draft` + `GET→commit 全程`。尤其要改 L1 context、tradeoff、integration table、invariant #1、`release_log.jsonl` schema。 |
| 2 | `open-questions.md:143-153,237,266`; `T019.md:31-36,42-47` | Q7 body 还写着默认假设 `(a) LLM 从笔记 wiki 抽取`, 但 R3 summary 和 T019 已改成 `human 手编 checklist + LLM 仅评分`。 | MEDIUM | 把 Q7 正文默认假设改成与 T019 一致, 不要只改 summary 行。 |
| 3 | `T020.md:49`; `architecture.md:684-687`; `spec.md:267`; `T022.md:27-30,49-53` | B-lite 仍是两套故事: spec/T022 说 CLI-only, 但 T020/architecture 还说 alert banner 点击 / meta_decisions UI 一键 toggle。 | HIGH | 二选一并全文统一。若坚持 R3 cut, 就把 T020/architecture 全改成“banner 只显示 CLI/runbook 提示”。 |
| 4 | `spec.md:166`; `T011.md:28,42-43` | D16 还写“简单 CSS 着色”, 但 T011 R3 已明确砍掉颜色着色。 | LOW | 要么把 D16 改成纯 table, 要么恢复颜色着色并补回估时。 |

## R3 spec-writer 主动提出 6 个待审点

1. T008 503 失败模式 (B-R2-2 替代) — 我的裁定:
   **acceptable**。`T008.md:54-55,147-148` 和 `architecture.md:480-483,503` 的 hard fail 虽然增加首次 miss 摩擦, 但比 placeholder 蒙混 commit 安全得多, 更符合 PRD 的 calibration-first 和 R10 红线。

2. Rebuttal fast-path ≤ 3s 工程估算 (B-R2-3 配套) — 我的裁定:
   **CONCERN**。`architecture.md:600-606` / `T013.md:29-31,42-49,96-103` 已把假设写诚实, 但 `p95 ≤ 3s` 仍是工程估算, 不是 contract 级证据。网络抖动或 API 波动时很可能越线; 好在当前设计把越线后果定义成 503 + OP-1 mitigation, 所以这是风险而不是立即 blocker。

3. T015/T016 砍法是否影响 PRD scope — 我的裁定:
   **CONCERN**。T015 砍到纯 SQL + markdown 仍能保留月度证据聚合主干 (`T015.md:42-45,78-83`), 我不把它视为出 scope。T016 更弱: PRD 明写“自动去重, 下次 agent 不重复解释”(`PRD.md:55-57,90-93`), 但 T016 只剩 content_hash + SQL LIKE (`T016.md:34-40,48,64-65`), 这里是 scope 保真度下降。

4. T022 CLI 替代 UI toggle (M2 + B-R2-4) — 我的裁定:
   **CONCERN**。T022 自身是自洽的 (`T022.md:35-47,55-59`), 但 emergency path 在低能量状态下让 human 切终端跑 CLI, 摩擦明显高于一键 UI; 再加上 T020/architecture 还残留 UI 说法, 现在更像是“省工时成功, 叙事没收口”。

5. lane `__init__` contract 过紧 (⚠️ B3) — 我的裁定:
   **acceptable**。`architecture.md:238-257,327-331` 与 `T007.md:39-40,75-113,141-145` 的确把注入面锁得很死, 但这是为了把 R9 从“口头解耦”变成“contract 级解耦”。未来若要加 metrics/tracing, 走 provider 包装层是合理代价。

6. strict mode 默认值 (⚠️ H1) — 我的裁定:
   **CONCERN**。`T020.md:97-100,109-110,123` 提供了 strict 机制, 但 package 里没有定义“何时 CI 必须用 strict”。如果 T015 ship 后 CI 仍默认 graceful, wiring drift 仍可能静默溜过去。

## 算术验证 (B-R2-4 关键)

- `sum(T001-T022 estimated_hours) = 160h`
- `spec.md §5 总估 = ~120-160h` (`spec.md:216-219`)
- `risks.md OP-2 mitigation = 181h → 160h` (`risks.md:38`)

是否三处一致 ≤ 160h?

- **是**。这次算术终于收口了。

## Verdict

- **BLOCK**

## Why this verdict

R3 已经把最关键的两件事做对了一大半: placeholder fallback 真删了 (`T008.md:54-55`; `architecture.md:480-483`), 工时算术也终于统一到 `160h` (`spec.md:216-219`; `risks.md:38`)。  
但这轮唯一头号目标是把**整个 spec 包**统一回 PRD §S1 / §6 O5 原口径, 而当前 `architecture.md` 的多处正文还残留 R2 的 `≤10s` / commit-only 语义 (`architecture.md:47,635,711,758,782-788`)。  
再加上 B-lite 仍然在 CLI-only 与 banner/UI 叙事之间摇摆 (`spec.md:267`; `T020.md:49`; `architecture.md:684-687`; `T022.md:27-30`), 以及 Q7 正文仍保留旧默认假设 (`open-questions.md:151`), 这份 package 还不是“单一、无歧义、可直接交给 build worker”的状态。  
所以我的结论不是“方向错了”, 而是“**R3 还差一次 package-level 收口**”; 不到这一步, 我不建议进 build。

## 给 operator 的最终建议 (≤ 5 条)

1. 先做一次 **全文收口 patch**: 把 `architecture.md` 里所有现行 `10s` / commit-only O5 文案全部改成 R3 的 `≤5s draft` + `GET→commit 全程`。
2. 把 `release_log.jsonl` schema 改成与 `T009` 一致的全程口径, 不要再记录成 commit-only (`architecture.md:782-788` vs `T009.md:70-84`)。
3. 真正统一 B-lite: 若保留 R3 cut, 就把 T020/architecture 中所有“点击 link / meta_decisions UI”改成“CLI + runbook 提示”。
4. 修 `open-questions.md` Q7 正文, 让它和 `T019` 的 human-yaml 方案一致; 同时把 spec §8 的删除线 Q11 收到 revision history, 不要挂在正文里。
5. 如果 spec-writer 做完以上收口, 我预期下一轮不需要 human 改 PRD, 只需再跑一次短审即可决定 `CLEAN` 或 `CONCERNS`。
