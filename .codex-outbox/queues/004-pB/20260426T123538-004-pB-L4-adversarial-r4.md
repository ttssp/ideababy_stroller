# Adversarial Review · 004-pB · R4

**Reviewed**: 2026-04-26T20:41:45+08:00
**Reviewer**: GPT-5.4 xhigh
**Spec version**: v0.3.1

## R3 4 finding 收口验证

| R3 ID | R4 修订声明 | 实际修了吗? | 行号佐证 |
|-------|------------|-------------|---------|
| #1 BLOCK arch.md 多处残留 | R4 改 5 处 + 横扫 | ⚠️ | `architecture.md:48,636,712,759,789-791` 已按 R4 修向改对; 但横扫仍命中 `architecture.md:492` (`commit 阶段 wall-clock < 1s`), 且 `architecture.md:636` 仍保留字面 `≤ 10s` |
| #2 HIGH B-lite 两套故事 | R4 改 T020:49 + arch:684-687 | ⚠️ | `T020.md:49` 已改成 CLI/runbook; `architecture.md:685-689` 与 `spec.md:270` 也一致; 但 `T020.md:144` 仍写 alert banner 的“降级 B-lite”链接 |
| #3 MEDIUM Q7 body | R4 改 (b) human 手编 | ✅ | `open-questions.md:148-152` |
| #4 LOW D16 CSS 着色 | R4 改纯 HTML table | ✅ | `spec.md:169` |
| Q11 残留半套 | R4 整段删除 | ✅ | `spec.md:18-19,275-313` |
| 待审 #6 strict mode | R4 加不变量 #15 | ✅ | `architecture.md:773` |

## 横扫验证 (R4 全文收口承诺)

- [ ] `architecture.md` 全文搜 `≤ 10s` 0 命中? 否。除 Revision History 外，`architecture.md:636` 仍有字面 `"miss ≤ 10s"`。
- [ ] `architecture.md` 全文搜 `commit 阶段 wall-clock` 0 命中? 否。`architecture.md:492` 仍写 `commit 阶段 wall-clock < 1s`。
- [ ] `T020.md` / `architecture.md` / `spec.md` 三处 B-lite 描述全部一致 (CLI + runbook)? 否。主叙述已一致 (`T020.md:49`; `architecture.md:685-689`; `spec.md:270`)，但 `T020.md:144` 仍残留 banner link 说法。
- [x] `sum(T001-T022 estimated_hours)` 仍 = 160h? 是。逐文件 `estimated_hours` 相加 = `160`。

## 新引入问题 (R4 衍生, 若有)

- `T020.md:56-57,143` 仍把 OP-1 风险快照定义成 `commit 阶段 wall-clock`。这与 `spec.md:62,236` 的 O5 全程口径不一致，也会漏掉真正高风险的 draft LLM 延迟。严重度: Medium。

## Verdict

- **CONCERNS**

## Why this verdict

R4 的主修补大体到位: Q7 已改成 human 手编 checklist (`open-questions.md:152`), D16 已回到纯 HTML table (`spec.md:169`), Q11 正文已删且 Revision History 仍保留审计旁证 (`spec.md:18-19,275-313`), strict mode 默认值也落进不变量 #15 (`architecture.md:773`)。  
但 R4 自己承诺的“全文收口”没有完全做到: `architecture.md:492` 还留着 `commit 阶段 wall-clock`，`architecture.md:636` 仍保留字面 `≤ 10s`，而 `T020.md:144` 继续讲 banner link，和 `T020.md:49` / `architecture.md:685-689` / `spec.md:270` 的 CLI-only 叙事不一致。  
这些都还是文字/任务包层面的残留，不需要回滚 PRD 或重启 spec 决策，所以我不判 `BLOCK`；但也不足以叫 `CLEAN`。

## 下一步建议

- operator 决定: (a) 先补一轮极小 R4.1 文案收口, 把 `architecture.md:492,636` 与 `T020.md:144,56-57,143` 清干净; (b) 带风险 ship, 并把上述残留记入 `risks.md` / build kickoff 备注。
