# Adversarial Review · 003-pA · R1

**Reviewed**: 2026-04-24T07:35:30Z
**Reviewer**: gpt-5.4 xhigh
**Files read**: `specs/003-pA/spec.md`, `architecture.md`, `tech-stack.md`, `SLA.md`, `risks.md`, `non-goals.md`, `compliance.md`, `dependency-graph.mmd`, `tasks/T002.md`, `T004.md`, `T010.md`, `T011.md`, `T012.md`, `T015.md`, `T016.md`, `T017.md`, `T018.md`, `T019.md`, `T020.md`, `T021.md`, `T022.md`, `T023.md`, `T026.md`, `T027.md`, `T028.md`, `discussion/003/003-pA/PRD.md`, `discussion/003/003-pA/FORK-ORIGIN.md`, `discussion/003/L3/moderator-notes.md`
**Tasks sampled**: `T002,T004,T010,T011,T012,T015,T016,T017,T018,T019,T020,T021,T022,T023,T026,T027,T028`

## Blockers(必须在继续前修复)

| # | Location | Issue | Why it blocks | Fix direction |
|---|---|---|---|---|
| 1 | `spec.md:57-60,130,159`; `architecture.md:269-295`; `tasks/T017.md:47-55,79`; `tasks/T022.md:40-42,70` | **Stuck 状态机契约仍不闭合**。PRD/spec 说 GPU/子进程 CPU/磁盘 IO 任一活跃即 not stuck，但 architecture 的状态图只把 GPU/CPU/网络真正接进转移条件；`disk_io` 只在 probe 列表里出现。`60s` 窗口的采样粒度也在 T017 留成“视实现”。 | O7/C12/C18 是硬约束；现在同一功能在 PRD/spec/architecture/task 间有语义漂移，builder 无法实现一个可证明“不误杀=0”的状态机，也无法定义 cold-start 真卡死、bursty 下载、false negative 是否算失败。 | 先收敛成**唯一真相源**: 明确采样周期、窗口 inclusive/exclusive、disk/net 各自作用、5min 豁免期内真卡死策略、false negative 的成败定义、`needs_human_review` 的人工清除路径；然后补 T017/T022 的正负样本矩阵。 |
| 2 | `spec.md:62-66,129-130,233`; `architecture.md:137,225-227`; `tasks/T010.md:46-49`; `tasks/T018.md:48-52,61-67,80-81` | **“USD 硬上限”目前只是事后记账 + 轮询 SIGINT，不是硬帽**。T010 的 `api_log.jsonl` 是请求后回调日志，T018 每 60s 才读取并超限后停 worker。 | O5/C11 把 `<$30` 写成硬要求，但当前实现允许单个大请求、重试链、长请求完成后才发现超支；spec 自己还承认 worker 可通过 proxy 烧 budget。这个 contract 不能带着“硬上限”进 build。 | 把预算控制前移到 proxy/request path: 预留或上界估算每次请求成本，余额不足直接拒绝；失败/重试/缓存 miss 要同步入账；SIGINT 只做第二道保险。 |
| 3 | `architecture.md:239-246`; `tasks/T012.md:39-46,59-60,67-72,86-89` | **`.claude/` 只读分离在 fallback 路径上可退化成可绕过模式**。T012 明写最后 fallback 是纯 `chmod -R a-w`，同时仍允许 `Bash(python:*)`；deny 只覆盖 shell 级 `chmod/chflags/chattr`。 | C16/R6 是硬安全边界，但当前 task 允许在没有真正只读 mount/immutable flag 时继续运行。那时 worker 仍可用 Python 或其他 syscall 路径改权限，再改 hooks/settings。post-run SHA 校验只能发现事后事故，不能防止事故。 | fail-closed。生产运行只能接受“真只读 mount”或“不可变位已成功设置”；纯 `chmod` 只能用于测试且必须禁止进入真实 worker 流程。再补一个验证: 在允许的 Python 面下也不能把 `.claude/` 改回可写。 |

## High-severity concerns(应修;可作为跟进项带 owner + 截止)

| # | Location | Issue | Suggested action |
|---|---|---|---|
| 1 | `architecture.md:33-34,221,225-227,408`; `tasks/T011.md:39-40,78` | **HF_TOKEN 风险被低估**。当前论证只有“scope 小”，但 gated/private repo token 未必小，也没有最小 scope、只读要求、或镜像缓存优先策略。 | 默认改成“无 `HF_TOKEN` 也能跑公开 demo”；需要 token 时要求最小 scope，并在 README/runbook 明写不要给 write/private mirror 权限。 |
| 2 | `PRD.md:169-174`; `FORK-ORIGIN.md:36-39,45-49`; `architecture.md:369-377`; `tasks/T019.md:35-37,80-82`; `tasks/T027.md:69-73` | **C9/H200 可移植性只写成了手工片段，没有被真实定义或验证**。spec 同时声称 portable，又把 cross-machine resume 排除掉。 | 二选一: 1) 把 claim 收窄成“仅脚本/ckpt 手工迁移可运行”；2) 加一条真实 smoke task，验证 4090→H200→4090 的一次往返。 |
| 3 | `PRD.md:149-155`; `spec.md:229-235`; `tasks/T023.md:70-77`; `tasks/T026.md:39-42` | **O1 的验证不忠于 PRD**。PRD 要“1 个 baseline + 1-2 个 LoRA 变体 + 决策记录，一周内完成”；当前只验证一次 tiny happy-path run，而且 T026 明说“不实际验证一周内”。 | 要么把 O1 重写成 spec 现有能验证的版本并回 PRD 走人审，要么补一个包含 `retry/variant` 的 7-day operator script 验证。 |
| 4 | `spec.md:135`; `architecture.md:256-259`; `tasks/T015.md:35-39,78` | **pip 白名单发生 contract 漂移**。源规范只允许 `pip install -r requirements-locked.txt --require-hashes`，T015 又放进 `uv pip install` 和 `uv sync --frozen`。 | 统一 source of truth。若 `uv` 路径也合法，就在 spec/architecture 显式写出来；否则删掉 task 里的扩张。 |
| 5 | `compliance.md:39-49,79-112`; `tasks/T027.md:41-55`; `tasks/T028.md:37-49` | **合规只检查 Python 包 license，没有覆盖 demo 模型/数据/LoRA 产物能否分发**。Qwen/Llama/gated 模型条款、adapter/checkpoint 是否可公开，没有 owner。 | 在 `compliance.md` 增一节“demo artifact distribution checklist”，并让 T027/T028 release gate 明确依赖它。 |

## Medium / Low findings

- `SLA.md:54` 的 `test_worker_env_no_api_key` 依赖 `/proc/<pid>/environ`，这不能覆盖支持矩阵里的 macOS。
- `tasks/T020.md:81` 说 `validate_quality` 可只做警告，但 `tasks/T021.md:54` 又要求它 pass；这两个任务需要统一。
- `compliance.md:110-112` 要求独立 `license-check.yml`，但 `tasks/T028.md:37-44` 没把它分配出来。
- `tasks/T002.md:66-68` 已经预设“如果 macOS 上 lock 不动就先把 Unsloth 注释掉”，这削弱了 single-operator 本机路径的可信度。

## PRD faithfulness check

- [ ] 每个 PRD Outcome (O1-O7) 有对应 spec 验证
- [x] 没有 spec 功能越出 PRD Scope IN
- [x] 没有 spec 功能进了 PRD Scope OUT

逐项证据:

- **O1**: `PRD.md:149` 要的是“1 baseline + 1-2 LoRA 变体 + 决策记录”；`spec.md:229` 只量从一次 `pars sft start` 到 `report.md` 的 wall-clock；`T026.md:39-42` 还明确不测“一周内”。所以是**部分映射，不是忠实验证**。
- **O2**: `PRD.md:150` 对应 `spec.md:230`，并在 `architecture.md:301-338`、`T021.md:47-55` 落成可校验 schema。这个映射是忠实的。
- **O3**: `PRD.md:151` 对应 `spec.md:231` 与 `T019.md:39-45`。单机 resume 路径有明确测试，但跨机器不在此 outcome 内。
- **O4**: `PRD.md:152` 对应 `spec.md:232` 与 `T027.md:54-55`。忠实，但依赖真实 demo 与人工复现签字。
- **O5**: `PRD.md:153` 对应 `spec.md:233` 与 `T018.md:48-52`。有映射，但因为预算控制只是轮询记账，**验证存在、机制不硬**。
- **O6**: `PRD.md:154` 对应 `spec.md:234`，且 DAG 中 `T025` 专门负责 compare。映射成立。
- **O7**: `PRD.md:155` 对应 `spec.md:235`、`T022.md:19-24,40-42`。有映射，但被 Blocker #1 的状态机契约问题削弱。

Scope IN/OUT 证据:

- **没有越出 Scope IN**: `PRD.md:60-107` 与 `spec.md:38-85` 基本一致；`architecture.md:415-430` 还继续把 Docker、DB、scheduler、MCP、sub-agents 砍掉，没有额外产品面。
- **没有落入 Scope OUT**:
  - workflow 插件契约: `PRD.md:119-121` 明确 OUT；`non-goals.md:36-42` 继续禁止，spec 没偷偷加插件接口。
  - sidecar: `PRD.md:114-116` OUT；`spec.md:45-46` 继续坚持 1 worker 严格顺序。proxy/monitor 是实现内部件，不是 sidecar agent。
  - multi-workflow: `PRD.md:119-121` OUT；`T016.md:56-59` 只保留 baseline / lora / eval 三个模板，没有扩到 prompt/scaffold 工作流。

## Top 3 things the operator personally must verify

1. **你要不要真的把 `<$30` 当硬帽**。如果要，就必须接受 proxy 级前置拒绝/预留方案，而不是继续用“60s 轮询 + SIGINT”。
2. **你的实际运行环境能不能提供 fail-closed 的 `.claude/` 只读能力**。如果只能落到纯 `chmod`，这条 spec 现在不该进 build。
3. **C9 到底要承诺到哪一层**。是“脚本/ckpt 手工可迁移”就够，还是你真的希望 4090↔H200 往返是受支持工作流？这个需要你亲自定边界。

## Model routing / budget concerns

- 我用 28 个 `tasks/T*.md` 的元数据重算后，**DAG 无环**。
- 关键路径不是 inbox 里提到的“11 tasks / 61h”，而是 **10 tasks / 60h**:
  `T001 -> T004 -> T005 -> T012 -> T013 -> T017 -> T023 -> T024 -> T027 -> T028`
- 任务工时总和也不是 inbox 文中的 `142h`，而是 **146h**。这与 `spec.md:191-221` 的 phase-level **310-410h** 明显不一致，说明“buffer ~200h”不是保守，而是**计划口径不一致**。
- 模型路由按**任务数**计算:
  - Sonnet `13/28 = 46.4%`
  - Opus `8/28 = 28.6%`
  - Codex `4/28 = 14.3%`
  - Haiku `3/28 = 10.7%`
- 模型路由按**工时**计算:
  - Opus `56/146 = 38.4%`
  - Sonnet `52/146 = 35.6%`
  - Codex `30/146 = 20.5%`
  - Haiku `8/146 = 5.5%`
- 如果目标是把 Opus/Codex 压在 10-15%，当前 spec **明显偏贵**，尤其 Opus。建议只把 Opus 留给 `T012/T017/T020` 这种安全/contract-heavy 审核与收口任务，确定性编码任务下放到 Sonnet/Codex。
- “Phase 1 并发度 6” 对 solo operator 不现实。`T010/T011/T012/T014/T015` 共用同一安全边界与 hook/env/api_log contract，纸面可并行不等于人类上下文可并行；真实安全并发更像 1-2 个上下文。

## Verdict

- **BLOCK** — 必须先修 Blocker #1-#3 才能进 R2

Claude Code orchestrator note: 本轮共发现 **3 blocker / 5 high / 4 medium-low**。我最不确定的判断是 **C9 是否要求 pars 级跨机 resume，还是只要求脚本与 checkpoint 可手工迁移**。下一轮建议聚焦 **budget hard-cap 机制、`.claude/` 只读 fail-closed、stuck 状态机唯一契约与测试矩阵**。
