# Adversarial Review · 003-pA · R2

**Reviewed**: 2026-04-24T09:55:15Z
**Reviewer**: gpt-5.4 xhigh
**Previous round**: R1 BLOCK (3 blocker / 5 high / 4 m-l)
**Files read**: `.codex-outbox/20260424T071844-003-pA-L4-adversarial-r1.md`, `discussion/003/003-pA/PRD.md`, `discussion/003/003-pA/FORK-ORIGIN.md`, `specs/003-pA/spec.md`, `architecture.md`, `tech-stack.md`, `SLA.md`, `risks.md`, `non-goals.md`, `compliance.md`, `dependency-graph.mmd`, `tasks/T002.md`, `T010.md`, `T011.md`, `T012.md`, `T013.md`, `T015.md`, `T017.md`, `T018.md`, `T019.md`, `T020.md`, `T021.md`, `T022.md`, `T023.md`, `T024.md`, `T025.md`, `T026.md`, `T027.md`, `T028.md`

## R1 findings status

### Blockers
- **B1 Stuck 契约闭合**: **STILL_OPEN** — `architecture.md:358-433` 已把 5s 采样、12 样本窗口、8 场景矩阵写成唯一真相源；但 `tasks/T017.md:86,109` 仍允许 `deque(maxlen=6)`/“60s 轮询”，`tasks/T022.md:75,93` 又按 60s tick / 1min 粒度跑合成 trace。builder 仍可能按错误粒度实现。
- **B2 USD 硬帽前置拒绝**: **CLOSED** — `spec.md:65-70,147,168`, `architecture.md:231-277`, `tasks/T010.md:55-69,83-89`, `tasks/T018.md:21-24,46-53,68-72` 已把主控制前移到 proxy prereject；T018 明确退成第二道 60s 兜底。
- **B3 `.claude/` fail-closed**: **CLOSED** — `spec.md:82-87,148,169,276`, `architecture.md:281-320`, `tasks/T012.md:29-35,55-63,67-79,105-110`, `risks.md:129-144,217` 已明确 fail-closed、refuse-to-start、OQ5 开放项与 Python 面 EACCES 验证。

### High
- **H1 HF_TOKEN 最小 scope**: **STILL_OPEN** — 默认公开 demo 无 token 已落地(`architecture.md:33-34`, `tech-stack.md:202-216`, `tasks/T011.md:39-40,46,73,79-80`)；但 `spec.md:78` 仍写 `HF_TOKEN` 不进 worker env，而 `architecture.md:221`、`tasks/T011.md:39-40,46` 明确允许 gated 下载期注入 read-only `HF_TOKEN`。安全契约仍分叉。
- **H2 O1 7-day rehearsal**: **STILL_OPEN** — `tasks/T027.md:12,18,81-85` 已把 rehearsal script 和 baseline+2 LoRA+compare 放进 T027；但 `spec.md:244` 把 O1 验证指向不存在的 `T029`，source-of-truth 仍未闭合。
- **H3 pip/uv 契约统一**: **CLOSED** — `spec.md:81,170`, `architecture.md:333-348`, `tasks/T015.md:21-29,42-49,77-82` 三条白名单一致。
- **H4 compliance demo artifact**: **CLOSED** — `compliance.md:110-149`, `tasks/T027.md:17,83-89`, `tasks/T028.md:46-53,76` 已补 checklist 且 release gate 明确引用。
- **H5 R1 已标处理完成项无 regression**: **CLOSED** — 跨机边界仍维持“仅手工 rsync 后远端重跑、非跨机 resume”：`spec.md:131,149,176,246`, `architecture.md:505-531`, `non-goals.md:77,136`。

### Medium/Low
- **M1 macOS env test**: **CLOSED** — `SLA.md:54`, `tasks/T011.md:82` 已统一为 `psutil.Process(pid).environ()`.
- **M2 `validate_quality` strict gate**: **CLOSED** — `spec.md:245`, `tasks/T020.md:45-46,82`, `tasks/T021.md:54` 全部改为 strict fail，不再有 warn-only 半态。
- **M3 `license-check.yml` 归属**: **CLOSED** — `compliance.md:110-113`, `tasks/T028.md:12,46-53,76` 已明确归属 T028。
- **M4 T002 Unsloth lock fail**: **CLOSED** — `tasks/T002.md:66-68` 改为 lock 失败即 fail，不再允许“先注释 Unsloth”。

### 操作员决策忠实度
- **D-OP1 USD 真硬帽**: **CLOSED** — `spec.md:65-70,147,168`, `architecture.md:231-277`, `SLA.md:20`, `tasks/T010.md:55-69,83-89`, `tasks/T018.md:21-24` 都已落成 proxy 前置拒绝 + 60s 兜底。
- **D-OP2 MacFUSE 默认 + OQ5 开放**: **CLOSED** — `spec.md:18,82-87,148,169,276`, `architecture.md:285-302`, `tasks/T012.md:21,29-35,55-63,105-108` 既保留推荐默认，也保留 chflags/chattr 与 refuse-to-start。
- **D-OP3 仅手工 rsync**: **CLOSED** — `spec.md:14,131,149,176,246`, `architecture.md:505-531`, `non-goals.md:77,136`, `tasks/T019.md:22-25,42-46,76-86`, `tasks/T027.md:56-58,71-76,89` 都落成“远端重跑，不教跨机 resume”。

## New findings in R2

### New Blockers(若有)
| # | Location | Issue | Why block |
|---|---|---|---|
| — | — | 无 | — |

### New High(若有)
| # | Location | Issue | Suggested action |
|---|---|---|---|
| 1 | `tasks/T019.md:36-46,83-85`; `architecture.md:518-521` | 机器指纹 hard reject 把 `python_version` 与精确 `os_release` 也算进 mismatch，超出 architecture 只要求的 OS/GPU/CUDA 边界；同机 patch 升级后可能被误判成 cross-machine。 | hard reject 只保留 `gpu_name` / `cuda_version` / OS major；`python_version` 与 patch-level OS 变更降为 warning。 |

### New Medium/Low(若有)
- `spec.md:203-236` 与 `dependency-graph.mmd:5-6` 的估时口径仍不一致：spec 写 146h/~220h，graph 写 152h/228h；我按 `tasks/T001-T028.md` 的 `estimated_hours` 独立重算也是 152h。关键路径按 DAG + 工时重算约 74h，不是 inbox 里的 59h。

### Regression(若有)
- `tasks/T017.md:86,109` 与 `tasks/T022.md:75,93` 把 `architecture.md:364-366` 已固定的 5s/12-sample 契约又写回成 10s/60s 粒度，这是修 B1 时引入的文档回退。
- `spec.md:244` 把 O1 验证写成不存在的 `T029`，但真实执行责任已在 `tasks/T027.md:12,18,81-85`；这是修 H2 时引入的 source-of-truth 回退。

## PRD faithfulness check(复核,R1 曾判 O1 部分映射)
- [ ] **O1** — `PRD.md:149` 要求一周内 baseline + 1-2 LoRA + 决策记录；`tasks/T027.md:81-85` 已补实跑 rehearsal，但 `spec.md:244` 仍把正式验证指向不存在的 `T029`。
- [x] **O2** — `PRD.md:150`, `spec.md:245`, `tasks/T020.md:45-46`, `tasks/T021.md:47-55` 映射忠实。
- [ ] **O3** — `PRD.md:151`; `architecture.md:518-521` 只要求同 OS/GPU/CUDA 的同机 resume；`tasks/T019.md:36-46,83-85` 又加了 `python_version` / `os_release` hard reject。
- [x] **O4** — `PRD.md:152`, `tasks/T027.md:43-58,81-90` 忠实。
- [x] **O5** — `PRD.md:153`, `spec.md:248`, `architecture.md:231-277`, `tasks/T010.md:55-69` 忠实。
- [x] **O6** — `PRD.md:154`, `spec.md:249`, `tasks/T025.md:21-24,45-49` 忠实。
- [ ] **O7** — `PRD.md:155`, `spec.md:250`, `architecture.md:358-433` 的主契约已补，但 `tasks/T017.md:86,109`、`tasks/T022.md:75,93` 仍允许错误粒度实现。
- [x] **Scope IN/OUT 无泄漏** — `PRD.md:58-141`、`spec.md:38-114`、`non-goals.md:15-147` 仍保持单 worker、无 Docker、无 Web UI、无跨机 resume。

## Model routing 复核(R1 曾 concern)
- 按 `tasks/T001-T028.md` 的 `estimated_hours` 独立重算：**152h** 总工时；Opus **23h = 15.1%**，Sonnet **104h = 68.4%**，Codex **16h = 10.5%**，Haiku **9h = 5.9%**。这与 `dependency-graph.mmd:5-6` 一致。
- **目标 Opus ≤ 15% 未达成**：当前是 **15.1%**，只高 0.1pp，但严格说仍未过线。
- 降级后的关键任务 acceptance 仍然严苛，不是“换 reviewer = 降标准”：`tasks/T010.md:65-89`, `tasks/T011.md:43-74`, `tasks/T013.md:60-81`, `tasks/T019.md:76-86`, `tasks/T023.md:70-97` 都保留了明确验证与失败路径。
- spec 的 phase estimate 仍未同步 task reality：`spec.md:203-236` 说 146h/~220h，但 `dependency-graph.mmd:5-6` 与 task metadata 都是 152h/228h。关键路径我按 `depends_on + estimated_hours` 重算为约 **74h**，路径为 `T001→T004→T005→T007→T010→T013→T017→T019→T023→T024→T027→T028`，明显不支持 inbox 里的 59h 说法。

## Top 3 things operator still must verify

1. **先把 B1 真关掉**：清掉 `T017/T022` 中所有 10s/60s/1min stale 指令，只剩 `architecture.md §8` 的 5s/12-sample 契约。
2. **把 source-of-truth 对齐**：`spec.md:244` 的 O1 正式验证应回指 T027，而不是 phantom `T029`；`spec.md:78` 也要与 `architecture.md:221` / `T011` 对齐，明确 `HF_TOKEN` 的 gated-model 例外。
3. **收窄 resume 指纹 hard reject**：否则同机 Python 或 OS patch 更新就可能打穿 O3 的 >95% resume 目标。

## Verdict

- **BLOCK** — B1 仍未真正关闭；另有 H1/H2 source-of-truth 分叉与 1 条新 high。现在还不该进 build。

## Orchestrator note

- 本轮共发现 **0 new blocker / 1 new high / 2 regression**
- 最不确定判断：**H1/H2 应算 STILL_OPEN 还是文档级 concern**。我倾向 STILL_OPEN，因为它们都还会误导 builder，而不仅是排版瑕疵。
- 若进 R3 建议聚焦：**T017/T022 stuck 粒度回退、O1 `T029` 删除并回指 T027、HF_TOKEN 契约统一、T019 指纹粒度收窄**。
