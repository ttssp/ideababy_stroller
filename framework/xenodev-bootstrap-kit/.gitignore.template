# XenoDev .gitignore
# per ideababy_stroller framework/xenodev-bootstrap-kit/

# === Safety Floor 件 1:凭据隔离 ===
# 这些文件不应进入 git history(per SHARED-CONTRACT.md §1 第 1 件)
.env.production
.env.production.*
.env.prod
.env.prod.*
.env.local
secrets/production/
secrets/prod/

# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# === macOS ===
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# === IDE / Editor ===
.vscode/
.idea/
*.swp
*.swo
*~
.claude/settings.local.json

# === XenoDev runtime ===
# Eval event log 不应进 git history(append-only,本地分析用)
.eval/events.jsonl

# Worktrees(parallel-builder 用)
.worktrees/

# Logs
logs/
*.log

# === TDD 证据入仓豁免(KG-B3 修复 · forge 006 v8 簇④)===
# red-green-log 是 TDD 红→绿证据,必须入 git(per SHARED-CONTRACT · 证据入仓)。
# 路径无关豁免:不锚定仓根 tests/(XenoDev 项目在 projects/<feature>/tests/),
# 匹配任意深度的 red-green-log/*.log。必须放在 *.log 之后才能覆盖其兜底。
!**/red-green-log/
!**/red-green-log/*.log

# === 测试产出 ===
.coverage
htmlcov/
.tox/

# === Node(若 XenoDev 用 TS/JS skill 实装)===
node_modules/
*.tsbuildinfo
.next/
out/
