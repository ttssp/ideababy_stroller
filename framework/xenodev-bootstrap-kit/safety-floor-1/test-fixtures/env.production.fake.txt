# FAKE FIXTURE — NOT REAL CREDENTIALS
# Filename note: real fixture uses ".env.production.fake" naming, but Claude Code
# permission settings deny writing to .env.* paths. To work around this, the
# fixture is shipped as "env.production.fake.txt" in the kit; operator can
# rename it to ".env.production" in a tmpdir for true scan testing:
#
#     TMPDIR="$(mktemp -d)"
#     cp env.production.fake.txt "$TMPDIR/.env.production"
#     bash ../scan-credentials.sh "$TMPDIR"
#     # 期望: exit 1 + stderr 报 .env.production
#     rm -rf "$TMPDIR"

DATABASE_URL=postgresql://fake-user:fake-password@fake-host:5432/fake-db
API_KEY=fake_sk_test_abc123def456
JWT_SECRET=fake_jwt_secret_for_testing
