# T016: 个人金融笔记 wiki (R3 简化版, 无 LLM 自动去重)

**spec_ref**: specs/004-pB/spec.md#2-1 · spec.md#7-glossary · risks.md#OP-7
**phase**: 2
**depends_on**: [T002, T003, T005]
**blocks**: [T019]
**parallelizable_with**: [T014, T015, T018]
**file_domain**:
  - src/decision_ledger/services/note_service.py
  - src/decision_ledger/ui/router_notes.py
  - src/decision_ledger/ui/templates/notes/index.html
  - src/decision_ledger/ui/templates/notes/edit.html
  - src/decision_ledger/ui/templates/notes/_note_card.html
  - tests/unit/services/test_note_service.py
  - tests/integration/ui/test_notes_routes.py
**estimated_hours**: 4
**recommended_model**: sonnet-4-6
**risk_level**: low
**risk_refs**: [OP-7]
**constraint_refs**: [C17]

---

## R3 修订摘要 (B-R2-4 真砍)

| R3 修订 | 说明 |
|---------|------|
| **R3 B-R2-4 真砍** | 7h → **4h**. 范围简化: <br/>- **保留**: CRUD (创建/编辑/删除); SQLite FTS5 全文搜索 (Python 标准库, 无 LLM); content_hash 去重 (普通字符串 hash, 不调 LLM); agent 引用笔记接口 (`list_summaries_for_topics`, 简单 SQL LIKE 查找). <br/>- **砍掉**: LLM 语义去重 (note_dedup prompt 删除); `docs/prompts/note_dedup_v1.md` 删除. v0.2+ 加 LLM 去重. |

estimated_hours **4h**.

---

## Goal (R3 简化版, 4h)
个人金融笔记 wiki (D14 nav 第 4 项): human 写关于关键概念的解释 (e.g. "P/E vs P/B" /
"FOMC 分点投票"). **R3 简化职责**:
- CRUD: 笔记可创建/编辑/删除
- **R3 砍 LLM**: 仅 content_hash 同步去重 (字符串 hash 命中提示已有相同笔记); **删除 LLM 语义去重**, 节约 LLM cost + 时间
- agent 引用笔记 (R3 简化): SQL LIKE 查找含 topic 关键词的笔记 list (不调 LLM)
- 全文搜索 (SQLite FTS5)

## Inputs
- T002 `Note` 模型 (content_hash 字段)
- T003 `NoteRepository.find_by_content_hash`, `search_fulltext`
- T005 LLMClient (R3: 不再调用)

## Outputs (R3 简化, 4h)
- `services/note_service.py`: CRUD + content_hash dedup (同步, 无 LLM)
- `ui/router_notes.py`: GET `/notes`, GET `/notes/new`, POST `/notes`,
  GET/POST `/notes/{id}/edit`, DELETE `/notes/{id}`, GET `/notes?q=keyword`
- 模板: 列表 (按 updated_at DESC) + 编辑表单 + 卡片 partial
- ~~`note_dedup_v1.md` prompt 模板~~ (R3 删除)
- alembic migration `0003_notes_fts.py`: 增加 SQLite FTS5 virtual table 同步触发器
- 单测 (R3 简化): hash 命中 + 搜索 hit
- 集成测: 创建笔记 → 创建相同 hash → UI 显示 dedup 提示

## Implementation plan (R3, 4h)
1. service: content_hash dedup 同步 (字符串 hash; **R3 砍 LLM 语义 dedup**)
2. router CRUD + search
3. FTS5 migration (002 + new 003)
4. 模板用 Pico CSS, 笔记卡片简洁
5. 单测 (R3 简化): hash collision case + 非命中 case (无 LLM mock)
6. 集成测: TestClient 创建 → 创建相同 hash → 命中 dedup
7. agent 引用笔记的接口 (R3 简化): `note_service.list_summaries_for_topics(topics: list[str])
   -> list[NoteRef]`, **R3: 用 SQL LIKE 查找, 不调 LLM**, T015 / T019 调用

## Verification
- [ ] `uv run pytest tests/unit/services/test_note_service.py -v` 全绿
- [ ] `uv run pytest tests/integration/ui/test_notes_routes.py -v` 全绿
- [ ] 浏览器: `/notes/new` 输入 "P/E ratio" → 保存; 再创建相同 (相同 hash) 主题, dedup 提示 (R3: 仅 hash 命中, 不语义)
- [ ] 全文搜索: `/notes?q=FOMC` 命中含关键词
- [ ] `note_service.list_summaries_for_topics(["P/E"])` 返回笔记 ref 列表 (R3: SQL LIKE)
- [ ] **R3 cut 验证**: NoteService 路径 LLM call 计数 = 0

## Known gotchas
- ~~LLM dedup~~ (R3 删除, v0.2+)
- FTS5 中文分词差; 用 Trigram tokenizer 改善
- content_hash 只对 normalize 后内容算 (lower + strip)
- **R3 真砍**: LLM 语义去重已删. 任何 PR 想加回 prompt 必拒 (R3 已锁简化版)

## Out of scope for this task
- 学习检查抽取 (T019; 复用本 task 提供的 list_summaries)
- 知识图谱 / 自动标签 (v0.5+)
- 多语言 (v1.0+)
