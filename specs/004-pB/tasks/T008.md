# T008: 决策档案录入 UI · draft → preview → commit 双阶段 (B1 修订)

**spec_ref**: specs/004-pB/spec.md#4-prior-decisions D13/D19/D21 · architecture.md#3-3-录入双阶段流程 · SLA.md#1-2
**phase**: 1
**depends_on**: [T002, T003, T004, T005, T007]
**blocks**: [T009, T010, T013, T018]
**parallelizable_with**: [T006, T011, T012]
**file_domain**:
  - src/decision_ledger/ui/router_decisions.py
  - src/decision_ledger/ui/templates/decisions/new.html
  - src/decision_ledger/ui/templates/decisions/draft_preview.html
  - src/decision_ledger/ui/templates/decisions/show.html
  - src/decision_ledger/ui/templates/decisions/list.html
  - src/decision_ledger/ui/templates/decisions/_partials_hx.html
  - src/decision_ledger/ui/static/decisions.js
  - src/decision_ledger/ui/static/decisions.css
  - src/decision_ledger/services/decision_recorder.py
  - src/decision_ledger/services/draft_gc_worker.py
  - tests/integration/ui/test_decisions_routes.py
  - tests/unit/services/test_decision_recorder.py
**estimated_hours**: 16
**recommended_model**: opus-4-7
**risk_level**: high
**risk_refs**: [OP-1, OP-3, TECH-5]
**constraint_refs**: [C11, C21]

---

## R3 修订摘要 (Codex R2 BLOCK B-R2-1 + B-R2-2 解决)

| Codex finding | 修订 |
|---------------|------|
| **R3 B-R2-1** spec.md O5 / C11 度量回退 PRD 原口径 | timing 度量改回**全程 wall-clock**: 起点 = GET /decisions/new 200 OK, 终点 = POST commit 200 OK; 全程 < 30s 包含 draft 阶段 LLM. 删除 R2 "preview 200 OK 才计时" 字样. |
| **R3 B-R2-2** placeholder-then-commit 路径违反 R10 | **删除整个 placeholder fallback 路径**. draft 阶段 ConflictReport + Rebuttal 同步生成超时 (> 5s wall-clock 因 D13/D21 R3 修订把上限拉到 5s) → **draft 创建失败 (返回 503 + 错误页 "系统繁忙, 请等 cache warmer 完成或重试")**, 不允许 commit 用 placeholder 继续. 加 architecture.md §9 不变量 #13: ConflictReport.signals 永远来自真实 LLM, 无 placeholder. |

继承 R2 修订: B1 (draft/commit 双阶段) + M1 (would_have_acted 强制 yes/no).

estimated_hours 16h 不变 (R3 仅修行为, 不增范围; placeholder fallback 删除省微小工时, GC worker 维持).

---

## Goal
本 task 是 PRD 硬门槛 C11/O5 (**录入全程 < 30 秒, 守 PRD §S1 原口径, R3 修订**) 的工程实现 — 双阶段录入流.

### 路由设计 (R3 修订)
- `GET /decisions/new`: 一屏 draft 表单 (3 字段: ticker / intended_action / draft_reason), 默认填上次值 (D13). **R3: 这是 C11 30s 全程的计时起点**.
- `POST /decisions/draft`:
  1. INSERT decision_drafts (status='draft', refs=NULL)
  2. **同步并发** (asyncio.gather):
     - `ConflictReportAssembler.assemble(signals=[lane.analyze(...)] for 3 lanes, env_snapshot)` (T010 提供) — cache 命中常态 ≤ 1s, miss ≤ 5s
     - `DevilAdvocateService.generate(draft, env_snapshot)` (T013 提供, fast-path ≤ 3s)
  3. UPDATE decision_drafts SET conflict_report_ref=..., devils_rebuttal_ref=...
  4. 302 → `/decisions/draft/{draft_id}/preview`
  - **R3 修订: 超时行为**: 双调用并发 wall-clock > 5s (asyncio.wait_for 超时) → **draft 创建失败 → 返回 503 + 错误页 "系统繁忙, 请等 cache warmer 完成或重试"**. 不允许 placeholder fallback (B-R2-2 修复). 不放行 commit. (走 OP-1 mitigation)
- `GET /decisions/draft/{draft_id}/preview`: 渲染三列等宽 + 顺序 hash 随机化 (T010 提供 ConflictReport.rendered_order_seed) + 反方一句话 + commit 表单 (final_action / final_reason / **强制 yes/no would_have_acted_without_agent**)
- `POST /decisions/{draft_id}/commit`:
  1. 校验 draft 存在且 status='draft'
  2. 校验 final_reason ≤ 80 char + would_have_acted_without_agent ∈ {true, false} (M1: 不接受 NULL)
  3. BEGIN; INSERT decisions (status='committed', 复用 draft 的 refs); UPDATE decision_drafts SET status='committed', committed_at=now(); COMMIT
  4. 302 → `/decisions/{trade_id}`
  5. **绝对禁止 LLM 调用** (架构不变量 §9.1, R2 修订: commit 路径无 LLM)

### UX 细节 (R2)
- ticker 字段: HTMX autocomplete 关注股 30-50 (本地 cache, 不查 LLM)
- intended_action: 4 个 radio (BUY/SELL/HOLD/WAIT, label 显示 "1/2/3/4" 数字键盘快捷键)
- draft_reason: textarea max 80 char, 实时倒数显示
- draft submit: Enter 触发
- preview 页:
  - 三列等宽 (CSS grid 1fr 1fr 1fr)
  - 顺序按 ConflictReport.rendered_order_seed (T010 输出) 排列, 不固定 advisor 第 1 / agent_synthesis 第 3
  - 一句反方在三列下方
  - 下面是 commit form
- commit form 字段:
  - final_action (默认 = intended_action, human 可改, 数字键 1/2/3/4)
  - final_reason (默认 = draft_reason, human 可改)
  - **would_have_acted_without_agent: 两个 radio (yes / no), 都未选时 commit 按钮 disabled** (R2 M1)
- commit submit: Enter (commit 阶段无 LLM, wall-clock < 1s)

### GC
- `services/draft_gc_worker.py`: 后台 task 每 5 分钟扫一次, status='draft' AND created_at < now()-30min → status='abandoned'

### D2 / R11 (不变)
- 决策档案 tab 是 nav 首页 (T004 已配); list 页按 action 分类显示, hold/wait 与 buy/sell 同等显眼

## Inputs
- T002 `Decision` / `DecisionDraft` / `EnvSnapshot` 模型 + validators (reason ≤ 80, status enum, would_have_acted_without_agent NOT NULL)
- T003 `DecisionRepository.insert`, `DecisionDraftRepository.insert`/`update_refs`/`commit`/`gc_expired`, `PortfolioRepository.latest_snapshot`, `AdvisorRepository.latest_for_week`
- T004 base 模板与 nav
- T005 LLMClient (draft 阶段同步调用)
- T007 StrategyModule registry (3 lane analyze)
- D13 完整双阶段 UX 路径 (R3 守 PRD 原口径)
- D21 cache 命中常态 ≤ 1s, miss ≤ 5s; Rebuttal fast-path ≤ 3s (R3 修订)
- D22 / D23 (T010 / T013 提供 assemble / generate)

## Outputs
- `services/decision_recorder.py`:
  ```python
  class DecisionRecorder:
      async def create_draft(self, payload) -> tuple[DraftId, ConflictReport, Rebuttal]:
          # INSERT decision_drafts (status='draft', refs=NULL)
          # try:
          #     async with asyncio.timeout(5.0):  # R3 D21 上限 5s
          #         conflict_report, rebuttal = await asyncio.gather(
          #             ConflictReportAssembler.assemble(...),    # T010, ≤ 5s
          #             DevilAdvocateService.generate(...),        # T013, fast-path ≤ 3s
          #         )
          # except asyncio.TimeoutError:
          #     # R3 B-R2-2: 不允许 placeholder fallback
          #     # 标记 draft as 'abandoned' (或保留 'draft' 由 GC 清理)
          #     raise HTTPException(503, "系统繁忙, 请等 cache warmer 完成或重试")
          # UPDATE refs
          # return (draft_id, conflict_report, rebuttal)

      async def commit_draft(self, draft_id, payload) -> TradeId:
          # 校验 status='draft' + would_have_acted ∈ {true,false}
          # 校验 draft.conflict_report_ref + devils_rebuttal_ref 都已填 (R3 不变量 #13: 真实 LLM)
          # BEGIN; INSERT decisions; UPDATE drafts; COMMIT
          # **NO LLM CALL**
          # return trade_id
  ```
- `services/draft_gc_worker.py`: 5min cron, status='draft' AND created < now()-30min → 'abandoned'
- `ui/router_decisions.py`:
  - `GET /decisions/new`
  - `POST /decisions/draft` (上述 4 步)
  - `GET /decisions/draft/{draft_id}/preview` (渲染三列 + 反方 + commit form)
  - `POST /decisions/{draft_id}/commit`
  - `GET /decisions/{trade_id}` (success show)
  - `GET /decisions` (list)
  - **注意**: `show.html` 内提前放好链接 `<a href="/decisions/{trade_id}/post-mortem">回填事后回报</a>` 但 endpoint 在 T018 接管
- 模板:
  - `new.html`: draft 表单
  - `draft_preview.html`: 三列等宽 (按 rendered_order_seed 排列) + 反方 + commit form (R2)
  - `show.html`: success 页 (post-commit)
  - `list.html`: 按 action 分类
  - `_partials_hx.html`: HTMX swap (autocomplete + radio 高亮)
- `decisions.js`: 监听 `Enter` → form submit; 数字键 1-4 切 action; reason 倒数; **commit form 在 yes/no 都未选时 disable submit button** (R2 M1)
- 集成测试:
  - `test_decisions_routes.py::test_new_form_default_fill`: 上次决策 ticker / action / reason 默认填好
  - `test_decisions_routes.py::test_post_draft_creates_with_refs`: POST /decisions/draft → decision_drafts 表新增 + conflict_report_ref + devils_rebuttal_ref 都已填 (同步生成完毕)
  - `test_decisions_routes.py::test_post_draft_lat_under_5s`: cache 命中场景 ≤ 1s, mock cache miss 场景 ≤ 5s (R3 D21)
  - `test_decisions_routes.py::test_preview_renders_three_columns_random_order`: HTML 含 3 个 `[data-source]` 元素, 顺序按 rendered_order_seed (R10 + R2 D22)
  - `test_decisions_routes.py::test_commit_no_llm`: POST /decisions/{draft_id}/commit 路径 mock LLMClient 应该 0 次调用 (R2 不变量 §9.1)
  - `test_decisions_routes.py::test_full_flow_wall_clock_under_30s`: **全程从 GET /decisions/new 200 OK 到 POST commit 200 OK < 30s** (R3 C11, 守 PRD 原口径)
  - `test_decisions_routes.py::test_commit_requires_yes_no`: 不勾选 would_have_acted 时 422 (R2 M1)
  - `test_decisions_routes.py::test_reason_length_limit`: > 80 char 返回 422
  - `test_decisions_routes.py::test_draft_gc_after_30min`: 模拟 31min, draft worker 标 abandoned
  - **R3 新增** `test_decisions_routes.py::test_draft_timeout_blocks_commit`: mock cache miss + LLM 调用 6s 强制超时 → POST /decisions/draft 返回 503 (而非 placeholder fallback), 不允许后续 commit (B-R2-2)
  - **R3 新增** `test_decisions_routes.py::test_no_placeholder_in_conflict_report`: cache miss + LLM 失败时, 不能创建 draft 含 placeholder ConflictReport.signals; 必须 503 raise (R3 不变量 #13)
- 单测: DecisionRecorder mock T010/T013/repos, 验证 EnvSnapshot 装配 + 双阶段事务

## Implementation plan
1. 模板 + decisions.js (键盘快捷键 / Enter submit / autocomplete / **commit button gating**)
2. router_decisions.py 5 个路由 (含 draft + preview + commit 三段)
3. DecisionRecorder service:
   - `create_draft`: 写 draft (status='draft', refs=NULL) → **`asyncio.timeout(5.0)` 包裹** asyncio.gather(T010.assemble, T013.generate) → UPDATE refs (R3 D21 ≤ 5s 上限)
   - **R3 B-R2-2: 超时 → raise HTTPException(503), 不写 placeholder, 不放行 commit**
   - `commit_draft`: 仅 SQLite UPDATE + INSERT, 严格无 LLM (R2 不变量); 校验 draft.refs 非 NULL (不变量 #13)
4. draft_gc_worker: AsyncIOScheduler, 每 5 分钟一次
5. 集成测试用 TestClient + 真 SQLite (tmpdir alembic upgrade) + mock T010/T013 service
6. UX 测试: 字段 tab order 验证, label 含 "1=买入 / 2=卖出 / 3=持有 / 4=等待"
7. 反 LLM 单测: commit 路径把 LLMClient.call 替换 raise, 测 commit 仍 200
8. **R3 新增**: 503 失败路径单测: mock T010 LLM call 6s sleep → asyncio.timeout 触发 → 503 + draft 状态 'abandoned' (B-R2-2)

## Verification
- [ ] `uv run pytest tests/integration/ui/test_decisions_routes.py` 全绿
- [ ] `uv run pytest tests/unit/services/test_decision_recorder.py` 全绿
- [ ] 浏览器手动:
  - `/decisions/new` 默认填 ticker=最近一只, intended_action=hold
  - 按 `1` intended_action=buy; 输入 draft_reason ≤ 80 字符; 按 Enter → 创建 draft
  - ≤ 5s 内 (cache miss 场景) 跳到 `/decisions/draft/{id}/preview`, 显示三列 (顺序随 hash 不固定) + 反方
  - 确认 final_action / 写 final_reason / 选 yes 或 no (would_have_acted) → Enter 提交
  - **全程 wall-clock < 30s (从 GET /decisions/new 到 POST commit 200 OK)**, 跳转 `/decisions/{trade_id}` (R3 守 PRD §S1 原口径)
- [ ] `test_commit_no_llm`: commit 路径 LLMClient 调用次数 = 0 (R2 不变量 §9.1)
- [ ] `test_commit_requires_yes_no`: 不勾选 would_have_acted 时 422 (R2 M1)
- [ ] `test_post_draft_creates_with_refs`: draft 创建后 refs 都已填 (R2 B1)
- [ ] **R3 新增** `test_draft_timeout_blocks_commit`: cache miss + LLM 6s sleep → 503, 不允许后续 commit (B-R2-2)
- [ ] **R3 新增** `test_no_placeholder_in_conflict_report`: 不会出现 placeholder ConflictReport.signals (R3 不变量 #13)
- [ ] reason > 80 字符 → 422
- [ ] mypy + ruff 通过

## Known gotchas
- HTMX form submit + Enter 处理: 用 `hx-trigger="submit, keyup[key=='Enter']"` 慎重避免 IME 上屏被吞 (中文输入)
- ticker autocomplete 数据来自内存 cache (T012 录入后存内存), 不查 SQLite 每次
- post_mortem field T018 才填; new / preview 路径不渲染
- D2 / R11: list 页 action 列必须含 hold + wait, 不能只显示 buy/sell
- **R3 修订**: draft 阶段 LLM 调用 **≤ 5s** 上限 (D21), **超时 → 503, 不允许 placeholder fallback** (B-R2-2). 任何 PR 想 fallback 必拒. 系统失败信号 → OP-1 mitigation.
- **R2**: commit 阶段绝对无 LLM, 任何后续 PR 加 LLM 调用必拒
- **R2 M1**: would_have_acted_without_agent 在 schema 是 `INTEGER NOT NULL` (无默认), commit endpoint 校验 ∈ {0, 1}, 不接受 NULL
- **R3 不变量 #13**: ConflictReport.signals 永远来自真实 LLM. commit 路径单测必须验证 draft.conflict_report_ref + devils_rebuttal_ref 都非 NULL 且对应 row 的 signals 字段非 placeholder 内容

## Out of scope for this task
- E2E timing test 自动化 (T009)
- ConflictReportAssembler 实现 (T010)
- DevilAdvocateService 实现 (T013)
- post_mortem UI (T018)
- Telegram 触发 (T017)
