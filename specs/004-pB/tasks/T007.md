# T007: StrategyModule IDL (analyze only) + 三个独立 lane 占位实现

**spec_ref**: specs/004-pB/spec.md#4-prior-decisions D6/D7 · architecture.md#3-1-idl-边界与三服务拆分 · open-questions.md#Q6
**phase**: 1
**depends_on**: [T002, T003, T005]
**blocks**: [T010, T013]
**parallelizable_with**: [T006, T008, T011, T012]
**file_domain**:
  - src/decision_ledger/strategy/__init__.py
  - src/decision_ledger/strategy/base.py
  - src/decision_ledger/strategy/advisor_strategy.py
  - src/decision_ledger/strategy/placeholder_model.py
  - src/decision_ledger/strategy/agent_synthesis.py
  - src/decision_ledger/strategy/registry.py
  - docs/prompts/advisor_strategy_v1.md
  - docs/prompts/agent_synthesis_v1.md
  - tests/unit/strategy/test_idl_contract.py
  - tests/unit/strategy/test_advisor_strategy.py
  - tests/unit/strategy/test_placeholder_model.py
  - tests/unit/strategy/test_agent_synthesis.py
  - tests/unit/strategy/test_registry.py
  - tests/unit/strategy/test_lane_isolation.py
**estimated_hours**: 12
**recommended_model**: opus-4-7
**risk_level**: high
**risk_refs**: [TECH-4, OP-7, OP-6]
**constraint_refs**: [C5, C9, C17]

---

## R2 修订摘要 (Codex R1 BLOCK B2/B3 解决)

| Codex finding | 修订 |
|---------------|------|
| **B2** agent_synthesis 既当 lane 又当裁判 | StrategyModule IDL 仅保留 `analyze()`. `conflict_resolve` 拆出到 T010 ConflictReportAssembler (中立服务, D22), `devil_advocate` 拆出到 T013 DevilAdvocateService (D23). 三 lane 完全平权, 无总结者. |
| **B3** StrategyModule IDL 自相矛盾 (analyze 不含 env_snapshot 但描述综合) | analyze 签名加 `env_snapshot` 参数. agent_synthesis lane 用 env_snapshot 综合, 但**不读**其他 lane 输出 (单测 `test_lane_isolation.py` 强制). |

estimated_hours 14h → 12h (减 2h, 因 IDL 简化).

---

## Goal
本 task 是架构枢纽 (architecture.md §3.1). 实现:
1. `StrategyModule` Protocol (R2 修订: **唯一方法 `analyze()` + `source_id` 字段**, 不再
   有 conflict_resolve / devil_advocate; 架构锁死, **不可增减**, TECH-4 mitigation)
2. 三个占位实现 (R2 修订: 仅 analyze, 三 lane 平权, 无总结者):
   - `AdvisorStrategy` (source_id="advisor"): 取本周咨询师对该 ticker 的观点, LLM
     prompt 转 StrategySignal; 无观点 confidence=0.0, direction="no_view"
   - `PlaceholderModelStrategy` (source_id="placeholder_model"): Q6 默认假设 = 永远
     no_view (confidence=0.0), 不假装"私有模型有意见"
   - `AgentSynthesisStrategy` (source_id="agent_synthesis"): 用 advisor_report +
     portfolio + env_snapshot 综合 LLM 出 signal; **不读其他 lane 输出** (R9 解耦);
     **R2: 与其他两个 lane 平权**, 不再承担 conflict_resolve 主体职责
3. `StrategyRegistry`: 用 FastAPI Depends 风格的依赖注入, 列出所有已注册 module,
   每个独立可替换 (R9 / 不变量 §9.6: 不直接读他模块状态); 预留
   `register_custom(CustomStrategyModule)` 扩展位 (D7)
4. **不变量约束**: `StrategySignal.rationale_plain` 永远非空 (R3 / OP-7)
5. **R6 反诱导**: agent_synthesis prompt 中明确"≥ 50% analyze 输出建议不动",
   prompt 模板单测覆盖 (sanity check 移到 lane 内, 不再依赖 conflict_resolve)
6. **R2 新增**: lane 隔离单测 — 每个 lane 单元测试不能 mock/import 其他 lane (R9 工程化)

## Inputs
- T002 `StrategySignal` pydantic 模型 + validators
- T003 `AdvisorRepository.list_for_ticker`, `PortfolioRepository.latest_snapshot`
- T005 LLMClient + tool use schema
- architecture.md §3.1 IDL 完整签名 (R2 修订)
- non-goals.md D5 / D6 / D8 (不诱导高频, 必有白话, 不路径依赖单一咨询师)

## Outputs

### IDL 与基类
- `strategy/base.py`:
  ```python
  class StrategyModule(Protocol):
      source_id: str
      async def analyze(
          self,
          advisor_report: AdvisorWeeklyReport,
          portfolio: Portfolio,
          ticker: str,
          env_snapshot: EnvSnapshot,    # R2 加: 解决 B3
      ) -> StrategySignal: ...
  class CustomStrategyModule(StrategyModule):
      """v0.5+ 扩展位 (D7)"""
  ```
  **不再有** `conflict_resolve` / `devil_advocate` (它们在 T010 / T013)

### 三个 lane 实现 (R2: 仅 analyze)
- `strategy/advisor_strategy.py`: `analyze()` 调 advisor_repo + LLM; prompt 模板版本号引用 advisor_strategy_v1.md
- `strategy/placeholder_model.py`: `analyze()` Q6 默认: 永远 no_view + 占位 rationale_plain (e.g. "占位模型无观点 — 真模型 v0.5+ 替换")
- `strategy/agent_synthesis.py`: `analyze()` 调 LLM 综合 (advisor_report + portfolio + env_snapshot) → signal; **不读** AdvisorStrategy / PlaceholderModelStrategy 输出 (R2 R9 解耦)

### Registry
- `strategy/registry.py`: `StrategyRegistry` + DI helper; `register_custom(...)` 扩展位

### Prompts
- `docs/prompts/advisor_strategy_v1.md`: 取观点 → signal
- `docs/prompts/agent_synthesis_v1.md`: 综合 (advisor + portfolio + env_snapshot) 出 signal; ≥ 50% "不动" 指令 (R6); **不引用其他 lane 输出**

### Tests
- `tests/unit/strategy/test_idl_contract.py`:
  - `test_protocol_attrs_locked`: 断言 `len(set(dir(StrategyModule)) & {"source_id","analyze"}) == 2` AND `not hasattr(StrategyModule, "conflict_resolve")` AND `not hasattr(StrategyModule, "devil_advocate")` — IDL 锁死, 任何 PR 加方法立刻挂
  - `test_analyze_signature_has_env_snapshot`: 断言 inspect.signature(analyze).parameters 包含 'env_snapshot' (R2 B3)
  - `test_no_extra_methods`: 锁字段
- `test_advisor_strategy.py`: mock LLM, 测有/无观点两支, rationale_plain 非空
- `test_placeholder_model.py`: 测永远 no_view + confidence=0.0 + rationale_plain 非空
- `test_agent_synthesis.py`:
  - `test_uses_env_snapshot`: 调用时 env_snapshot 必传, signal.inputs_used 含 'env_snapshot'
  - `test_no_lane_state_read`: agent_synthesis 实现里**不能 import** advisor_strategy 或 placeholder_model 模块 (静态 grep + AST 单测)
  - `test_50pct_no_action`: 50 次 random fixture, ≥ 25 次 direction ∈ {wait, neutral, no_view} (R6 sanity)
- `test_registry.py`: register/get_all/不可重复注册 source_id
- **R2 新增** `tests/unit/strategy/test_lane_isolation.py`:
  - 解析每个 lane 模块的 import 树, 断言不包含其他 lane 名字
  - 用 mock 验证 `analyze()` 调用链: agent_synthesis.analyze 不调用 advisor_strategy.analyze 或 placeholder_model.analyze

## Implementation plan
1. `base.py` Protocol (only `source_id` + `analyze`) + CustomStrategyModule abstract (R2 B3)
2. `advisor_strategy.py`: `analyze` 调 advisor_repo + LLM
3. `placeholder_model.py`: `analyze` 返回 no_view + 占位反驳 (R2: 不再有 conflict_resolve / devil_advocate)
4. `agent_synthesis.py`: `analyze` 综合 (advisor_report + portfolio + env_snapshot) → signal; **不读其他 lane** (R2 R9)
5. prompt 模板: advisor_strategy_v1 (取观点 → signal); agent_synthesis_v1 (综合 + ≥ 50% "不动" 指令; **不引用其他 lane**)
6. `registry.py` 单进程注册表, FastAPI Depends 注入
7. 单测覆盖 IDL contract + 每个实现 + R6 sanity check + **lane 隔离 (R2)**
8. **架构锁**: PR 任何修改 IDL 字段或方法数 → 单测 `test_idl_contract.py` 立刻断言失败

## Verification
- [ ] `uv run pytest tests/unit/strategy/ -v` 全绿
- [ ] `test_idl_contract.py::test_protocol_attrs_locked` 锁住 IDL — 加 conflict_resolve / devil_advocate 立刻挂 (R2 B2/B3)
- [ ] `test_idl_contract.py::test_analyze_signature_has_env_snapshot` 通过 (R2 B3)
- [ ] `test_lane_isolation.py` 全部通过: 每个 lane 模块**不 import 其他 lane** (R2 R9)
- [ ] R6 sanity: agent_synthesis.analyze 在 mock LLM 任意输入下, 50 次调用 ≥ 25 次返回 direction ∈ {wait, neutral, no_view}
- [ ] R3 不变量: rationale_plain 为空字符串时 `StrategySignal.__init__` raise ValidationError
- [ ] mypy strict 通过 (含 Protocol 类型)

## Known gotchas
- Protocol vs ABC 选 Protocol (D7), 用 `@runtime_checkable`
- agent_synthesis 的 analyze 用 env_snapshot 综合, 但 prompt 中**不引用其他 lane 输出** (R2 R9). 综合的对象是 (advisor_report 原始内容 + portfolio + env_snapshot), 不是 (其他 lane 已生成的 signal).
- placeholder_model Q6 默认 no_view 不是"懒", 是"反对假精度"; 注释里写明
- prompt 版本号 bump 时, T005 cache 自动失效
- **R2**: ConflictReport / Rebuttal 完全在 T010 / T013 处理, 不在本 task 范围

## Out of scope for this task
- ConflictReportAssembler 中立服务 (T010, R2 D22)
- DevilAdvocateService 独立服务 (T013, R2 D23)
- 录入 UI / 录入流程 (T008)
- 冲突报告 UI 渲染 (T010)
- 真私有模型 (non-goals.md A2, v0.5+)
