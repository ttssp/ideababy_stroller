# T015: 月度 review generator (R3 简化版) + pause hook + partner audience 删除

**spec_ref**: specs/004-pB/spec.md#2-1 · spec.md#8-Q1 · open-questions.md#Q1
**phase**: 2
**depends_on**: [T002, T003, T011, T013, T014]
**blocks**: [T022]
**parallelizable_with**: [T016, T018]
**file_domain**:
  - src/decision_ledger/services/monthly_review_service.py
  - src/decision_ledger/services/monthly_scheduler.py
  - src/decision_ledger/ui/router_monthly_review.py
  - src/decision_ledger/ui/templates/reviews/monthly.html
  - tests/integration/services/test_monthly_review.py
  - tests/unit/services/test_monthly_scheduler_pause_hook.py
**estimated_hours**: 3
**recommended_model**: sonnet-4-6
**risk_level**: medium
**risk_refs**: [OP-3, OP-6]
**constraint_refs**: [C3, C7, C21]

---

## R3 修订摘要 (R2 继承 + ⚠️ H1 旧 blocks 删除 + B-R2-4 真砍 to 3h)

R2 修订继承: H1 (pause hook 显式暴露) / M5 (partner audience 删) / H2 (cut list 候选).

| R3 修订 | 说明 |
|---------|------|
| **R3 ⚠️ H1** task header `blocks: [T017, T020, T022]` 旧约束 | 删除 T017 / T020. T020 已提前到 Phase 1 末段, 不再 blocked by T015; T017 不依赖月度 review (Telegram 仅推周报). 仅保留 `blocks: [T022]` (T022 B-lite 调 MonthlyScheduler.pause hook). |
| **R3 B-R2-4 真砍** | 8h → **3h**. 范围简化: <br/>- **保留**: 基础 SQL 聚合 (committed decisions count, would_have_acted count); MonthlyScheduler 类 + pause/resume hook; 静态 markdown 文本汇总 + Web view (无图表); 错位矩阵月末快照 (复用 T011 的 HTML table). <br/>- **砍掉**: PNG 图表生成; 月度自定义模板 (固定模板); LLM 摘要由 "calibration 偏好叙述 + 强制至少一段" 简化为 "纯 SQL 数据 markdown render, 不调 LLM 摘要" (省 LLM call cost + 时间); 12 个月对标可视化 (仅文字 placeholder "样本 N 个月, 暂不出年化"). |
| **R3 partner_partial.html 删除** | R3 file_domain 删除 `_monthly_partial.html` (未拆 partial, 单 monthly.html 即可). |

estimated_hours **3h** (B-R2-4 真砍).

---

## Goal (R3 简化版, 3h)
月度 review (PRD §S4 monthly), **R3 极简实现**:
- `apscheduler` cron 每月最后一日 21:00 (使用 T014 scheduler) 调 `MonthlyReviewService.generate(month_id)`
- **R2 修订继承: 移除 audience 参数, 仅 self** (M5)
- **R3 简化范围**:
  1. 校准证据聚合 (纯 SQL): 本月决策档案 (committed only) count / 不动 (hold+wait) 占比 / `would_have_acted_without_agent` count
  2. 错位矩阵月末快照: **复用 T011 已有的 HTML table** (iframe / partial 嵌入即可, 不新做)
  3. 风险快照 (静态文本): OP-1 / OP-2 / TECH-2 / O10 简短表 (无颜色)
  4. 12 个月对标 placeholder: "样本 N 个月, 暂不出年化对标" 文字
- **R3 砍掉**:
  - LLM 摘要 (省 LLM cost + 时间, 用纯 markdown 文本 render)
  - PNG 图表 / 月度自定义模板
  - tab 打开计数引用 (留给 T020 dashboard)

### R2 H1 新增: MonthlyScheduler pause hook
```python
class MonthlyScheduler:
    _paused: bool = False  # R2 H1

    def pause(self) -> None: self._paused = True
    def resume(self) -> None: self._paused = False
    def is_paused(self) -> bool: return self._paused

    async def run_monthly_job(self):
        if self._paused:
            logger.info("MonthlyScheduler is paused, skipping this run")
            return
        await monthly_review_service.generate(month_id=...)
```

T020 / T022 在 B-lite engage 时直接调 `monthly_scheduler.pause()` (R2 H1).

## Inputs
- T002 / T003 完整数据
- T011 错位矩阵 service (复用, 不重写)
- T013 rebuttal 数据
- T014 scheduler + weekly_maintenance_log
- ~~D18 audience 接口~~ (R2 删除)
- Q1 默认假设

## Outputs (R3 简化, 3h)
- `services/monthly_review_service.py`: `generate(month_id) -> dict for template` (R3: 纯 SQL 聚合, 不调 LLM)
- `services/monthly_scheduler.py`: `MonthlyScheduler` 类 + pause/resume/is_paused hook (R2 H1, 必须保留)
- `templates/reviews/monthly.html`: 单文件, 4 section markdown render (无 partial 拆分)
- ~~`monthly_review_v1.md` prompt~~ (R3 删除, 不调 LLM)
- 集成测试: seed 1 个月数据 → generate → 模板含 4 section (count/avg/snapshot/placeholder)
- 测试: < 12 个月样本时 "样本不足" 文案出现
- **R2 新增** `tests/unit/services/test_monthly_scheduler_pause_hook.py` (保留):
  - `test_pause_resume`: 调 pause() 后 is_paused()=True, run_monthly_job 立刻 return; resume 后恢复
  - `test_pause_does_not_lose_data`: pause 状态下 decisions 表 + weekly review 仍正常 (B-lite 数据完整性, R2 Glossary)

## Implementation plan (R3 简化, 3h)
1. service: 纯 SQL 聚合月度数据 (`status='committed'` filter); 不调 LLM
2. ~~audience 路径~~ R2 删除
3. 风险快照: 静态文本 4 行 (OP-1/OP-2/TECH-2/O10), 无颜色
4. 12 月对标 placeholder: 检查 month_id - first_decision_month >= 12, 否则 placeholder 文字
5. ~~LLM prompt~~ R3 砍 (节约 ~3h)
6. 模板单文件 monthly.html: HTML table + markdown render; 错位矩阵嵌 T011 已有 partial (iframe / hx-include)
7. cron 注册到 T014 scheduler (每月最后一日)
8. **R2 H1**: MonthlyScheduler 类暴露 pause/resume hook, 单测覆盖

## Verification
- [ ] `uv run pytest tests/integration/services/test_monthly_review.py -v` 全绿
- [ ] `uv run pytest tests/unit/services/test_monthly_scheduler_pause_hook.py -v` 全绿 (R2 H1)
- [ ] 浏览器: `/reviews/monthly?month=2026-04` 显示 4 个 section (markdown render)
- [ ] `MonthlyReviewService.generate` signature 不含 audience 参数 (R2 M5 验证)
- [ ] 第 1 个月 (样本不足) 显示 placeholder 文字, 不是误导性年化数字 (Q1 mitigation)
- [ ] **R2 H1**: `MonthlyScheduler.pause()` / `resume()` / `is_paused()` hook 存在并可调用
- [ ] **R3 cut 验证**: 服务 generate() 路径 LLM call 计数 = 0 (R3 简化, 不调 LLM)

## Known gotchas
- "12 个月对标" 是 Q1 OPEN, 实现时务必用 placeholder 不假装精度 (Q1 默认假设)
- decisions 表查询必须 `WHERE status='committed'` (R2: draft 不算入月度统计)
- **R2 H1**: pause hook 必须在 outputs 显式存在, T022 依赖
- **R3 真砍**: 已从 8h → 3h. 去除 LLM 摘要 + 图表 + 自定义模板. 任何 PR 想加回 LLM 摘要必拒 (R3 已锁简化版)
- **R3 ⚠️ H1**: blocks 不再含 [T017, T020] (T020 已 Phase 1 末段提前, T017 不依赖月度 review)

## Out of scope for this task
- 真年化 alpha 计算 (O9 12 月后 human 手算)
- ~~partner 视图实现~~ R2 删除 (推后 v1.0+)
- 学习检查 (T019)
- B-lite UI toggle (T022, 仅调 pause hook)
