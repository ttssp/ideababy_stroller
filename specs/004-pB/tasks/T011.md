# T011: 错位矩阵 HTML table (R3 简化版, 无颜色着色)

**spec_ref**: specs/004-pB/spec.md#4-prior-decisions D16 · open-questions.md#Q8 · architecture.md#7-2
**phase**: 1
**depends_on**: [T002, T003, T007]
**blocks**: [T015]
**parallelizable_with**: [T006, T008, T009, T010, T012, T013]
**file_domain**:
  - src/decision_ledger/services/matrix_service.py
  - src/decision_ledger/ui/router_matrix.py
  - src/decision_ledger/ui/templates/matrix/index.html
  - src/decision_ledger/ui/templates/matrix/_table.html
  - tests/unit/services/test_matrix_service.py
  - tests/integration/ui/test_matrix_routes.py
  # R3 删除: src/decision_ledger/ui/static/matrix.css
**estimated_hours**: 3
**recommended_model**: sonnet-4-6
**risk_level**: low
**risk_refs**: [OP-6]
**constraint_refs**: [C7]

---

## R3 修订摘要 (B-R2-4 真砍)

| R3 修订 | 说明 |
|---------|------|
| **R3 B-R2-4 真砍** | 6h → **3h**. 范围简化: <br/>- **保留**: HTML table render (行 = ticker, 列 = 三方向 + 持仓 %); 基础 SQL 计算; OP-6 nav 第三位置. <br/>- **砍掉**: CSS 颜色着色 (yellow/red/neutral 都用默认色); 排序按错位强度 (默认按 ticker 字母排); `static/matrix.css` 文件删除 (用 Pico 默认表格样式); 错位强度计算简化为 `direction_diff = abs(advisor_score - holding_pct)` (不区分黄/红); v0.2+ 加颜色 polish. |

estimated_hours **3h**.

## Inputs
- T002 schema (ticker / holdings / advisor_reports / signals)
- T003 `PortfolioRepository`, `AdvisorRepository`, signals 缓存
- T007 三 StrategyModule (用 `analyze` 拿三方向)
- Q8 默认维度

## Outputs (R3 简化, 3h)
- `services/matrix_service.py`: `MatrixService.build()` 返回二维表格 dict 列表;
  方向 score: long=+1 / short=-1 / wait/neutral/no_view=0
- `ui/router_matrix.py`: GET `/matrix` 渲染 table
- `templates/matrix/_table.html`: 严格 HTML table; **R3 简化: 无 colored cell class, 用 Pico 默认样式**
- ~~`matrix.css`~~ (R3 删除, 用 Pico 默认)
- 单测 (R3 简化): `test_matrix_service::test_build_returns_rows` (≥ 30 行); `test_direction_score` (long=+1 / short=-1 / wait=0)
- 集成测: `/matrix` 返回 HTML 含 `<table>`, ≥ 30 行 (R3: 无颜色断言)

## Implementation plan (R3, 3h)
1. matrix_service: 拉 watchlist + holdings_snapshot + 最近 advisor_reports +
   running 三 signal cache (复用 T010 写入的 conflict_reports 中 signals)
2. **R3 简化: 不计算错位强度, 不排序; 按 ticker 字母序输出**
3. 模板: `_table.html` thead 4 列 + tbody 30-50 行; 单 cell 仅 direction text
4. ~~CSS 三色~~ (R3 删除)
5. 单测 (R3 简化): rows count + score 计算 (≥ 2 case)
6. 集成测试 mock seed data, 断言 row count

## Verification
- [ ] `uv run pytest tests/unit/services/test_matrix_service.py -v` 全绿
- [ ] `uv run pytest tests/integration/ui/test_matrix_routes.py -v` 全绿
- [ ] 浏览器: `/matrix` 渲染 30 行表格 (R3: 无颜色)
- [ ] grep `plotly\|matplotlib\|d3\|chart.js` 在 src + templates 0 命中 (D16)
- [ ] OP-6 验证: 错位矩阵 tab 是 nav 第 3 (T004 已锁), 不是首页
- [ ] **R3 验证**: matrix.css 文件不存在 (B-R2-4 cut)

## Known gotchas
- 30-50 行表格在 Pico CSS 下默认会很高, CSS overflow: scroll + sticky thead
- D16 决定 "无图表库"
- "错位强度" 是占位算法, v0.5+ 私有模型上线后可能重新定义
- **R3 真砍**: 颜色着色 + 排序已删. v0.2+ polish 可加回

## Out of scope for this task
- 月度 review 引用矩阵 (T015)
- 主动 push 错位告警 (不在 v0.1)
- 真量化错位算法 (占位 OK, non-goals.md A2)
