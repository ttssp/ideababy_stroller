# T030: 单元测试 · judge / state / format / paths / grep

**spec_ref**: specs/007a-pA/spec.md#5-task-breakdown(Phase 3 T030)+ spec §6 Tech 静态规则正确性
**phase**: 3
**depends_on**: [T001, T002, T003, T010, T013]
**blocks**: []
**parallelizable_with**: [T031, T032]
**file_domain**:
  - projects/007-007a-pA/tests/__init__.py
  - projects/007-007a-pA/tests/test_paths.py
  - projects/007-007a-pA/tests/test_state.py
  - projects/007-007a-pA/tests/test_format.py
  - projects/007-007a-pA/tests/test_judge.py
  - projects/007-007a-pA/tests/test_grep.py
**estimated_hours**: 2.0(R1 修订:+0.5h D8 修订网格 + D16 confidence-feature + D19 redact 测试扩充)
**recommended_model**: codex-5.5-mini
**risk_level**: low

---

## Goal(R1 修订)

用 `unittest`(stdlib · tech-stack §1)覆盖 5 个 helper 模块的核心契约。重点(R1 修订):
- spec.md §6 Tech 行的 **D8 修订白名单 3 tool × {成功/失败} × {黑名单关键字 命中/不命中} 共 12 case + Read tool 必 skip 反向 4 case**(R1 H4)
- **D16 confidence-feature 决策表 8 case**(每种 confidence 边界 input 至少 1 个;R1 B1)
- **`test_judge_threshold_actually_filters` 关键测试**(R1 B1 主断言:threshold 真过滤 — 失败 = TECH-2 mitigation 失效 = ship blocker)
- **D19 redact pattern 6 case**(R1 SEC-2)
- **state schema_error_count backward-compat + bump 累加 + bump fail-safe 6 case**(R1 H3)

## Inputs

- 已实现的 `paths / state / format / judge / grep` 模块(Phase 0 + Phase 1)
- stdlib `unittest` / `tempfile` / `unittest.mock`

## Outputs

- `tests/test_paths.py` — `repo_root` 在仓内 / 仓外行为;`friction_log_path` 拼接正确
- `tests/test_state.py` — default 写入 / round-trip / invalid schema raise / chmod 600 / **schema_error_count backward-compat / bump 累加 / bump fail-safe 6 case(R1 H3)**
- `tests/test_format.py` — agent_emit / operator_cli 单行格式 / 200char stderr 截断 / 4096 char 上限 / **D19 secret-redact 6 case(R1 SEC-2)**
- `tests/test_judge.py` — **D8 修订网格 12 case(R1 H4 · 3 tool × 2 exit × 2 marker)+ Read tool 必 skip 反向 4 case + D16 confidence-feature 8 case + `test_judge_threshold_actually_filters`(R1 B1 主断言)+ 5 个 short-circuit case**(state.disabled / blacklist marker / block=True / non-whitelist tool / threshold 三档)**共 ≥ 30 case**
- `tests/test_grep.py` — fixture markdown 读入 → tag_ratios / count_since / has_recent_agent_emit / **CLI `--count-agent-emit` smoke test(R1 B2)**
- 全部测试用 `unittest.main()` 直接 runnable

## Implementation plan

1. **test_judge.py** D8 修订网格 + D16 + R1 B1 关键测试(TECH-2 主控):
   ```python
   # D8 修订白名单 12 case(R1 H4 · 移除 Read)
   for tool in ["Bash", "Edit", "Write"]:
     for exit_code in [0, 1]:
       for has_marker in [False, True]:
         # 12 = 3 tool × 2 exit × 2 marker · 固定 threshold=medium

   # Read tool 反向断言 4 case(R1 H4 · 验证 Read 移出后必 skip)
   for exit_code in [0, 1]:
     for has_block in [False, True]:
       # tool="Read" + 上述各组合 → 只有 block=True 时 emit,否则 skip

   # D16 confidence-feature 8 case(R1 B1)
   # H 边界 ≥ 4:block=True / Bash + "Error:" / Edit fail / Write fail
   # M 边界 ≥ 2:Bash + 100 char stderr 无强信号 / 不同 task_desc M case
   # L 边界 ≥ 2:Bash + 短 stderr / Bash + 空 stderr

   # 关键 R1 B1 主断言 test_judge_threshold_actually_filters
   # fixture event 集合 = {H_event, M_event, L_event}
   # threshold=high → assert M_event 与 L_event 都 ("skip", None);H_event 是 ("emit", "H")
   # threshold=medium → assert L_event 是 ("skip", None);H/M 是 emit
   # threshold=low → 全 emit
   ```
   **共 ≥ 24 + 5 short-circuit + 1 关键过滤断言 = ≥ 30 case**。
2. **test_state.py** 用 `tempfile.TemporaryDirectory` 隔离 `state_path()`(monkeypatch `paths.state_path`)。
3. **test_format.py** 用 `tempfile.NamedTemporaryFile` 模拟 friction-log,验证写入字节级(UTF-8 + ↵ 替换)。
4. **test_paths.py** 用 `os.chdir(tmp)` + 子进程 git init / `os.chdir("/tmp")` 走仓外分支。
5. **test_grep.py** 准备 fixture 字符串(直接 inline,无外部 fixture 文件):
   ```
   2026-05-08T10:00:00Z [agent-emit] [confidence:H] [private-to-operator] · Bash exit 1 · "..." · task: ...
   [acked] looks right
   2026-05-08T11:00:00Z [agent-emit] [confidence:M] [private-to-operator] · Edit exit 1 · ...
   [disputed] semantic ok
   ...
   ```
6. 顶部 docstring 引用 spec §6 Tech / risks TECH-2。

## Verification (MUST be runnable)

- [ ] `cd projects/007-007a-pA && python -m unittest discover tests -v` 全 green
- [ ] `python -m unittest tests.test_judge -v` **至少 30 case**(R1 修订:12 D8 网格 + 4 Read 反向 + 8 D16 confidence + 5 short-circuit + 1 关键 R1 B1 过滤断言)
- [ ] `python -m unittest tests.test_state -v` 至少 6 case(default / round-trip / invalid / **backward-compat schema_error_count / bump 累加 / bump fail-safe**)
- [ ] `python -m unittest tests.test_format -v` 至少 8 case(基本 + 4096 上限 + 截断 + **D19 redact 6 case** = 11 case)
- [ ] coverage(可选 stdlib `coverage` 工具)`coverage run -m unittest discover tests && coverage report --include='friction_tap/*'` 各 helper 模块 ≥ 85%
- [ ] 任一 test 失败 → `python -m unittest discover tests` exit 1
- [ ] 测试不依赖 `~/.config/friction-tap/state.json` 真实存在(monkeypatch / tmp dir)
- [ ] **R1 B1 关键测试 `test_judge_threshold_actually_filters` 必跑**:`python -m unittest tests.test_judge.TestThresholdFilter -v` 必 green(失败 = TECH-2 mitigation 失效 = ship blocker)

## Known gotchas

- `test_state.py` 不能污染真实 `~/.config/friction-tap/state.json` — 必须 monkeypatch `paths.state_path` 或 monkeypatch `Path.home()` 返回 tempdir
- `test_paths.py` 的"仓外"测试要 `cd /tmp` 而非 `cd $TMPDIR`(macOS 的 $TMPDIR 可能在 git ignored 子路径下,git 仍能找到 toplevel)
- `unittest.mock.patch.dict` 用于 monkeypatch 模块级常量(WHITELIST_TOOLS 不要改;mock state instead)
- 测试不要写 README 中的 example 命令(那是 T032 / T033 集成测试的事)
- 不要测 hook silently swallow(那是 T031 集成测试的事)

## Out of scope for this task

- ❌ 不写集成测试(T031 负责)
- ❌ 不测 launchd / install scripts(T031/T033 负责)
- ❌ 不测 trust_summary 渲染(T031 负责)
- ❌ 不写 pytest fixtures(stdlib only)
